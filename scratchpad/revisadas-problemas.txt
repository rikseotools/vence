
==========================================================================================
T-689 [alta] La cola de revisión se muere de hambre justo cuando la flota rinde: solo se reparten revisiones a trabajadores LIBRES
(revisó w3-vence-flota-w1-fdac7b)

--- ENTREGA:
Rama flota/T-689-alerta-edad-cola-revision (commit 4e17af037), pusheada. Diagnóstico y arreglo: (1) el 'arreglo en una frase' que pedía la ficha (revisar antes que tarea nueva, dentro de for(f of libres)) YA estaba en main desde T-486 -- git log -S'PRIMERO REVISAR, LUEGO HACER' -- scripts/flota/flota.cjs da un solo commit, 195f59a23, 2026-08-06T09:43:24+02:00, DOS DÍAS antes de abrirse esta ficha (08/08 01:15). Repetirlo habría sido rehacer trabajo ya hecho. (2) MEDIDO con consultas propias contra DATABASE_URL/VENCE_LECTOR_URL: con la prioridad ya puesta, en 48h hay 158 encargos flota_turno tipo=revision contra 150 tipo=backlog (la revisión SÍ gana más veces que la tarea nueva). Pero hay esperas reales de 2.5-7.9h en tareas ya revisadas post-fix (T-612 4.1h, T-163 7.2h, T-197 4.3h, T-205 4.4h, T-548 3.0h, T-656 7.9h), y en el momento de resolverse cada una tenía SOLO 1-3 entregas pendientes a la vez (conteo verificado con consulta propia) -- muy por debajo del umbral de conteo (COLA_AMBAR=8) que usa lib/flota/salud.cjs para el semáforo del panel /admin/salud-sistema. (3) REPRODUCIDO directamente: node -e con S.saludFlota({entregas:[{review_requested_at: hace 20h}]}) devuelve estado='verde'. Una sola entrega de 20 horas no mueve el semáforo. Este es el agujero real: observabilidad, no reparto. ARREGLO: lib/flota/salud.cjs añade ESPERA_AMBAR_H=2 (el objetivo que la propia ficha declara textualmente: 'ningún par de horas sin veredicto'), condición de ámbar independiente del conteo. No toca scripts/flota/flota.cjs (el reparto ya es correcto) ni las dos trampas que la ficha pedía evitar (no se reordena nada del bucle; 'nadie revisa lo suyo' sigue intacto). docs/runbooks/health-check.md §9 actualizado con el nuevo umbral para que panel y runbook no diverjan. Ficha movida a ## Hechas con ✅. CAPAS: __tests__/flota/salud.test.ts +2 casos (12/12 verde: entrega de 3h dispara ámbar aunque el conteo sea 1; entrega de 1.5h se queda en verde). __tests__/guardrails/backlogRegistry.guardrail.test.ts (25/25) y toolRegistry.guardrail.test.ts (16/16) en verde -- salud.cjs ya estaba registrado en lib/admin/toolRegistry.ts desde T-486, no hace falta entrada nueva. robustez-push-guard.cjs y contexto-push-guard.cjs sin objeciones (exit 0). PENDIENTE, no verificado por mí: npm run typecheck (tsc --noEmit del proyecto completo) hizo OOM en esta máquina tras 313s de espera al candado compartido (T-682/T-708) -- 'JavaScript heap out of memory', un problema de contención de memoria YA DOCUMENTADO en el propio scripts/typecheck.cjs, no algo causado por mi diff (4 líneas de lógica + 2 tests, mismo patrón exacto que el código ya tipado alrededor). No pude confirmar el typecheck completo por esta vía; quien revise puede correrlo en un momento de menos carga. Necesita que otro worker/persona revise y mergee -- yo no puedo mergear a main.

--- HALLAZGOS:
LEI el diff completo de origin/flota/T-689-alerta-edad-cola-revision contra origin/main (4 ficheros: lib/flota/salud.cjs, __tests__/flota/salud.test.ts, docs/runbooks/health-check.md, docs/roadmap/tareas-pendientes.md). COMPROBADO CADA CLAIM CONTRA LA REALIDAD, no solo releida la prosa: (1) 'el arreglo en una frase ya estaba en main desde T-486' -- confirmado con git log -S'PRIMERO REVISAR, LUEGO HACER' -- scripts/flota/flota.cjs (un solo commit, 195f59a23, 2026-08-06T09:43:24+02:00) y git merge-base --is-ancestor 195f59a23 origin/main = YES, ademas lei el propio diff de ese commit: el bloque porRevisar SI vive dentro de for(f of libres) y SI prioriza revision antes que tarea nueva -- coincide exacto con lo que la ficha original pedia como 'arreglo en una frase'. (2) '158 revision vs 150 backlog en 48h' -- reproducido con consulta propia contra DATABASE_URL (event_type=flota_turno, metadata->>fase=encargado, metadata->>tipo): obtuve 162 vs 154 unas horas despues, misma direccion y magnitud, coincide. (3) Las 6 esperas citadas (T-612 4.1h, T-163 7.2h, T-197 4.3h, T-205 4.4h, T-548 3.0h, T-656 7.9h) -- reproducidas EXACTAS consultando backlog_tasks.review_requested_at/reviewed_at, sin margen de error (4.1023h->4.1h etc). (4) 'solo 1-3 entregas pendientes a la vez en cada resolucion' -- reproducido reconstruyendo el estado historico de la cola en cada timestamp de reviewed_at: T-197:2, T-656:3, T-548:3, T-612:1, T-205:3, T-163:1 -- exacto. CORRI SUS TESTS: 12/12 en __tests__/flota/salud.test.ts (worktree aislado con node_modules symlinked), y el area completa __tests__/flota + __tests__/health: 916/916 verdes, mas backlogRegistry.guardrail.test.ts 25/25 (su edicion de la ficha no rompe el guardarraii). npm run tools:buscar -- 'salud flota' no muestra colision: extiende lib/flota/salud.cjs existente, no crea herramienta nueva, asi que no hace falta toolRegistry.

PROBLEMA REAL ENCONTRADO, cuantificado, no soy yo puliendo detalles: el hilo de produccion que alimenta esperaMaxH tiene un agujero que el propio CLAUDE.md (seccion T-486) ya advertia por escrito -- 'review_requested_at dice que hay entregable y reviewed_at que ya lo miro alguien -- leer solo el primero anuncia entregas pendientes que ya estan revisadas'. app/api/admin/system-health/route.ts (NO tocado por este diff, codigo preexistente) consulta entregas con WHERE review_requested_at IS NOT NULL AND status <> 'done' -- NO filtra reviewed_at IS NULL, a diferencia de porRevisar en flota.cjs que si lo hace bien. MEDIDO AHORA MISMO contra DATABASE_URL: la query del panel devuelve 10 filas 'pendientes' de las que 7 YA estan revisadas (reviewed_at con review_verdict=ok, ej T-681, T-679, T-532, T-530, T-513, T-486 mismo, T-465) y solo 3 son de verdad reviewed_at IS NULL. Con esas 10 filas contaminadas, esperaMaxH = 3.8h; con las 3 reales, esperaMaxH = 0.5h. El umbral que este PR introduce es ESPERA_AMBAR_H=2 -- es decir AHORA MISMO, con la cola real sana (0.5h), el panel en produccion mostraria AMBAR por una entrega que hace tiempo que tiene veredicto. Es un falso positivo activo, no hipotetico, y ataca justo el objetivo de la ficha (una senal fiable de cola vieja). El nucleo saludFlota() esta bien y sus tests son correctos -- el problema es la fuente de datos que lo alimenta, que el entregado no comprobo (solo uso consultas propias limpias, nunca la query real del panel). ARREGLO: en app/api/admin/system-health/route.ts, cambiar 'AND status <> ok'"done"'' por 'AND reviewed_at IS NULL' en la query de entregas, igual que ya hace porRevisar en scripts/flota/flota.cjs -- mismo criterio en los dos sitios que leen esta cola (evita la trampa de dos guardas con criterio distinto que CLAUDE.md ya senala en otros casos).
