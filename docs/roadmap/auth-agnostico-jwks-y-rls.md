# Roadmap — Auth agnóstico (Auth.js + RS256/JWKS) + retirada de RLS

> **Estado:** 🟢 EN CURSO. **Fase A ✅ COMPLETA (A1+A2+A3)** + limpieza segura + **guardrails** (commits `495886aa`, `c9aeb287`, `aaf22f7f`, `86ecc0a7`, `8a4ac51e`). **Fase C1 EN CURSO** (tier público ✅ + **43 unidades migradas** —ver #1–#43 abajo—; **8 patrones probados**: público server→Drizzle, read user-scoped, write user-scoped, auth-opcional, borrado dead-code, RPC→endpoint, cross-user con gate de privacidad, tier admin con `requireAdmin`). Ratchet `.from` cliente **128→3** sitios / 43→2 ficheros + trinquete `.rpc` cliente **17→7** (ambos solo decrecen). **Los 3 `.from` de cliente restantes son TODOS bloqueados:** AuthContext (dual-path flag-gated, no tocar) + loadProblematicArticles×2 (canary FASE 4/5). **Migración tractable de cliente COMPLETA.** **Barrido `lib/` server-side COMPLETO** (`motivationalAnalyzer`/`conversionTracker`/`dailyLimit`/`deviceLimit`/`runWithLogging`/`testFetchers`/`lawFetchers`/`psychometricDifficulty`/`adaptiveQuestionSelection` desacoplados de supabase). **C2 ✅ HECHO** (guardrail `user-scoping-c2`: toda query a una de las 43 tablas user-scoped debe acotar por el token o ser admin; 0 violaciones, con self-tests). **C3 ✅ HECHO** (suite `crossUserIsolationC3`: 16 tests conductuales smuggle-B → el SQL usa el token, nunca el id colado). Siguiente: **C4** (drop RLS — tras reposar C1+C2+C3 ~1 semana en prod), **Fase B** (Auth.js RS256/JWKS). Pendiente decisión producto: cerrar la exposición de correct_option en psicotécnicos exige mover validación a /api/answer/psychometric (no elegido — feedback instantáneo).
>
> **Resumen de progreso C1 (2026-06-24):** **~95% del acoplamiento PostgREST de cliente eliminado** (`.from` 128→6) y `.rpc` de cliente casi liquidado (17→7, varios de los 7 restantes son falsos positivos en comentarios). **~50 endpoints v2 nuevos** (user-scoped con `verifyAuth`+`WHERE user_id` del token, admin con `requireAdmin`, públicos sin auth). **TODAS las páginas admin migradas** (fraudes, feedback, notificaciones×3, newsletters, conversiones). **Analytics psicotécnicas migradas** (#38). **6 `.from` de cliente restantes, todos delicados/bloqueados:** 2 en `loadProblematicArticles` (BLOQUEADO por canary `problematic-articles` FASE 4/5 — no forzar), **páginas de test psicotécnicas** (`test/psicotecnicos/*` cargan `psychometric_questions.*` → sensible anti-scraping + selección adaptativa), `TestLayout` (supabase pasado como prop a hijo). **PENDIENTE tractable:** `lib/` hot-paths (`testFetchers`/`lawFetchers`/`adaptiveQuestionSelection`/`psychometricDifficulty` — no cuentan en el ratchet de cliente pero rompen Neon: PostgREST no existe). Después: C2 (guardrail user_id), C3 (tests aislamiento), C4 (drop RLS), Fase B.
> **Propietario:** equipo Vence.
> **Coste recurrente añadido:** 0 € (Auth.js self-hosted, claves en SSM, infra existente).
> **Última actualización:** 2026-06-26 (cierre de deuda de tests legacy: 11→0 suites rojas + patrón adaptador SQL→RPC, ver §"Deuda oculta").

## Progreso

- **A1 ✅ (2026-06-20):** todos los `supabase.auth.*` de CLIENTE enrutados por el puerto `auth` (22 ficheros). 3 `.js`→`.tsx` tipados strict (GoogleOneTap, AvatarChanger, repaso-fallos-oficial/page). Puerto ampliado con `signInWithIdToken`. typecheck verde, 2382/2383 tests OK (el fallo restante es pre-existente: conteo fijo en `OposicionDetector.test.tsx`, ajeno a auth). Tests `client.test.ts`/`complete-test/client.test.ts`/`testSaveSessionExpired.test.ts` actualizados (sesiones mock ahora con `user`, el puerto descarta sesiones sin usuario). **Hallazgo:** `components/ChatInterface.js` era código muerto (0 importadores; el chat vivo es `AIChatWidget`) → **BORRADO 2026-06-20**. **Diferido (no es A1):** `signInWithOAuth` de login/landing/premium (= funnel C2), `app/auth/callback/page.tsx` (Fase B), server `verifyAuth`/`adminApiGuard` (Fase B).

- **A2 ✅ (2026-06-20):** las 5 RPCs de ciclo de vida → **3 endpoints Drizzle agnósticos** (`POST /api/v2/auth/ensure-profile`, `/api/v2/access/check`, `/api/v2/premium/activate`), invocan las mismas funciones plpgsql vía `getAdminDb().execute(sql\`SELECT fn(named => args)\`)` (notación nombrada = orden infalible). **user_id/email del TOKEN (verifyAuth), nunca del body.** `contexts/AuthContext.tsx` (ensureUserProfile/checkAccess/activatePremium) + `lib/campaignTracker.ts` (otro caller de `create_google_ads_user`) cableados con **ambos paths** detrás del flag compartido `LIFECYCLE_VIA_API` (`lib/auth/lifecycleFlag.ts`, `NEXT_PUBLIC_AUTH_LIFECYCLE_VIA_API`, default false → flip = redeploy). 11 tests nuevos (`authLifecycleEndpoints.test.ts`) + AuthContext 80/80. typecheck verde. **Rollout:** mantener flag false, validar endpoints en log-paralelo, luego flip. **Cabo pendiente (con C2):** `app/landing/premium-edu/page.js:58` también llama `create_google_ads_user` — es landing del funnel C2 (su `signInWithOAuth` ya está diferido ahí); migrar junto con C2 para no dejarla incoherente.

- **A3 ✅ (2026-06-20):** eliminadas las RPCs de admin `is_current_user_admin`/`is_user_admin` (auth.uid(), no portables). **Allowlist client-safe** `lib/auth/adminEmails.ts` (fuente única; `lib/api/shared/auth` la reexporta). Migrados: cliente `app/Header.tsx`, `components/UserAvatar.tsx`, `components/Admin/ProtectedRoute.tsx`, `app/admin/fraudes/page.tsx` + 5 `.js` `admin/notificaciones/*` → `setIsAdmin(isAdminEmail(user.email))`; server `lib/finance/auth.ts` → `isAdminEmail(auth.email)` (quita el cliente Supabase user-scoped). **Borrado** el `POST` muerto de `app/api/admin/email-events/route.ts` (0 callers, usaba `is_user_admin`). **Verificado en BD:** único admin por rol = `manueltrader@gmail.com` (en la allowlist) → cero lockout. El gate REAL sigue server-side (`requireAdmin`); el cliente es solo UI. typecheck verde, 354/354 tests, lint sin errores nuevos. **Cabos de calidad (NO agnosticismo):** (1) `components/ChatInterface.js` **BORRADO** (muerto, 0 importadores). (2) `admin/notificaciones/email/subscripciones/page.js` **→ `.tsx`** strict (era endpoint-based vía `adminFetch`, conversión limpia). (3) Los otros 4 (`overview/users/events/email`) **siguen `.js` a propósito**: usan `supabase.from()` intensivamente (cliente `any`) → convertirlos a strict ahora obligaría a tipar a mano datos `any` que se **re-tiparán al migrar esos `.from()`→endpoints en Fase C**. Se convierten **junto con su migración de Fase C** (evita retrabajo). El RPC admin ya está migrado, así que no bloquean Neon.

- **C1 (arranque 2026-06-20):** ratchet PostgREST cliente puesto (`__tests__/guardrails/postgrest-from-ratchet.test.ts`, commit `8a4ac51e`) — **baseline 128 sitios / 43 ficheros**, solo puede DECRECER. **Sin sesión paralela ya** (riesgo de colisión eliminado). **Plan priorizado por riesgo** (uno a uno, reversible, ritual completo): (a) **público en server-components** → swap `supabase.from`→Drizzle in-place (sin endpoint, sin auth, sin user_id = riesgo mínimo); (b) **user-scoped** → endpoint `verifyAuth` + `WHERE user_id` (del TOKEN, no del prop) + test de aislamiento; (c) **admin / datos de otros** → endpoints `requireAdmin` (los últimos). Ritual por fichero: schema Drizzle → endpoint → test aislamiento → recablear cliente preservando forma → typecheck+tests → **bajar el baseline del ratchet** → commit. **Primer target user-scoped scopeado:** `components/QuestionEvolution.tsx` (1 query a `test_questions` con embed `tests(...)` → leftJoin + reconstrucción; el `userId` debe salir del token). Las 4 `.js` (`overview/users/events/email`) se convierten a `.tsx` **al migrar sus `.from`** (sus tipos reales salen del endpoint).
  - **✅ Tier público HECHO (2026-06-20)** — 4 server-components, swap a Drizzle, comportamiento idéntico (fechas `::text`, mismas proyecciones), verificado typecheck+guardrails+ratchet: `app/oposiciones/page.tsx` (`5511442f`), `app/oposiciones/[filtro]/page.tsx` +catalogadas (`2d91b87a`), `app/cursos/[slug]/page.tsx` (`05c6a46c`), `app/page.tsx` home — getTopLaws N+1→1 JOIN, COUNT sin exponer `correct_option` (`8677e1b7`). **Ratchet 128→117 / 43→39.**
  - **🟢 Tier user-scoped EN CURSO** (endpoints `verifyAuth`+`WHERE user_id` del TOKEN+test aislamiento). **#1 ✅ `components/QuestionEvolution.tsx`** → `GET /api/v2/question-evolution/history` (leftJoin tests reconstruido, fechas `to_char AT TIME ZONE UTC`, 6 tests incl. aislamiento, `withErrorLogging`) — commit `dd1ee2fb`, ratchet 117→116/39→38. **#2 ✅ `components/PsychometricQuestionEvolution.tsx`** → `GET /api/v2/psychometric-evolution/history` (sin embed, 5 tests incl. aislamiento) — commit `b9f82cee`. **#3 ✅ `hooks/useDailyGoal.ts`** → `GET /api/v2/daily-goal/status` (4 COUNT meta diaria; fechas del cliente para preservar el corte de medianoche por tz; 8 tests incl. aislamiento+semana) — commit `3d044822`. **#4 ✅ `contexts/OposicionContext.tsx`** → `GET /api/v2/oposicion/target` (target_oposicion + JSONB; 5 tests) — commit `2607365a`. **#5 ✅ `hooks/useNewMedalsBadge.ts`** (1ª con WRITE) → `/api/v2/medals/badge` GET (medallas) + POST (marca vistas, `UPDATE ... viewed=false` preservado); 6 tests incl. aislamiento GET/POST — commit `883fa60d`, ratchet 109→107. **#6 ✅ `app/pregunta/[id]/page.tsx`** (auth OPCIONAL) → `POST /api/v2/shared-question/track` (INSERT shared_question_responses; visitor_user_id del token o null si anónimo, nunca del body; 5 tests) — commit `70f33677`, ratchet 107→106. **#7 ✅ `hooks/useTestCompletion.ts`** → `handleTestCompletion` era **dead code** (0 callers vivos; TestLayout solo usa `notifyTestCompletion`, que no toca PostgREST; los tests se guardan ya por el endpoint v2 complete-test). **Borrado** en vez de migrado (principio del proyecto: borrar dead-code, no construir endpoint para código sin uso — igual que `ChatInterface.js` y el POST de email-events). Eliminaba un INSERT PostgREST a `tests` no portable + se limpiaron `supabase`/`TestData` huérfanos. Ratchet 106→105/33→32. (⚠️ GOTCHA recurrente: el comentario de borrado NO debe contener el literal `.from('` o infla el ratchet.) **#8 ✅ `hooks/useOnboarding.ts`** (4 `.from('user_profiles')`) → `GET /api/v2/onboarding/status` (perfil de onboarding del token, fallback de AuthContext) + `POST /api/v2/onboarding/skip` (incremento de contador **atómico** `UPDATE...RETURNING`, elimina la carrera read-then-write del original); el UPDATE de `handleComplete` se **eliminó por redundante** (el modal ya llama a `completeOnboardingOnServer`, que resetea los contadores, justo antes de `onComplete` y solo si hubo success). 7 tests incl. aislamiento GET+POST. Ratchet 105→101/32→31. **5 patrones probados: público (server→Drizzle), read user-scoped (GET), write user-scoped (POST/UPDATE), auth-opcional (anónimo), borrado de dead-code.** **#9 ✅ `app/perfil/page.tsx`** (4 `.from`) → `GET/POST /api/v2/account/deletion-request` (solicitud de borrado en `user_feedback`; POST **idempotente** `INSERT...WHERE NOT EXISTS`, sin la carrera check-then-insert del cliente) para los 3 `user_feedback` (2 COUNT + 1 INSERT), y `createInitialProfile` (INSERT `user_profiles`) **delega en `POST /api/v2/auth/ensure-profile`** (función SQL canónica `create_organic_user`, la misma que AuthContext en el 1er login) + re-fetch `/api/profile`. 7 tests incl. idempotencia + aislamiento. Limpia `supabase`/`SupabaseClient` huérfanos. Ratchet 101→97/31→30. **#10 ✅ `components/OnboardingModal.tsx`** (2 `.from('user_profiles')` + 2 `.rpc` custom-oposición) → reusa `GET /api/v2/onboarding/status` (loadExistingProfile; le añadí `target_oposicion_data`, additivo) + `POST /api/v2/onboarding/save-field` (guardado progresivo con **WHITELIST de columnas por switch literal** —imposible escribir plan_type/is_premium— y casts por tipo: age/horas integer, target_oposicion_data jsonb) + `GET /api/v2/custom-oposiciones/popular` (RPC `get_popular_custom_oposiciones` vía Drizzle) + `POST /api/v2/custom-oposiciones` (RPC `create_or_select_custom_oposicion`; p_user_id del token, nombre-display del body). 11 tests incl. whitelist + inyección de user_id. Firmas de las funciones SQL verificadas en BD (pg_get_functiondef). **Añadido trinquete `.rpc(` de cliente** (baseline 17) al mismo guardrail —las funciones plpgsql viven en Neon pero el transporte `.rpc()` no. Ratchet `.from` 97→95/30→29. **#11 ✅ `hooks/useDisputeNotifications.ts`** (5 `.from`: 2 SELECT + 3 UPDATE sobre `question_disputes`/`psychometric_question_disputes`) → `GET /api/v2/disputes/notifications` (normales con embed questions→articles→laws reconstruido en SQL con `json_build_object`, FKs verificadas en BD; + psicotécnicas; ventana 30d + is_read=false server-side) + `POST /api/v2/disputes/mark-all-read` (2 UPDATE) + `POST /api/v2/disputes/appeal` (UPDATE solo si `user_id`+`status='rejected'`, RETURNING). El hook conserva su Zod+mapping. 10 tests incl. aislamiento de las 5 queries. (`markAsRead` single ya usaba endpoint pre-existente.) Ratchet `.from` 95→90/29→28. **#12 ✅ `components/OposicionDetector.tsx`** (4 `.from`: SELECT + upsert/insert/update de `user_profiles`) → reusa `GET /api/v2/onboarding/status` (chequeo de oposición ya asignada) + nuevo `POST /api/v2/oposicion/assign` (UPDATE atómico con `first_oposicion_detected_at = COALESCE(...)`). DESCUBRIMIENTO: `email` es NOT NULL sin default → el INSERT/UPSERT del original **nunca** podía crear un perfil nuevo (violaba el NOT NULL); el único path real era el UPDATE (perfil ya creado por AuthContext) → endpoint UPDATE-only fiel a eso. 5 tests incl. anti-inyección de id. Ratchet `.from` 90→86/28→27. **#13 ✅ `components/AvatarChanger.tsx`** (3 `.from` update/upsert de `public_user_profiles`: avatar predefinido/imagen/reset) → `POST /api/v2/avatar/public-profile` (un endpoint, mismas 5 columnas; UPDATE-only porque `display_name` NOT NULL hacía inválido el insert del upsert). Limpia `supabase`/`useAuth` huérfanos. 5 tests. Ratchet `.from` 86→83/27→26. **#14 ✅ `components/ArticleModal.tsx`** (3 `.from`) → reusa `GET /api/v2/onboarding/status` (target_oposicion) + `POST /api/feedback` existente (que ya hace el doble insert user_feedback + feedback_conversations vía Drizzle) → los 2 `.from` de feedback colapsan en 1 fetch. Sin endpoints nuevos. Ratchet `.from` 83→80/26→25. **#15 ✅ `app/Header.tsx`** (2 `.from`) → `GET /api/v2/streak` (user-scoped, current_streak del token) + `GET /api/v2/admin/feedback/open-count` (**1º del tier admin**, `requireAdmin`, conversaciones abiertas no vistas). Limpia `supabase` huérfano (incluido un guard innecesario en checkAdminStatus). 6 tests. Ratchet `.from` 80→78/25→24. **#16 ✅ `lib/notifications/motivationalAnalyzer.ts`** (2 `.from('tests')`) → `GET /api/v2/motivational/recent-tests?days=N` (Drizzle, token); el analyzer **deja de recibir `supabase`** en el constructor (ya era client-only: usa fetch relativo) → `new MotivationalAnalyzer(user.id)`. Además **borrado el bloque `loadDisputeNotifications` COMENTADO** en `useIntelligentNotifications.ts` (dead-code que inflaba el ratchet, ya movido a useDisputeNotifications). 4 tests. NOTA: el resto de `useIntelligentNotifications` (11→10 `.from` vivos + 1 `.rpc`) NO se tocó: `loadProblematicArticles` tiene **su propia migración flag-gated en curso** (canary `problematic-articles` FASE 4/5, RPC `get_user_problematic_articles_weekly` con DROP pendiente) → forzarlo colisionaría; merece pasada dedicada. Ratchet `.from` 78→77 (lib/ no cuenta; baja por el comentado). **#17 ✅ `hooks/useDailyQuestionLimit.ts` (2 `.rpc`) + `lib/services/conversionTracker.ts` (1 `.rpc`)** → `GET /api/v2/daily-question/status` (get_daily_question_status) + `POST /api/v2/daily-question/increment` (increment_daily_questions) + `POST /api/v2/conversion-event` (track_conversion_event). conversionTracker se vuelve agnóstico: **todas las funciones `track*` pierden el 1er parámetro `supabase`** y llaman al endpoint; callers actualizados (`app/premium/page.tsx` ×3, `components/UpgradeLimitModal.tsx` ×3, el propio hook ×1). useDailyQuestionLimit queda **sin supabase**. Firmas SQL verificadas en BD. 7 tests incl. anti-inyección de user_id. (UpgradeLimitModal conserva sus rpcs propios de mensajes A/B — unidad aparte.) Ratchet `.rpc` 17→15. **#18 ✅ `components/MotivationalMessage.js`** (1 `.rpc get_personalized_message` + 2 `.from user_message_interactions`) → `GET /api/v2/motivational-message` (rpc + reacción previa en 1 llamada) + `POST /api/v2/motivational-message/interaction` (INSERT con `ON CONFLICT DO NOTHING`, preserva el no-op del 23505 original). Firma rpc verificada en BD. 6 tests. Ratchet `.from` 77→75/24→23, `.rpc` 15→14. **#19 ✅ `components/UserProfileModal.js`** (3 `.from` CROSS-USER: lee perfil de OTRO usuario desde ranking) → `GET /api/v2/user-public-profile?userId=X` con **gate de privacidad que replica la RLS**: `public_user_profiles`+`user_avatar_settings` son públicos (RLS `USING true`), pero `tests` SOLO se devuelven si `viewer===target` o admin (RLS `auth.uid()=user_id`+admin) — sin este gate, migrar a getAdminDb habría **filtrado los tests privados de cualquiera**. Fechas de hoy del cliente (corte medianoche por tz). 6 tests incl. el anti-fuga. RLS de las 3 tablas verificada en BD. Ratchet `.from` 75→72/23→22, `.rpc` 14→13 (comentario deflactado). **#20 ✅ `hooks/useAdminNotifications.ts`** (3 `.from` admin: 2 embeds de feedback + COUNT rate-limit) → `GET /api/v2/admin/pending-feedback-counts` (`requireAdmin`, embeds reconstruidos con `json_agg` y **lógica `needsAttention` portada verbatim** del hook para no divergir en los badges; rate-limit incluido). El hook pasa de 7 a 5 fetches paralelos, todos vía `adminFetch`, sin supabase. 5 tests incl. los casos de needsAttention. Ratchet `.from` 72→69/22→21. **#21 ✅ `app/admin/conversiones/page.tsx`** (3 `.from` admin: SELECT `upgrade_message_impressions` con embed + 2 UPDATE `upgrade_messages`) → `GET /api/v2/admin/upgrade-messages/impressions` (embed reconstruido) + `POST /api/v2/admin/upgrade-messages/update` (weight/is_active), ambos `requireAdmin` vía `adminFetch`. Quita `supabase`/`useAuth` huérfanos (gate vestigial; el acceso lo da el layout + requireAdmin). 6 tests. Ratchet `.from` 69→66/21→20. **NOTA: `app/admin/fraudes/page.tsx` (14 `.from`) NO migrada — 604 líneas con lógica de detección de fraude intrincada (agregación cruzada premium/devices/sessions); rushearla arriesga falsos positivos/negativos → merece pasada dedicada con contexto fresco.** **#22 ✅ páginas single-query batch 1:** `app/{guardia-civil,policia-nacional}/test/ortografia/page.tsx` (server components, `spelling_questions` público vía `createClient(SERVICE_ROLE)`+PostgREST → **Drizzle `getAdminDb` in-place**, sin endpoint; render único con ISR, sin trampa SSG) + `app/teoria/[law]/LawArticlesClient.tsx` (client, `target_oposicion` → reusa `GET /api/v2/onboarding/status`). Ratchet `.from` 66→63/20→17. **#23 ✅ `app/soporte/page.tsx`** (UPDATE `notification_logs` por conversación, filtro jsonb `@>`) → `POST /api/v2/notifications/mark-conversation-read` (user_id del token). 3 tests. Ratchet `.from` 63→62/17→16. **#24 ✅ `components/test/TestHubPage.tsx`** (server-component, `topics` público → Drizzle `getAdminDb` in-place, coherente con `getThemeQuestionCounts` ya-Drizzle del mismo fichero) **+ `app/test-personalizado/page.js`** (3 `.from` content_sections/content_scope/articles → `GET /api/v2/content-scope-config` que consolida los 3 + **corrige el N+1** de artículos; contenido público sin auth). 4 tests. Ratchet `.from` 62→58/16→14. NOTA: las analytics psicotécnicas (`app/mis-estadisticas/psicotecnicos`, `components/Statistics/PsychometricWeakAreasAnalysis`) tienen **embeds anidados profundos con filtro inner-join** → reconstrucción SQL delicada, pendientes de pasada cuidadosa. **#25 ✅ 3 unidades tractables:** `components/SharePrompt.js` (`.rpc get_user_share_stats` → `GET /api/v2/share-stats`, verifyAuth) + `app/landing/premium-edu/page.js` (`.rpc create_google_ads_user` → **reusa `POST /api/v2/auth/ensure-profile`** con `registrationSource:'google_ads'`+campaignId; su `signInWithOAuth` sigue siendo auth/Fase B) + `app/admin/configuracion/page.js` (`.from email_logs` con embed → `GET /api/v2/admin/email-logs`, requireAdmin). 5 tests. Ratchet `.from` 58→57/14→13, `.rpc` 13→11. **#26 ✅ `components/TestLayout.tsx`** (`.from test_questions`: dedup de question_orders ya guardados en el guardado en segundo plano) → `GET /api/v2/test-questions/saved-orders?testId=X` (JOIN `tests` + `WHERE t.user_id = TOKEN` → solo orders de tests propios; replica la RLS). `supabase` se mantiene en el componente porque se pasa como prop a un hijo (migración aparte). 3 tests. Ratchet `.from` 57→56/13→12. **#27 ✅ `components/UpgradeLimitModal.tsx`** (4 `.rpc` A/B de mensajes de upgrade: `get_random_upgrade_message` + `track_upgrade_message_{shown,click,dismiss}`) → `GET /api/v2/upgrade-message` + `POST /api/v2/upgrade-message/track` (discriminador shown/click/dismiss; shown usa user del token y devuelve impressionId). Se **elimina la prop `supabase`** del modal y de los **4 renderers** (PsychometricTestLayout, ExamLayout, TestLayout, OfficialExamLayout). Firmas SQL verificadas en BD. 7 tests. Ratchet `.rpc` 11→7. **#28 ✅ `app/admin/notificaciones/overview/page.js`** (4 `.from`: notification_events/email_events/user_notification_metrics/user_profiles) → `GET /api/v2/admin/notification-overview` (`requireAdmin`, enriquecimiento con perfil server-side; la agregación hourly/weekly/device/summary se queda en el cliente sobre los arrays enriquecidos — sin divergencia). 3 tests. Ratchet `.from` 56→52/12→11. **#29 ✅ `app/admin/notificaciones/events/page.js` (2 `.from` embed) + `users/page.js` (6 `.from`: lista + detalle)** → `GET /api/v2/admin/notification-events` (embed user_profiles!inner reconstruido) + `GET /api/v2/admin/notification-users` (métricas+perfil+conteos 30d vía COUNT/GROUP BY exacto) + `GET /api/v2/admin/notification-user-events?userId=X` (detalle por usuario), todos `requireAdmin`. 7 tests. Ratchet `.from` 52→44/11→9. **Trío admin/notificaciones COMPLETO** (overview+events+users). **#30 ✅ `app/admin/newsletters/page.tsx`** (1 `.from user_profiles` con `createClient` propio, flujo reintento de fallidos) → `POST /api/v2/admin/newsletters/resolve-users` (`requireAdmin`, emails→usuarios). Quita `getSupabase`/`createClient` huérfanos. 3 tests. Ratchet `.from` 44→43/9→8. **#31 ✅ `app/admin/feedback/page.tsx`** (13 `.from` dispersos en 2895 líneas, flujo de soporte) → **7 endpoints `requireAdmin` nuevos** (`feedbacks-list`, `messages` con embed sender, `waiting-conversations`, `user-conversations` con embed feedback, `conversation-feedback-id`, `update-feedback` —admin_user_id del token—, `start-conversation`) + **reuso** de `mark-viewed` (admin_viewed_at) y `close` (cerrar+resolver). **BUG PRE-EXISTENTE ARREGLADO:** el INSERT original de `start-conversation` usaba columnas inexistentes (`admin_id`, `started_by_admin`) → estaba roto (PGRST204); ahora usa `admin_user_id` (FK real). `supabase` se mantiene solo para la suscripción Realtime (`.channel`, Fase 5) + el helper `respondViaFeedbackEndpoint` (ninguno es `.from`). Las 7 queries verificadas contra BD. 8 tests. Ratchet `.from` 43→30/8→7. **#32 ✅ `app/admin/fraudes/page.tsx`** (14 `.from` en 5 funciones de detección de fraude) → **5 endpoints `requireAdmin`** (`/api/v2/admin/fraud/{premium,multi,bots,scripts,blocked}`), cada uno con la lógica de agregación **portada verbatim** server-side (queries Drizzle + mismos mapas/umbrales). Quita `supabase` huérfano. **BUG PRE-EXISTENTE ARREGLADO:** `bots` filtraba por `test_questions.answered_at` (columna **inexistente** → detección de bots rota/siempre vacía); ahora usa `created_at`. Las 5×queries verificadas contra BD. 3 tests. Ratchet `.from` 30→16/7→6. **TODAS las páginas admin migradas** (notificaciones, conversiones, newsletters, feedback, fraudes). **#33 ✅ `hooks/useIntelligentNotifications.ts` (parcial — 8 de 10 `.from` NO-canary)**: achievements → reusa `GET /api/v2/motivational/recent-tests?days=7`; study-reminders → query muerta `lastTest` **borrada** (no se usaba); system → `GET /api/v2/notifications/system`; avatar-rotation (2 `.from`) → `GET /api/v2/avatar/rotation`; avatar mark-read → `POST /api/v2/avatar/rotation/mark-read`; dispute mark-read → reusa `/api/dispute/mark-read`; system mark-read → `POST /api/v2/notifications/mark-read`. **Quedan SOLO los 2 `.from` + 1 `.rpc` de `loadProblematicArticles`, BLOQUEADO por su canary FASE 4/5** (no forzar). 4 endpoints verificados contra BD. 7 tests. Ratchet `.from` 16→8/6→6. **#34 ✅ `lib/api/dailyLimit.ts` (server-only, 4 `.rpc`)** → Drizzle in-place: `increment_daily_questions`×2, `get_device_daily_usage`, `get_daily_question_status` ahora vía `getAdminDb().execute(sql\`SELECT … fn(…)\`)` (quita `createClient(service_role)`+`supabase.rpc`). **Inicia el barrido de `lib/` server-side** (no cuenta en el ratchet de cliente, pero rompe Neon: PostgREST no existe allí). Ruta crítica (guardado de respuestas + límite diario); 4 RPCs verificadas contra BD. **#35 ✅ `lib/api/deviceLimit.ts` (server-only, 2 `.rpc`)** → `register_device`, `get_accounts_on_device` vía Drizzle (quita `createClient`+`supabase.rpc`). Antifraude device-limit, ruta crítica; verificadas contra BD. (Falsos positivos ya migrados: `chat/domains/stats`, `avatar-settings/queries`, `lifecycleFlag`, `setTargetOposicion`, `rollout/problematic-articles-logs` — sus matches eran comentarios.) **#36 ✅ `lib/cron/runWithLogging.ts` (3 `.rpc` cron_run_start/end)** → Drizzle `getAdminDb` (pool max:12, NO el getDb max:1 que el comentario evitaba — getAdminDb no satura). Se **mantiene el param `supabase`** en la firma (los crons lo siguen usando para su propio trabajo) → cero cambios en callers. Verificadas contra BD. NOTA: las páginas de psicotécnicos (`test/psicotecnicos/*`) cargan `psychometric_questions.*` con embeds → **delicadas (anti-scraping: no exponer respuesta + deep embeds + selección adaptativa)**, deferidas como las analytics. **#38 ✅ analytics psicotécnicas (historial PROPIO del usuario, NO anti-scraping — respuestas ya dadas, el embed NO incluye la opción correcta):** `app/mis-estadisticas/psicotecnicos/page.js` (1 `.from psychometric_test_sessions`) → `GET /api/v2/psychometric/sessions` (verifyAuth, user del token, filtro `session_type='psychometric'`+`days`); `components/Statistics/PsychometricWeakAreasAnalysis.js` (1 `.from psychometric_test_answers` con **embed anidado profundo** answers→questions→sections→categories + sessions) → `GET /api/v2/psychometric/weak-areas` (JOINs + `json_build_object` anidados reconstruyen el embed). **Ambos verificados contra la BD real antes de commitear.** **2 BUGS PRE-EXISTENTES ARREGLADOS:** (a) weak-areas pedía `difficulty_level`/`estimated_time_seconds`/`question_type`/`score` —columnas **INEXISTENTES**→ la query erraba y el análisis de áreas débiles salía SIEMPRE vacío; mapeo a `difficulty`/`time_limit_seconds`/`question_subtype`, `score` omitido. (b) sessions filtraba categoría por un embed-path inexistente en la tabla → roto; omitido (el cliente ya agrupa byCategory). 4 tests. Commit `b767771c`. Ratchet `.from` 8→6/6→4. **Quedan delicadas SOLO las test-pages psicotécnicas (anti-scraping).** Luego: useIntelligentNotifications loadProblematicArticles (canary FASE 4/5), test-pages psico, lib/ hot-paths (`testFetchers`/`lawFetchers`/`adaptiveQuestionSelection`/`psychometricDifficulty`), C2, C3, C4, Fase B.

### Barrido `lib/` hot-paths COMPLETO (2026-06-24) — no cuentan en el ratchet de cliente pero rompían Neon (PostgREST no existe allí)
- **#39 ✅ `lib/testFetchers.ts`** (commit `8db1f501`): queda SIN supabase. `fetchMantenerRacha` lee tests propios vía `GET /api/v2/studied-topics` (verifyAuth, token). `batchedTestQuestionsQuery` (2 `.from test_questions`) era DEAD CODE (0 callers) → borrado. **ARREGLA 5 tests rojos pre-existentes** de otra sesión (`remainingFetchersMigration.test.ts`): el mock de `getSession` no incluía `user` y `mapSession()` del puerto auth devuelve null sin user → token null → Bearer ausente; mock corregido + tests de `fetchMantenerRacha` actualizados a la pre-llamada studied-topics. 31/31 verde.
- **#40 ✅ `lib/lawFetchers.ts`** (commit `c3457d9c`): 661→194 líneas. Solo `getLawStats` (único con callers) sobrevive y YA era agnóstico. `fetchQuestionsByLaw` (300 líneas, `supabase.from('questions')` con `correct_option` DIRECTO a cliente = patrón anti-scraping prohibido) + 7 fetchers/wrappers + helpers + tipos = DEAD CODE → borrado.
- **#41 ✅ `lib/psychometricDifficulty.ts`** (commit siguiente): las 2 funciones vivas (PsychometricTestLayout las llama por pregunta) → `GET /api/v2/psychometric/difficulty` (rpc `get_effective_psychometric_difficulty` + fallback) + `GET /api/v2/psychometric/first-attempt`, ambos user_id del token. Dead code borrado (getQuestionDifficultyStats, analyzeDifficultyRebalance + tipos). Conservados helpers puros de presentación.
- **#42 ✅ `lib/adaptiveQuestionSelection.ts`** (commit siguiente): wrapper delgado → `POST /api/v2/psychometric/adaptive-select` (porta priorización no-vistas + filtro dificultad; **N+1 rpc → 1 query batched CROSS JOIN LATERAL** con array param validado UUID). FIX seguridad: historial filtrado por user_id del token. Dead code: analyzeCurrentPerformance (importada pero nunca llamada), shouldApply/generateRecommendation. logAdaptiveDecision OMITIDO (insertaba en `psychometric_adaptive_logs`, tabla inexistente → siempre no-op).
- **#43 ✅ test-pages psicotécnicas (carga de preguntas)** (commit siguiente): `app/auxiliar-administrativo-estado/test/psicotecnicos/page.js` (1 `.from` multi-categoría) + `[categoria]/page.js` (2 `.from`: questions + answered-count) → `GET /api/v2/psychometric/questions` (PÚBLICO + rate-limit anti-scraping; `to_jsonb(q.*)` + embeds categories/sections → shape IDÉNTICO al `!inner`) + `POST /api/v2/psychometric/answered-count` (verifyAuth, user del token, UUID-validado). **DECISIÓN PRODUCTO (Manuel 24/06): migración 1:1 — se CONSERVA `correct_option` en la carga** para preservar el feedback INSTANTÁNEO client-side del PsychometricTestLayout (líneas 340-348 hacen validación client-side; el guardado va por cola offline `psychometricSaveQueue`). Cerrar esa exposición exigiría mover la validación a `/api/answer/psychometric` (cambio de UX, no elegido). 6 tests. Ratchet `.from` 6→3/4→2.
- **ESTADO C1:** ratchet cliente **3/2** — **migración tractable de cliente COMPLETA**. Los 3 `.from` de cliente restantes son TODOS bloqueados: (a) 1 en `AuthContext` (dual-path flag-gated `AUTH_LIFECYCLE_VIA_API`, no tocar hasta el flip de Fase B), (b) 2 en `loadProblematicArticles` (BLOQUEADO por canary `problematic-articles` FASE 4/5). El barrido de `lib/` server-side también **COMPLETO**. Siguiente: desbloquear canary + flip Fase B para los 3 últimos; mientras, **C2** (guardrail user_id en CI), **C3** (tests aislamiento cross-user), **C4** (drop RLS).
  - **⚠️ GOTCHA CRÍTICO (regresión 23/06, ya corregida):** una ruta **SSG con `generateStaticParams`** (p.ej. `/oposiciones/[filtro]`, ~50 páginas) **NO debe consultar BD por conexión directa Drizzle (`getDb` max:1) en build**. `next build` pre-renderiza esas páginas con 13 workers en paralelo y CI pasa `DATABASE_URL` como build-arg → las conexiones directas **saturan el pooler** → páginas >180s → **el build de Docker falla y bloquea TODOS los deploys**. (Antes con `supabase.from`/HTTP escalaba; el swap a conexión directa lo rompió — commit `2d91b87a`.) **Fix:** `generateStaticParams() => []` + `export const dynamicParams = true` + `revalidate` → on-demand en runtime (max:1 por instancia, sin contención), cero DB en build. **Regla para C1:** antes de migrar una ruta `[param]` a Drizzle, comprobar si tiene `generateStaticParams`; si sí → quitar el pre-render. Páginas de render único (home, `/oposiciones`) son OK (1-2 conexiones en build, no fan-out); rutas dinámicas sin `generateStaticParams` (`/cursos/[slug]`) son OK (0 queries en build).
  - **Observabilidad del endpoint:** cada endpoint nuevo va con `withErrorLogging` (errores → `observable_events`/`validation_error_logs`) + tests de aislamiento. **NO se hace "todo observable" por migración** (canary HTTP, e2e Playwright, métricas success-path, SLOs, tracing son el roadmap propio `observability.md`/`canary-y-simulaciones.md`, en MVP). Win barato pendiente: añadir los endpoints user-scoped nuevos al canary HTTP (Nivel 3) cuando se monte.

### Deuda oculta: suites unit *legacy* que la migración deja en rojo (2026-06-26)

**Síntoma:** migrar un módulo de `supabase.rpc`/`createClient` a Drizzle (`getAdminDb().execute(sql\`\`)`) **no toca su test unit existente**, que sigue mockeando `@supabase/supabase-js` → el mock queda muerto (el código ya no lo llama) y la suite se pone roja en CI **sin que sea una regresión de producto**. Cada `#NN` de C1 añadió tests del *endpoint nuevo* pero las suites *legacy del módulo migrado* quedaron como deuda. Pasada de cierre 26/06: **11 → 0 suites rojas** (las 3 billing-critical de límites + el ratchet incluidas).

**Suites saneadas (commits `d228a450`, `05b9b72f`, `df4525fa`, `9d96547e`):**
- `__tests__/hooks/useDailyQuestionLimit.test.ts` (16) — mock `@/lib/auth` + `getAuthHeaders` + `fetch` URL-aware (delega el estado a `mockRpc` reutilizado) + firma `trackLimitReached(userId, …)` sin `supabase`.
- `__tests__/api/daily-limit-enforcement.test.ts` (49) y `device-limit-enforcement.test.ts` (35) — adaptador SQL→RPC (abajo).
- `__tests__/guardrails/auth-agnostic-ratchet.test.ts` (4) — la allowlist es un **ratchet que solo encoge**: al migrar `lib/api/admin-delete-user/queries.ts` (ya sin `supabase.auth.*`) su entrada quedó *stale* y el test exige quitarla.

**Patrón reutilizable — adaptador jest SQL→RPC** (para no reescribir los ~50/~40 escenarios al migrar una suite supabase→Drizzle):
1. Mockea la nueva capa: `@/db/client` `getAdminDb().execute` (→ `mockExecute`), `@/lib/api/auth/verifyAuth` (`verifyAuthOptional`/`verifyAuth`), `@/lib/cache/redis` `getOrSet` **passthrough** (`(_k,_ttl,fn)=>fn()`) + `invalidate`.
2. En `beforeEach`, un `mockExecute.mockImplementation` **traduce cada `execute(sql)` al viejo contrato `mockRpc('fn', {params})`** → así los setups `{data,error}` y las aserciones `toHaveBeenCalledWith('fn',{…})` de cada escenario siguen **intactos**:
   - **Enrutado por nombre de función:** `JSON.stringify(sqlObj).includes('increment_daily_questions' | 'register_device' | …)`.
   - **Extracción de params:** Drizzle deja los valores interpolados como **escalares crudos** en `queryChunks` (los StringChunk del texto son `{value:[…]}`; un `${null}` es un chunk `null` crudo). Filtro: `chunks.filter(c => !(c && typeof c==='object' && Array.isArray(c.value)))` → params en orden, **nulls preservados** (clave para `device_label`/`hw_fingerprint`).
   - **Moldeo de la respuesta:** `{error}` ⇒ `throw` (Drizzle lanza ante error de query → el código real hace fail-open/degraded/null igual que con supabase); `{data}` ⇒ `{ rows: Array.isArray(data) ? data : [data] }` (o `[{ total: data }]` para RPCs escalares `… AS total`).
3. Para auth, un shim de `verifyAuthOptional` que lee el `Bearer` del request y delega en el viejo `mockGetUser` (`{data:{user},error}`) conserva los escenarios de token/ataque sin tocarlos.
4. **Aviso TDZ:** la factory de `jest.mock` no puede referenciar el `const mock*` directo (se evalúa antes de inicializarse) → usar refs perezosas dentro de la factory (`(...a) => mockX(...a)`), no `() => ({ x: mockX })`.

**Por qué importa para lo que queda:** las unidades pendientes (`testFetchers`/`lawFetchers`/`adaptiveQuestionSelection`/`psychometricDifficulty` hot-paths, test-pages psico) arrastrarán la misma deuda — aplicar este adaptador evita reinventarlo y mantiene los escenarios verbatim. Antecedente ad-hoc del mismo problema: emailService (mock `@supabase`→`@/db/client` cadena thenable Drizzle-like, 11/11).

### Inventario RPCs restantes (para no perderlas)
- **Fase C (otras RPCs PostgREST de cliente):** OnboardingModal (`get_popular_custom_oposiciones`, `create_or_select_custom_oposicion`), `get_effective_psychometric_difficulty` (`lib/adaptiveQuestionSelection.ts`, `lib/psychometricDifficulty.ts`), `get_personalized_message` (MotivationalMessage), `get_user_share_stats` (SharePrompt), `get_random_upgrade_message`/`track_upgrade_message_{shown,click,dismiss}` (UpgradeLimitModal), `get_user_problematic_articles_weekly` (useIntelligentNotifications), `track_conversion_event` (conversionTracker), `get_daily_question_status`/`increment_daily_questions` (useDailyQuestionLimit cliente). + RPCs server-side ya en `getSupabaseAdmin().rpc` (dailyLimit, deviceLimit, admin endpoints) — PostgREST, migrar a Drizzle en el barrido de Fase C.

## Relación con el resto de roadmaps

Cuelga del [`docs/ARCHITECTURE_ROADMAP.md`](../ARCHITECTURE_ROADMAP.md) → **Fase C (swap Auth)** + **Fase P (desacople PostgREST/RLS)**. Es el **plan de ejecución unificado** de:

- La **Fase 4 (Auth)** de [`agnosticismo-supabase.md`](agnosticismo-supabase.md) — la facade/puerto y el cutover del hub ya están avanzados; este doc define el **emisor** (Auth.js) y su **firma** (RS256/JWKS).
- La parte **RLS** de [`desacople-postgrest-rls.md`](desacople-postgrest-rls.md) — su Fase P2/P3.

⚠️ **Cambio de dirección respecto a `desacople-postgrest-rls.md` §0:** aquel asumía *"el IdP se queda en Supabase Auth"*. **Esta decisión lo supersede**: migramos el IdP a Auth.js para cerrar la portabilidad de verdad. Lo demás de ese doc (PostgREST → Drizzle) sigue vigente y es prerrequisito de la Fase C de aquí.

## Contexto (por qué)

Queremos que **una futura migración de BD (Supabase → Neon/RDS) sea solo cambiar `DATABASE_URL`**, sin que falle ni el login ni la seguridad. Hoy quedan dos acoplamientos a Supabase que lo impedirían:

1. **Auth (emisión de tokens):** el login/sesión/OAuth los emite Supabase Auth (GoTrue). La *verificación* del token ya es agnóstica (`verifyJwtLocal`), pero la *emisión* no.
2. **RLS:** ~32 tablas de usuario tienen políticas con `auth.uid()` (función propietaria de Supabase). En Postgres plano `auth.uid()` no existe → esas lecturas devuelven 0 filas o petan, y si se quita RLS con tablas aún accesibles desde el cliente, hay **fuga cross-user**.

Decisiones tomadas (listón: **robusto, fiable, profesional, pensando en grande**):
- **Emisor:** Auth.js (NextAuth v5) self-hosted, $0, agnóstico. Solo Google OAuth (la app no tiene email/contraseña → **sin migración de hashes**).
- **Firma:** **RS256/JWKS asimétrica** (no el HS256 heredado). El emisor firma con clave **privada**; frontend Next y backend NestJS verifican con la **pública** publicada en un JWKS. Nadie con acceso a un verificador puede falsificar tokens; rotación de claves vía `kid` sin compartir secretos. Estándar para arquitectura multi-servicio.
- **RLS:** se deja de usar. Autorización en capa de app (`verifyAuth` + `WHERE user_id`), respaldada por **guardrail en CI + tests sistemáticos de aislamiento cross-user** (no se reimplementa `auth.uid()`).
- **Despliegue:** cada acoplamiento se voltea por separado, detrás de flag, reversible en <5 min, con **modo shadow** y **ventana de doble-aceptación** de tokens (cero logout forzado).

## Estado del terreno (confirmado en código)

- **Consumo de token ya agnóstico:** `lib/api/auth/verifyJwtLocal.ts` (HS256, `aud:'authenticated'`, claims `sub→userId/email/role`, `clockTolerance:5`) y wrapper `lib/api/auth/verifyAuth.ts` (modos `off/shadow/on` vía `JWT_LOCAL_VERIFY_MODE`).
- **Backend NestJS comparte semántica y secreto:** `backend/src/auth/jwt-verifier.ts` + `jwt.guard.ts` + `auth.module.ts` usan `jsonwebtoken`, HS256, mismo `SUPABASE_JWT_SECRET`, `aud:'authenticated'`. (El doc-comment que dice `jose` está stale.)
- **Emisión ya detrás de un puerto:** `lib/auth/types.ts` (`AuthClientPort`), `lib/auth/client.ts` (singleton, único punto de swap), `lib/auth/adapters/supabaseAdapter.ts`. `getAccessToken()` → `lib/api/authHeaders.ts` (singleflight + cooldown 30s).
- **Admin server:** `lib/auth/server.ts` `authAdmin.getUserById/deleteUser` vía `supabase.auth.admin.*` (única dependencia auth-admin de servidor).
- **Exposición RLS real (cliente sobre PostgREST, mayor de lo estimado):** `lib/testFetchers.ts` (`test_questions`, `tests`), `components/QuestionEvolution.tsx`, `components/PsychometricQuestionEvolution.tsx`, `app/Header.tsx` (`user_streaks`, `feedback_conversations`), `app/perfil/page.tsx` (`user_feedback`, `user_profiles`), `contexts/OposicionContext.tsx`, `hooks/useOnboarding.ts`, `components/OnboardingModal.tsx` (+RPCs custom-oposicion), `app/pregunta/[id]/page.tsx`, `lib/adaptiveQuestionSelection.ts`, `lib/psychometricDifficulty.ts`, y páginas admin (`fraudes/feedback/conversiones/newsletters`).
- **Acoplamientos de schema que romperían Neon:** ~32 `pgPolicy(auth.uid())` en `db/schema.ts`; `backend/src/email/medal-email.service.ts` (`JOIN auth.users`); RPCs `is_current_user_admin`/`is_user_admin` (leen `auth.uid()`); `lib/security/adminApiGuard.ts` (remote `supabase.auth.getUser`); `lib/auth/server.ts` (`supabase.auth.admin.*`); cualquier `supabase.from()/rpc()` restante (PostgREST no existe en Neon).
- Comandos: `npm run test`, `npm run test:unit`, `npm run test:integration`, `npm run test:precommit`, `npm run typecheck`, `npm run lint`, `npm run build`; backend `cd backend && npm test`. `next-auth`/`jose` aún NO instalados.

## Diseño profesional del token (RS256/JWKS)

- **Emisor (Auth.js):** firma el access token con **clave privada RSA** (alg `RS256`), `kid` en la cabecera. Clave privada en **AWS SSM Parameter Store** (`/vence-frontend/AUTH_JWT_PRIVATE_KEY`, SecureString) — AWS-native, igual que el resto de secretos.
- **JWKS público:** endpoint `GET /.well-known/jwks.json` (Next route) sirviendo la(s) clave(s) pública(s) con su `kid`. Cacheable. Rotación: añadir clave nueva → firmar con `kid` nuevo → retirar la vieja tras propagación.
- **Contrato de token (documentado, no truco):** `sub` = `user_profiles.id` (UUID existente, ver detalle crítico abajo), `email`, `role:'authenticated'`, `aud:'authenticated'`, `iss` propio (p.ej. `https://www.vence.es`), `iat/exp` (~1h), `kid` en header.
- **Verificadores (Next `verifyJwtLocal` + backend `JwtVerifier`):** se **amplían** para (a) aceptar `RS256` verificando contra el JWKS por `kid` (con caché, p.ej. `jose`/`createRemoteJWKSet`), y (b) **durante la transición**, seguir aceptando los `HS256` viejos de Supabase con el secreto compartido. Tras expirar los viejos (~1h post-cutover) se retira la rama HS256.
- ⚠️ Con RS256 la doble-aceptación **no es automática** (con HS256 reutilizado sí lo sería): es **explícita** — los verificadores prueban RS256/JWKS y, si falla, HS256-secreto durante la ventana. Más trabajo, pero correcto y futuro-proof.

## Detalle crítico de correctitud: el `sub`

Hoy `sub` = `auth.users.id` = `user_profiles.id`, y TODA la data del usuario cuelga de ese UUID. Google entrega un `sub` propio (distinto). En el callback `signIn`/`jwt` de Auth.js hay que **buscar `user_profiles` por email y fijar el `sub` del JWT al `user_profiles.id` existente** (y crear la fila para usuarios nuevos). Si se hace mal, cada usuario existente "pierde" sus tests/suscripciones. Se valida en shadow mode con el check `userid_mismatch` ya existente en `verifyAuth`.

---

## FASE A — Preparación agnóstica (sin cambio visible, sin tocar el emisor)

**Objetivo:** quitar todo acoplamiento Supabase que no sea el emisor, para que la Fase B sea un flip contenido.

- **A1. Enrutar TODO `supabase.auth.*` de cliente por el puerto** (`auth.*` de `@/lib/auth` o `getAuthHeaders()`): `lib/api/v2/{answer-and-save,complete-test,complete-onboarding}/client.ts`, `lib/testFetchers.ts`, `components/{PersistentRegistrationManager,OposicionDetector,ProgressiveRegistrationModal}.tsx`, `components/test/TemaTestPage.tsx`, `hooks/useDailyQuestionLimit.ts`, `app/premium/page.tsx`, varias `app/**/test/**/page.tsx`. Añadir `signInWithIdToken(idToken)` al `AuthClientPort` + adapter (GoogleOneTap). Refactor 1:1.
- **A2. Convertir 5 RPCs de ciclo de vida a endpoints Drizzle** (`create_google_ads_user`, `create_meta_ads_user`, `create_organic_user`, `check_user_access`, `activate_premium_user`, en `contexts/AuthContext.tsx`). Plpgsql param-driven (ya portables): mantener la función SQL pero llamarla desde nuevos endpoints `verifyAuth` (`POST /api/v2/auth/ensure-profile`, `/api/v2/access/check`, `/api/v2/premium/activate`) vía Drizzle. Detrás de flag `AUTH_LIFECYCLE_VIA_API`. Inventariar (Fase C) RPCs de `OnboardingModal` y psicotécnicas/adaptativas.
- **A3. Sustituir `is_current_user_admin`/`is_user_admin`** por el allowlist de email ya agnóstico: servidor → `requireAdmin()`/`isAdminEmail()` de `lib/api/shared/auth.ts`; gating de cliente → claim `email`/`role` del JWT. Mantener `manueltrader@gmail.com` en el allowlist.
- **A4. Confirmar estrategia de claves** (read-only; secreto compartido confirmado). Preparar clave privada RSA + JWKS para Fase B; marcar `medal-email.service.ts` (`JOIN auth.users`) para Fase C.

**Verificar:** `npm run typecheck && npm run lint && npm run test:unit`; smoke manual. **Rollback:** revert por fichero / flip `AUTH_LIFECYCLE_VIA_API`. **Riesgo:** bajo (A1) / medio (A2 = ruta de dinero → log-paralelo 24h antes de flip).

## FASE B — Cutover del emisor (Supabase Auth → Auth.js RS256/JWKS), riesgo más alto

> 📋 **Plan de ejecución paso-a-paso** (comandos SSM, diffs, tests, secuencia de flags, rollback y precondiciones): [`fase-b-ejecucion-authjs-rs256.md`](./fase-b-ejecucion-authjs-rs256.md). Esta sección es el QUÉ; ese doc es el CÓMO. Fase B desbloquea §3.1 de [`migracion-vercel-a-aws.md`](./migracion-vercel-a-aws.md) (BD→RDS/Neon) y cierra el SPOF del 503.

- **B1.** Auth.js v5 + Google con `jwt` callback (claims del contrato) y `jwt.encode/decode` custom que firman **RS256 con la privada de SSM** + `kid`. Mapear `sub = user_profiles.id` (lookup por email). Endpoints `GET /.well-known/jwks.json` y `GET /api/auth/token` (protegido por cookie) que devuelve el access token RS256 para el Bearer.
- **B2.** Adapter `lib/auth/adapters/authjsAdapter.ts` (implementa `AuthClientPort`). `getAccessToken()` lee de `/api/auth/token` preservando singleflight de `authHeaders.ts`. `completeOAuthCallback()` se simplifica → **borrar** la maquinaria PKCE/tres-canales/localStorage de `supabaseAdapter`. `onAuthStateChange` emulado desde `useSession()`.
- **B3.** Ampliar `verifyJwtLocal.ts` y `backend/src/auth/jwt-verifier.ts` a RS256/JWKS por `kid` + doble-aceptación HS256 en la ventana. Tests: token Auth.js pasa ambos verificadores.
- **B4. Secuencia (cada paso reversible):** (1) desplegar Auth.js dormido; (2) `JWT_LOCAL_VERIFY_MODE=shadow` 24-48h, 0 divergencias; (3) flip `AUTH_PROVIDER=authjs` (una línea en `lib/auth/client.ts`); (4) tokens viejos válidos hasta expirar (~1h), refresh re-emite RS256, sin logout forzado; (5) tras 1 semana limpia: `mode=on` + retirar rama HS256; (6) retirar `adminApiGuard` remoto → local; (7) retirar `authAdmin.deleteUser/getUserById` → `DELETE FROM user_profiles` (CASCADE) + `deleteUserData`; (8) retirar `supabaseAdapter` + PKCE + superficie auth de `lib/supabase.ts`.

**Verificar:** typecheck/build/test + backend test + E2E (signup nuevo id correcto; usuario existente ve histórico = prueba del `sub`; Bearer a `/api/v2/*` y `api.vence.es`; premium; admin); monitor `validation_error_logs`. **Rollback:** pasos 3-6 = flip `AUTH_PROVIDER=supabase` + `mode=shadow` (instantáneo). Pasos 7-8 = punto de no retorno (solo tras semana verde). **Riesgos:** mismatch `sub` (lookup email + assertion shadow); JWE vs JWT (test de decode); entrega del token al browser (no romper el 429).

## FASE C — Retirada de RLS (autorización en app + guardrails)

- **C1.** Migrar los `.from()` de cliente sobre tablas de usuario a endpoints `verifyAuth` con `WHERE user_id=<userId verificado>`. Páginas admin que leen datos de otros usuarios → endpoints `/api/admin/*` con `requireAdmin`. Tras esto, el browser no tiene cliente de datos anon-key.
- **C2. ✅ HECHO (2026-06-24, commit `39cd6bce`).** Guardrail `__tests__/guardrails/user-scoping-c2.test.ts` (corre en `test:unit`/CI, igual que el ratchet de C1). Escanea cada bloque `sql\`\`` en `app/api/**` + `lib/**` y FALLA si toca una de las **43 tablas user-scoped** (fixture extraído de los `pgPolicy(auth.uid())` de `db/schema.ts`) sin: (a) interpolar el id del token (`auth.userId`/`user.id`/**alias local resuelto** `const uid = auth.userId`), (b) ser endpoint `requireAdmin`, o (c) estar en la **ALLOWLIST** de excepciones revisadas (solo encoge). Allowlist actual (10): cron de integridad, 4 módulos de agregación admin, `medals` (ranking GROUP BY user_id), `official-exams/queries` (scoped por userId param), `official-exams/complete` (ownership-check en JS), `user-public-profile` (gate de privacidad #19), `emailService.server` (scoped por destinatario). **0 violaciones hoy.** Incluye self-tests que prueban que la detección tiene dientes. (Limitación conocida: análisis estático per-block; el scoping vía param de función o vía ownership-check en JS no es detectable y va por allowlist → la garantía conductual es C3.)
- **C3. ✅ HECHO (2026-06-24, commit `ee875f62`).** Suite `__tests__/security/crossUserIsolationC3.test.ts` (16 tests): para 8 endpoints user-scoped representativos (read/write/embed/post-con-ids), autenticado como A pero **colando el id de B** en body/query (`?userId=B`, `{userId:B}`), verifica que el SQL se acota a A y **B nunca aparece** en el SQL → el id sale siempre de `verifyAuth`, no del input. Además: sin token → 401 y 0 queries. Cubre psychometric/{sessions,weak-areas,difficulty,first-attempt,answered-count,adaptive-select} + studied-topics + disputes/mark-all-read. (Los endpoints de sesiones previas ya traen su propio test de aislamiento por-endpoint; esta suite añade el chequeo sistemático smuggle-B sobre la superficie nueva.)
- **C4. Drop de políticas `auth.uid()` (migración reversible).** 🟡 **DRAFT REGENERABLE** en `docs/roadmap/c4-drop-rls.draft.sql` vía `scripts/gen-c4-drop-rls.cjs` (FUERA de `supabase/migrations/` a propósito; **NO aplicar** hasta cumplir precondiciones). Lee `pg_policies` (autoritativo) y emite UP (`DROP POLICY IF EXISTS`) + DOWN (recreate verbatim comentado, rollback). **Estado 2026-06-26: 125 políticas `auth.uid()` sobre 57 tablas.** ⚠️ **El draft a mano del 25/06 estaba INCOMPLETO (99/40): matcheaba solo la cláusula `USING`/qual y se dejaba 26 políticas INSERT/UPDATE cuyo `auth.uid()` vive en `WITH CHECK`** → aplicarlo así dejaría esas 26 vivas y en Neon/RDS (sin `auth.uid()`) las **escrituras a ~17 tablas romperían** (`user_feedback`/`question_disputes`/`feedback_conversations`…). El generador captura `qual OR with_check` → cero huérfanas. **Como las tablas crecen al añadir oposiciones, RE-GENERAR el draft justo antes de aplicar y revisar el diff** (precondición #4). Resto de precondiciones del header: (1) C1+C2+C3 ~1 semana en prod; (2) resolver los 3 `.from` de cliente (AuthContext flag-gate + loadProblematicArticles canary) — si no, dropar RLS con ellos vivos = fuga; (3) solo dropa las auth.uid() (public-read/lockdown se quedan); probar contra staging. **Neon-blockers asociados:** ✅ `medal-email.service.ts` (`JOIN auth.users` eliminado, commit `e70cf3f6`); ✅ funciones `is_*_admin` ya no se llaman (reemplazadas por allowlist email en A3 — verificado, 0 llamadas reales).

**Verificar:** `test:integration` + guardrail + build + backend test; migración contra copia de staging antes. **Rollback:** down-migration recrea políticas. **Riesgo:** medio → desplegar C1+C2+C3 y reposar 1 semana en prod ANTES de C4 (el drop es entonces no-op).

## Qué seguiría rompiendo el swap a Neon/RDS si se deja sin hacer

> 🔎 **Verificable y re-ejecutable:** `node scripts/neon-readiness-audit.cjs` (solo lectura) audita la BD viva y reporta los blockers de ESQUEMA (puntos 1-3 + 8-10). Estado **2026-06-26**: 125 RLS auth.uid() · 3 fns auth.* · **51 FKs→auth.users** · 1 vista auth · 3 ext no-portables. (Cifras de `db/schema.ts` quedan desfasadas por lag de introspección — la BD viva manda.)

1. **125** políticas `pgPolicy(auth.uid())` sobre 57 tablas (no las ~32 de `db/schema.ts`, stale) → C4 (y C1 antes para no exponer). Regenerar con `gen-c4-drop-rls.cjs`.
2. `backend/src/email/medal-email.service.ts` `JOIN auth.users` → C4. ✅ eliminado (commit `e70cf3f6`).
3. **3 funciones SQL public** que invocan `auth.uid()/jwt()/role()`: `is_current_user_admin`, `assign_role` (se guarda con `is_current_user_admin()`), `get_current_user_roles`. **Verificado 26/06: 0 callers de app** (solo comentarios + migraciones de grant/revoke). PERO **2 RLS policies en `user_roles`** ("Admins can view all roles" / "Only admins can manage roles") las invocan → **acoplamiento auth INDIRECTO que C4 NO caza** (su match es por `auth.uid()` literal, y estas usan `is_current_user_admin()`). **DROP del cluster de 3 funciones + esas 2 policies juntos** (coordinado con C4). Sin esto, en RDS esas policies llaman a una función que usa `auth.uid()` → rotas.
4. `lib/security/adminApiGuard.ts` remote `supabase.auth.getUser` → B4 paso 6.
5. `lib/auth/server.ts` `authAdmin.*` → B4 paso 7.
6. Cualquier `supabase.from()/rpc()` restante → A2/A3 + C1 + barrido de crons/admin. Barrera anti-regresión: guardrail `createclient-service-role-ratchet`.
7. Renombrar envs `SUPABASE_*` → `DATABASE_URL`/`JWT_SECRET` (cosmético, último).
8. **🚨 53 FKs de tablas de APP → `auth.users`** (`user_profiles`, `user_medals`, `psychometric_*`, `user_feedback`, + columnas admin `reviewed_by`/`admin_user_id`/`resolved_by`…). **`auth.users` NO existe en Neon/RDS** (schema de GoTrue) → cada FK rompe el swap. **Blocker de esquema mayor y NO documentado antes.** **DRAFT regenerable** `docs/roadmap/fk-auth-users-to-profiles.draft.sql` vía `scripts/gen-fk-auth-to-profiles.cjs`: dropa `user_profiles.id → auth.users` (la identidad pasa a ser raíz) + **re-apunta 52 FKs a `user_profiles(id)`** preservando ON DELETE (38 CASCADE/10 NO ACTION/5 SET NULL). Coherente con "Dos capas distintas": la identidad se queda en `user_profiles`. **⚠️ PRECONDICIÓN DE DATOS (verificada 26/06): 18 columnas con 504 filas huérfanas** (user-ref en `auth.users` pero NO en `user_profiles` = usuarios sin perfil; las peores `*_pre_outbox`=143 c/u archivo, `pwa_sessions`=109, `pwa_events`=41) → el `ADD CONSTRAINT` FALLA hasta limpiarlas (DELETE de la huérfana o backfill del perfil). El draft trae el orphan-check baked-in; re-ejecutar el generador antes de aplicar.
9. **Vista `v_attribution_coverage`** hace `FROM auth.users u LEFT JOIN user_acquisition ua` (usa `u.created_at::date` = día de alta + `u.id`). Reescritura directa contra `user_profiles` (tiene `id` + `created_at`), **aplicable ya** (quita el blocker temprano, sin esperar al swap). Único delta verificado 26/06: `auth.users`=8790 vs `user_profiles`=8782 → **8 usuarios sin perfil** desaparecen del conteo `altas` (0.09%, despreciable para un dashboard de cobertura). SQL listo:
   ```sql
   CREATE OR REPLACE VIEW public.v_attribution_coverage AS
   SELECT up.created_at::date AS dia, count(*) AS altas,
          count(ua.user_id) AS con_canal,
          round(100.0 * count(ua.user_id)::numeric / NULLIF(count(*),0)::numeric, 1) AS pct_cobertura,
          count(*) FILTER (WHERE ua.channel='google_ads') AS google_ads,
          count(*) FILTER (WHERE ua.channel='meta_ads') AS meta_ads,
          count(*) FILTER (WHERE ua.channel='organic') AS organic,
          count(*) FILTER (WHERE ua.channel='direct') AS direct,
          count(*) FILTER (WHERE ua.user_id IS NULL) AS sin_atribucion
   FROM public.user_profiles up
   LEFT JOIN user_acquisition ua ON ua.user_id = up.id
   GROUP BY (up.created_at::date);
   ```
10. **Extensiones no portables por defecto a Neon/RDS** (verificado uso real 26/06): **`pg_net` + `supabase_vault` SÍ se usan** — la función viva `notify_temario_change` hace `net.http_post(...)` leyendo el secreto de `vault.decrypted_secrets` (webhook que revalida el temario en Vercel, `supabase/migrations/20260406_webhook_revalidate_temario.sql`). En RDS hay que reescribir ese trigger→HTTP (mover la revalidación a la app/Fargate; secreto → SSM/Secrets Manager). **`http` (standalone): 0 funciones la usan → DROPPABLE** (un blocker menos). Las demás (`vector`, `pg_trgm`, `uuid-ossp`, `pgcrypto`, `unaccent`, `pg_stat_statements`) sí están disponibles.

## Secuencia de PRs
Fase A: 3 PRs (A1 refactor; A2 endpoints tras flag; A3 admin-check). Fase B: 1 PR (Auth.js dormido + adapter RS256/JWKS + verificadores) + flips por env. Fase C: PR C1 (endpoints), PR C2/C3 (tests), PR C4 (migración). Cada PR desplegable y revertible.

## Ficheros críticos
- `lib/api/auth/verifyJwtLocal.ts` + `lib/api/auth/verifyAuth.ts` (RS256/JWKS + doble-aceptación + flip de modo)
- `backend/src/auth/jwt-verifier.ts` (ampliar a RS256/JWKS)
- `lib/auth/client.ts` (swap `AUTH_PROVIDER`), `lib/auth/types.ts`, `lib/auth/adapters/supabaseAdapter.ts` (plantilla → `authjsAdapter.ts`)
- `contexts/AuthContext.tsx` (5 RPCs → endpoints; mapeo `sub`)
- `lib/auth/server.ts` (retirar `authAdmin.*`), `lib/security/adminApiGuard.ts`
- `db/schema.ts` (~32 políticas a dropear en C4), `backend/src/email/medal-email.service.ts`
- Nuevos: `app/api/auth/[...nextauth]`, `app/.well-known/jwks.json/route.ts`, `app/api/auth/token/route.ts`, `lib/auth/adapters/authjsAdapter.ts`, endpoints A2/C1.
