### [T-102] ✅ [CERRADO 24/07] Outlook Ctrl+S: flip de clave de la pregunta `39cb186d` (needs_human) + responder a Laura
- **✅ Cerrada 24/07:** flip aplicado `correct_option` C(Ctrl+S)→**B(Ctrl+U)** + explicación reescrita + `needs_human→approved` (`admin_marked_perfect`, `is_active=true`). **Clave del enredo:** la página **ES** de Microsoft está mal traducida (arrastra Subrayar=Ctrl+S a la fila "no leído"); verificado contra la página **EN** de Microsoft → no leído = **Ctrl+U** en todas las versiones. La impugnación de Laura `8cdcba44` ya estaba `resolved` con Ctrl+U → el flip cumple lo prometido, nada que reenviar. Detalle: memoria `project-informatica-atajos-es-vs-en`.
- **Qué (histórico):** en la auditoría del artículo de atajos de Outlook (T-079), la pregunta **`39cb186d`** quedó en `needs_human`: su clave marcada era **C) Ctrl+S** para "marcar como no leído", que es **FALSO** (Ctrl+S = Guardar). La correcta es **B) Ctrl+U** (verificado multi-fuente + su pregunta hermana `301c6cbd` + el artículo `bf87a5b0`). Requiere **flip de clave C→B + reescribir la explicación** — política: **NUNCA auto-flip**, lo decide un humano.
- **Cabo asociado:** tras el flip, cerrar la **impugnación de Laura** (`8cdcba44`) que originó T-079 (flujo de disputes: borrador + OK de Manuel).
- **Estado:** la pregunta está fuera de servicio (`is_active=false`) con el diagnóstico completo en las notas del AVR. Solo falta la decisión humana del flip.

