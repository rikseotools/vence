### [T-639] 🟡 [ABIERTO 07/08] `vence_lector`: RLS activo sin política bloquea `daily_question_usage` (0 filas en silencio), no cubierta por T-573/T-574

**CÓMO SE DESTAPÓ.** Investigando [T-450] (el modo examen no cobra cupo diario, tarea que estaba revisando): su propio canario `scripts/canary-cupo-vs-respuestas.cjs` necesita `daily_question_usage` para comparar respuestas reales contra cupo consumido. Con `VENCE_LECTOR_URL` la consulta completa fallaba con `permission denied for table user_profiles` (el JOIN para filtrar `plan_type='free'`) — un error DISTINTO. Aislando la tabla sola (`SELECT count(*) FROM daily_question_usage`) no dio error: dio `0`, indistinguible de "tabla vacía de verdad" salvo cruzando el catálogo.

**MEDIDO contra RDS (07/08), no supuesto:**
- `pg_class.relrowsecurity = true` para `daily_question_usage`.
- `pg_policies` con esa tabla = **0 filas** (las 3 políticas que la tabla ya tiene — "Users can read/insert/update own usage" — son para el rol `public` de Supabase Auth vía `auth.uid()`, no aplican a `vence_lector`, rol distinto).
- `information_schema.role_table_grants`: el SELECT a `vence_lector` SÍ está concedido (de `20260805_rol_lector_flota.sql`).
- Mismo mecanismo, exactamente, que [T-573] arregló para `test_questions`/`tests` y que [T-574] cazó como falso-verde del canario (`lib/db/rlsSelectBlocked.cjs`). **Ya estaba anotada como sospechosa sin confirmar** en la lista de 85 tablas de esas dos fichas: *"daily_question_usage, psychometric_test_sessions… que podrían estar SILENCIOSAMENTE ciegas igual que estas dos"*. No estaba en `DEBE_LEER` de `scripts/canary-rol-lector.cjs` en aquel momento, así que ni T-573 ni T-574 la vieron como en-alcance. Ahora sí hace falta: la usa el canario de [T-450].
- **Columnas revisadas** (`db/schema.ts`): `id`, `user_id` (uuid), `usage_date`, `questions_answered`, `last_question_at`, `created_at`, `updated_at`. Ningún identificador directo — mismo perfil de riesgo que `test_questions`/`tests`, ya concedidas.

**HECHO:**
- Migración `supabase/migrations/20260807_rls_daily_question_usage_lector.sql` — política `flota_lector_lee`, `FOR SELECT TO vence_lector USING (true)`, idempotente (`DROP POLICY IF EXISTS` antes), solo esa tabla, solo `vence_lector` (NO `vence_coordinacion`). Con guarda propia: aborta si el GRANT de tabla no está.
- `daily_question_usage` añadida a `DEBE_LEER` en `scripts/canary-rol-lector.cjs`, con su motivo (T-450). Corrido contra RDS vivo (07/08): pasa de "no comprobada" a **roja, correctamente** (18/21, antes 18/19 con test_questions y ai_verification_results ya rojas de antes — la tabla nueva se suma al mismo estado, no lo empeora).
- **Capas:** `__tests__/db/rlsDailyQuestionUsageLectorMigration.test.js` (6 tests, mismo patrón que `rlsTestQuestionsLectorMigration.test.js` de T-573: forma del fichero, idempotencia, alcance exacto, solo SELECT, guarda de GRANT) — verde. `__tests__/db/rlsSelectBlocked.test.js` (núcleo ya existente de T-574) sigue verde, sin tocar.

**NO PODIDO: aplicar la migración a producción.** Igual que T-573: este worker no tiene privilegio `CREATE POLICY` sobre RDS — es DDL en producción, decisión de Manuel. Recomiendo agruparla con la aplicación pendiente de [T-573] (misma naturaleza, mismo momento, mismo riesgo bajo) en vez de una DDL suelta.

**Consecuencia si NO se aplica:** [T-450] (revisión de otra entrega, en curso) sigue sin poder correr `canary:cupo-vs-respuestas` ni reconciliarlo contra el "corte directo" con mis credenciales — el bloqueo de `daily_question_usage` se suma al de `user_profiles` (ese segundo es estructural y no lo resuelve esta migración: el filtro `plan_type='free'` necesita `user_profiles`, que sigue —correctamente— denegada por PII).

**Relacionadas:** [T-450] (quien la destapó y quien la necesita), [T-573] (el arreglo gemelo para test_questions/tests, mismo patrón, aplicación pendiente agrupable), [T-574] (el detector que demuestra que esto es real y no un falso positivo).
