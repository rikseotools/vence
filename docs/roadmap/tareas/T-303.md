### [T-303] 🟠 [ABIERTO 30/07 — 06/08: arreglado y testeado, esperando deploy] El detector `bot_detected` marca opositoras REALES: 5 señales, 5 usuarias distintas, la misma huella de Chrome Android
- **Qué pasa:** `bot_detected` (detección client-side, `botScore 90`) lleva **5 señales en 14 días sobre 5 usuarias distintas**, todas con la MISMA huella: `no_plugins` + `zero_dimensions` + `botd:headless_chrome`, siempre sobre `Chrome 150 / Android 10`. **Las revisadas eran todas usuarias reales**, y no de forma dudosa:
  - `mariasoledadparrabaeza` (29/07): registrada el 21/05, **199 respuestas**, 12 tests practice completados y un examen, estudiando hasta las 23:41.
  - `cabrerayurely` (30/07): cuenta creada a las 21:55 y **25 respuestas reales** entre las 22:03 y las 22:40 — exactamente el tope del plan free. Un bot no completa tests hasta agotar su cupo.
- **La causa probable (a verificar, no conclusión):** `botd` clasifica como *headless* a los **navegadores embebidos / WebView** de Android — el que abre Instagram, Facebook o Gmail al pinchar un enlace. `zero_dimensions` encaja con eso. Si es así, estamos marcando como bots justo a las usuarias que nos llegan desde redes, que es por donde entra la publicidad.
- **Impacto:** 🟠 hoy **no bloquea a nadie** (F0 es solo detección), así que el daño es de RUIDO: cada falso positivo gasta triaje humano y, sobre todo, **entrena a ignorar el badge** — la misma lección de [T-047]/[T-113]/[T-179]. Pero si algún día se cablea enforcement sobre esta señal sin arreglarla antes, estaríamos bloqueando clientas.
- **Cómo (mismo método que funcionó en [T-179], que es el precedente directo):** NO tocar el umbral a ojo. Sacar la **población** de señales `bot_detected` y mirarlas una a una contra la actividad real de cada cuenta (respuestas guardadas, tests completados, minutos de sesión); cruzar con `userAgent` y `screenResolution` para confirmar si el patrón es WebView; y solo entonces decidir el discriminante — que probablemente NO sea el `botScore`, igual que en cosecha no era el ratio. Un candidato obvio: **una cuenta que responde y completa tests no es un bot**, por mucho que su fingerprint sea raro. Añadir los casos medidos como test de regresión.
- **Origen:** triaje de señales de fraude del 29 y 30/07. Las 5 están `dismissed` con su evidencia en las notas.


- **📌 Contexto para retomarla (30/07):** el método que funcionó en [T-179] es el que hay que repetir — **sacar la población y mirarla una a una**, no tocar el umbral a ojo. Allí resultó que el discriminante no era el ratio sino una dimensión que no se medía (la amplitud temporal); aquí probablemente tampoco sea el `botScore`. Candidato razonable: **una cuenta que responde y completa tests no es un bot**, por raro que sea su fingerprint. Las 5 señales están `dismissed` con la evidencia en sus notas.

- **MEDIDO (no de la ficha): la matemática exacta del score 90.** `hooks/useBotDetection.ts` puntúa `zero_dimensions`=+30 y `botd:headless_chrome`=+60; `no_plugins` da **0 puntos desde el 15/04/2026** (el propio código ya documenta que Chrome moderno no expone `navigator.plugins` a nadie, humano o no). 30+60 = **90 exacto** — justo el umbral en el que `lib/security/botAlertPolicy.ts` (T-185, 28/07) decide abrir expediente. T-185 ya había subido el umbral de 60 a 90 para matar 261 falsos positivos históricos de ESTE MISMO patrón — pero como estas 5 alertas llevan `zero_dimensions` de más, quedan justo `>=90` y siguen pasando el filtro.
- **La causa probable, y sigue sin demostrarse del todo — lo digo así, no vestido de certeza:** el patrón encaja con navegadores **WebView embebidos** de Android (el que abre Instagram/Facebook/Gmail al pinchar un enlace), que comparten con un headless real la falta de chrome UI. **No pude confirmarlo contra las 5 alertas originales**: `fraud_alerts` tiene `relrowsecurity=true` y **CERO políticas** para `vence_lector` — mismo mecanismo ya diagnosticado en [T-573]/[T-574] (RLS activo sin política = 0 filas SIN error, indistinguible de "no hay alertas"). Medido en vivo: `SELECT count(*) FROM fraud_alerts` da 0 con cualquier filtro, y `SELECT relrowsecurity, ... FROM pg_policy` confirma el bloqueo. **No lo he intentado arreglar**: `fraud_alerts.details` lleva la IP cruda, así que este bloqueo probablemente sea el MISMO comportamiento correcto que T-573 ya dejó escrito para `fraud_confirmations` (PII, bloqueado a propósito) — no una migración pendiente como `test_questions`.
- **Lo que SÍ pude medir, con una muestra MÁS ANCHA que las 5 del ficha:** `observable_events` con `event_type='scraping_force_challenge_set'` (dispara con el MISMO `score>=90`) SÍ es legible. **10 usuarias distintas a score EXACTO 90** entre el 12/07 y el 06/08 — todas con **0 filas en `test_questions`** (nunca respondieron). Pero **una de esas diez** (`f7716a15…`) tiene 11 días de actividad con `daily_questions_served` subiendo a **~800 preguntas SERVIDAS** y `daily_question_usage` completamente vacío: la firma exacta de `harvest_no_answer` (cosecha real, con navegador, sin responder jamás). **La muestra ampliada NO confirma "son todo WebViews"** — mezcla el falso positivo del ficha con al menos un cazador real, y sin poder leer `evidence`/`userAgent` de cada caso no se puede separar uno de otro por señal técnica.
- **EL ARREGLO, por eso, NO fue "esta combinación de evidencia nunca alerta"** (habría dejado pasar a `f7716a15…`): es el discriminante que la propia ficha ya proponía — **una cuenta que ha respondido de verdad no es un bot, por raro que sea su fingerprint** — cruzado en servidor, nunca fiado del cliente:
  - `lib/security/botAlertPolicy.ts`: `decideBotAlert` para `bot_detected` ahora exige, ADEMÁS del score, que NO haya evidencia DURA de automatización (`webdriver_detected`/`automation_framework`/`puppeteer_detected` — esas nunca se eximen, aunque la cuenta responda: un navegador controlado que además rellena respuestas reales es un caso MÁS grave, no menos) y que la cuenta tenga **≥5 respuestas reales guardadas** (`MIN_REAL_ANSWERS_TO_TRUST`, documentado: entre las 0 de un cosechador real medido y las 25-199 de las usuarias reales del ficha). Sin ambos datos nuevos (evidencia + respuestas), se comporta exactamente como antes — fail-safe.
  - `app/api/fraud/report/route.js`: para `bot_detected` consulta ahora `test_questions` en servidor (mismo patrón que ya usaba `suspicious_behavior`) y pasa `evidence`+`realAnswers` a la política.
- **Capas:** `__tests__/security/botAlertPolicy.test.ts` (9 casos nuevos: exime con 199/25 respuestas + patrón blando, sigue alertando con 0 respuestas — el caso `f7716a15…` — y con <5, nunca exime evidencia dura aunque haya 1000 respuestas, fail-safe sin datos) + `__tests__/security/fraudReportAuth.test.ts` (5 casos nuevos, extremo a extremo con el mock de `db().execute` real). **Efecto lateral útil:** el mock de `drizzle-orm` en ese fichero NO tenía `sql` mockeado — la consulta de servidor de `suspicious_behavior` (ya existente) llevaba desde su creación pasando SIEMPRE por el catch/fail-safe sin que ningún test ejerciera el `.execute()` de verdad. Arreglado de paso (ver comentario en el test).
- **NO desplegado — necesita build+deploy de frontend** (toca `app/api/...` y `lib/security/...`). Tarea pausada `--tras-deploy` en vez de cerrada en falso.
- **Relacionadas:** [T-179] (el método que se repitió: sacar la población, no tocar el umbral a ojo), [T-185] (donde nació `botAlertPolicy.ts` y el umbral 90), [T-573]/[T-574] (mismo mecanismo de RLS-sin-política, ahí para `test_questions`, aquí probablemente correcto para `fraud_alerts` por la IP).

> **🔁 RE-EMPUJADA (08/08, w1) — el fix nunca llegó a mergearse, y el resume_check pasado hacía
> parecer que solo faltaba desplegar.** La rama original (`flota/w3`, commit `7bce7173a`) se creó
> ANTES de que [T-532] ("una ficha = un fichero") migrara `docs/roadmap/tareas-pendientes.md` a
> índice generado — confirmado: `git merge-base --is-ancestor 53d05e6a8 7bce7173a` da NOT
> ancestor. Y **nunca se mergeó**: confirmado con `git branch -r --contains 7bce7173a` (solo en
> ramas de trabajador, nunca `origin/main`) y con `grep MIN_REAL_ANSWERS_TO_TRUST` sobre
> `origin/main:lib/security/botAlertPolicy.ts` → sin resultado. La tarea se reabrió (quedó `open`
> con el `resume_check` de "verificar en producción tras desplegar" intacto) probablemente porque
> algún deploy de frontend NO RELACIONADO disparó el `wake_on_deploy` sin que este fix concreto
> viniera dentro — el mecanismo despierta por SUPERFICIE, no por commit. Verificar "en
> producción" en ese estado habría confirmado que el bug SIGUE ahí, sin decir por qué (parecería
> un fix roto, cuando el fix nunca llegó a existir en producción).
> - **Reaplicado limpio sobre `origin/main` actual** (`git cherry-pick 7bce7173a`, conflicto SOLO
>   en el monolito generado — resuelto descartándolo y escribiendo aquí, en el fichero fuente
>   real). El código (`lib/security/botAlertPolicy.ts`, `app/api/fraud/report/route.js`) y los
>   dos ficheros de test aplicaron BYTE A BYTE sin conflicto.
> - **Re-verificado, no solo re-aplicado:** las 14 pruebas nuevas (9+5) corridas en worktree
>   aislado sobre `origin/main` fresco: **44/44 verde** (`botAlertPolicy.test.ts` +
>   `fraudReportAuth.test.ts` juntos). `npx eslint` limpio en los 4 ficheros tocados. `tsc --noEmit`
>   del proyecto completo (con `NODE_OPTIONS=--max-old-space-size=6144`, el heap por defecto hace
>   OOM en esta máquina): **0 errores**.
> - **Pausada correctamente esta vez**, con `--tras-deploy --superficie frontend`, para que el
>   próximo deploy de frontend la despierte — y esta vez SÍ llevará el fix dentro, porque está en
>   la rama que se entrega ahora.
> - **⚠️ CORRECCIÓN GRANDE, encontrada al re-verificar antes de empujar (no la busqué a propósito,
>   apareció al comprobar la afirmación del `f7716a15…` con datos frescos):** la línea 15 de esta
>   ficha (y el comentario espejo en `lib/security/botAlertPolicy.ts`) afirmaban que las 10
>   usuarias a `scraping_force_challenge_set` score=90 tenían **0 filas en `test_questions`**, y
>   que `f7716a15…` era un **"cazador real confirmado"** por la firma `daily_questions_served`
>   alto + `daily_question_usage` vacío. **Re-medido con la MISMA consulta que usa el propio
>   fix** (`user_answer IS NOT NULL AND <>'' AND <>'BLANK'`) contra `test_questions` real: **es
>   falso — las 10 tienen respuestas reales**, entre **16 y 5.734**, ninguna en 0.
>   `f7716a15…` en concreto: **5.734 respuestas reales** (no 0), ya tenía **115 esa misma tarde**
>   del primer aviso (12/07), con `time_spent_seconds`/`created_at` de una sesión real (5-97 s
>   por pregunta, espaciadas minutos, no una ráfaga de inserciones).
>   - **SOSPECHO, sin poder confirmarlo** (`user_profiles` es PII, fuera de `VENCE_LECTOR_URL`):
>     que la cuenta es PREMIUM y por eso `daily_question_usage` — que solo importa para el tope
>     diario del plan FREE (`lib/api/daily-limit/queries.ts`: *"Premium users: no limit"*) — dejó
>     de escribirse para ella; no por cosecha. No es una conclusión, es la explicación más simple
>     que encaja con los datos que SÍ pude ver.
>   - **Esto NO invalida el arreglo — lo refuerza.** Si el 100% de la muestra medida (10 de 10)
>     son falsos positivos y CERO tienen un caso confirmado de cosecha real, el discriminante
>     (actividad real ⇒ no abrir expediente) es más defendible, no menos: hoy no hay ningún dato
>     que sostenga que la vía "score alto + evidencia blanda + 0 respuestas" case algo de verdad,
>     así que mantenerla fail-safe (seguir alertando SIN datos, no exentar a ciegas) es prudencia,
>     no una prueba de que haga falta.
>   - **Corregido en el código y en los tests, no solo aquí**: el comentario grande de
>     `botAlertPolicy.ts` y el de `MIN_REAL_ANSWERS_TO_TRUST` ya no citan a `f7716a15…` como
>     caso confirmado; el test que antes decía *"(cosechador real)"* ahora dice *"(caso sintético,
>     fail-safe)"* — la aserción no cambió (sigue siendo el comportamiento correcto que se quiere
>     mantener), solo la NARRATIVA que decía tener una prueba real cuando no la tenía.
>   - **Lección para quien retome fraude/bot-detection con datos de `observable_events`:** un
>     dato agregado (`served` alto, `usage` vacío) puede tener una explicación mundana (premium)
>     que solo se descarta cruzando con la tabla de comportamiento real (`test_questions`), no
>     con otro agregado. Cruzar agregado-contra-agregado sin la fuente primaria es exactamente el
>     tipo de error que esta ficha ya advertía evitar en el "Cómo" original: *"NO tocar el umbral
>     a ojo… mirarlas una a una contra la actividad real de cada cuenta"* — y aun así, un pase
>     anterior lo hizo con UN caso sin la fuente primaria.
