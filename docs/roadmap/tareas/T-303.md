### [T-303] 🟠 [ABIERTO 30/07] El detector `bot_detected` marca opositoras REALES: 5 señales, 5 usuarias distintas, la misma huella de Chrome Android
- **Qué pasa:** `bot_detected` (detección client-side, `botScore 90`) lleva **5 señales en 14 días sobre 5 usuarias distintas**, todas con la MISMA huella: `no_plugins` + `zero_dimensions` + `botd:headless_chrome`, siempre sobre `Chrome 150 / Android 10`. **Las revisadas eran todas usuarias reales**, y no de forma dudosa:
  - `mariasoledadparrabaeza` (29/07): registrada el 21/05, **199 respuestas**, 12 tests practice completados y un examen, estudiando hasta las 23:41.
  - `cabrerayurely` (30/07): cuenta creada a las 21:55 y **25 respuestas reales** entre las 22:03 y las 22:40 — exactamente el tope del plan free. Un bot no completa tests hasta agotar su cupo.
- **La causa probable (a verificar, no conclusión):** `botd` clasifica como *headless* a los **navegadores embebidos / WebView** de Android — el que abre Instagram, Facebook o Gmail al pinchar un enlace. `zero_dimensions` encaja con eso. Si es así, estamos marcando como bots justo a las usuarias que nos llegan desde redes, que es por donde entra la publicidad.
- **Impacto:** 🟠 hoy **no bloquea a nadie** (F0 es solo detección), así que el daño es de RUIDO: cada falso positivo gasta triaje humano y, sobre todo, **entrena a ignorar el badge** — la misma lección de [T-047]/[T-113]/[T-179]. Pero si algún día se cablea enforcement sobre esta señal sin arreglarla antes, estaríamos bloqueando clientas.
- **Cómo (mismo método que funcionó en [T-179], que es el precedente directo):** NO tocar el umbral a ojo. Sacar la **población** de señales `bot_detected` y mirarlas una a una contra la actividad real de cada cuenta (respuestas guardadas, tests completados, minutos de sesión); cruzar con `userAgent` y `screenResolution` para confirmar si el patrón es WebView; y solo entonces decidir el discriminante — que probablemente NO sea el `botScore`, igual que en cosecha no era el ratio. Un candidato obvio: **una cuenta que responde y completa tests no es un bot**, por mucho que su fingerprint sea raro. Añadir los casos medidos como test de regresión.
- **Origen:** triaje de señales de fraude del 29 y 30/07. Las 5 están `dismissed` con su evidencia en las notas.


- **📌 Contexto para retomarla (30/07):** el método que funcionó en [T-179] es el que hay que repetir — **sacar la población y mirarla una a una**, no tocar el umbral a ojo. Allí resultó que el discriminante no era el ratio sino una dimensión que no se medía (la amplitud temporal); aquí probablemente tampoco sea el `botScore`. Candidato razonable: **una cuenta que responde y completa tests no es un bot**, por raro que sea su fingerprint. Las 5 señales están `dismissed` con la evidencia en sus notas.

- **✅ EL ARREGLO YA ESTÁ EN `main` (06/08, commit `7bce7173a`) — pendiente SOLO de deploy frontend, como cualquier otro trabajo agrupado.** `decideBotAlert` (`lib/security/botAlertPolicy.ts`) exige ahora, para `bot_detected` con score≥90: ausencia de evidencia DURA de automatización (`webdriver_detected`/`automation_framework`/`puppeteer_detected` — ésas NUNCA se eximen) y `MIN_REAL_ANSWERS_TO_TRUST=5` respuestas reales guardadas en `test_questions` (nunca fiado del cliente, `app/api/fraud/report/route.js` lo consulta en servidor). Sin ambos datos, se comporta exactamente igual que antes (fail-safe). 29 tests en `__tests__/security/botAlertPolicy.test.ts`.

- **🚨 CORRECCIÓN IMPORTANTE (08/08) — la ficha/resume_check de un pase anterior afirmaba que este commit "nunca llegó a mergearse" y REABRIÓ el trabajo por error, generando una rama duplicada.** Verificado hoy con `git merge-base --is-ancestor 7bce7173a origin/main` → **SÍ es ancestro**, y `git log --oneline origin/main -- lib/security/botAlertPolicy.ts` solo tiene dos commits, uno de ellos `7bce7173a`. El error de verificación de aquel pase (probablemente un `grep` sobre un checkout local no actualizado, o sobre el `.js` en vez del `.ts`) llevó a re-aplicar el mismo fix por cherry-pick sobre una base de `main` que en realidad NO era la actual — el resultado (rama `flota/T-303-bot-detected-webview-exime`, commit `ed6d19c21`, YA PUSHEADA a origin) **queda desactualizada y no debe mergearse tal cual**: comparada contra el `origin/main` real de hoy, borra ~30 ficheros (tests, `CLAUDE.md`, etc.) que sí existen en main — no porque los borre a propósito, sino porque su base es vieja y le faltan los commits que los añadieron. **Recomendación para quien la revise: cerrar/descartar esa rama, no mergearla** — el fix que aporta ya está en main.
  - **Lo único de valor real que tenía esa rama** (y que SÍ he verificado y traído, en rama nueva y limpia): el 06/08 el fix citaba una cuenta (`f7716a15…`) como "cazador real confirmado" con el patrón `scraping_force_challenge_set score=90 + daily_questions_served alto + daily_question_usage vacío" — y esa afirmación es **FALSA**. Verificado por mí, independientemente, hoy (08/08): consulta directa a `observable_events` (vía `DATABASE_URL`) para sacar las 10 cuentas distintas con `scraping_force_challenge_set` a score EXACTO 90 (12/07-06/08), y cruce con `test_questions` real (vía `VENCE_LECTOR_URL`) contando `user_answer IS NOT NULL AND <> '' AND <> 'BLANK'` — **las 10 tienen respuestas reales, entre 16 y 5.734, ninguna en 0**. `f7716a15…` en concreto: **5.734 respuestas reales, actividad hasta HOY mismo (08/08)** — justo lo contrario de "cosecha sin responder". No cambia la lógica ya mergeada (el discriminante se queda igual, y de hecho queda MÁS defendible: si el 100% de la muestra son falsos positivos, no hay ningún caso real que sugiera bajar el umbral) — solo corrige el COMENTARIO en el código, que de haber quedado así habría hecho creer a cualquiera que lea `botAlertPolicy.ts` después que existe un cazador real confirmado a este patrón, cuando no lo hay.
  - **Rama nueva, limpia, sobre `origin/main` actual: `flota/T-303-correccion-comentario-f7716a15` (commit `57e4c10cb`), pusheada.** Toca SOLO el comentario/narrativa de `lib/security/botAlertPolicy.ts` y `__tests__/security/botAlertPolicy.test.ts` (ninguna aserción cambia). 29/29 tests verdes, eslint y `tsc --noEmit` limpios.
  - **Falta, y no lo puede hacer este worker:** desplegar frontend (53 commits acumulados esperando deploy agrupado, T-303 es uno de tantos — ver `npm run deploy:pendiente`) y mergear la rama de corrección de comentario. Tras el deploy: confirmar que `bot_detected`/`scraping_force_challenge_set` deja de dispararse para cuentas con ≥5 respuestas reales y sigue disparándose para score≥90 sin actividad real o con evidencia dura. No hay un caso conocido de cosechador real confirmado con este patrón blando para contrastar — verificar simplemente que el volumen baja sin que aparezcan denuncias de fraude real coladas.
- **Relacionada:** [T-179] (precedente directo del método), [T-047]/[T-113] (la lección de no entrenar a ignorar el badge), [T-573]/[T-574] (mismo patrón de RLS sin política bloqueando la lectura de `fraud_alerts`).

