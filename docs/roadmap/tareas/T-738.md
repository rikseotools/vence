### [T-738] 🔴 [ABIERTO 08/08] 861 preguntas activas sin NINGUNA explicación, 248 ya servidas: el opositor falla, abre la explicación y no hay nada que leer

- **CÓMO SE VIO:** de rebote, midiendo el lote de [T-734] (las 17 explicaciones cruzadas). Nadie la buscaba.
- **LO MEDIDO contra RDS (08/08):**
  - **1.801 preguntas activas** en TODO el banco tienen `explanation` en blanco — cadena vacía, **no `NULL`**, que es justo lo que la hace invisible a cualquier comprobación de `IS NOT NULL`.
  - **861 de esas 1.801 (48%) salen de un solo lote**, el del 23/04/2026 14:17 (13.965 preguntas importadas).
  - **248 de las 861 YA SE HAN SERVIDO** a usuarios (hay fila en `test_questions`).
- **Por qué importa:** el opositor que falla abre la explicación **para entender por qué**. Ahí es donde se aprende, y es lo que separa un test de una lista de aciertos. Una explicación vacía no le dice que falte: le deja pensando que no hay nada que explicar.
- **NO es lo mismo que [T-734]** y por eso va aparte: allí la explicación existe y habla de otra materia (defecto de CONTENIDO cruzado, 17 casos); aquí no hay texto ninguno (defecto de AUSENCIA, 1.801 casos). Se encontraron a la vez y comparten lote de origen, pero el arreglo no se parece en nada.
- **Punto ciego que lo explica:** `explicacion_truncada` mira si el texto se corta a media frase, `audit_note_explanation` si es una nota de auditoría colada, y `explicacion_estructura_rota` si se pinta mal. **Los tres presuponen que HAY texto.** Ninguno pregunta si lo hay, así que 1.801 preguntas pasan los tres detectores sin despeinarse. Mismo patrón que `articulo_servido_sin_texto` ([T-596]), que nació exactamente igual: nadie miraba si lo que servimos se puede leer.
- **Cómo empezar, y en qué orden:** por las **248 ya servidas**, que son las que alguien ha visto de verdad; y dentro de ellas, por las más servidas. El resto es deuda que todavía no ha tocado a nadie.
- **⚠️ NUNCA rellenar la explicación de memoria.** Es contenido clínico y jurídico: se escribe contra el artículo o la fuente que la pregunta examina, con `scripts/aplicar-explicacion.ts`, y sin tocar `correct_option`.
- **Antes de escribir nada, comprobar si la clave es correcta:** una pregunta que se importó sin explicación pudo importarse mal del todo. La explicación vacía es lo que se ve; no se puede dar por hecho que lo demás vino bien.
- **Falta decidir (es de Manuel):** si mientras tanto estas preguntas se siguen sirviendo. Desactivar 861 de golpe cambia la oferta de varios temas; no se hace sin decirlo.
- **Relacionadas:** [T-734] (mismo lote de origen, defecto distinto), [T-596] (`articulo_servido_sin_texto`, el mismo modo de fallo en la teoría), y la familia de detectores de explicación del runbook de salud del contenido.
