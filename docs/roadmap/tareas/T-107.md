### [T-107] 🟡 [ABIERTO 24/07] Campaña de clonado de epígrafes oficiales (Paso 1) — falso verde de scope a escala
- **📌 28/07 — DESBLOQUEADAS 2 de las 3 "flagged", y en las DOS el flag era un FALSO POSITIVO de método:**
  - **`auxiliar_administrativo_diputacion_cadiz` (164 usuarios) ✅ 24/24 literal.** Se comparaba contra el `programa_url`, que apuntaba al **anuncio del BOE** (extracto) de un proceso **ANTERIOR** (33 plazas, BOP 27 de 09/02/2023). El vigente es el Decreto de **4/02/2026: 44 plazas** (12+16+16), y contra SU Anexo I la BD es **verbatim en los 24**. Enlace repuntado y verificado en la landing. **GOTCHA:** la Diputación tiene DOS procesos de auxiliar vivos (`aux_admvo_a` 33 plazas · `aux_administrativo` 44) y la carpeta `admto_a` es **ADMINISTRATIVO C1** — comprobar el cuerpo DENTRO del PDF, nunca por el nombre de carpeta. Queda [T-211] (estado `examen_realizado` con convocatoria de 2026).
  - **`auxiliar_administrativo_ayuntamiento_murcia` (87 usuarios) ✅ 20/20 literal.** La convocatoria vigente (BORM 901, 28/02/2026) es multi-categoría y **NO trae el temario**: para AUXILIAR ADMINISTRATIVO/A remite al **Acuerdo de 14/10/2022, ANEXO II (BORM 248, 26/10/2022, anuncio 5413)**, donde el C2 son 4 temas del bloque `GRUPO C/C2` + 16 del bloque `AUXILIAR ADMINISTRATIVO`. 19 de 20 verbatim; el 20.º difiere en **una tilde que le falta al BORM** (*"Titulo II"*), y se conserva nuestra forma correcta en vez de replicar la errata.
  - **`auxiliar_administrativo_asturias` (54 usuarios) → `provisional` con motivo, NO cerrada.** Es la tercera "flagged" y la única cuyo flag NO era falso positivo: aquí la discrepancia es de **ESTRUCTURA**. El IAAP publica solo tres temarios y el rotulado *CUERPO AUXILIAR* descarga en realidad el **específico de discapacidad intelectual** (20 temas, con otro Tema 1) — se ve abriendo el PDF, no por el rótulo. Nuestros 25 siguen el *"temario del Servicio de Selección"* del turno GENERAL, que hoy **no está publicado** ahí, y el programa legal de la convocatoria (BOPA 23/02/2022) son ~10 items. **Siguiente paso concreto:** localizar el BOPA completo de 23/02/2022 (los enlaces que publica el IAAP dan 404) o pedir el temario del turno general, y decidir contra cuál se acredita.
  - **Lección transversal:** *"el `programa_url` no casa con el temario de BD"* casi nunca significa "nuestro temario está inventado". Significa **que el temario está en otro documento**. Antes de flaggear: leer la norma que la convocatoria declara programa exigible y seguir la remisión.
- **📌 SESIÓN 27/07 (2.ª, worktree `t107-epigrafes-b`) — 4 oposiciones más. Lo que aporta al método:**
  - **`auxiliar_administrativo_clm` ✅ 24/24 literal** · Paso 2 12 correct / 9 issues (Excel 2019 y Teams/OneDrive). Su temario está en la Resolución de 20/11/2024 (DOCM 231), que publica los de TODOS los cuerpos: acotar al Anexo I · Subgrupo C2. **GOTCHA: el DOCM escribe "Tema 16 ", "Tema17." y "Tema18." sin punto ni espacio** — un parser estricto pierde tres temas en silencio.
  - **`auxiliar_administrativo_baleares` 34/36 literal.** T11 queda `drift` (el texto extraído repite una frase y es agramatical: fusión de columnas del PDF bilingüe) y T20 también (*"herramientas digitales CAIB: NOTIB, ROLSAC…"* **no está en el temario oficial**; está oculto y con 0 preguntas, es un placeholder). **GOTCHAS del BOIB: bilingüe, "Tema 14" sin punto, el pie (D.L./ISSN/URL) se cuela DENTRO de las frases y el último tema arrastra el ANEXO 4 entero (16.889 chars).** Metí esa basura en BD y la cazó `sim-materias-ganadas` al señalar "materias" absurdas tipo *eboibfront*; el escritor lleva ya un validador que se niega a escribir textos con URL, email, `ANEXO` o longitud desmesurada.
  - **`auxiliar_enfermeria_gva` → `drift` (T-196).** Su Annex IV se publica **solo en valenciano**, también en el PDF `_es`. Nuestros epígrafes son traducción (21 castellano + 3 valenciano). **NO es incoherencia nuestra: varía por Orden** — la hermana `administrativo_gva` sí tiene "TEMARIO PARTE GENERAL" en castellano, comprobado. Decisión de producto pendiente.
  - **`tcae_sas` → `drift` (T-205). ⚠️ CORRECCIÓN (06/08): esta línea es un FALSO POSITIVO, medido contra el mismo PDF ya clonado — el Anexo VIII tiene 10+19=29 temas, NO 55, y nuestros 29 corresponden uno a uno con los oficiales.** Detalle completo en la ficha [T-205]. Se deja el texto original de esta entrada sin reescribir (es el registro histórico de lo que se creyó ese día), pero que quede dicho aquí también para quien lea este bloque suelto y no baje hasta T-205.
  - **Herramienta nueva:** `plan-paso2-tras-literal.cjs` (compañera de `sim-materias-ganadas`), porque el mismo script se escribió 3 veces a mano.
  - **⚠️ EL PATRÓN DOMINANTE, ya medido: de las 10 oposiciones tocadas hoy, CINCO tenían el programa REPARTIDO en 2-3 documentos**, y en ninguna lo decía el `programa_url`. Asúmelo como caso normal: lee la norma de la convocatoria que declara el programa exigible y sigue la remisión.
- **📌 SESIÓN 27/07 (worktree `sesion-27jul-e`) — 6 oposiciones trabajadas, 5 capacidades nuevas del pipeline y el coste de la campaña RECALCULADO. Traspaso completo:**

  **A) Estado de lo tocado (Paso 1 / Paso 2):**

  | Oposición | Usuarios | Paso 1 | Paso 2 |
  |---|---|---|---|
  | `auxiliar_administrativo_cantabria` | 43 | 25/25 literal | 25/25 correct |
  | `tcae_murcia` | 53 | 44/44 literal | 37 correct · **6 issues** |
  | `tcae_galicia` | 30 | 31/31 literal | 25 correct · **4 issues** |
  | `ordenanza_ayuntamiento_cordoba` | 55 | 10/10 literal | — |
  | `auxiliar_administrativo_madrid_2027` | 33 | 21/21 literal | 17 correct · 2 issues · 1 `needs_human` |
  | `correos_personal_operativo` | 43 | `provisional` con motivo | — |

  Los **10 `issues`** NO son deuda opaca: cada uno lleva escrito en `findings` el bloque de materia que el tema no sirve (*"Dietas terapéuticas"*, *"Técnicas de deambulación"*, *"Cadena epidemiológica"*, *"Representación, participación y negociación colectiva"*…). Entran directos en la cola de generación de [T-115] sin re-investigar.

  **B) Los tres errores de método que casi cuestan caro (léelos antes de tocar nada):**
  1. **El `programa_url` puede apuntar a una Orden SUPERADA.** Cantabria: la PRE/76/2024 está modificada por la **PRE/12/2026**, que cambia el T16 y la parte específica entera. Comparando contra el `programa_url` estuve a punto de revertir el temario CORRECTO "por fidelidad al boletín". Lo destapó **una usuaria** (07/07), no el sistema. Señal de alarma: *si la parte general casa verbatim y solo diverge una sección entera, casi nunca es que nos lo inventáramos — es que esa sección se modificó.*
  2. **Antes de concluir, LEE LOS FEEDBACKS de esa oposición.** Si alguien pidió el cambio, hay una razón documentada y probablemente una fuente.
  3. **El programa suele estar REPARTIDO.** Galicia: común en la convocatoria + específica en la Resolución de 2019 + un tema por mandato de la Ley 7/2023. Murcia: común en una Resolución y específica en otra, **asignada POR OPCIÓN en el anexo**. La convocatoria a menudo no trae el temario: trae la remisión.

  **C) Capacidades nuevas (úsalas; no repitas a mano):**
  - `verify:epigrafe apply <pt> <plan.json> [--apply] [--fuente-manual]` — reescribe al literal con guarda de los 4 campos de display + literalidad obligatoria; dry-run por defecto.
  - **Conflicto de fuentes:** si el parseo automático y el manual discrepan, PARA y enseña los dos (el parser se desalinea en boletines con varios turnos/bloques: en Galicia mezcló el turno de discapacidad). `--fuente-manual` decide y queda anotado.
  - `verify:scope` admite **añadir una ley** a un tema (`ley_nueva`), siempre por puerta de juicio.
  - `scripts/temario/sim-materias-ganadas.cjs <pt>` — **OBLIGATORIO tras reescribir a literal**: mide qué materia GANÓ el temario y si la servimos. Sin esto, o se re-sella el Paso 2 sin medir o se queda todo en `stale`.
  - El clonado de documentos guarda ya **su texto** (sin texto, el documento es invisible para el aviso de "documentos que afinan el programa").

  **D) El coste de la campaña estaba INFLADO:** el *"~30% de boletines no parsean"* incluía un bug nuestro (`maxBuffer` de 1 MB en `pdftotext`) que hacía ilegibles **justo los boletines con MÁS temario**. Arreglado. `tcae_murcia` —que la ficha daba por bloqueada por el WAF del BORM— se cerró entera. **Mide de nuevo antes de asumir trabajo manual.**

  **E) Gotchas de extracción medidos (ahorran una hora cada uno):**
  - **BOP Córdoba:** intercala el CSV de firma, la cabecera de página y una banda lateral *DENTRO* de las frases → tres temas parecen drift y no lo son.
  - **BOCM:** el pie (`BOCM-AAAAMMDD-N`) se cuela al saltar de hoja.
  - **BOC Cantabria / BORM:** un solo PDF con los programas de TODOS los cuerpos → acotar SIEMPRE al bloque del cuerpo correcto.
  - Varios `programa_url` traen un **espacio final** en la URL.

  **F) Punto de retomada:** `node scripts/temario/detect-temario-revision.cjs` da la cola por usuarios (~73 pendientes). Las 3 primeras (`cadiz`, `ayuntamiento_murcia`, `asturias`) siguen **flagged a propósito** (`drift_detected`: su `programa_url` no casa con el temario de BD) — no las toques sin confirmar antes la convocatoria vigente.

- **Qué:** el sistema de verificación tiene DOS pasos y el **Paso 1 (clonar el epígrafe LITERAL de la convocatoria oficial → `topics.epigrafe` + `verified_literal`) es BLOQUEANTE** antes del Paso 2 (scope↔epígrafe). Medido 24/07 en RDS: de **127 oposiciones activas, solo 11** tienen algún tema con epígrafe `verified_literal`; **95 tienen 0 epígrafe clonado PERO ≥1 tema con scope `verified_correct`** → el scope se verificó contra una referencia SIN validar. **2.626 temas** en esa situación (scope `verified_correct`, epígrafe sin clonar).
- **Impacto:** **falso verde a escala.** Un scope "verified_correct" contra un epígrafe que podría ser paráfrasis/incompleto no garantiza nada (casos T17 GVA, SMS T11, y el 24/07 `administrativo_extremadura`: casi se rechaza una impugnación de scope de Sara con el epígrafe `never_sourced`). El enforcement nuevo en los dossiers (`scripts/impugnaciones/lib/scope-enforcement.cjs`) **AVISA** cuando un usuario se queja, pero no cierra el gap de fondo.
- **Cómo:** campaña por oposición **priorizada por USUARIOS reales** (`user_profiles.target_oposicion`). Por cada una, Paso 1: bajar el temario LITERAL del `programa_url` (`curl` + `pdftotext -layout`; ~30% de boletines no parsean → a mano contra el PDF del boletín) → comparar **verbatim** con `topics.epigrafe` → `verify-epigrafe-literality.cjs record` con `source_url` → si hay drift, actualizar `topics.epigrafe` (dispara re-scope). LUEGO Paso 2: re-verificar el scope contra el epígrafe ya de fiar. Runbook: `verificar-epigrafes-scope.md` §"Sistema 2 / Clonación MANUAL". **NUNCA inventar temario** (regla nuclear).
- **Origen:** 24/07, hilo impugnación **Sara García** (`administrativo_extremadura`: scope `verified_correct` contra epígrafe `never_sourced`). Cuantificado en RDS. Ata con `scope-enforcement.cjs` (que ya avisa del gap por-oposición al abrir el dossier).
- **Progreso (24/07, sesión worktree `t107-epigrafes`):** worklist priorizada por usuarios reales generada (top: auxiliar_administrativo_estado 2175, auxiliar_administrativo_madrid 1393, administrativo_estado 701, tramitacion_procesal, auxiliar_administrativo_extremadura 296, canarias 266, cyl 244, auxilio_judicial 204…). **CERRADAS 2/95 (los dos buques AGE, 2.876 usuarios):**
    - ✅ `auxiliar_administrativo_estado` — 28/28 `verified_literal`, verbatim idéntico al oficial.
    - ✅ `administrativo_estado` — 45/45 `verified_literal`, verbatim idéntico.
  - **HALLAZGO REUTILIZABLE — BOE conjunto:** el `programa_url` de ambas apunta al MISMO doc (`BOE-A-2025-26262`, Resolución 18/12/2025 Función Pública), que convoca a la vez varios cuerpos AGE por anexos: **ANEXO I=Aux C2, ANEXO III=Administrativo C1, ANEXO V=Téc.Aux.Informática, ANEXO VII/IX=Gestión A2…**. → **una descarga cubre varias oposiciones**; parsear SIEMPRE **acotado a la región de SU anexo** (`3./4./5./6. Programa.` hasta el siguiente `ANEXO`) — si el parser no se acota, `bloque` persiste entre anexos y **sobreescribe con el programa de otro cuerpo** (me pasó: coló el temario de redes del ANEXO V en el Bloque II del auxiliar). Numeración BD: bloque I→T1.., resto→T{bloque*100+item} (Aux: I→T1-16, II→T101-112; Admin C1: I→T1-11, II→T201-204, III→T301-307, IV→T401-409, V→T501-506, VI→T601-608).
  - **Herramienta:** `verify-epigrafe-literality.cjs dump` NO auto-parsea el BOE AGE (`fetch=html, 0/N`) → extracción manual del anexo + `record` con `source_url`=el BOE. 0 drift en ambas → no hizo falta tocar `topics.epigrafe` ni re-scope; el scope `verified_correct` previo queda ya sobre base firme.
  - ✅ `auxiliar_administrativo_madrid` (1393 usuarios) — 21/21 `verified_literal`. Fuente = **BOCM Núm. 41, 18/02/2026 (BOCM-20260218-2.PDF), ANEXO III** (programa acceso libre C2). `pdftotext -layout` SÍ parsea (filtrar boilerplate del BOCM: `BOCM`, `Pág.`, `BOLETÍN OFICIAL`, `MIÉRCOLES…`). 19/21 idéntico; **2 drifts REALES corregidos** alineando `topics.epigrafe` al oficial: T12 ("Ley Reguladora de la Hacienda"→"Ley de Hacienda de la Comunidad de Madrid", misma Ley 9/1990) y T16 (comillas rectas→angulares «»). El trigger invalidó su scope → T12/T16 quedan `stale` (re-verificación Paso 2 pendiente, scope sin cambio de contenido). **Gotcha web:** el `programa_url` (comunidad.madrid) NO tiene el temario inline → hay que seguir al PDF del BOCM de la convocatoria (buscar el que tenga el marcador distintivo del temario, aquí "Estatuto de Autonomía"+"Power BI").
  - **CERRADAS 3/95 · 4.269 usuarios.** Siguiente por usuarios: `tramitacion_procesal` (332, Justicia), `auxiliar_administrativo_extremadura` (296), `canarias` (266), `cyl` (244), `auxilio_judicial` (204)… cada una con su fuente propia.
- **📊 ESTADO REAL MEDIDO EL 26/07 — la ficha decía "3/95" y va MUY desfasada. Lo real: 47 de 123 oposiciones activas COMPLETAS (todos sus temas `verified_literal`), 76 con cero.** Entre las cerradas desde entonces por otras sesiones están `tramitacion_procesal` (37 temas) y `auxilio_judicial` (26). Antes de planificar nada, **medir**: la lista de "siguientes por usuarios" de arriba está obsoleta.
- **🔴 Lo que queda NO es mecánico, y conviene saberlo antes de estimar (triaje 26/07):**
  - **Las 3 primeras por usuarios están PARADAS a propósito**, no pendientes de ejecutar: `auxiliar-administrativo-diputacion-cadiz` (163 usuarios), `auxiliar-administrativo-ayuntamiento-murcia` (87) y `auxiliar-administrativo-asturias` (54) están registradas como `drift_detected` porque su `programa_url` NO casa con el temario de BD (desfase de convocatoria, anuncio multi-categoría, o programa legal vs temario de estudio). Reescribirlas movería fronteras de tema y obligaría a re-mapear `topic_scope` con orphan-check → es **judgment_gate**. Detalle en la memoria `project_cadiz_temario_desalineado_scope`.
  - **El parser automático del `dump` falla en TODAS las candidatas probadas**: `correos_personal_operativo` (0/12, `fetch=html`), `auxiliar_administrativo_cantabria` (0/25, `fetch=pdf_empty`), `tcae_murcia` (0/44, `fetch=pdf` pero texto vacío), `auxiliar_administrativo_clm` (0/24), `auxiliar_administrativo_baleares` (0/36). Es decir: **la extracción del bloque oficial es MANUAL en la práctica**, con la fuente y los gotchas propios de cada boletín (ver memoria `reference_verify_epigrafe_fuentes_multibloque`).
  - **Y algunas fuentes están bloqueadas:** el `programa_url` de `tcae_murcia` apunta al **BORM, que bloquea la IP a nivel de red (Radware)**. Para la Universidad de Murcia la vía alternativa es `convocum.um.es` (ver [T-119]), pero **para el Servicio Murciano de Salud NO sirve** — hay que buscarle otra.
  - **Consecuencia para planificar:** esto no es una campaña de "N oposiciones × script"; es **~76 casos manuales**, cada uno con su fuente, y varios con decisión de scope encima. Trocear por oposición y hacer claim por lote, como dice la propia ficha.

- **Provenance HUB (24/07, worktree `t107-epigrafes`) — unificados los dos silos:** el `source_url` de texto libre de la verificación de epígrafe no casaba con el documento clonado en `convocatoria_documentos` (`txt.php` ≠ `/pdfs`) → falso verde de provenance. Construido por fases (todo en RDS + pusheado):
    - **Fase 1:** `lib/convocatoria/canonicalizeBoletinUrl.cjs` (puro, 12 tests) — identidad canónica `doc_key` (BOE/BOCM + reserva por URL).
    - **Fase 2:** migración `20260725_provenance_doc_hub.sql` — `convocatoria_documentos.doc_key` + índice único parcial (dedup), `topic_epigrafe_verification.source_documento_id` (FK), y función **`ensure_convocatoria_documento`** (ÚNICO camino de escritura, idempotente, la llaman backend y scripts).
    - **Fase 3:** `verify-epigrafe-literality.cjs record` canonicaliza→ensure→fija `source_documento_id`. Backfill `doc_key` en 6.565 filas legacy (`backfill-doc-key.cjs`); enlazadas las 94 de las 3 AGE+Madrid + 85 huérfanos enlazables de otras 5 opos (`link-epigrafe-docs.cjs`). 0 dups nuevos, invariante `verified_literal⇒source_documento_id` verificado.
    - **Fase 4:** detector `epigrafe_provenance_no_doc` en el sweep backend + frase-gatillo *"revisa la provenance de epígrafes"* (`runbookRegistry` + CLAUDE.md + guardarraíl). Quedan 133 huérfanos SIN `source_url` (verificados antes del hub) → los re-sourcea la campaña.
    - **Fase 5:** manuales `provenance-convocatorias.md` §0.bis + `verificar-epigrafes-scope.md`.
    - **Fase backend (25/07, HECHA — requiere deploy):** decisión "separar por tipo" — el índice único `(conv, doc_key)` excluye `tipo='nota'`; `boletin_doc_key(url)` (espejo SQL para el backend). `clonar-documento.ts` (canónicos) enrutado por `ensure_convocatoria_documento` + preserva metadata curada; `detect-notas` (notas) rellena `doc_key` sin deduplicar (mantiene historial). Backend typecheck OK, ambas rutas verificadas en RDS.
    - **Items 1-3 de unificación (25/07, HECHO lo automatizable):**
      - **Item 1 señales OEP:** FK `oep_detection_signals.source_documento_id` (migración `20260725_oep_signal_source_documento`). `link-by-dockey.cjs` enlaza hitos+señales a docs ya clonados por doc_key canónico → **81 señales enlazadas** (0 cross-oposición verificado). FK **disperso por diseño**: 986/1001 señales apuntan a páginas de listado, no documentos. El linker (no wiring de productor) es el mecanismo continuo.
      - **Item 2 monitoreo: NO se enlaza — sería fake.** `seguimiento_last_hash`/`programa_last_hash`/`seguimiento_checks.content_hash` son **cachés de detección-de-cambios** (hashean página normalizada, no el texto del doc → solo **2/11204** coinciden con el corpus), no provenance. Su OUTPUT (documentos detectados) ya cae al hub como notas. Añadir FK ahí sería unificación falsa.
      - **Item 3 cobertura:** hitos 157→**158** (backfill exacto; los 147 restantes con url NO tienen doc clonado → flujo `convocatoria_docs_incompletos`). Epígrafe **179/318**; los ~133 huérfanos restantes NO tienen `source_url` (verificados antes del hub) → los re-sourcea la campaña por-oposición.
      - **Regresión cazada y arreglada:** `content-sweep-parity` exigía el detector `epigrafe_provenance_no_doc` también en `scripts/health-sweep.cjs` (gemelo). Añadido. 895 tests verdes, backend typecheck OK.
    - **PENDIENTE:** deploy del backend (activa el enrutado de clonado + el detector nuevo); re-sourcing de los 133 epígrafes huérfanos; cobertura de canonicalización JS más allá de BOE/BOCM (resto por URL).
- **Progreso (25/07, sesión worktree `t107-epigrafes`) — +16 cerradas, 3 flagged, 1 provisional:**
  - **CERRADAS a `verified_literal` (16, fuente clonada en el hub):** `auxiliar_administrativo_valencia` 24 (DOGV; 6 epígrafes reescritos a literal), `canarias` 40 (BOC), `cyl` 28 (BOCYL, ANEXO II auxiliar), `auxilio_judicial` 26 (BOE ANEXO VI.c, NBSP), `andalucia` 22 (BOJA), `administrativo_seguridad_social` 36 (BOE, bloques 1-23+101-113), `galicia` aux 17 (DOG, LibreOffice), `aragon` 20 (portal mia CSV, **Playwright**), `diputacion_cordoba` 20 (BOP, LibreOffice), `policia_nacional` 46 (BOE; 4 epígrafes→literal + inglés), `administrativo_gva` 35 (DOGV, MS365), `ayuntamiento_valencia` 21 (BOP València), `guardia_civil` 25 (temario oficial GC; BD era condensación fiel → reescritos 25 a verbatim), `universidad_uned` 21 (BOE), `administrativo_galicia` 19 (DOG; títulos de leyes condensados→literal), `diputacion_zaragoza` 20 (BOPZ tras WAF, **Playwright evento download**).
  - **Reescritos a literal (dispara re-scope → Paso 2 pendiente en esos):** Valencia T6/T8/T9/T21/T22/T23, Policía T8/T18/T19/T41, Guardia Civil los 25, Admin Galicia los 19.
  - **🚩 FLAGGED (drift_detected, NO botcheados) — PATRÓN `programa_url` ≠ temario de BD:** `auxiliar_administrativo_diputacion_cadiz` (BD desalineado del BOP; T2 fusiona oficial 2+3, resto corrido; BOP 27/2023 vs 29/2024), `auxiliar_administrativo_ayuntamiento_murcia` (BORM 901/2026 multi-categoría, no trae el auxiliar C2 de BD), `auxiliar_administrativo_asturias` (programa legal 2022 = 10 items vs 25 temas de BD; BD sigue el "temario del Servicio de Selección"). **Requieren confirmar convocatoria vigente + qué doc es el temario real ANTES de reescribir.** Diagnóstico completo en memoria `project-cadiz-temario-desalineado-scope`.
  - **🟡 PROVISIONAL:** `correos_personal_operativo` (12) — empresa pública, bases en portal propio (no boletín, no verbatim en BOE); estructura de 12 temas confirmada y BD casa, pero falta el PDF oficial de bases para literalidad verbatim.
  - **Infraestructura reutilizable (código pusheado a `origin/main`, tests 19 verdes):** 4 patrones nuevos en `canonicalizeBoletinUrl.cjs` + espejo SQL `boletin_doc_key`: **BOC** (Canarias), **BOJA** (Andalucía), **DOG** (Galicia, _es/_gl convergen), **MIA** (portal CSV Aragón, SPA y API convergen). Commits: `fdc6d33c3`, `45955583c`, `b36a103dd`, `d5cb1479d`. Técnica Playwright para WAF/SPA documentada en memoria `reference-fetch-boletin-waf-playwright` (variantes `page.on('response')` para SPA-CSV y evento `download` para BOPZ). Método multi-bloque/multi-cuerpo en `reference-verify-epigrafe-fuentes-multibloque`.
  - **➡️ PUNTO DE RETOMADA:** quedan **~91 oposiciones** en `detect-temario-revision.cjs` (ya <47 usuarios cada una). Correr `node scripts/temario/detect-temario-revision.cjs` para la cola priorizada por usuarios. Por cada una: `verify:epigrafe dump <pt>` → si el dump no parsea/da drift masivo, descargar la fuente a mano (curl BOE/DOGV/BOCYL/BOJA/BOC/DOG; Playwright para BOA/mia/BOPZ con WAF) → extraer el bloque del cuerpo correcto → comparar fold → reescribir solo drift REAL de contenido → clonar en hub + `verify:epigrafe record`. **Si `programa_url` ≠ temario de BD (frase de BD T1 ausente en la fuente): FLAGGEAR drift, NO reescribir** (patrón Cádiz/Murcia/Asturias).
  - **CABOS SUELTOS de esta tanda:** (a) **Paso 2 (scope↔epígrafe)** pendiente en las oposiciones con epígrafes reescritos (su `topic_scope_verification` quedó `stale` por el trigger; re-verificar con `verify:scope`, sin cambio de contenido de scope); (b) resolver los **3 flagged** (confirmar convocatoria vigente); (c) conseguir las **bases oficiales de Correos** (portal-gated) para pasar de provisional a literal.

