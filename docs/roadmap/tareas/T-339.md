### [T-339] 🟠 [ABIERTO 30/07] 🙋 ENTREGADA 07/08, esperando revisión de Manuel — Elegir una oposición personalizada sin temario te deja en el vacío sin avisar

- **QUÉ PASA (ficha original).** Al elegir como objetivo una oposición personalizada (`custom_oposiciones`) que no tiene temario montado, la app **no lo dice**: la persona aterriza en una pantalla sin contenido y sin ninguna explicación de por qué.
- **CUÁNTA GENTE (medido 30/07):** **303** usuarios con una de esas como objetivo, **127 sin un solo test**, **121** dados de alta en los últimos 30 días.
- **CASO QUE LO DESTAPÓ:** Pedro (`pedro.delibros`), eligió *«Subalterno»* (etiqueta de marzo, 25 personas), 5 días mirando, 0 tests, baja el 29/07.
- **LO QUE ENCONTRÉ AL INVESTIGAR (07/08): [T-508] ya cerraba esto — por UNA sola puerta.** [T-508] (04/08, desplegada) construyó exactamente el guardarraíl que esta ficha pedía: `personalizadaUtilizable(temas)`, el 409 en `PUT /api/profile/target`, y la pantalla `AvisoTemarioVacio` en las rutas de temario/test en vez del 404. Pero **`POST /api/v2/onboarding/save-field`** (el guardado progresivo de `OnboardingModal.tsx`, usado por `handleSelectOfficial`/`handleSelectCustom`/`handleCreateCustom`) escribía `target_oposicion` con un `UPDATE` directo, **sin pasar por `personalizadaUtilizable` en absoluto** — una SEGUNDA puerta con CERO criterio, no una distinta. Medido con la función REAL que usa el onboarding (`get_popular_custom_oposiciones(10)`): **las 10 personalizadas "más populares" que se ofrecen a un usuario nuevo tienen las 10 CERO temas activos**, incluyendo la misma "Subalterno" del caso Pedro y la "universidad" de Esther Lázaro (el caso que motivó T-508). El agujero seguía completamente abierto para cualquier alta nueva.
- **QUÉ SE HIZO:**
  - `buscarPersonalizada`/`PersonalizadaResuelta` extraídas de `target/route.ts` a `lib/api/oposicionPersonalizada/consultas.ts` (compartidas por las dos puertas server-side, para que no puedan volver a divergir).
  - `ERROR_PERSONALIZADA_SIN_TEMARIO` (código + mensaje) centralizado en `lib/oposicion/objetivoPersonalizado.ts`.
  - `save-field` aplica el MISMO gate (409 + telemetría `objetivo_personalizado_vacio`, `endpoint:'/api/v2/onboarding/save-field'`) antes del `UPDATE`, cuando `field==='target_oposicion'` y el valor es una personalizada sin temas.
  - `OnboardingModal.tsx` ya no traga el rechazo en silencio (antes: pintaba la selección de forma optimista y guardaba en segundo plano sin mirar el resultado). `handleSelectCustom` y `handleCreateCustom` ahora **esperan** la respuesta de `save-field` y, si viene bloqueada, llaman a `setError(...)` con el motivo real en vez de fingir que se guardó.
  - Guardarraíl `testsDeOposicionPersonalizada.guardrail.test.ts` ampliado: pasa de comprobar DOS puertas (botón + endpoint) a TRES (+ onboarding), y añade una comprobación de que las dos puertas server-side importan la MISMA `buscarPersonalizada` en vez de reescribir el `SELECT`.
  - Tests nuevos en `onboardingModalEndpoints.test.ts`: personalizada sin temas → 409 y ningún `UPDATE`; con temas → guarda; catálogo (no personalizada) → ni siquiera consulta temas; personalizada inexistente/ajena → fail-open.
- **⚠️ CONSECUENCIA QUE HAY QUE CONFIRMAR — pido el OK de Manuel antes de dar esto por cerrado del todo:** como toda personalizada **nace** con 0 temas (el tema se añade después, en el editor de [T-327]), el gate deja **"Crear oposición personalizada" del onboarding bloqueada de fijarse como objetivo el 100% de las veces** hasta que tenga al menos un tema — la fila se sigue creando (queda en "Mis oposiciones", con su etiqueta), pero ya no se convierte en objetivo activo desde ese flujo. Es fiel a la decisión YA tomada en [T-508] (una personalizada sin temas no puede ser el target, venga de donde venga), no una política nueva que yo esté inventando — pero es un cambio de producto grande y visible para el flujo más usado de `custom_oposiciones` (580 de 585 filas son justo este tipo de etiqueta sin temario). **Alternativa que NO implementé** por ser una decisión de producto y no un bug: dejar pasar la creación/selección sin bloquear y apoyarse solo en `AvisoTemarioVacio` (que ya existe) para explicar en el momento en que el usuario llega al temario/test — más parecido a la redacción literal de esta ficha ("decir la verdad y ofrecer una salida" en vez de "bloquear"). No he tocado el enlace al configurador por leyes que la ficha original sugería (offrecer una salida visible): con el bloqueo, el usuario simplemente ve el error y puede elegir otra oposición del propio modal — no añadí un CTA nuevo por mantener el cambio acotado.
- **NO verificado en navegador** (este worker no puede autenticarse como usuario): typecheck limpio (`npx tsc --noEmit`) y 4 suites de test en verde (81 tests), pero la UI del `OnboardingModal` (el `setError` pintándose, el flujo completo de creación) necesita una pasada visual humana.
- **Rama:** `flota/T-339-gate-onboarding-target` (commit `35b004203`), pusheada, no mergeada.
- **Relacionada:** [T-508] (el gate original, del que esta tarea cierra la segunda puerta), [T-327] (la funcionalidad de armarse el temario, solución de fondo), [T-328] (la landing, que NO debe publicarse antes que esto esté resuelto).

