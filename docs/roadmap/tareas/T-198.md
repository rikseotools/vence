### [T-198] ✅ [RESUELTA 28/07] El 54% de las impugnaciones llega SIN una sola palabra del usuario: el formulario se autoenvía al pulsar el motivo
- **Qué pasa:** en `components/QuestionDispute.tsx:306-311`, el `onChange` del radio **envía la impugnación en ese mismo clic**, con `description = ''`. El usuario elige «Pregunta no literal» y ya está enviado: no llega a ver dónde escribir. Solo el motivo **«otro»** abre el textarea.
- **Y lo peor: el hueco no es de esquema ni de API, es de UI — el componente YA renderiza los campos, pero son inalcanzables.** El textarea «Información adicional (opcional)» (`:565-587` inline, `:436-446` modal) tiene por condición `disputeType && disputeType !== 'otro'`, que es exactamente el caso en el que el auto-envío ya ha disparado: **código muerto**. La columna `question_disputes.description` existe y es `NOT NULL` (`db/schema.ts:1726`), el Zod ya acepta 10-500 caracteres (`lib/api/v2/dispute/schemas.ts:27`) y el insert ya lo persiste (`lib/api/v2/dispute/queries.ts:183`). Lo único roto es el clic.
- **📊 MEDIDO EN PRODUCCIÓN (27/07), y el coste es real:**
  - **1.024 de 1.882 impugnaciones (54%)** tienen como descripción literalmente `«Motivo: <tipo>»` — cero palabras del usuario. **188 solo en los últimos 30 días.**
  - Cinco motivos tienen **UNA sola descripción distinta en toda la historia**: `tema_incorrecto` (160), `mal_formulada` (152), `desacuerdo_correcta` (114), `explicacion_confusa` (86), `explicacion_mejorable` (62). Es la prueba de que nadie ha podido escribir nunca en ellos.
  - **Las que llegan sin texto se RECHAZAN al doble: 42% (424 de 1.019) frente al 22% (186 de 856) de las que sí traen explicación.** El revisor decide sin saber qué quiso decir el usuario.
  - **246 de las 408 apelaciones** (`appeal_text`) vienen de impugnaciones que se enviaron sin poder explicarse: el usuario acaba escribiendo… en el recurso, cuando ya se ha rechazado.
- **Sugerencia (lo barato y suficiente):** quitar el auto-envío y exigir el botón «Enviar impugnación», dejando el textarea **siempre visible y opcional** salvo en `otro`, donde ya es obligatorio. **El patrón ya existe en el repo y funciona**: el recurso (`components/NotificationBell.tsx:1070-1090`, textarea + contador + botón deshabilitado hasta que hay texto) y la pestaña de impugnación del `FeedbackModal.tsx:709-741`, que hace justo lo correcto. Cero migraciones, cero cambios de API.
- **⚠️ Ojo al cerrar:** `__tests__/components/QuestionDispute.test.tsx:272,309,369-382` **congela el auto-envío como comportamiento esperado** (`expect(body.description).toBe('Motivo: no_literal')`). Hay que reescribir esos casos, no borrarlos: son la prueba de que el cambio es deliberado.
- **Cabo aparte:** hay una **segunda vía duplicada** de impugnación en `FeedbackModal.tsx:640-742` que apunta al endpoint viejo `/api/dispute`. Decidir si se retira, o al menos que las dos guarden lo mismo.
- **Impacto:** 🟠 no rompe nada, pero envenena la cola de impugnaciones —la principal señal de calidad del contenido— y duplica los rechazos. Origen: observación de Manuel (27/07).
- **✅ Hecho (28/07):** quitado el auto-envío en `handleRadioChange` (`components/QuestionDispute.tsx`): elegir el motivo ya solo selecciona el motivo, el envío es siempre por el botón «Enviar impugnación».
- **Idas y vueltas del texto, con el resultado final (28/07):** se probó **obligatorio en todos los motivos** y Manuel lo **revirtió al verlo desplegado** → queda **OPCIONAL salvo en `otro`** (donde el Zod del servidor exige 10-500 y sin motivo tipificado no hay nada que revisar). El razonamiento que gana: el motivo del radio ya dice qué falla, y exigir 10 caracteres a quien solo marca «respuesta incorrecta» es **cobrarle un peaje por reportar un fallo NUESTRO**. El texto se PIDE (etiqueta y aviso lo animan), no se impone.
- **Lo que NO se revierte y era el fondo del asunto: el envío explícito.** Elegir el motivo ya no envía; el usuario llega al textarea y decide. Ese era el defecto que dejaba al 54% sin poder explicarse.
- **Bug de coherencia cazado en producción por Manuel (captura):** la etiqueta decía `*` obligatorio mientras el placeholder seguía diciendo «Opcionalmente, explica…». Los dos textos ahora dicen lo mismo.
- **Tests reescritos, no borrados** (como pedía la ficha): el que congelaba el auto-envío ahora prueba lo contrario — `T-198: elegir el motivo NO envía la impugnación` — y se añadieron dos casos para el body (`Motivo: <tipo>` a secas y con detalles). 27/27 en verde. El test de DEBOUNCE (bug de María José) se mantiene, ahora sobre el botón.
- **Se cerró junto a la recompensa de 1 € por impugnación aceptada**, y no por casualidad: pagar por aceptada con un formulario que se autoenvía premia el volumen. Ver `docs/runbooks/embajadores-recompensas.md`.
- **Cabo NO cerrado:** sigue existiendo la segunda vía de impugnación en `FeedbackModal.tsx:640-742` contra el endpoint viejo `/api/dispute`. Queda abierto decidir si se retira.


