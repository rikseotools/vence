### [T-564] 🟠 [ABIERTO 05/08] 19 oposiciones ACTIVAS tienen el seguimiento de convocatorias en `error` y ningún detector lo mira

- **Cómo apareció:** al hacer el rollover de `auxiliar-biblioteca-estado` a la OEP 2026 ([T-562] de contexto), quedó un cabo: las plazas de la Sección Bibliotecas las fijará la **convocatoria**, que aún no se ha publicado. Al comprobar quién vigilaría esa publicación → `seguimiento_change_status = 'error'`. El cron mira todos los días (`seguimiento_last_checked` = ayer) y **falla todos los días**.
- **MEDIDO el 05/08: 19 oposiciones activas** en ese estado. Muestra, con su fase — o sea, gente estudiando ahora mismo:
  | oposición | estado |
  |---|---|
  | `administrativa-universidad-de-murcia` | pendiente_examen |
  | `administrativo-galicia` · `administrativo-junta-general-asturias` | pendiente_examen |
  | `administrativo-extremadura` | lista_admitidos |
  | `auxiliar-administrativo-ayuntamiento-marbella` | convocada |
  | `auxiliar-administrativo-ayuntamiento-madrid` | oep_aprobada |
- **El punto ciego, que es lo que justifica la ficha.** Hay dos detectores de seguimiento y **ninguno cubre este caso**:
  | detector | qué mira | ¿este caso? |
  |---|---|---|
  | `seguimiento_url_stale` | la URL apunta a un ciclo YA CERRADO | ❌ la URL puede ser la correcta |
  | `seguimiento_fuente_ciega` | responde 200 pero el contenido no llega (SPA, WAF) | ❌ **necesita un 2xx**; aquí ni eso |
  Los dos parten de que la petición funciona. Cuando el cron **falla**, se quedan mudos: `seguimiento_change_status='error'` no lo lee nadie, no pinga badge y no sale en `/admin/salud-sistema`.
- **Por qué importa más de lo que parece:** una oposición con el seguimiento roto **no detecta su convocatoria nueva**. Y ese es justo el momento en que la landing capta (plazo abierto). Es el mismo modo de fallo que ya costó caro en [T-165]: *«parece que hay monitoreo y no lo hay»* — pero un escalón antes, porque allí al menos había respuesta.
- **Por dónde:**
  1. Núcleo puro que clasifique el `seguimiento_change_status='error'` **por antigüedad y por fase**: no es lo mismo un error de un día en una oposición sin convocatoria que un error persistente en una `convocada` o `pendiente_examen`. La banda `error` debería mirar la fase, igual que hace `hito_registro_sin_fuente`.
  2. Mirar `seguimiento_last_error` (si se guarda) antes de decidir la remediación: un 403/WAF pide fetcher headless (`oposiciones.fetcher_type`, que YA existe y tiene 67 filas), un 404 pide repuntar la URL con `repuntar-url.cjs` (que rehúsa escribir una URL no vigilable).
  3. **NO repuntar sin abrir la página**: la regla de la casa.
- **Cabo hermano ya anotado:** `auxiliar-biblioteca-estado` es una de las 19, y su convocatoria es la que traerá el reparto de las 86 plazas de la escala 5431 entre Archivos, Bibliotecas y Museos.

- **✅ CONSTRUIDO (07/08) — kind `seguimiento_fuente_error` en los dos gemelos, rama `flota/T-564-seguimiento-fuente-error` (commit `b5d90d798`), pusheada.** Núcleo puro
  `lib/convocatoria/seguimientoFuenteError.cjs`, severidad por fase (reutiliza
  `procesoConFichaViva` de `seguimientoUrlSalud.cjs`, no la reinventa): `error` con
  convocatoria de ficha viva (11/18 medidas), `warn` sin ella (7/18). Medido entonces: 18
  oposiciones activas en `seguimiento_change_status='error'`, solo 3 con algún hallazgo de
  seguimiento por otro motivo, CERO con `seguimiento_fuente_ciega` — confirma el punto ciego
  que esta ficha describe.

- **🔧 REBASADO Y VERIFICADO (08/08, w2) — la rama original tenía conflictos REALES con `main`
  actual, no ruido; resueltos, más un hueco de paridad cerrado. Rama nueva:
  `flota/T-564-seguimiento-fuente-error-rebase` (commit `1583ccf6d`), pusheada. La rama original
  ya no hace falta mirarla.**
  - **La rama estaba 794 commits detrás de `main`.** Antes de darla por buena, merge de prueba
    real (scratch, abortado luego): salió CONFLICT de verdad en `CLAUDE.md`,
    `runbookRegistry.test.ts`, `content-health-sweep.service.ts` y `scripts/health-sweep.cjs` —
    los cuatro habían sido tocados entretanto por [T-311] (`notas_convocatoria_sin_vigilancia`)
    y por [T-408] (`pregunta_duplicada`) en el MISMO punto de inserción (dos detectores nuevos
    añadidos justo después del mismo bloque). Resuelto conservando los DOS lados en cada caso
    (nunca "el que tenga más cambios") — verificado que ningún bloque se pisa: cada kind sigue
    con su propio SQL/función intactos.
  - **`docs/roadmap/tareas-pendientes.md`:** descartado el lado de la rama (era el monolito de
    antes de la migración [T-532] a "una ficha = un fichero", ya obsoleto) y regenerado el
    índice desde `docs/roadmap/tareas/*.md` actual.
  - **Hueco real encontrado al verificar, y cerrado — no solo detectado:** a diferencia de
    `pregunta_duplicada`/`veredicto_verificacion_rojo`/`audit_note_explanation` (que sí tienen un
    test de COMPORTAMIENTO comparando núcleo↔backend en `content-sweep-parity.test.ts`),
    `seguimiento_fuente_error` solo tenía la paridad GENÉRICA de "el kind existe en los dos
    ficheros" — que no cazaría un `ESTADOS_FICHA_VIVA` que divergiera en la copia INLINE del
    backend (no puede importar el núcleo del frontend) sin que nadie se enterara. Añadido un
    bloque de mirror dedicado en `content-sweep-parity.test.ts` que compara el `Set` del backend
    contra `procesoConFichaViva()` del núcleo por COMPORTAMIENTO (el núcleo no exporta el Set
    directamente) sobre el universo real de `estado_proceso`, más la severidad de
    `diagnosticarSeguimientoErrorInline` vs `diagnosticarSeguimientoError`. **Verificado por
    MUTACIÓN, no solo escrito:** quitada a mano `'pendiente_examen'` del Set del backend, el test
    nuevo FALLA exactamente ahí (`expect(received).toBe(expected) — Expected: true, Received:
    false`); restaurado el fichero, vuelve a pasar (`git diff --stat` limpio tras restaurar).
  - **Verificado, con capas:** 238 tests relevantes en verde (217 de
    `content-sweep-parity.test.ts` — incluidos los de [T-408] que también viven ahí, confirmando
    que el merge no rompió nada de otra tarea — + 9 de `seguimientoFuenteError.test.js` + 12 de
    `runbookRegistry.test.ts`) + 50 del backend (`content-health-sweep` completo). `tsc` limpio
    frontend y backend. 2 errores nuevos de `eslint`/prettier en el bloque wireado del backend,
    corregidos a mano SIN tocar el resto del fichero (269/268 problemas antes y después de mi
    cambio, igual que en `origin/main` sin tocar — confirmado con `git stash`). `robustez-push-
    guard.cjs` sobre el rango completo: exit 0.
  - **Cabo suelto visto de paso, NO arreglado aquí (fuera de alcance):** el fichero del backend ya
    tenía, ANTES de esta tarea, una TERCERA copia independiente de un set de fases parecido
    (`ESTADOS_FICHA_VIVA_INLINE`, línea ~445, de otro detector). No es de T-564 y no lo toco, pero
    queda anotado por si alguna vez se decide consolidar las tres copias (`seguimientoUrlSalud.cjs`
    + las dos INLINE del backend) en un solo sitio importable.
  - **Sigue pendiente, sin cambios:** el `merge`/deploy (decisión de una persona) y, tras
    desplegar, la verificación en producción que la ficha original pedía — que `/admin/salud-
    sistema` y `/admin/contenido` muestren el kind con hallazgos para las 18-19 oposiciones (el
    número exacto puede variar si alguna se repunta antes), y que el `@Cron` nocturno lo escriba
    de verdad en `content_health_findings` (mismo patrón de "el deploy puede tardar más que el
    próximo tick del cron" ya visto en [T-408] el mismo día).

