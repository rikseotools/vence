### [T-327] 🟠 [ABIERTO 30/07] Oposición personalizada: que el usuario arme su propio temario eligiendo leyes y artículos

- **🔎 (06/08, w3) CAUSA RAÍZ REPRODUCIDA — por qué "el test aleatorio, el simulacro y el test personalizado de toda la oposición NO existen para las personalizadas" (el cabo (a) que quedó pendiente el 01/08), y por qué NO es un arreglo pequeño.**
  - **Confirmado leyendo el código, no supuesto:** `app/test/aleatorio/page.js` (y por el mismo patrón `aleatorio-examen`, `personalizado`) resuelve la oposición con `getOposicionConfig(targetOposicion)` (`lib/config/oposiciones.ts:11136`), que a su vez sale de `OPOSICIONES` — un **array literal hardcodeado en el fichero fuente** (`lib/config/oposiciones.ts:153`, ~11.000 líneas de catálogo estático). Una oposición personalizada (`personalizada_<id>`) es una fila dinámica en `topics`/`topic_scope`, creada por un usuario en tiempo de ejecución: **no puede estar, ni estará nunca, en ese array.** `getOposicionConfig('personalizada_xxxx')` devuelve `null` siempre, por construcción — no es un bug puntual, es una imposibilidad estructural del camino actual.
  - **Consecuencia para el usuario, si hubiera un enlace que lo llevara ahí:** `TestAleatorioContent` (la página) tiene `if (loading || !oposicionConfig) return <Cargando…>` — con `oposicionConfig` eternamente `null`, la pantalla se queda en el spinner **para siempre, sin error**. Peor que un 404: parece que la app se colgó.
  - **Y la buena noticia, medida igual de en serio:** **hoy nadie puede llegar ahí.** El hub que SÍ funciona para personalizadas (`app/oposicion-personalizada/[id]/test/page.tsx`) es una página **deliberadamente propia** —su propio comentario de cabecera explica por qué NO reutiliza `TestHubPage`: el hub del catálogo está construido sobre el config estático, y forzar una personalizada ahí "obligaría a inventarle un config falso"— y **solo enlaza a tests por TEMA** (`/oposicion-personalizada/[id]/test/tema/[numero]`), nunca a `/test/aleatorio` ni hermanos. No hay enlace roto porque no hay enlace: es una ausencia de funcionalidad, no un enlace muerto que alguien vaya a pisar mañana.
  - **Por qué esto cambia el arreglo:** la vía obvia ("pásale el `positionType` a `/test/aleatorio` y que ya está") **no funciona aquí** — el propio código explica por qué se descartó ese camino para el hub, y aplica igual a estas tres pantallas: harían falta bloques, iconos y estructura que una personalizada no tiene. El patrón correcto, siguiendo la decisión de diseño YA tomada el 01/08 para el hub, es **una página propia por modo** bajo `/oposicion-personalizada/[id]/test/aleatorio` (y sus hermanas), que consulte `topics`/`topic_scope` directamente —igual que ya hace `app/oposicion-personalizada/[id]/test/page.tsx`— en vez de forzar el camino estático. Es trabajo de feature nueva (una pantalla de selección de temas + generación de test multi-ley para cada uno de los tres modos), no una línea.
  - **Por qué no lo construí ahora:** sin fecha comprometida (a diferencia de la parte A, que sí la tenía y se cumplió), sin decisión de producto cerrada sobre el punto (c) de abajo (editar reescribe el temario público sin aviso), y con riesgo real de colisión — otra sesión (w2) estaba tocando `components/test/TestExamenPage.tsx` en vivo mientras yo investigaba, que es territorio adyacente al modo examen/simulacro. Construir tres pantallas nuevas a medias en el rato que quedaba de turno habría sido peor que dejarlo bien diagnosticado.
  - **Devuelvo la tarea al pool** (`release`) con esto escrito para que quien la coja no tenga que releer el sistema entero: el diagnóstico y el patrón a seguir ya están aquí.

- **✅ (06/08, w4) MODO "ALEATORIO" CONSTRUIDO — el primero de los tres, siguiendo el patrón exacto que dejó escrito w3.** Antes de construir se comprobó algo que el diagnóstico de w3 no necesitaba mirar para su alcance: si el MOTOR de test (no el picker) también dependía del config estático. **No lo hace** — leyendo `TestPageWrapper.tsx`, `OposicionDetector.tsx`, `OposicionGuard.tsx` y `TestLayout.tsx` no hay ni una mención a `getOposicionConfig`/`getOposicion(`, y `fetchAleatorioMultiTema` (`lib/testFetchers.ts`) llama a `/api/questions/filtered` con `positionType` + `multipleTopics` (números de tema) como parámetros explícitos, que a su vez tampoco referencia el config estático. Es decir: **el único código roto era el picker**, no la cadena de generación de test — lo que hace tratable construir la página nueva sin tocar (ni arriesgar) el motor compartido con el resto del catálogo.
  - **Construido:** `app/oposicion-personalizada/[id]/test/aleatorio/page.tsx` (servidor: valida forma del id, carga temas con su recuento de preguntas servibles — misma query que el hub) + `components/oposicionPersonalizada/AleatorioPersonalizadoPicker.tsx` (cliente: lista plana de temas con casillas — sin bloques, que una personalizada no tiene —, número de preguntas, dificultad, y al pulsar "Empezar" monta `TestPageWrapper` directamente con `positionType`/`themes` explícitos). **Enlazado desde el hub** (`test/page.tsx`): antes NO había ningún enlace a esta pantalla, que es la causa raíz que diagnosticó w3 de por qué "nadie la echaba en falta" pese a estar rota — ahora sí, así que deja de ser alcanzable-pero-invisible para pasar a alcanzable-y-descubrible.
  - **Verificado, con las capas que SÍ pude correr como worker (sin `DATABASE_URL` de escritura ni credenciales de app — mismo límite que toda entrega de esta sesión):**
    - `NODE_OPTIONS="--max-old-space-size=6144" npx tsc --noEmit` → limpio (valida también que todos los imports resuelven).
    - La query SQL del servidor, ejecutada tal cual contra RDS real vía `VENCE_LECTOR_URL` sobre una personalizada real con 15 temas (`personalizada_0f2202a0fd1444f482fd77cd292880b2`): devuelve 15 filas con recuentos de preguntas sensatos (de 49 a 3.087 por tema) — la query es correcta contra datos reales, no solo contra mi cabeza.
    - **Guardarraíl nuevo** (noveno eslabón en `__tests__/guardrails/testsDeOposicionPersonalizada.guardrail.test.ts`, mismo patrón de inspección estática que los otros ocho): fija que el hub siga enlazando, que la ruta nueva NO importe `lib/config/oposiciones` (la trampa exacta de esta ficha), que el picker pase `positionType` explícito a `TestPageWrapper`, y que ningún bloque/`getAllThemes` se cuele de vuelta. 36/36 tests en verde.
    - `npx eslint` sobre los 3 ficheros tocados → limpio. 123 tests de `__tests__/oposicionPersonalizada*` + `__tests__/components/oposicionPersonalizada` en verde (sin regresiones).
  - **Lo que NO pude hacer, y por qué (dicho explícito, no escondido):** no hay prueba en NAVEGADOR real ni con `npm run sim:rutas-oposicion-personalizada` (que crea una personalizada efímera vía escritura directa a Postgres) — las dos exigen `DATABASE_URL` de escritura sobre `custom_oposiciones`/`topics`, que este worker no tiene (`vence_coordinacion`, 4 tablas de coordinación). **SOSPECHO, sin poder confirmarlo hoy, que el flujo funciona de extremo a extremo** — la cadena de código está verificada pieza a pieza y el patrón es idéntico al de `test/tema/[numero]` (que SÍ está verificado en producción desde el 01/08) — pero falta el paso final: alguien con escritura tiene que correr `npm run sim:rutas-oposicion-personalizada` (o probar a mano) tras el deploy, antes de darlo por bueno para un usuario real.
  - **Alcance de ESTA entrega, explícito:** solo "aleatorio". **`aleatorio-examen` y `personalizado` (el configurador con filtros) siguen sin construir** — mismo patrón ya probado, mismo motor ya verificado genérico; es la continuación natural con la receta ya escrita. No los até por priorizar terminar UNO completo y bien verificado antes que tres a medias.
  - **Hallazgo de paso, menor:** `custom_oposiciones` da **0 filas siempre** para `vence_lector` (`relrowsecurity=true`, 0 políticas) — mismo mecanismo de bloqueo silencioso que T-573/T-038 en otras tablas, no catalogado en `scripts/canary-rol-lector.cjs`. No es lo que bloqueaba esta tarea (`topics`/`topic_scope`/`questions` sí se pueden leer, que es lo que hacía falta) — se deja anotado para quien amplíe ese canario, no se abre ficha propia por un hallazgo de una línea. until, y no es un plazo interno.** El 31/07 se le escribió a Sergio en el feedback `bd8b92d0` que **«en unos días lo tendrás para probarlo»** y se le pidió que lo revisara y dijera si le sirve. Es la segunda vez que se le anuncia (la primera, sin fecha, fue en `dfc8d0e2` el 30/07), así que **este aviso solo vale si la fecha se cumple**: repetir la promesa sin entregar es peor que no haberla hecho. Sale la primera en `backlog.cjs list`. Si el 05/08 no puede estar, la salida NO es dejarlo correr: es escribirle antes de que venza.
- **De dónde sale:** Sergio (`pcsergio0@gmail.com`, premium, feedback `dfc8d0e2`): *«posibilidad de crear tu oposición con el temario de las leyes y artículos que uno seleccione»*. **Ya respondido**: se le ha dicho que lo vamos a implementar.
- **Ya lo está haciendo a mano, y eso es la mejor prueba de que hace falta.** Tiene **3 configuraciones de test guardadas** (`user_test_favorites`) cuyos nombres son epígrafes de temario: *«TEST DE LOS TRES TEMAS CONSTI»* (CE, 12 artículos), *«Iniciación del procedimiento de oficio por la administración»* (Ley 39/2015, 8 artículos), *«ª Inicio del procedimiento a solicitud del interesado»* (Ley 39/2015, 4 artículos). Se está fabricando su temario bloque a bloque con lo que hay.
- **Por qué lo necesita:** su oposición (`agente-hacienda`) **no está montada** — ficha inactiva, 0 temas — así que usa el catálogo de Auxiliar del Estado como cantera (las 3 configuraciones están guardadas con `position_type=auxiliar_administrativo_estado` mientras su perfil es `agente_hacienda`). Hay **2 premium y 14 free** apuntados a esa oposición.
- **Público objetivo (decisión de Manuel, 30/07):** quien **ya sabe exactamente qué tiene que estudiar**, y las oposiciones de **A1 y A2**, que son **más específicas y menos generalistas** — cada opositor necesita su propia combinación de leyes y artículos, y un temario cerrado no le sirve.
- **Qué habría que diseñar (no está decidido, esto es el punto de partida):**
  - Un contenedor propio («mi oposición») que agrupe varios bloques de leyes+artículos, en vez de N configuraciones sueltas sin relación entre ellas.
  - Que cada bloque se pueda nombrar como un tema (ya lo hacen: los nombres que pone Sergio son epígrafes).
  - Qué se hereda del temario real y qué no: estadísticas por tema, repaso de fallos, progreso, PDF de teoría… Decidir cuáles aplican a un temario propio y cuáles no tienen sentido.
  - **Lo que NO debe pasar:** que un temario propio se confunda con una oposición del catálogo (con su convocatoria, sus plazas y su SEO). Son cosas distintas y mezclarlas ensucia el catálogo.
- **Reutiliza lo que ya existe:** `user_test_favorites` (leyes + artículos por configuración, con `position_type`), el configurador por leyes y su selección de artículos, y el fetcher `lib/lawFetchers.ts`. No hace falta un motor nuevo de preguntas: hace falta la capa de organización encima.
- **Impacto:** 🟠 es funcionalidad nueva, no un arreglo. El valor está en que **abre Vence a las oposiciones que no tenemos montadas** sin construir cada temario a mano — que es hoy el cuello de botella del catálogo.
- **🔬 MEDIDO EL 30/07 — la mitad que reparte YA ESTÁ CONSTRUIDA, y la que sirve no.** Investigando una baja salió que `custom_oposiciones` existe desde marzo y ya tiene `is_public`, `times_selected` y `created_by_username`: **crear, nombrar, publicar y que otros la elijan como objetivo funciona hoy**. Lo que no hay es temario detrás — la fila es solo una etiqueta (nombre, categoría, administración). Números:
  - **303 usuarios** tienen una oposición personalizada como objetivo. **127 no han hecho ni un solo test.** **121 se dieron de alta en los últimos 30 días**, así que sigue pasando.
  - Lo público multiplica el daño: la etiqueta *«Subalterno»* que creó un usuario en marzo se la han llevado ya **25 personas**.
  - **Los nombres genéricos son la señal de alarma:** *Estudiante* (10 de 14 sin tests), *Renfe* (7 de 8), *Administrativo* (8 de 12), *Auxiliar administrativo Comunidad* (8 de 10). Cuanto más vaga la etiqueta, más gente que no llega a estudiar.
  - **Caso con nombre y apellidos:** Pedro (`pedro.delibros`) se dio de alta el 24/07, eligió *«Subalterno»*, estuvo cinco días entrando a mirar temarios y **se dio de baja el 29/07 sin hacer un test**. Su baja no es un cese natural: es alguien que no pudo empezar.
- **📐 DISEÑO DECIDIDO POR MANUEL (01/08) — esto ya no es «qué habría que diseñar», es lo que hay que construir.** Palabras suyas, ordenadas:
  1. **Una página para crear la oposición personalizada.** El usuario escribe el nombre y nosotros la publicamos como **«Oposición \<nombre\> by \<nombre e iniciales de quien la creó\>»**. La autoría va EN EL NOMBRE a propósito: es lo que sostiene la visión de seguidores de abajo, y de paso hace que una etiqueta vaga tenga dueño (los nombres genéricos son justo la señal de alarma medida el 30/07).
  2. **Pública, y de solo lectura para los demás.** Otros usuarios **pueden elegirla, no modificarla**. Solo el creador la edita.
  3. **El creador arma sus TEMAS.** Selecciona leyes y **artículos** —y **títulos cuando los tengamos**— y los añade a un tema que él nombra (*«Tema 1»*, o el epígrafe que quiera), y repite con **tantos temas como quiera**. Es exactamente lo que Sergio ya hace a mano con 3 configuraciones sueltas: aquí pasan a ser un temario con estructura.
  4. **REUTILIZAR EL BUSCADOR DE `/teoria`, y este es el punto fino.** Busca **por nombre de ley Y por contenido del articulado**, y eso es lo que lo hace imprescindible aquí: *«a veces los programas y los epígrafes no hablan de leyes explícitamente sino implícitamente, entonces hay que saber en qué ley está ese contenido»*. Quien arma su temario parte de un epígrafe en prosa, no de una lista de leyes — sin buscar por contenido no sabe ni qué ley elegir. **No construir otro buscador.**
  5. **Al guardar, se convierte en una oposición de Vence NORMAL, con su `topic_scope`.** Nada de un formato paralelo: la misma estructura que el resto del catálogo, así que hereda tests, estadísticas y todo lo que ya cuelga de `topic_scope` sin trabajo extra.
  6. **Entrada desde el icono de tests del Header:** al pincharlo, el usuario va a SU oposición personalizada.
- **🧩 LO QUE YA EXISTE Y HAY QUE ENGANCHAR (auditado el 01/08, no reconstruir nada de esto):**
  | Pieza | Dónde | Estado |
  |---|---|---|
  | Crear/nombrar/publicar/elegir una oposición de usuario | tabla `custom_oposiciones` (`is_public`, `times_selected`, `created_by_username`) | **funciona desde marzo** — le falta el temario detrás |
  | Buscar ley **por nombre** | `searchTeoriaCatalog` en `lib/api/laws/teoriaCatalog.ts` (matview `mv_teoria_law_catalog`, pg_trgm + unaccent) | listo |
  | Buscar **por contenido** del articulado | `searchTeoriaContent` en el mismo fichero (FTS Postgres, config `public.spanish_unaccent`, columna `articles.teoria_content_tsv`) | listo |
  | UI del buscador | `components/TeoriaSearch.tsx` + `app/teoria/page.tsx` (que es quien ejecuta la búsqueda) | listo |
  | Selección ley → artículos, con `question_count` por artículo | `components/TestConfigurator.tsx`, `/api/v2/test-config/articles`, `lib/api/test-config/queries.ts` | listo |
  | Selección por **secciones/títulos** de una ley | `components/SectionFilterModal.tsx`, `getScopedLawSections` | listo — cubre el «títulos cuando los tengan» |
  - **⚠️ EL HUECO EXACTO, medido:** la búsqueda **no tiene endpoint que devuelva resultados** (`/api/teoria/search` es SOLO el gate de cuota, 200/429; la búsqueda ocurre dentro del Server Component de `/teoria`). Y `TestConfigurator` **tiene selección pero no buscador**: elige de una lista precargada. **Unir esas dos mitades es el trabajo.**
  - **Cuota:** `/teoria` limita a 5 búsquedas/día a free y anónimos (`featureLimits`, `feature: 'teoria_search'`). Decidir si el buscador DENTRO del creador de temario usa esa misma cuota o una propia — armar un temario son muchas búsquedas seguidas y 5 se agotan en un minuto.
- **✅ CONSTRUIDA ENTERA EL 01/08 — falta solo verificarla en producción (deploy lanzado, `515aa0f8b`).** El circuito completo: crear → buscar → armar temas → guardar → editar → hacerla tu objetivo → estudiar con ella.
  - **El hueco real no era el buscador, era que NO TENÍA SALIDA.** El motor de `/teoria` busca por nombre de ley **y por contenido del articulado**, pero su único endpoint era un gate de cuota que devuelve 200/429 sin resultados; y `TestConfigurator` tenía la selección de leyes y artículos **sin buscador**. Media función en cada lado. `/api/v2/laws/search` las une, y **buscar por contenido es lo que la hace usable**: los programas oficiales nombran la materia, no la ley («silencio administrativo» → Ley 39/2015 art. 24).
  - **Decisiones que no se ven y sostienen el resto:** «toda la ley» se guarda como `article_numbers` **NULL** (enumerar los artículos de hoy sería una foto que envejece); el `position_type` **deriva del id** de la fila, no del nombre (25 personas se han llevado la etiqueta «Subalterno» — un slug por nombre mezclaría dos temarios); los títulos salen de **`law_sections`**, no de `articles.title_number` (vacía en casi todo el catálogo) y las **disposiciones** (`DA1`, `DT3`) van a un grupo propio porque no caen en ningún rango numérico y **desaparecerían de la pantalla** sin dar error.
  - **⚠️ EL PATRÓN QUE SE REPITIÓ SEIS VECES, y es lo que hay que recordar:** una personalizada **no está en el config estático**, y la app pregunta por él en muchos sitios independientes — el menú del Header, el icono 🎯, el API de temas, su esquema Zod, `isValidTema`, el temario. Cada uno cae a su valor por defecto **sin dar error**: sirve preguntas de OTRA oposición, o manda al usuario a Auxiliar de Madrid. Se arregló pasando el `position_type` **explícito** (siempre opcional: sin él nada del catálogo cambia).
  - **🕷️ LO QUE MÁS APORTA A FUTURO: `npm run sim:rutas-oposicion-personalizada`.** Comprobar pantalla por pantalla **no escala** — una lista escrita a mano solo cubre lo que uno se acordó de mirar. El rastreador **descubre** las rutas siguiendo cada enlace, y encontró cuatro agujeros que ninguna prueba veía, incluido que **el botón de EMPEZAR el test llevaba a una ruta inexistente** (se podía armar el temario y no llegar a estudiar nunca). **No se fía del HTTP 200:** estas páginas son cascarones que devuelven 200 y luego pintan el error.
  - **⏳ CABO SUELTO, escrito en el código:** en ese rastreador el **test en sí** queda **NO CONCLUYENTE** con la cuenta efímera. Con cuenta real carga y se responde (comprobado a mano, preguntas de la LOTC en pantalla); con la efímera la pantalla sale vacía. Descartado el plan (probado premium y free), el onboarding y el aviso de cookies. **Falta aislar qué más necesita esa cuenta.** Se deja NO CONCLUYENTE a propósito: un rojo que no distingue «roto» de «mi cuenta no lo representa» se acaba ignorando, y forzarlo a verde sería mentir.
  - **✅ VERIFICADO EN PRODUCCIÓN (01/08, frontend 598 · backend 158).** Con navegador real sobre la oposición REAL de Manuel: el banner naranja **ya no sale**, se ve su nombre propio, el botón de empezar está y el tema sirve **95 preguntas**. Rastreador contra `www.vence.es`: **6 rutas, 0 rotas**. **Avisado Sergio** (feedback `bd8b92d0`, con el OK de Manuel): email confirmado, se le explica dónde está, que busque por CONTENIDO si su programa no nombra la ley, y que pulse «Hacer mi oposición objetivo». **Cumple el plazo** (vencía el 05/08).
  - **▶ LO QUE QUEDA, y NO es el entregable:** (a) **el test aleatorio, el simulacro y el test personalizado de toda la oposición NO existen** para las personalizadas — comprobado, ninguna de esas rutas está; **nadie las echará en falta hasta que alguien pulse**, igual que pasó con «empezar test», y el rastreador NO las ve porque no hay enlace que las descubra; (b) el **test dentro de la simulación** sigue NO CONCLUYENTE con cuenta efímera (todo lo descartado está en el fichero); (c) decisión de producto pendiente: **editar reescribe el temario entero** y estas oposiciones son públicas — hoy se avisa de cuánta gente la estudia pero no se impide nada; (d) la **visión de seguidores** sigue sin construir.

- **🎯 VISIÓN AMPLIADA (Manuel, 30/07): temarios de usuario con SEGUIDORES.** Que cada opositor pueda ampliar o recortar su temario, publicarlo, y que otros se suscriban o lo elijan como objetivo — con la implicación y el reconocimiento («tener seguidores») como motor. Encaja con lo que ya existe (`times_selected`, `created_by_username`) y ataca el cuello de botella del catálogo por el lado bueno: **la gente amplía el catálogo sin que nosotros construyamos cada temario a mano**.
  - **La palanca que más rinde, y que no es la social:** los temarios más seguidos son una **cola de demanda medida**. Hoy se decide a ciegas qué oposición montar; con esto, los usuarios lo señalan con los pies. Lo que muchos sigan, se monta oficial.
  - **⚠️ Freno 1 — el daño no es simétrico.** Un temario ajeno mal montado perjudica a la vez a todos sus seguidores, y en oposiciones eso son meses de estudio equivocado; además la culpa recaerá sobre Vence, no sobre el autor. **Separar siempre lo oficial de lo comunitario.**
    - **DECIDIDO (Manuel, 30/07) — dos cosas visibles y no en la letra pequeña:** (a) decir **en claro que ese temario NO está verificado por Vence**, y (b) **identificar bien a su autor**. No es un descargo legal: es lo que hace que la información sea juzgable. Quien se suscribe tiene que poder decidir de quién se fía, y quien lo monta responde con su nombre de lo que publica — que es, además, el único incentivo real para mantenerlo al día.
    - **Y el reverso, que importa igual:** si el autor aparece, aparece **bien**. El nombre que se enseñe debe ser el que esa persona haya elegido mostrar, no su correo ni su nombre completo por defecto (mismo criterio de privacidad que ya aplica el panel de referidos, `abbreviateReferredName`). Publicar un temario no puede convertirse en publicar tus datos.
  - **⚠️ Freno 2 — cuidado con qué se premia.** Si la fama son los seguidores a secas, gana quien mejor nombre pone, no quien mejor lo monta (los datos de arriba ya lo enseñan). Atar el reconocimiento a que el temario esté **completo y mantenido** (bloques con preguntas reales, actualizado tras cada convocatoria), no al número de fans. **Un temario con 200 seguidores y abandonado hace más daño que uno sin nadie.**
  - **Consecuencia de orden:** publicar debería **ganarse al completar**, no ser un interruptor desde el minuto cero. Hoy es lo segundo, y por eso hay 127 personas apuntadas a etiquetas vacías.
- **✅ ALCANCE DECIDIDO PARA EL PLAZO (Manuel, 01/08) — se preguntó porque en 4 días no caben las dos mitades, y la respuesta NO fue «solo la privada».** Lo que tiene que estar el **05/08** es **A + un trozo mínimo de B**:
  - **A (privada), entera:** crear «mi oposición», añadirle bloques que el usuario nombra (*«Tema 1 — Iniciación del procedimiento»*, Ley 39/2015 arts. 54-62…) y hacer tests por bloque con sus estadísticas. Es literalmente lo que Sergio ya se fabrica a mano con sus 3 configuraciones guardadas.
  - **De B, solo lo que hace la información juzgable:** **autoría visible** (con el nombre que esa persona haya elegido mostrar, no su correo — ver el reverso del Freno 1) y el aviso **«no verificado por Vence»**. Son las dos cosas que Manuel ya había marcado el 30/07 como «visibles y no en la letra pequeña».
  - **Fuera del plazo, a propósito:** seguidores, suscripción y todo lo que premie la fama. Queda diseñado arriba para después.
  - **Por qué así y no «solo A»:** publicar sin autor y sin el aviso es exactamente el estado que hoy tiene 127 personas apuntadas a etiquetas vacías. Si en la entrega ya se puede publicar algo, esas dos marcas tienen que ir desde el primer día; añadirlas después es tarde.
- **⚠️ AL RETOMARLA, MIRA ANTES `esquina-inferior-izquierda` — hay trabajo empezado que puede no estar en `main`.** El 01/08 ~16:10 esa sesión estaba **viva** y con un commit de esta tarea **sin pushear**: `4e4d401e4 feat(T-327): la tarjeta del tema se lee como una tarjeta, y el lápiz dice que se puede escribir` (encima de `5619cb497`). No empieces de cero sin comprobar si ya está en `origin/main`; si aquel worktree sigue ahí sin pushear, su commit es lo más parecido a sus notas.
- **Cabo suelto de coordinación del 01/08, por si se repite:** T-327 se reclamó a las 11:22 desde el worktree `sesion-01ago-c`, esa sesión **se cayó a las 11:40 sin escribir código** (worktree limpio, 0 commits) y el lease venció solo. Al re-reclamarla a las 16:10 el propio `claim` fue quien avisó del commit de `esquina-inferior-izquierda`, así que **se soltó**: el lease había caducado pero la otra sesión estaba viva y con código encima — la señal de vida manda sobre el lease. Queda **libre y sin trabajo perdido**.
- **Relacionada:** [T-326] (el filtro que pidió en su otro hilo), [T-328] (la landing), y el precedente de vender premium con temario incompleto.

