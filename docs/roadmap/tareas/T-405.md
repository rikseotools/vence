### [T-405] 🟠 [ABIERTO 31/07] El veredicto ROJO de una verificación no llega a ninguna cola: dijo «opciones corruptas» y la pregunta siguió sirviéndose 12 días

- **El caso que lo destapa (31/07):** la usuaria Estela impugnó `8cd4ee16` (`b816cd3a`, RD 889/2022 art. 13). El enunciado preguntaba cómo se presentan el título y la certificación académica y **las cuatro opciones eran las de otra pregunta** (art. 21, recursos), con la clave marcada en una opción que además es falsa en su propio contexto. **La verificación del 19/07 ya lo había escrito, palabra por palabra**: `answer_ok=false`, `options_ok=false`, explicación *«OPCIONES CORRUPTAS: enunciado y explicacion sobre legalizacion/apostilla (art13.1) pero las 4 opciones hablan de recursos; opcion A marcada no responde a la pregunta»*. **Doce días `approved` y sirviéndose** hasta que la cazó una persona.
- **Dónde está el hueco, y por qué NO es [T-036]:** aquella pregunta *qué se mira* (la matriz de dimensiones: le faltaba `enunciado_ok`, los distractores no se comprobaban). Esta pregunta **qué se hace con lo que ya se vio**. Escribir la fila en `ai_verification_results` **no cambia el `lifecycle_state`, no crea señal, no pinga ningún badge y no abre ninguna cola**. El veredicto se queda de dato histórico, y el único consumidor real es una sesión que lo mire a mano.
- **Medido hoy (última verificación no descartada, preguntas ACTIVAS):**
  | Señal | Activas |
  |---|---|
  | `options_ok = false` | **400** |
  | `answer_ok = false` | 19 |
  | `enunciado_ok = false` | 8 |
  | **Total (unión)** | **420**, de las cuales **409 sin `fix_applied`** |
  - La más antigua sin tocar es del **23/05**. **⚠️ El 420 NO es «420 preguntas rotas»:** el grueso de esas `options_ok=false` son notas de **auditoría ciega**, y la propia campaña de junio midió que **el auditor ciego se equivoca más que la pasada que audita** (ver memoria `project-answer-false-active-sweep`). Por eso el trabajo NO es desactivar en bloque, ni un gate automático que oculte 420 preguntas.
- **Lo que sí se sostiene:** hay veredictos **inequívocos** que nadie tiene que adjudicar — «opciones corruptas», «la opción marcada no responde a la pregunta»— y hoy mueren en la tabla. Ese subconjunto es el que debe salir a una cola. Medido: la etiqueta literal `OPCIONES CORRUPTAS` aparece en **1 sola** pregunta de todo el banco (la de Estela, ya reparada), así que **el caudal es pequeño y el triaje es barato** — el problema no es el volumen, es que no hay tubería.
- **Propuesta (por orden de coste):**
  1. Que un veredicto rojo **deje señal visible** — un kind de `content_health_findings` con su frase-gatillo, alimentado por el barrido, en vez de que haya que ir a la tabla a mirar. Es el patrón que ya usa todo lo demás de salud de contenido.
  2. Separar en ese kind **lo inequívoco** (bandas `error`: opciones que no responden al enunciado, clave que contradice el artículo citado) de **lo opinable** (banda de cola, que no pinga el badge), para no repetir el fallo de la bandeja que grita todas las noches ([T-317]).
  3. Decidir si un veredicto inequívoco debe además **bajar el `lifecycle_state`** a `needs_review`. Ojo: eso sí es una puerta con dientes y necesita OK de Manuel, porque oculta preguntas.
- **Capa que lo vigile:** el propio kind es la capa (hoy no hay ninguna). Cuando exista, un guardarraíl de paridad entre lo que la verificación escribe y lo que el barrido lee.

- **✅ HECHO (07/08) — puntos 1 y 2 de la propuesta. Punto 3 (bajar `lifecycle_state`) NO tocado a propósito: necesita OK de Manuel.**
  - **Kind nuevo `veredicto_verificacion_rojo`** en `scripts/health-sweep.cjs` + espejo a mano en `backend/src/content-health-sweep/content-health-sweep.service.ts` (este servicio nunca importa `lib/` del frontend, así que se reimplementa en TS, como ya hacía `opciones_duplicadas` — NO es CLI-only, corre en el `@Cron` de verdad). Núcleo puro `lib/health/veredictoRojoInequivoco.cjs`. Enganchado a `runbookRegistry` + frase **«revisa los veredictos de verificación en rojo»** en CLAUDE.md (paridad verificada).
  - **Query:** la ÚLTIMA verificación no descartada (`DISTINCT ON (question_id) ... ORDER BY verified_at DESC`) de cada pregunta ACTIVA, con al menos uno de `options_ok`/`answer_ok`/`enunciado_ok` en `false` y `fix_applied` no aplicado — así una fila vieja que una verificación posterior ya corrigió no sigue sonando.
  - **DOS BANDAS, tal como pedía la propuesta:** `error` (banda `inequivoco`) cuando el texto describe el defecto sin ambigüedad — tres patrones: la etiqueta «opciones corruptas», «la opción marcada no responde a la pregunta», «las opciones son de otra pregunta». `warn` (banda `opinable`) para el resto — el pool grande, mayormente ruido de auditoría ciega.
  - **⚠️ CALIBRACIÓN — dicho claro, no maquillado: NO validada contra el banco real.** Al ir a medir contra RDS con `VENCE_LECTOR_URL` para reproducir la tabla de arriba (400/19/8/420), **la consulta corre sin error y devuelve 0 filas siempre** — medido: `ai_verification_results` tiene `relrowsecurity=true`, `has_table_privilege(vence_lector,...,'SELECT')=true` pero `pg_policies` **vacío** para esa tabla → RLS sin política deniega TODO en silencio. Mismo patrón exacto que ya mordió en [T-573]/[T-579]/[T-581], esta vez en una tabla más. Confirmado por otra vía que esto es del ROL de lectura del trabajador y no de producción: hay rutas API vivas (`/api/admin/lifecycle/queue`, `/api/topic-review/verify`) que leen/escriben esta tabla con normalidad con la credencial real de la app. **Consecuencia:** el patrón de la banda `error` se construyó sobre el ÚNICO caso confirmado (Estela) — no está calibrado como sí lo está `lib/health/auditNoteExplanation.cjs` (96 casos reales muestreados dos veces). SOSPECHO que cubre razonablemente esa familia de defecto, pero no puedo afirmar que no vuelva a pasar lo mismo que le pasó DOS VECES a `audit_note_explanation` (lista de literales que se queda corta la próxima remesa). **Falta:** correr esto contra RDS con una credencial que SÍ vea la tabla, medir cuántas filas caza cada banda y contrastar con una muestra leída a mano.
  - **Capas:** núcleo puro con 16 tests (incluida reproducción literal del texto real de la verificación de Estela) + bloque `mirror del detector veredicto_verificacion_rojo` en `content-sweep-parity.test.ts` (5 tests: el CLI consume el núcleo sin copiar los patrones, los 3 patrones del backend son byte-idénticos —`.source`/`.flags`— a los del núcleo, los dos gemelos filtran por `DISTINCT ON`+`discarded`, los dos excluyen `fix_applied`, los dos emiten el kind) + `runbookRegistry.test.ts` (paridad CLAUDE.md↔registro). Typecheck limpio frontend y backend.
  - **NO tocado, y es a propósito:** el punto 3 de la propuesta (bajar `lifecycle_state` a `needs_review` cuando el veredicto es inequívoco). La propia ficha ya lo marca como decisión de producto con dientes — oculta preguntas — así que se deja para cuando Manuel lo apruebe explícitamente.
  - **Pendiente de verificar EN VIVO tras el deploy del backend** (no puedo desplegar desde aquí): que el `@Cron` emita `veredicto_verificacion_rojo` la primera noche y que las cifras reales (con credencial completa) confirmen o corrijan la calibración del patrón `error`.

- **✅ CALIBRACIÓN CERRADA (08/08) — el RLS que bloqueaba se arregló por otro camino, y la calibración destapó una recaída real, arreglada.**
  `ai_verification_results` ahora tiene política `flota_lector_lee` para `vence_lector` (arreglado
  en otra tarea, no ésta) — `SELECT count(*) FROM ai_verification_results` da **223.064** filas.
  Reproducida la tabla original con la MISMA definición (última verificación no descartada de
  cada pregunta activa): **options_ok=false 392 · answer_ok=false 8 · enunciado_ok=false 8 · unión
  405, de las cuales 394 sin `fix_applied`** (vs. 400/19/8/420/409 del 31/07 — drift normal de 8
  días, con filas fijadas/descartadas de por medio). **La más antigua sin tocar sigue siendo del
  23/05**, coincide exacto con lo que ya decía la ficha.
  - **Sobre el pool VIVO (394 filas), la banda `error` da 0** — con el patrón original Y con el
    corregido: hoy no hay ningún caso "opciones corruptas" sin atender (el único real, el de
    Estela, ya está `fix_applied`). Eso NO significa que el patrón esté sin probar.
  - **La recaída real apareció al mirar el HISTÓRICO COMPLETO** (11.313 filas rojas de siempre,
    incluidas descartadas/ya corregidas — la única forma de medir precisión con más de un caso
    real): `PATRON_NO_RESPONDE` enganchaba, además del caso de Estela, **5 filas más** que eran
    FALSOS POSITIVOS — notas normales de verificación explicando por qué una opción no es la
    mejor respuesta («no responde ADECUADAMENTE a la pregunta», «no responde LÓGICAMENTE», «no
    responde AL CONTENIDO PRINCIPAL»), no el defecto estructural de Estela («las 4 opciones hablan
    de recursos; opcion A **marcada** no responde a la pregunta» — sin matiz, y nombrando expreso
    que es la MARCADA la que no encaja). **Es la MISMA recaída que ya tuvo `audit_note_explanation`
    dos veces** (lista de literales corta) — aquí se cazó ANTES de que doliera en vivo, porque las
    5 estaban `discarded` y por tanto invisibles para el detector hoy, pero habrían sonado el día
    que una fila parecida quedara sin descartar.
  - **Arreglado:** `PATRON_NO_RESPONDE` ahora exige la palabra "marcada" (la opción ELEGIDA, no
    cualquier opción comentada) cerca de "no responde", y que "no responde" vaya seguido
    directamente de "pregunta" sin un adverbio de matiz en medio. Verificado que sigue reconociendo
    el caso real de Estela (test de regresión con su texto literal) y sus variantes de fraseo, y
    que deja de marcar las 5 filas falsas (fixture con paráfrasis de las 5 en el test, sin copiar
    contenido real de BD). `PATRON_ETIQUETA` y `PATRON_OTRA_PREGUNTA` no engancharon ningún falso
    positivo sobre las 11.313 (0 y 0) — se quedan igual. Arreglado en el núcleo puro
    (`lib/health/veredictoRojoInequivoco.cjs`, +5 tests de regresión) Y en el gemelo del backend
    (`content-health-sweep.service.ts`, mismo patrón, byte a byte — confirmado con
    `content-sweep-parity.test.ts`, 199/199 verde). Typecheck limpio en los dos lados.
  - **Sigue pendiente, y no lo puede hacer este worker:** desplegar el BACKEND para que el `@Cron`
    emita el kind de verdad — hoy solo corre en el CLI. Al desplegar, verificar que la banda
    `error` sigue en 0 salvo que haya aparecido un caso nuevo real.

- **🚨 VERIFICACIÓN DEL DEPLOY (08/08, w3) — el detector SÍ está desplegado, pero no ha escrito NI
  UNA fila jamás. Causa estructural encontrada y arreglada; causa última del registro concreto
  que la dispara, sin confirmar.**
  - **Primero, la corrección de un "✅ DESPLEGADO" que no lo era del todo:** el `falta` con el que
    se retomó esta tarea decía «✅ DESPLEGADO 50a89878 — falta SOLO verificar», pero ese texto
    venía justo después de describir la CALIBRACIÓN (`1f2a26925`), y esa rama **no está mergeada**
    (`git merge-base --is-ancestor 1f2a26925 origin/main` → no). Lo que SÍ está desplegado es el
    detector ORIGINAL (`834ceee86`, confirmado ancestro de `50a89878`, el deploy vivo — verificado
    con `curl https://api.vence.es/health`) — con el `PATRON_NO_RESPONDE` VIEJO, sin calibrar. Dos
    cosas distintas bajo el mismo "✅ DESPLEGADO"; solo una lo estaba.
  - **MEDIDO, no solo leído: `content_health_findings` no tiene NI UNA fila `veredicto_verificacion_
    rojo` desde que existe.** El sweep SÍ corre (578 hallazgos de otros 42 kinds, última pasada
    08/08 07:33 UTC) y el detector SÍ se evalúa (`kindsEvaluados` lo marcaría — ver más abajo por
    qué esto no basta para asegurarlo). Reconstruida la query exacta del detector contra
    `VENCE_LECTOR_URL`: **393 candidatos reales** en banda `warn` (0 en banda `error`, coincide con
    la calibración de arriba — el patrón viejo Y el nuevo dan 0 ahí). Con el sweep sano y 393
    candidatos reales, debería haber una fila `warn` — y no hay ninguna.
  - **No es lo único que falta: OTROS 7 kinds del mismo fichero están en la misma situación**
    (`audit_note_explanation`, `psicotecnico_integridad`, `opciones_duplicadas`,
    `pregunta_duplicada`, `visual_deixis_no_image`, `enunciado_norma_sin_nombrar`,
    `explicacion_estructura_rota`) — 0 filas en `content_health_findings` pese a que su código
    está en el fichero y el sweep produce hallazgos de otros 42 kinds la misma noche. **Descartado
    con evidencia, no por descarte fácil:** (a) NO es un `sweep_incompleto` — 0 filas de ese kind
    en todo el periodo, así que la fase de DETECCIÓN no se corta; (b) NO es un `statement_timeout`
    — la query de `veredicto_verificacion_rojo` corre en 464-721 ms contra RDS real (medido con
    `EXPLAIN ANALYZE`), muy lejos de los 30 s del pool; (c) NO son NUL bytes en el texto de origen
    (Postgres los rechazaría ya al escribir `ai_verification_results`, así que no pueden estar ahí
    para empezar); (d) NO es un `category`/`severity` inválido contra el `CHECK` de la tabla — los
    únicos combos usados por el `add()` principal son los 4 válidos.
  - **CAUSA ESTRUCTURAL encontrada leyendo el código, y arreglada — el mismo patrón de T-307, un
    escalón más abajo, sin extenderse:** la fase de DETECCIÓN (`detectarTodo()`) tiene su try/catch
    desde T-307 y aísla un detector que revienta. La fase de ESCRITURA (`for (const f of F) { …
    INSERT … }`, justo después) **no tenía ninguno** — un `INSERT` que Postgres rechaza tira el
    bucle entero, y como el `DELETE` de los kinds evaluados ya se ejecutó ANTES de insertar nada,
    todo lo que viniera DESPUÉS del registro que revienta se queda sin reponer, en silencio. El
    patrón de kinds ausentes intercalados con kinds presentes (no un corte limpio) encaja con esto:
    una fila puntual, no un fallo global.
  - **SOSPECHO, sin poder confirmarlo, cuál es el registro concreto que dispara esto.** Lo que
    tengo: la causa ESTRUCTURAL (el bucle sin aislar) está demostrada por lectura de código y
    arreglada; el DATO exacto que la dispara cada noche no lo he podido aislar sin logs de
    aplicación ni permiso de escritura para reproducir el `INSERT` real. Descartadas NUL bytes y
    `CHECK` inválido como se explica arriba. **Falta para confirmarlo del todo:** desplegar este
    fix y leer el hallazgo `sweep_escritura_incompleta` nuevo (ver abajo) la primera noche — dirá
    el `kind` y el mensaje de error exactos del registro que fallaba.
  - **Arreglado (no solo diagnosticado):** `for (const f of F)` ahora envuelve CADA `INSERT` en su
    propio try/catch — si uno falla, se registra (`kind` + error, recortado a 300 caracteres) y se
    sigue con el resto, sin reintentar (un dato que Postgres rechaza no se cura repitiendo el mismo
    `INSERT`). Si hay fallos, se emite un hallazgo propio `sweep_escritura_incompleta`
    (`app`/`error`, registrado en `runbookRegistry.ts` junto a su hermano `sweep_incompleto`) con
    el resumen y hasta 20 fallos en el `detail`. Aplicado en los DOS gemelos:
    - Backend (`content-health-sweep.service.ts`): reimplementado inline en TS, mismo patrón que
      el resto de esta ficha (el servicio nunca importa `lib/` del frontend).
    - CLI (`scripts/health-sweep.cjs`): usa el núcleo compartido nuevo
      `lib/db/escrituraResiliente.cjs` (`escribirConAislamiento`/`mensajeEscrituraIncompleta`).
      Vive en `lib/db/` y NO en `lib/health/` a propósito: no es un detector de contenido que
      necesite calibrarse contra el banco (como sus vecinos, con su guardarraíl de "anclas",
      T-718) — es infraestructura de escritura, como `lib/db/filasAfectadas.ts` (T-613).
  - **Capas:** 9 tests del núcleo (`__tests__/lib/db/escrituraResiliente.test.ts`, incluida la
    reproducción explícita del defecto: una lista con un `insertar` que revienta a mitad, y se
    comprueba que el resto SÍ se escribe) + 5 tests nuevos de paridad en
    `content-sweep-parity.test.ts` (el CLI consume el núcleo, el backend aísla cada INSERT con su
    propio try/catch, los dos emiten `sweep_escritura_incompleta`, los dos registran sin
    reintentar) + registro en `runbookRegistry.ts`. `anclasDetectores.guardrail.test.ts` (T-718)
    sigue en verde tras mover el fichero fuera de `lib/health/` — de haberse quedado ahí, habría
    subido el trinquete de "detectores sin anclas" de 21 a 22 sin ser un detector de verdad.
    2.013 tests del área en verde (guardrails + health + db + parity), 1.599/1.600 del backend
    completo (1 fallo preexistente no relacionado, `canary-registry.spec.ts`). `tsc --noEmit`
    limpio en frontend y backend.
  - **Rama `flota/T-405-cola-y-resiliencia-escritura` (incluye la calibración de arriba, extraída
    limpia de `flota/T-405-calibracion-patron` + esta resiliencia), pusheada.**
  - **Falta, y no lo puede hacer este worker:** desplegar backend + frontend. Tras el deploy: (1)
    confirmar que `veredicto_verificacion_rojo` empieza a aparecer con ~393 en banda `warn` y 0 en
    `error`; (2) confirmar lo mismo para los otros 7 kinds afectados (deberían empezar a aparecer
    también, si el arreglo estructural es correcto); (3) si aparece algún `sweep_escritura_
    incompleta`, leer su `detail.fallos` — ESO dirá el registro/kind exacto que seguía fallando y
    cerrará la sospecha que aquí queda sin confirmar; (4) el punto 3 de la propuesta original
    (bajar `lifecycle_state`) sigue sin tocar, necesita OK explícito de Manuel.
- **Relacionada:** [T-036] (la matriz de verificación: qué dimensiones se miran), memoria `project-answer-false-active-sweep` (por qué NO se actúa en bloque sobre este pool), [T-317] (la lección de no llenar el badge de hallazgos no accionables), [T-573]/[T-579]/[T-581] (mismo patrón de RLS sin política bloqueando al rol de lectura del trabajador, en otras tablas), [T-307] (el mismo aislamiento, un escalón más arriba: la fase de detección).

