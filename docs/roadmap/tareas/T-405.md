### [T-405] 🟠 [ABIERTO 31/07] El veredicto ROJO de una verificación no llega a ninguna cola: dijo «opciones corruptas» y la pregunta siguió sirviéndose 12 días

- **El caso que lo destapa (31/07):** la usuaria Estela impugnó `8cd4ee16` (`b816cd3a`, RD 889/2022 art. 13). El enunciado preguntaba cómo se presentan el título y la certificación académica y **las cuatro opciones eran las de otra pregunta** (art. 21, recursos), con la clave marcada en una opción que además es falsa en su propio contexto. **La verificación del 19/07 ya lo había escrito, palabra por palabra**: `answer_ok=false`, `options_ok=false`, explicación *«OPCIONES CORRUPTAS: enunciado y explicacion sobre legalizacion/apostilla (art13.1) pero las 4 opciones hablan de recursos; opcion A marcada no responde a la pregunta»*. **Doce días `approved` y sirviéndose** hasta que la cazó una persona.
- **Dónde está el hueco, y por qué NO es [T-036]:** aquella pregunta *qué se mira* (la matriz de dimensiones: le faltaba `enunciado_ok`, los distractores no se comprobaban). Esta pregunta **qué se hace con lo que ya se vio**. Escribir la fila en `ai_verification_results` **no cambia el `lifecycle_state`, no crea señal, no pinga ningún badge y no abre ninguna cola**. El veredicto se queda de dato histórico, y el único consumidor real es una sesión que lo mire a mano.
- **Medido hoy (última verificación no descartada, preguntas ACTIVAS):**
  | Señal | Activas |
  |---|---|
  | `options_ok = false` | **400** |
  | `answer_ok = false` | 19 |
  | `enunciado_ok = false` | 8 |
  | **Total (unión)** | **420**, de las cuales **409 sin `fix_applied`** |
  - La más antigua sin tocar es del **23/05**. **⚠️ El 420 NO es «420 preguntas rotas»:** el grueso de esas `options_ok=false` son notas de **auditoría ciega**, y la propia campaña de junio midió que **el auditor ciego se equivoca más que la pasada que audita** (ver memoria `project-answer-false-active-sweep`). Por eso el trabajo NO es desactivar en bloque, ni un gate automático que oculte 420 preguntas.
- **Lo que sí se sostiene:** hay veredictos **inequívocos** que nadie tiene que adjudicar — «opciones corruptas», «la opción marcada no responde a la pregunta»— y hoy mueren en la tabla. Ese subconjunto es el que debe salir a una cola. Medido: la etiqueta literal `OPCIONES CORRUPTAS` aparece en **1 sola** pregunta de todo el banco (la de Estela, ya reparada), así que **el caudal es pequeño y el triaje es barato** — el problema no es el volumen, es que no hay tubería.
- **Propuesta (por orden de coste):**
  1. Que un veredicto rojo **deje señal visible** — un kind de `content_health_findings` con su frase-gatillo, alimentado por el barrido, en vez de que haya que ir a la tabla a mirar. Es el patrón que ya usa todo lo demás de salud de contenido.
  2. Separar en ese kind **lo inequívoco** (bandas `error`: opciones que no responden al enunciado, clave que contradice el artículo citado) de **lo opinable** (banda de cola, que no pinga el badge), para no repetir el fallo de la bandeja que grita todas las noches ([T-317]).
  3. Decidir si un veredicto inequívoco debe además **bajar el `lifecycle_state`** a `needs_review`. Ojo: eso sí es una puerta con dientes y necesita OK de Manuel, porque oculta preguntas.
- **Capa que lo vigile:** el propio kind es la capa (hoy no hay ninguna). Cuando exista, un guardarraíl de paridad entre lo que la verificación escribe y lo que el barrido lee.

- **✅ HECHO (07/08) — puntos 1 y 2 de la propuesta. Punto 3 (bajar `lifecycle_state`) NO tocado a propósito: necesita OK de Manuel.**
  - **Kind nuevo `veredicto_verificacion_rojo`** en `scripts/health-sweep.cjs` + espejo a mano en `backend/src/content-health-sweep/content-health-sweep.service.ts` (este servicio nunca importa `lib/` del frontend, así que se reimplementa en TS, como ya hacía `opciones_duplicadas` — NO es CLI-only, corre en el `@Cron` de verdad). Núcleo puro `lib/health/veredictoRojoInequivoco.cjs`. Enganchado a `runbookRegistry` + frase **«revisa los veredictos de verificación en rojo»** en CLAUDE.md (paridad verificada).
  - **Query:** la ÚLTIMA verificación no descartada (`DISTINCT ON (question_id) ... ORDER BY verified_at DESC`) de cada pregunta ACTIVA, con al menos uno de `options_ok`/`answer_ok`/`enunciado_ok` en `false` y `fix_applied` no aplicado — así una fila vieja que una verificación posterior ya corrigió no sigue sonando.
  - **DOS BANDAS, tal como pedía la propuesta:** `error` (banda `inequivoco`) cuando el texto describe el defecto sin ambigüedad — tres patrones: la etiqueta «opciones corruptas», «la opción marcada no responde a la pregunta», «las opciones son de otra pregunta». `warn` (banda `opinable`) para el resto — el pool grande, mayormente ruido de auditoría ciega.
  - **⚠️ CALIBRACIÓN — dicho claro, no maquillado: NO validada contra el banco real.** Al ir a medir contra RDS con `VENCE_LECTOR_URL` para reproducir la tabla de arriba (400/19/8/420), **la consulta corre sin error y devuelve 0 filas siempre** — medido: `ai_verification_results` tiene `relrowsecurity=true`, `has_table_privilege(vence_lector,...,'SELECT')=true` pero `pg_policies` **vacío** para esa tabla → RLS sin política deniega TODO en silencio. Mismo patrón exacto que ya mordió en [T-573]/[T-579]/[T-581], esta vez en una tabla más. Confirmado por otra vía que esto es del ROL de lectura del trabajador y no de producción: hay rutas API vivas (`/api/admin/lifecycle/queue`, `/api/topic-review/verify`) que leen/escriben esta tabla con normalidad con la credencial real de la app. **Consecuencia:** el patrón de la banda `error` se construyó sobre el ÚNICO caso confirmado (Estela) — no está calibrado como sí lo está `lib/health/auditNoteExplanation.cjs` (96 casos reales muestreados dos veces). SOSPECHO que cubre razonablemente esa familia de defecto, pero no puedo afirmar que no vuelva a pasar lo mismo que le pasó DOS VECES a `audit_note_explanation` (lista de literales que se queda corta la próxima remesa). **Falta:** correr esto contra RDS con una credencial que SÍ vea la tabla, medir cuántas filas caza cada banda y contrastar con una muestra leída a mano.
  - **Capas:** núcleo puro con 16 tests (incluida reproducción literal del texto real de la verificación de Estela) + bloque `mirror del detector veredicto_verificacion_rojo` en `content-sweep-parity.test.ts` (5 tests: el CLI consume el núcleo sin copiar los patrones, los 3 patrones del backend son byte-idénticos —`.source`/`.flags`— a los del núcleo, los dos gemelos filtran por `DISTINCT ON`+`discarded`, los dos excluyen `fix_applied`, los dos emiten el kind) + `runbookRegistry.test.ts` (paridad CLAUDE.md↔registro). Typecheck limpio frontend y backend.
  - **NO tocado, y es a propósito:** el punto 3 de la propuesta (bajar `lifecycle_state` a `needs_review` cuando el veredicto es inequívoco). La propia ficha ya lo marca como decisión de producto con dientes — oculta preguntas — así que se deja para cuando Manuel lo apruebe explícitamente.
  - **Pendiente de verificar EN VIVO tras el deploy del backend** (no puedo desplegar desde aquí): que el `@Cron` emita `veredicto_verificacion_rojo` la primera noche y que las cifras reales (con credencial completa) confirmen o corrijan la calibración del patrón `error`.
- **Relacionada:** [T-036] (la matriz de verificación: qué dimensiones se miran), memoria `project-answer-false-active-sweep` (por qué NO se actúa en bloque sobre este pool), [T-317] (la lección de no llenar el badge de hallazgos no accionables), [T-573]/[T-579]/[T-581] (mismo patrón de RLS sin política bloqueando al rol de lectura del trabajador, en otras tablas).

