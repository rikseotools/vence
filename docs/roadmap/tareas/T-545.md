### [T-545] 🟡 [ABIERTO 04/08] Las oposiciones DE COMUNIDAD se ofrecen en el onboarding sin contenido detrás

**Qué pasa.** En el onboarding, entre las opciones de oposición aparecen las **creadas por usuarios** (`custom_oposiciones`), con el formato **«👥 Auxiliar administrativo ⭐ 22 usuarios»**. Quien la elige se queda con un `user_profiles.target_oposicion` que es un **UUID de `custom_oposiciones`**, no un `position_type` — y esa etiqueta **no tiene temas ni preguntas detrás** (la más elegida, `dd5c1b2b`, creada en enero por una usuaria: **0 temas**). El resto de la app espera un slug.

**Medido el 04/08/2026:**

| | |
|---|---|
| Perfiles con una oposición de comunidad como suya | **329** (de 11.539 con valor) |
| Altas así en los últimos 30 días | 137 (sigue ocurriendo hoy) |
| Oposiciones de comunidad públicas y activas | 589 |

**Con su base de comparación, que es lo que lo hace un hallazgo y no un número suelto** (altas de los últimos 90 días):

| Oposición elegida | Usuarios | Sin hacer **un solo** test |
|---|---|---|
| Una nuestra | 6.826 | 1.965 (**28,8 %**) |
| **De comunidad** | 237 | 99 (**41,8 %**) |

Es decir, ~1,45× más probable que no lleguen a hacer nada. No es un abismo, pero es una fuga en el primer minuto de vida del usuario y **se puede cerrar en el onboarding**.

**Cómo salió.** Investigando las dos bajas del 04/08. Las dos usuarias pulsaron ese mismo botón en el onboarding. Sara Chamorro se quedó con él y a continuación pulsó **«Cambiar oposición» seis veces en 90 segundos** antes de pedir la baja (feedback `88cd06d3`, [T-544]); María Aguado se cambió a mano a Castilla y León al minuto.

**Y lo que NO se puede afirmar, para que nadie lo lea de más:** de los 329, **solo 1** ha pedido la baja. La correlación con las bajas es de un caso; lo medido de verdad es el «sin un solo test».

**Qué mirar.**
1. ¿Debe el onboarding ofrecer una etiqueta creada por un usuario **al mismo nivel** que una oposición construida? Como mínimo, distinguirlas visualmente o mandarlas al final.
2. Si se elige una, la app debería **decir que aún no tiene contenido** y ofrecer la equivalente nuestra, en vez de dejar al usuario en una pantalla vacía dándole a «Cambiar oposición».
3. `target_oposicion` guardando dos cosas distintas (slug o UUID de otra tabla) es un contrato ambiguo: conviene decidir si eso se queda así a propósito y, si se queda, que los lectores lo traten explícitamente.

#### Re-medido 08/08 (w3), retomando desde T-562 — el punto 2 ya está cerrado, el 1 nunca estuvo abierto

Al retomarla desde el hermano [T-562] (mismo patrón: catalogada-vacía vs. construida, aquí
personalizada-vacía vs. construida), verificado punto por punto contra el código y el deploy real,
no contra lo que decía esta ficha:

- **Punto 2 — YA CERRADO por [T-339], confirmado desplegado en producción HOY.**
  `git merge-base --is-ancestor 35b004203 <sha del deploy live de www.vence.es>` → sí. Y leído
  `app/api/v2/onboarding/save-field/route.ts:59-69`: si la personalizada elegida no tiene temas
  utilizables, devuelve **409 + `ERROR_PERSONALIZADA_SIN_TEMARIO`** — `handleSelectCustom` en
  `OnboardingModal.tsx` ya NO guarda en silencio, espera la respuesta y pinta el error real. El
  «callejón sin salida silencioso» que motivó esta ficha (la persona se queda con la etiqueta
  vacía sin que nadie se lo diga) está cerrado para toda selección NUEVA desde que ese commit se
  desplegó. Lo que T-339 **no** hace (y esta ficha sí pedía): ofrecer la equivalente construida
  por nombre — solo bloquea y dice "elige otra". Extensión pequeña y de bajo riesgo, reusando
  `findBuiltEquivalent`/`builtDisplayName` que [T-562] ya construyó y testeó para el mismo patrón
  — **no la implemento aquí**: `OnboardingModal.tsx` tiene su PROPIA revisión pendiente sin cerrar
  (T-339 sigue "🙋 ESPERANDO REVISIÓN DE MANUEL" sobre si bloquear al 100% es la decisión correcta
  o debería ser un aviso más suave) — construir encima de una decisión aún no confirmada es
  invertir en la dirección equivocada si Manuel decide lo contrario.
- **Punto 1 — NUNCA estuvo realmente abierto: verificado con `git blame`, la distinción visual
  existe desde el 07/12/2025** (`components/OnboardingModal.js` original, antes del propio
  onboarding actual), mucho antes de que esta ficha se escribiera (04/08/2026). Las personalizadas
  ya se renderizan en **su propia sección, DESPUÉS de las oficiales**, con color morado distintivo,
  icono 👥 y el contador "⭐ N usuarios" — exactamente lo que el punto 1 pedía. No hay nada que
  arreglar aquí; probablemente se escribió sin mirar el render real.
- **Punto 3 — sigue abierto, es arquitectura, no un bug puntual.** No tocado.
- **Hallazgo nuevo, no estaba en la ficha original: T-339 NO es retroactivo.** El gate solo corta
  ESCRITURAS nuevas — los **329 perfiles medidos el 04/08** que YA tienen `target_oposicion`
  apuntando a una personalizada vacía siguen exactamente igual: nadie les corrigió el valor ni les
  avisó. Si esta gente vuelve a abrir la app, sigue viendo el mismo vacío que antes de T-339 (la
  pantalla de temario/test, no el onboarding — ahí sí actúa `AvisoTemarioVacio` de [T-508], que es
  posterior y SÍ es retroactivo porque lee en caliente, no en el momento de guardar). No confirmado
  con cifra fresca — **no re-medible**: `custom_oposiciones` y `user_profiles` siguen con RLS activo
  y CERO políticas para `vence_lector` (mismo patrón de falso-cero que [T-573]/[T-579]/[T-581]/
  [T-405] — confirmado de nuevo hoy, `custom_oposiciones` da `count=0` sin error, no es que esté
  vacía). Si alguien con credencial completa quiere recontar el 04/08→hoy, la consulta ya está en
  el bloque "Medido el 04/08" de arriba.
- **No hay más código que escribir aquí sin antes saber qué decide Manuel en [T-339].** Dejo la
  ficha corregida y el hallazgo de la falta de retroactividad escrito, que es lo que se habría
  perdido si esta sesión terminaba sin dejarlo por escrito.

