### [T-091] ✅ [FIX EN MAIN 24/07 — pendiente de que entre en el próximo deploy] El badge de Fraudes caía al contador viejo de rate-limit cuando las señales F0 eran 0
- **Qué:** el badge 🚨 de la pestaña Fraudes (`app/admin/layout.tsx`) tenía un **fallback**: pintaba `fraudSignals.pending || adminNotifications.rateLimitHits`. Cuando F0 dejaba las señales `fraud_alerts status='new'` en 0 pero había `rate_limit` hits sin revisar (24h) en `validation_error_logs`, el badge mostraba esa métrica VIEJA como si fueran fraudes.
- **✅ Resuelto (opción b, 24/07):** quitado el fallback en la condición Y el display del badge → ahora usa **solo** `fraudSignals.pending` (las señales antifraude F0). Los rate-limit hits dejan de inflar el badge de Fraudes. `adminNotifications` sigue vivo (lo usa el badge de feedback). Commit de código en `origin/main`; **toma efecto en el próximo deploy del frontend** (no auto-deploy en push). Verificado que el bug estaba latente al arreglarlo: había 4 señales F0 reales (badge mostraba 4, correcto) + 19 rate-limit hits que habrían aparecido en cuanto F0 llegara a 0.
- **Estado:** fix en main, esperando deploy. No hizo falta la opción (a) (bookkeeping deshonesto de marcar reviewed lo no revisado).

