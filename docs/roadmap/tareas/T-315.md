### [T-315] 🔴 [ABIERTO 30/07] El techo de 25 s de `answer-and-save` es el TIMEOUT DEL ANTIFRAUDE, no una degradación — y subirlo fue tratar el síntoma

#### ✅ APLICADA LA OPCIÓN B (07/08/2026) — pendiente de DEPLOY de backend para verificar
- **Lo desbloqueó un usuario, no una alerta:** Lourdes (feedback `e790c7bf`) escribió que la app «se queda colgada»; se le contestó que era el configurador de artículos ([T-623]) y **replicó que no**. Al mirar su rastro: el 07/08 entre las **08:30 y 08:32**, haciendo el test del tema 8, `/api/v2/answer-and-save` tardó **12-25 s** y devolvió **503** («Servicio saturado»); su cliente abortó a los 15 s **siete veces seguidas** y al terminar saltó `cupo_safety_net` (2 respuestas rellenadas). **No es suya sola:** 644 timeouts de la cola en **91 usuarios** (72 h), 503 en 5-10 usuarios/día, y la alerta `endpoint_latency_sustained` disparó ese mismo día a las 09:45 con **p95 de 25.082 ms**. La ventana 08:30-08:32 se repite los días 5, 6 y 7.
- **Decisión de Manuel (07/08): opción B** — el bloque de comprobaciones sigue bloqueando igual que hoy (sin cambio de semántica de producto), pero con **techo propio corto que NO sale del presupuesto de guardar**. La opción A (sacar los chequeos de dispositivo del camino crítico) queda como fase 2, con su propio trabajo.
- **Lo aplicado, en código:**
  - `backend/src/answer-save/presupuesto.ts` (núcleo puro, nuevo): **un** presupuesto declarado en un sitio — `ANSWER_SAVE_BUDGET_MS=20.000`, `COMPROBACIONES_MAX_MS=5.000`, `GUARDAR_MIN_MS=3.000`. Las fases consumen de él en vez de tener cuota fija.
  - `answer-save.controller.ts`: mueren los dos números sueltos (`ANTIFRAUD_TIMEOUT_MS=10.000`, `VALIDATE_AND_SAVE_TIMEOUT_MS=15.000`). El peor caso deja de ser una **suma abierta** (10+15, que vuelve a crecer en cuanto alguien sube uno) y pasa a ser el presupuesto declarado.
  - **Guardar no pierde nada y gana margen:** en el PEOR caso conserva sus 15.000 ms (20.000 − 5.000) y en el normal recibe **hasta 19.500** — antes se quedaba con 15.000 fijos aunque hubiera margen de sobra.
  - **Por qué 20 s y no 25 s:** con el backend presupuestando los mismos 25 s del proxy, quien cortaba SIEMPRE era el proxy, así que el 503 con `Retry-After` **no llegaba nunca** y el usuario recibía un abort sin causa. Con 20 s el backend termina dentro de la ventana del proxy y la respuesta que ve el cliente es la nuestra.
- **Y deja de ser ciego (era el hueco de [T-312] en el backend, que no emitía NI UN evento propio de este endpoint):** `answer_save_presupuesto_agotado` (`error`, con la **fase** culpable) en cada 503, y `answer_save_fases` (`warn`, con el desglose comprobar/guardar) en las lentas que sí acaban. Antes el único rastro era un `logger.warn` a CloudWatch: ni consultable ni alertable.
- **Capas:** 9 unitarios del núcleo puro + 4 del controller (reparto y rastro) + 9 de la regla de alerta nueva `answer_save_presupuesto_agotado` (≥5 cortes y ≥2 usuarios en 30 min: con un solo usuario puede ser su conexión) + **guardarraíl de la CADENA** `__tests__/backend/answerSavePresupuestoParidad.test.ts`, que lee los ficheros del frontend como texto y exige `presupuesto < proxy ≤ cliente < maxDuration` — que es justo lo que nadie ataba y por lo que cinco números en cuatro ficheros «cuadraban por casualidad».
- **Validado POR MUTACIÓN, no solo en verde:** devolver el presupuesto a 15.000 rompe 2 tests; subir el techo de comprobaciones a los 10.000 de antes rompe 1. ⚠️ Los tests del **controller** usan las constantes, así que comprueban el CABLEADO (que cada fase reciba el número del presupuesto) y **no pueden juzgar el valor**: eso lo fijan las aserciones absolutas del núcleo puro.
- **Verde:** 61/61 en `backend/src/answer-save`, 9/9 de la regla, 4/4 del guardarraíl de paridad, typecheck de los dos proyectos. Los 2 rojos de `alert-rules.kind-sin-evaluar.spec.ts` son **previos** (comprobado stasheando el cambio).

#### ✅ Y LA MITAD DEL CLIENTE, que era lo que ella describía (07/08/2026)
- **La pieza que no estaba en ninguna ficha:** el guardado NO era trabajo de fondo invisible. `components/TestLayout.tsx` esperaba **20 s** a que la cola drenara antes de cerrar el test, y hasta entonces no aparecían **ni la confirmación ni «Revisar fallos» ni «Practicar mis fallos»**. O sea: el día que el servidor se satura, la persona termina el test y se queda sin las acciones que buscaba — *«cuando termino un test y quiero hacer otro»*, *«ver las respuestas equivocadas»*, literal de su réplica. [T-315] lo trataba como latencia y nadie había atado esa latencia a una pantalla.
  - ⚠️ **Corrección de un dato que dije mal a mitad de análisis:** la pantalla de resultados SÍ se pinta enseguida (`setIsExplicitlyCompleted(true)` es síncrono). Lo que se queda esperando son la confirmación y esos dos botones, que van condicionados a `saveStatus === 'saved'`.
- **Esperar 20 s no compraba nada:** las respuestas ya están a salvo (localStorage + reintentos de la cola + safety-net del servidor, que además cobra el cupo de lo que estrena, T-260). La espera solo aliviaba al safety-net en el caso común… que drena en menos de un segundo. Los otros 17 s solo se pagan cuando algo va mal, que es cuando el usuario menos puede permitírselos. Ahora son **3 s** (`lib/tests/cierreDeTest.ts`), y NO 0 a propósito: si el safety-net rellenara siempre, el camino excepcional pasaría a ser el normal y dejaría de estar vigilado.
- **Y el estado de error dejaba a la persona sin NADA** (ni aviso, ni botones, ni forma de reintentar). Ahora dice que sus respuestas están a salvo —lo están— y ofrece **Reintentar**, que vuelve a ejecutar el cierre real (guardado en un ref), no un botón decorativo.
- **La señal que faltaba, que es lo más caro de esto:** lo único que había cuando la cola no drenaba era un `console.warn` → **6 apariciones en 10 días, NINGUNA de la usuaria que escribió**, mientras `cupo_safety_net` (la prueba de que el servidor tuvo que rellenar) salta **2-6 veces al día**, su sesión incluida. No se puede arreglar lo que no se cuenta: ahora emite `test_cierre_sin_drenar` (`warn`, con pendientes y espera). Tipo PROPIO y no `client_error` a propósito — no es un error del navegador, es consecuencia del servidor, y contarlo como error de cliente dispararía la alerta equivocada.
- **Capas:** 5 unitarios del núcleo puro + 6 del cableado (`__tests__/components/TestLayoutCierre.test.ts`). ⚠️ Esos 6 son **estáticos sobre el fuente** (misma familia y misma limitación que `TestLayoutReviewButton.test.ts`): comprueban el CABLEADO, no el comportamiento en un navegador. Lo que se ejecuta de verdad son las decisiones del núcleo puro. Verde: 100/100 de las suites de TestLayout, 1.819 de observabilidad/utils/guardarraíles, typecheck y lint (solo los 2 avisos de `exhaustive-deps` previos).
- **⏳ LO QUE SIGUE SIN EXPLICAR, y no se le vende como resuelto:** la pantalla **en blanco** y los cuelgues navegando por temario o por las falladas. En su rastro no hay nada que los respalde (cero errores de carga de la app en su cuenta en 7 días).
- **⏳ FALTA:** **deploy de BACKEND y de FRONTEND** (el cambio no está vivo hasta entonces) y, a las 24-48 h, comprobar contra producción que (a) los muros de 10-11 s y 15-16 s desaparecen de la distribución, (b) `answer_save_presupuesto_agotado` aparece con su fase, (c) `test_cierre_sin_drenar` empieza a contar lo que antes no se veía y (d) el goteo de 503 baja. Hasta eso, la tarea NO está verificada.

- **🔴 RE-DIAGNOSTICADO 06/08 — TODO LO DE ABAJO APUNTA AL FICHERO EQUIVOCADO. El muro real está en el BACKEND, no en `route.ts`.** `shouldRouteToBackend('answer-and-save')` es `true` **hardcoded desde el 24/05/2026**, sin % de rollout (`lib/api/backend-router.ts:38`) — para casi todo el tráfico real, `app/api/v2/answer-and-save/route.ts` reenvía la petición a `https://api.vence.es` y el código local (antifraude + `validateAndSaveAnswer`, con sus timeouts de 25000/25000) **solo se ejecuta si el proxy FALLA**. Es prácticamente código muerto para el camino feliz. Confirmado por TRES sesiones independientes con cifras casi idénticas en 36h (w2 el 05/08, w4 el 06/08 08:10 UTC, yo mismo el 06/08 ~23:30 UTC):
  ```sql
  -- Los timeouts de route.ts NUNCA disparan (14 días, 3 mediciones independientes: 0/0/0)
  SELECT count(*) FROM observable_events WHERE event_type='http_timeout'
    AND metadata->>'timeoutMs'='25000' AND ts > now() - interval '14 days';
  ```
  | banda de `duration_ms` | eventos (14 días) | % con `http_status=503` |
  |---|---|---|
  | 10.000-10.999 ms | 19 | **84%** |
  | 15.000-15.999 ms | 15 | **80%** |
  | 25.000-25.999 ms | 46 | 0% (78% con 200 — los que SÍ acaban a tiempo) |

  Ese doble muro (10-11s y 15-16s, no 25s) es la firma exacta del **backend real**: `backend/src/answer-save/answer-save.controller.ts:40-42` — `ANTIFRAUD_TIMEOUT_MS=10_000` y `VALIDATE_AND_SAVE_TIMEOUT_MS=15_000`, aplicados **SECUENCIALMENTE** (antifraude primero, luego validate-and-save), así que el peor caso es la SUMA: **25 s**, y al expirar cualquiera de los dos devuelve **503 vía `serviceSaturatedResponse()` — falla CERRADO**, no 200. El hallazgo #2 de más abajo ("el antifraude falla en abierto, devuelve 200") **tampoco se sostiene ni siquiera en `route.ts`** (su catch final SIEMPRE devuelve 503 vía `isDbTimeoutError`) — la premisa parecía errónea desde el origen, no solo mal atribuida al fichero.
  - **El backend NO emite NINGÚN evento de observabilidad propio para este endpoint** (ni `request_completed` ni nada — confirmado: cero filas `source='fargate'` para tráfico orgánico de `answer-and-save`, solo el canario sintético `canary-answer-save`). Todo lo de la tabla de arriba viene de `request_completed` que emite el WRAPPER de `route.ts` (mide el viaje completo: proxy + procesamiento del backend), no de instrumentación propia del backend. Su único rastro hoy es `this.logger.warn(...)` — CloudWatch, no consultable, no alertable.
  - **Por qué esto también corrige [T-312]:** el desglose de fases que se acababa de construir ese mismo día (`answer_save_lento` primero, `answer_save_ruta_lenta` después) instrumenta el código LOCAL de `route.ts` — el que casi nunca se ejecuta. Explica por qué `answer_save_lento` lleva 0 disparos en 7 días con el síntoma intacto: no solo porque su reloj arranca tarde (razón ya documentada en [T-312]), sino porque **el código que envuelve entero rara vez corre**. La instrumentación que de verdad hace falta va en `answer-save.controller.ts`, no en el frontend.
- **Decisión de Manuel (05/08, pregunta bloqueante #10, respondida 12:07 UTC):** *"Sí a las dos [backend, no frontend; y sí, rediseñar la secuencia] — el arreglo va al BACKEND. Aplicarlo a route.ts habría tocado código casi inerte, que es peor que no hacer nada porque deja la ficha cerrada y el muro intacto. […] Sobre el cómo: PRESUPUESTO ÚNICO, no realinear cuatro números. Cuatro timeouts independientes que solo funcionan si suman bien vuelven a desalinearse en cuanto alguien toca uno […] y 10s+15s SECUENCIALES significa que el peor caso es la SUMA, así que 'realinear' obliga a bajar los dos y a perder margen en el camino que sí importa. […] el antifraude —que es el que puede tardar y no es el que guarda la respuesta del usuario— fuera del camino crítico o con su propio límite que NO se le coma al guardado. NO lo apliques por tu cuenta: es un endpoint keystone y toca el antifraude. Escribe la propuesta […] y ENTRÉGALA."* Propuesta redactada abajo, en `docs/roadmap/answer-save-presupuesto-unico-timeout.md` — sin aplicar, a la espera de decisión.
- **🚨 Hallazgo aparte, con ficha propia [T-XXX] (reservada por separado, per instrucción de Manuel):** el `AbortController` de `route.ts` (línea ~98) envuelve la llamada AL PROXY con 25 s, y `maxDuration=30` en el mismo fichero. Si el proxy tarda esos 25 s y falla, quedan ~5 s de presupuesto de plataforma antes de que Vercel mate la función — insuficiente para completar (ni siquiera para arrancar del todo) el camino de fallback local, que a su vez tiene sus propios 25 s + 25 s. Ese fallo no pasa por ningún `catch` nuestro: la función muere por el límite de la PLATAFORMA, no por una excepción de nuestro código, así que **no genera ni `request_completed` ni log ni `errorRef`** — un 504 sin ningún rastro en `observable_events`. Mecanismo demostrado leyendo el código (AbortController 25s + `maxDuration=30` + fallback con sus propios timeouts de 25+25), NO medido en frecuencia real — eso necesita logs de ALB/Vercel fuera de mi alcance como worker.

- **⬇️ Todo lo de abajo, hasta la sección "Cómo (medir antes de tocar…)", queda como registro histórico del diagnóstico ORIGINAL (30/07) — apuntaba a `route.ts`, que resultó ser casi inerte para el tráfico real. No es la causa; no aplicar su "fix decidido" tal cual.**
- **Qué es realmente:** las peticiones que aparecen «a 25 segundos» no se están degradando: están **agotando `ANTIFRAUD_TIMEOUT_MS = 25000`** (`app/api/v2/answer-and-save/route.ts:42`). El propio código lo documenta: *«las 3 RPCs antifraude paralelas pueden tardar **10-20 s bajo carga BD**»* y *«el INSERT a `test_questions` + **27 triggers en cascada** tarda >15 s ocasionalmente»*.
- **🎯 La prueba: la distribución se amontona contra un muro** (14 días, peticiones de más de 5 s):

  | banda | peticiones |
  |---|---|
  | 5-10 s | 66 |
  | 10-20 s | 65 |
  | **20-24 s** | **6** |
  | **24-26 s** | **19** |
  | **>26 s** | **0** |

  Una lentitud orgánica **adelgaza** en la cola; ésta se acumula justo antes del corte y se acaba en seco. Y es **exclusivo de este endpoint**: ningún otro tiene una sola petición entre 24 y 26 s en 14 días, así que **no es el ALB ni la plataforma** — eso afectaría a todos.
- **Historia del parche, que es lo que hay que revertir:** el timeout subió **10.000 → 25.000 en dos días** (28 y 29/05) porque el cap de 10 s *«mataba ~49k req/día»* con 503. Es decir, se ensanchó el tubo para dejar de ver errores, **sin tocar por qué las RPCs tardan 10-20 s ni por qué un INSERT dispara 27 triggers**. El opositor dejó de ver un error y pasó a esperar 25 segundos.
- **⚠️ DOS hipótesis anteriores, y por qué las dos se quedaron cortas** (esto vale más que el hallazgo):
  1. **Estampida de crons del backend** ([T-254]): **REFUTADA midiendo** — se desplegó el escalonado y la CPU volvió al 100% igual al día siguiente.
  2. **Render de PDFs bloqueando el event-loop** ([T-270]): explica bien el incidente del 29/07 (correlación limpia, mecanismo respaldado por el `Dockerfile` del worker) pero **NO explica este techo**, que aparece también días sin un solo PDF de por medio (22, 25 y 26/07). Probablemente fueron **dos fenómenos distintos el mismo día** y se juntaron en una sola explicación.
  - **También se descartó el cron `detect-notas-convocatoria`** (18 min diarios, terminando a las 09:48 UTC, sospechoso por horario): hubo peticiones lentas los días 22, 25 y 26 en que **no corrió**.
- **🎯 DOS HALLAZGOS DEL 30/07 QUE DECIDEN EL FIX:**
  1. **La justificación del timeout está CADUCADA.** El comentario habla de «27 triggers en cascada», pero de los **26 triggers de `test_questions` solo 4 están ACTIVOS**: `calculate_retention_score`, `tg_test_questions_emit_outbox`, `update_user_streak` y el de timestamp. Los pesados (`user_daily_stats`, `user_article_stats`, `user_difficulty_stats`, `user_hourly_stats`, `user_question_history_v2`…) **ya se migraron al outbox y están desactivados**. Se subió el timeout por una condición que después se arregló por otra vía, y **nadie volvió a bajarlo**.
  2. **El antifraude FALLA EN ABIERTO.** Las peticiones que agotan los 25 s devuelven **200 OK**: si el chequeo expira, la respuesta sale igual. O sea, **se está esperando 25 segundos por un resultado que luego se ignora**.
- **✅ FIX DECIDIDO (y el 2.º hallazgo lo hace casi evidente): no bloquear la respuesta con una comprobación cuyo fallo se ignora.**
  - **Qué:** lanzar las 3 RPCs antifraude **sin esperarlas** para responder. Si su veredicto no cambia la respuesta cuando expira, tampoco tiene por qué cambiarla cuando tarda. El resultado se consume donde importe (registro/bloqueo posterior), no en el camino del opositor.
  - **Por qué es el robusto y no un parche:** ataca la causa —una comprobación lenta en el camino crítico— en vez de mover el número del timeout, que es lo que se hizo en mayo (10 s → 25 s en dos días) y solo cambió «el usuario ve un error» por «el usuario espera 25 segundos».
  - **Escalable:** el trabajo sale del camino de la petición, así que la latencia deja de depender de la carga de la BD en ese instante. Y encaja con lo que este repo ya hace — el **outbox** ya sacó de ahí los triggers pesados; esto es el mismo movimiento para el antifraude.
  - **Observable:** el desglose de [T-312] mide si funcionó (`fuera_de_fases` debe desplomarse), y el detector `latencia_techo_timeout` avisa si vuelve a formarse un techo en cualquier endpoint.
  - **Orden, y el orden importa:** (1) esperar al desglose desplegado para confirmar **cuál** de los dos manda —antifraude o los 4 triggers vivos—; (2) aplicar el fix al que domine; (3) **solo entonces** bajar `ANTIFRAUD_TIMEOUT_MS`. Bajarlo antes devuelve los ~49k req/día con 503 de mayo.
- **Cómo (medir antes de tocar, que es justo lo que no se hizo en mayo):**
  1. Con el desglose por fases de [T-312] ya desplegado, mirar dónde cae el tiempo: **`fuera_de_fases`** = el antifraude (corre en la ruta, ANTES de las tres fases instrumentadas); **`guardar`** = el INSERT y sus 27 triggers. Eso parte el problema en dos con respuesta.
  2. Según cuál sea: perfilar las 3 RPCs antifraude, o revisar la cascada de triggers de `test_questions` (¿cuántos son evitables? ¿alguno hace trabajo que podría ir al outbox?).
  3. **Solo entonces** bajar el timeout. Bajarlo antes devuelve los 503 de mayo.
- **⚠️ CORRECCIÓN 06/08 al punto 1: `fuera_de_fases` de `answer_save_lento` NO ve el antifraude — medido, no supuesto.** El reloj de ese evento arranca DENTRO de `validateAndSaveAnswer`, que la ruta llama DESPUÉS de las 3 RPCs de antifraude — así que el tiempo que se va ahí queda fuera de su ventana, no dentro de su `fuera_de_fases`. Prueba: `answer_save_lento` lleva **0 disparos en los 7 días que lleva desplegado** mientras `request_completed` seguía viendo el mismo 1,3% de siempre — si el antifraude fuera realmente el que domina (como aquí se sospecha), es justo el patrón esperado con un instrumento que no llega a verlo. **Detalle y arreglo en [T-312]:** nuevo evento `answer_save_ruta_lenta`, que sí mide desde antes del auth. Es el que hay que mirar para el paso 1 de arriba, no `answer_save_lento`.
- **Relacionada:** [T-312] (el desglose que lo parte en dos), [T-270] (el otro fenómeno del 29/07), [T-254] (la hipótesis refutada), `docs/roadmap/incidente-answer-save-503-28-05.md`.
#### ⚠️ EL DETECTOR QUE SALIÓ DE AQUÍ ESTÁ AHOGANDO EL BARRIDO NOCTURNO (medido el 31/07)
- **Qué se ve:** el barrido de salud (`content-health-sweep`) arrancó hoy a las 07:30 y **media hora después seguía corriendo**, cuando su marca histórica son ~90 segundos. En sus logs, dos veces: `techo de timeout no evaluado: Failed query:` — la firma de una consulta que se pasa del `statement_timeout` de 30 s del backend.
- **De dónde sale:** el detector `latencia_techo_timeout` que añadieron `aaa8bead4` y `3c32c095e` (esta ficha). Su forma es cara por construcción: para **cada endpoint lento** lanza una consulta con **seis subconsultas correlacionadas**, y cada una cuenta filas sobre **`observable_events`** — la tabla más grande de la base (**~3 GB, 12 M de filas**), que además estuvo ayer al 85,9% de páginas visibles ([T-275]).
- **Lo que NO es:** el barrido no se muere. Sobrevive porque el aislamiento de [T-307] lo protege (antes una consulta así se llevaba la pasada entera). El daño ahora es de **tiempo**, no de caída: un barrido que se arrastra media hora es un barrido que colisiona con la ventana de otros crons y que nadie espera a leer.
- **🔬 MEDIDO Y CORREGIDO EL 31/07 — la causa NO era la consulta, era DÓNDE estaba.** Lo de arriba («seis subconsultas caras») es cierto pero pequeño: medido con `EXPLAIN (ANALYZE, BUFFERS)`, al bucle interno entran **8 endpoints** y el peor (`/api/v2/topic-progress/theme-stats`, 321 casos) tarda **9,2 s**, así que una vuelta completa del detector cuesta **~74 s**. Eso no explica una pasada de 47 minutos.
  - **Lo que sí lo explica:** el bloque estaba **DENTRO del bucle `for (const o of opos)`**, y el detector **no depende de la oposición** — mira los endpoints de toda la app. Se ejecutaba **una vez por oposición activa (~123)** calculando 123 veces el mismo resultado: **~2,5 horas** de trabajo por pasada.
  - **Por qué el log no lo delataba:** solo deja rastro cuando la consulta FALLA (dos `Failed query` esa mañana). Las vueltas que terminan bien no escriben nada, así que el coste era invisible salvo mirando el reloj de la pasada entera. Es el mismo patrón que [T-307]: lo que no deja rastro no se puede triar.
  - **✅ ARREGLADO:** el bloque se ha movido fuera del bucle (una ejecución por pasada) con la explicación al lado para que no vuelva a caer dentro. Verde: 55 tests del barrido + paridad de los gemelos.
  - **🔥 Y NO ERA SOLO LENTITUD: el 31/07 tiró errores a USUARIOS REALES.** Entre las **07:49 y las 08:12** hubo **65 respuestas 5xx a 9 usuarios distintos**, con los mensajes `Servicio saturado momentáneamente` y `Database operation exceeded 8000ms timeout`, en `answer-and-save` (guardar la respuesta de un test), `questions/filtered`, `topics/[numero]` y `laws-configurator`. O sea: gente que estaba estudiando y no pudo.
  - **La comparación por días lo deja sin dudas** (5xx en la franja 07:30-09:00): **0** los días 25, 26, 27, 29 y 30; **6** el 28 (el día en que el barrido tardó 91 s, su pasada buena más larga); y **65 hoy**, que es cuando el barrido lleva 2 h 40 min. El worker de PDFs, que también es pesado y corre en esa ventana, funciona todos los días — no explica un estallido que solo aparece hoy.
  - **⚠️ Y hay que decirlo entero: esto se destapó PORQUE el barrido dejó de morirse.** El arreglo de [T-307] (29-30/07) quitó el aborto a los 80 s; hasta entonces el barrido nunca llegaba a este detector. El arreglo es correcto —un barrido tiene que completar— pero su efecto colateral fue exponer un detector que satura la base. Las dos cosas son ciertas a la vez y conviene no contar solo una.
  - **Mientras el arreglo no se despliegue, esto se repite MAÑANA a las 07:30.** No es deuda: es una avería con hora prevista.
  - **Lo que sigue abierto (y ahora sí se puede decidir con número):** ~74 s por pasada es asumible, pero si se quiere bajar, la vía es una sola lectura con `GROUP BY`/`width_bucket` en vez de seis conteos correlacionados. Ya no es urgente.
- **Cómo arreglarlo sin perder el detector** (ideas previas, antes de saber lo de arriba):
  1. **Una sola pasada en vez de seis subconsultas**: los seis tramos se pueden sacar con un `GROUP BY` sobre un `width_bucket`/`CASE` en una única lectura, en lugar de seis conteos correlacionados por endpoint.
  2. **Acotar la ventana**: 14 días sobre `observable_events` es mucho para una pregunta sobre la FORMA de la cola; con 2-3 días la forma ya se ve.
  3. **Sacarlo del barrido nocturno**: es un detector de latencia, no de contenido. Si vive aparte, su coste no penaliza a los otros ~40 detectores.
- **Medición pendiente y barata:** cronometrar esa consulta con `EXPLAIN (ANALYZE, BUFFERS)` para UN endpoint. Si son ~30 s por endpoint, el coste total del barrido es lineal en el número de endpoints lentos y se puede predecir exactamente.

