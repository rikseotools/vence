### [T-393] 🟠 [ABIERTO 31/07] Auxiliar de Archivos, Bibliotecas y Museos de Madrid: 50 temas publicados que sirven CERO preguntas, con 3 usuarios apuntados

- **Qué hay, medido el 31/07:** `auxiliar_archivos_bibliotecas_museos_madrid` tiene **50 temas activos, los 50 con epígrafe**, y **0 filas de `topic_scope`**. Como la pregunta llega al tema por el scope, esos 50 temas sirven **cero preguntas**. La oposición está en `oposiciones` con `is_active=false` (o sea, no la preparamos) y **sin entrada en `lib/config/oposiciones.ts`**, así que la app ni siquiera sabe enrutarla.
- **Y no es teórico: 3 personas la eligieron.** `ragacata@gmail.com` (alta 16/07), `zairetnorelys@gmail.com` y `pablo.simon.piriz@gmail.com` (ambas 21/07), las tres free. Tienen puesta como suya una oposición cuyo temario se ve entero y no da ni una pregunta.
- **Es un caso ÚNICO, no una familia** (comprobado en todo el catálogo): es la única con temas activos y cero scope. Eso lo hace acotado.
- **Lo que falta para terminarla** es exactamente la parte de juicio del manual de crear oposiciones: el **mapeo `scope` tema→(ley, artículos)** según cada epígrafe, más la entrada de config y la activación. El temario ya está escrito y con epígrafes, que suele ser lo caro.
- **La decisión es de Manuel y son dos caminos, no medio:**
  1. **Terminarla** (`docs/maintenance/crear-nueva-oposicion.md`: scope + config + gates `audit:oposicion`/`verify:scope`). 50 temas de Madrid con contenido muy reutilizable (CE, Estatuto de Madrid, procedimiento administrativo, transparencia) — buena parte del scope existe ya en otras oposiciones de Madrid.
  2. **Retirarla de la vista** hasta que se termine: desactivar los 50 temas para que nadie más la elija, y avisar a las 3 personas. **NO hacerlo en silencio** — han elegido esa oposición a propósito.
  **Lo que NO vale es dejarlo como está**, que es publicar un temario vacío a quien se ha apuntado a él.
- **Cómo salió:** de `positionTypeIntegrity` en el inventario real de [T-377] («1 DB position_type without config mapping»). El test no se quejaba de un detalle de configuración: señalaba una oposición a medias.
- **Relacionada:** [T-390] (esta misma oposición aparece ahí con sus 50 temas sin descripción — si se termina, se arreglan las dos), [T-377].

