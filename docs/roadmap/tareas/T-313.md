### [T-313] 🟠 [ABIERTO 30/07] Se puede elegir qué artículos entran en el test, pero nadie lo encuentra

- **ORIGEN.** Manolo García (premium, feedback `6df1e69a`, 30/07): *«no sé si existe la posibilidad de pedir test de diferentes artículos dentro de una Ley, por ejemplo si una Ley es larga, pongamos de 100 artículos y llevas estudiado la mitad, poder señalar dentro de los 50 que ya has estudiado sin tener que recurrir a darle a test completo de toda la Ley»*. **La función existe desde hace tiempo y hace exactamente eso.** Él lleva 22 días usando la plataforma a diario y no la había visto.
- **CONTEXTO, comprobado en producción (`/leyes/lo-3-2007`):**
  - El bloque **«📄 Filtrar por Artículos» viene PLEGADO** (`showLawsFilter = false` en `components/TestConfigurator.tsx`). Hay que pulsar **«Mostrar»** para que aparezcan los dos botones: **«🔧 Artículos»** (abre el selector, 136 casillas en esa ley) y **«📚 Títulos»**.
  - **El rótulo miente por defecto:** el bloque se llama «Filtrar por Artículos» pero dentro también está el filtro por **títulos**, que para una ley larga es lo que la mayoría querría.
  - **Dos cosas distintas con nombre casi igual en la MISMA pantalla:** el botón visible **«📚 Filtrar por Títulos»** que hay más abajo pertenece a la **lista de lectura** (`app/teoria/[law]/LawArticlesClient.tsx`) y NO afecta al test. El del test está escondido. Quien pulsa el visible cree que ya ha filtrado.
  - **La pista interior nombra un botón que no existe:** dice *«usando el botón "🔧 Filtrar artículos"»* y el botón se llama **«🔧 Artículos»**.
- **Por qué importa:** es una función que ya está construida y pagada, resuelve una necesidad real de estudio (ir practicando solo lo llevado) y **no cuesta nada activarla mejor**. Si un premium veterano no la encuentra, no la encuentra casi nadie.
- **Ideas (ninguna decidida — es UX, decide Manuel):**
  1. **Desplegado por defecto cuando hay una sola ley**: en `/leyes/<ley>` ya sabemos de qué ley habla, así que esconder su filtro no ahorra nada.
  2. **Renombrar el bloque** a lo que de verdad hace («Elegir qué entra en el test» o «Filtrar por títulos o artículos»).
  3. **Distinguirlo del filtro de lectura** para que no compitan en la misma pantalla.
- **SEGUNDA RÉPLICA (30/07, 17:35) — el motivo REAL, medido.** Se le indicó buscar «Filtrar por Artículos» y contestó: *«sigo sin verlo… estoy estudiando la LO 3/2007 y no veo eso de Filtrar por Artículos por ningún lado»*. Verificado con navegador real sobre la pantalla, **las dos partes teníamos razón**:
  - En `/leyes/lo-3-2007` (una sola ley) el rótulo SÍ dice «📄 Filtrar por Artículos», la casilla de la ley arranca marcada y los botones «🔧 Artículos» / «📚 Títulos» salen al pulsar «Mostrar». Ahí la indicación era correcta.
  - **Él estudia por su TEMA** (`auxiliar_administrativo_diputacion_cordoba`), donde hay varias leyes y el rótulo decía solo **«📖 Filtrar por Leyes»**: la palabra *artículos* NO aparecía en su pantalla, así que buscarla era imposible. La función estaba a dos clics, detrás de un nombre que hablaba de otra cosa.
- **RESOLUCIÓN (30/07).** En `components/TestConfigurator.tsx`: el rótulo pasa a **«📖 Filtrar por Leyes y Artículos»** cuando hay varias leyes (con una sola no cambia), y el texto de ayuda deja de citar un botón inexistente («🔧 Filtrar artículos») para nombrar los reales, «🔧 Artículos» y «📚 Títulos». Atado con `__tests__/guardrails/filtroArticulosDescubrible.test.ts` (4 tests): el rótulo debe nombrar los artículos en ambas ramas, la ayuda debe citar el botón por su nombre exacto, y las leyes deben arrancar marcadas (si arrancaran sin marcar, desplegar el filtro no enseñaría ningún botón — el mismo «no lo veo»).
- **Patrón, por tercera vez esta semana:** la función/dato existe y **lo que lo nombra no coincide** (opciones en posición de cuerpo → 405; `data.url` vs `checkoutUrl`; ahora el rótulo). Las ideas 1 y 3 de arriba siguen abiertas: son cambios de UX que decide Manuel.
  4. **Sincronizar el texto de la pista** con el nombre real del botón.
- **Al tocarlo, ojo:** el mismo componente sirve el modo por tema y el modo por ley, así que cualquier cambio hay que mirarlo en los dos. Existe ya el journey `contador-articulos-coherente` (Vence Sim) que abre ese filtro y podría verificar de paso que sigue siendo alcanzable.
- **🔁 TERCERA LECTURA (30/07, tarde) — hay un SEGUNDO camino, y no contradice al anterior: lo completa.** Al responderle por tercera vez se reconstruyó su journey y sus últimos pasos están en **`/leyes/lo-3-2007/avanzado`** y en **`/auxiliar-administrativo-diputacion-cordoba/temario/tema-4`**. Esa ruta `…/avanzado` **no es un configurador: es el test ya arrancado** (`LawTestPageWrapper`), sin ninguna opción de filtrado. O sea que, además del rótulo (ya arreglado), **hay un camino por el que el filtro es sencillamente inalcanzable**: quien entra a practicar desde su temario aterriza en las preguntas y no llega a ver la pantalla donde se elige qué entra.
  - **Las dos lecturas son compatibles y conviene no cerrar la ficha con solo una:** si llega por el configurador del tema, el problema era la PALABRA (resuelto hoy); si llega desde el temario al test de la ley, el problema es la PUERTA, y sigue abierto.
  - **Medición barata que decide cuál de los dos arreglos vale más:** cuántas sesiones llegan a `/leyes/*/avanzado` **sin** pasar antes por `/leyes/<ley>` (`user_interactions`). Si son la mayoría, la puerta es lo urgente y el rótulo era el síntoma menor.
  5. **Puerta desde el temario** (idea nueva, la única que ataca ese camino): que desde el temario se pueda entrar a la pantalla de configuración de la ley, y no solo al test directo. **Prometido POR ESCRITO** a Manolo el 30/07 en su hilo `6df1e69a` (*«vamos a hacer que se pueda llegar a ese filtro también desde tu temario»*), así que ya no es una idea suelta: es un compromiso con una persona concreta.
- **Relacionada:** [T-310] (el contador de esa misma pantalla, que anunciaba 798 artículos teniendo 134 — salió al verificar esta misma pregunta).

- **✅ IDEA 5 RESUELTA (06/08, w3) — REPRODUCIDO el mecanismo exacto, no solo sospechado.**
  - **La medición barata que pedía la ficha (`user_interactions` para contar sesiones que llegan a `/avanzado` sin pasar por el configurador) NO se pudo hacer**: `user_interactions` tiene `relrowsecurity=true` y **CERO políticas** para `vence_lector` — mismo mecanismo que [T-573]/[T-574] (RLS activo sin política da 0 filas SIN error). Confirmado con `pg_class`/`pg_policy` contra RDS; no es que la tabla esté vacía (el cron `archive-interactions` la usa a diario, backend/src/archive-interactions/), es que un trabajador de la flota no puede leerla. No lo arreglo yo (DDL, misma decisión pendiente que T-573).
  - **En su lugar, se REPRODUJO el camino leyendo el código, línea a línea — no es sospecha, es la ruta exacta:** `LawTestCTA`/`TopicContentView` («Hacer test Art. N» del temario) SÍ enlazan primero al configurador (`buildLawTestLink` → `/leyes/<slug>?selected_articles=N&source=temario`, correcto desde T-073). Pero `app/leyes/[law]/LawTestConfigurator.tsx` líneas 67-79 tiene un `useEffect` que, **si detecta `selected_articles` en la URL, redirige con `window.location.href` a `/avanzado` ANTES de que el usuario vea nada** — ni un frame del configurador. El filtro no es que esté escondido: es que el propio código salta por encima de él. Esto explica el journey exacto de Manolo sin necesitar la medición bloqueada.
  - **Y hay un SEGUNDO camino real, sin auto-redirect de por medio:** `app/leyes-de-oposiciones/page.js:328`, el botón **"🎯 Practicar con Tests"** enlaza directo a `` `/leyes/${slug}/avanzado?n=25` ``, sin pasar por el configurador en ningún momento (a diferencia del botón hermano "📖 Ver Ley Completa", que sí va a `/leyes/${slug}`). Confirmado por grep: de los 4 ficheros que mencionan `/avanzado` en toda la app, este es el único bypass fuera del propio configurador. No se ha tocado — es una landing SEO con su propia intención de "empezar rápido", y cambiar su comportamiento por defecto es la misma decisión de UX que la ficha ya marcaba como "decide Manuel"; el arreglo de abajo lo cubre igual sin tocarlo.
  - **EL ARREGLO — la puerta YA EXISTÍA, estaba rota igual que el rótulo.** `LawTestPageWrapper.tsx` construye `customNavigationLinks.backToLaw = { href: '/leyes/<slug>', label: '📚 Volver a <ley>', isPrimary: true }` para TODO test de ley (sin condición de `source` ni `selected_articles` — cubre los dos caminos de arriba por igual). Ese botón se pinta arriba del todo, visible desde la pregunta 1 y durante todo el test (`!isTestCompleted`, `components/TestLayout.tsx`). Pero el texto que se leía ahí venía de `config.customNavigationLinks?.backToLaw?.text` — **`.text` no existe en ese objeto, solo `.label`** (el propio `interface` de `LawTestPageWrapper.tsx` nunca declaró `.text`) — así que SIEMPRE caía al genérico "Volver a Tests" y nunca decía "Volver a {ley}". El bloque de resultados de fin de test (unas líneas más abajo, mismo fichero) sí leía `.label` bien: son DOS lecturas del MISMO dato que divergieron.
  - **Mismo patrón que toda esta ficha, otra vez:** la puerta no faltaba, faltaba que se viera. Arreglado con una función pura `backToLawButtonLabel` (`lib/navigation/backToArticleLink.ts`) que sustituye la lectura rota — 5 tests nuevos (`__tests__/navigation/backToArticleLink.test.ts`, incluida la regresión exacta: un objeto con `.text` y sin `.label`, la forma que el código roto esperaba, cae al genérico). 43/43 verde en `__tests__/navigation/` + los guardarraíles de esta misma ficha (`filtroArticulosDescubrible`) y de T-073 (`lawTestCtaNoBareLink`).
  - **Alcance real:** ahora, llegue por donde llegue (auto-redirect del temario o el bypass de `leyes-de-oposiciones`), el usuario ve desde la primera pregunta un botón que dice "📚 Volver a {ley}" y que efectivamente lleva al configurador — que ya muestra el filtro descubierto por la primera resolución de esta ficha. Cumple la promesa escrita a Manolo sin cambiar el arranque rápido que ya tienen los dos caminos (ninguna UX nueva inventada, solo la que ya estaba construida y rota).
  - **NO desplegado.** Rama `flota/w3`, pendiente de deploy de frontend.

- **✅ VERIFICACIÓN + CIERRE del "falta" pendiente (08/08, w3).**
  - **Rama extraída a `flota/T-313-boton-volver-a-ley` (commit `3656c14bd`), pusheada.** El fix
    vivía enterrado en la rama compartida `flota/w3` (mezclado con decenas de commits de otras
    tareas) sin una rama propia que un humano pudiera revisar — corregido: cherry-pick limpio
    contra `origin/main` (los 3 ficheros de código/test aplicaron sin conflicto; el único
    conflicto fue en `tareas-pendientes.md`, resuelto migrando el contenido al fichero-por-ficha
    `docs/roadmap/tareas/T-313.md`, que es la convención vigente desde [T-532] y no existía
    cuando se escribió el commit original del 06/08).
  - **Re-verificado, no dado por bueno:** `backToLawButtonLabel()` sigue leyendo `.label` (nunca
    `.text`) y está cableada en `TestLayout.tsx` en las dos lecturas del dato (botón de arriba
    durante el test + resultados finales). 18/18 tests (`backToArticleLink.test.ts` +
    `filtroArticulosDescubrible.test.ts`), sweep de `__tests__/navigation` +
    `__tests__/components/TestLayout` + `__tests__/guardrails` en verde (1.297/1.301 — los 4
    fallos son `temarioPdfQueue.integration.test.ts`, `permission denied for table
    temario_pdf_jobs`: necesita `DATABASE_URL` de escritura que este rol no tiene, no relacionado
    con este fix). `tsc --noEmit` limpio, `eslint` sin errores (2 warnings preexistentes de
    `react-hooks/exhaustive-deps` en `TestLayout.tsx`, en `useEffect`s que este fix no toca).
  - **Respuesta al "falta" nº2 (¿el bypass de `leyes-de-oposiciones/page.js:328` merece su propia
    tarea?): NO, verificado, no solo intuido.** `app/leyes/[law]/avanzado/page.js` (la ruta a la
    que enlaza el botón "🎯 Practicar con Tests") usa `LawTestPageWrapper` — el MISMO componente
    cuyo `customNavigationLinks.backToLaw` ya se arregló arriba. O sea que cualquier test lanzado
    desde esa landing SEO **ya hereda el botón corregido** ("📚 Volver a {ley}", que lleva al
    configurador con el filtro) sin tocar ese fichero. El problema que esta ficha ataca
    (descubribilidad del filtro) queda cubierto por CUALQUIER puerta de entrada al test de ley,
    no solo por las dos ya identificadas — no hace falta una ficha nueva.
  - **Falta real y única que queda: desplegar frontend.** Tras el deploy, verificar en producción
    (navegador real, no solo el código): abrir un artículo del temario → «Hacer test de este
    artículo» → confirmar que el botón de arriba del test dice «📚 Volver a {ley}» (no «Volver a
    Tests») desde la primera pregunta, y que lleva al configurador con el filtro visible. Repetir
    entrando por `/leyes-de-oposiciones` → «🎯 Practicar con Tests» para confirmar el segundo
    camino igual.

