### [T-618] 🟠 [ABIERTO 06/08/2026] `mecanico-conductor-estado` está publicada con «inscripción abierta» y sin fecha de cierre: no la ve nadie

- **Esfuerzo: minutos** (leer el PDF de bases y escribir una fecha), pero es defecto **visible para el usuario**.
- **QUÉ PASA:** la fila está `is_active=true` con `estado_proceso='inscripcion_abierta'` e
  `inscription_start=2026-07-11`, pero `inscription_deadline` es **NULL** en `oposiciones` **y** en su
  convocatoria vigente. Home, SEO y banner filtran por **fechas** (`isInscripcionAbierta`), no por
  `estado_proceso` → una oposición que decimos tener con el plazo abierto **es invisible en el front**.
- **CÓMO SALIÓ:** `npm run audit:estados` tras la tanda de triaje OEP del 06/08. Es la **única ❌** de
  las 2.682 oposiciones (hay además 39 🟡, que son otra cola). No la causó el triaje: la fila es del
  01/06 y su convocatoria se tocó por última vez el 29/07.
- **DÓNDE ESTÁ EL DATO:** la `seguimiento_url` (Parque Móvil del Estado,
  `hacienda.gob.es/…/2026procesoselectivopersonallaboralalpi.aspx`) **no publica el plazo en la página**:
  solo enlaza el PDF de **Bases de la Convocatoria del 13/07/2026**. Comprobado el 06/08. Hay que abrir
  ese PDF (o su extracto en el BOE) y sacar de ahí la fecha de cierre, con cita literal.
- **ARREGLO:** escribir `inscription_deadline` con **dual-write** (`oposiciones` + convocatoria vigente)
  y volver a correr `npm run audit:estados` (tiene que bajar a 0 ❌). Si el plazo ya hubiera cerrado,
  entonces lo que toca es avanzar el estado, no inventar una fecha futura.
- **NUNCA** poner una fecha a ojo para que el front la muestre: eso es justo lo que este audit existe
  para cazar.

Contexto: es un proceso de 196 plazas (98 libre + 98 promoción interna), grupo E2, del Parque Móvil.
