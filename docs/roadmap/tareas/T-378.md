### [T-378] 🟠 [ABIERTO 31/07] El badge de soporte va en el botón de Soporte, no sumado en la campana (que así nunca baja de 9+)

- **Lo reporta una usuaria premium y tiene razón en las dos cosas** (feedback `70cbcfc9`, Laura, 31/07): *«En la campanita siempre me pone 9+. Realizo los tres que hay en ese apartado, pero sigue apareciendo 9+. Nunca entendí esta opción, ya que mezcla preguntas de examen elegido como principal, y todas las conversaciones con soporte, pero nunca se pone en 0».*
- **Por qué nunca baja, medido:** `getTotalUnreadCount()` (`components/NotificationBell.tsx`) **suma tres orígenes** — avisos de estudio, respuestas a impugnaciones y avisos de la oposición. Ella tiene **13 respuestas a impugnaciones sin marcar como leídas**: solo con eso el badge se queda clavado en `9+` haga lo que haga con los tres avisos que sí ve al abrir. **Está despachando una cosa y el número lo sostiene otra.**
- **Y hay un segundo defecto debajo:** para los avisos de estudio, «marcar como leída» se guarda **en `localStorage`**, no en la BD (`markAsRead`, rama `else`). No viaja entre dispositivos, y como esos avisos **se recalculan solos** a partir de los fallos del usuario (ella tiene 663 artículos con fallos), vuelven a aparecer. Ese es el *«nunca se pone en 0»* literal.
- **Decisión de Manuel (31/07):** las respuestas de soporte **salen de la campana** y llevan **su propio badge en el botón 💬 de Soporte**, que ya existe en el header (móvil y escritorio, `app/Header.tsx`). El motivo es el que da ella: **no se atienden igual**. Un aviso de estudio es opcional y perecedero; una respuesta de soporte es una conversación dirigida a esa persona que espera lectura. Mezclados, ninguno de los dos números significa nada.
- **Cómo, sin inventar patrón nuevo:** el icono de recompensas ya resuelve esto mismo con un **núcleo puro** que decide el estado (`estadoIconoRecompensas` → `RewardsIcon`), sin reglas sueltas dentro del header. El badge de soporte debe seguir ese camino: núcleo puro testeado que, dado el número de conversaciones con respuesta sin leer, decida qué pintar.
- **Y el «leído» tiene que persistir en BD**, no en el navegador. Si no, el badge nuevo heredará el mismo defecto: bajará en un dispositivo y volverá a subir en el siguiente.
- **💶 AL DESPLEGAR: recompensar la sugerencia (orden de Manuel, 31/07).** La aportación de Laura (`opoauxilaradminitrativo21junio@gmail.com`, premium) es la que destapa esto, así que **cuando el arreglo esté VIVO** hay que concederle la recompensa por sugerencia **a mano** — el feedback no paga solo, a diferencia de una impugnación aceptada. **No se le menciona en el mensaje** (el badge 🎁 ya se lo comunica), y se le respondió sin prometer nada de dinero. Que sea al desplegar y no ahora es deliberado: se premia la mejora que llega al usuario, no la ficha escrita.
- **Cuidado al medir el arreglo:** que el número baje no basta. La prueba es la de ella — abrir la campana, despachar lo que hay dentro y **que el contador llegue a cero**; y que las respuestas de soporte se vean en Soporte, con su cuenta propia.
- **IMPLEMENTADO (08/08), mismo patrón que el icono de recompensas — sin migración, reusa BD existente:**
  - **Núcleo puro** `estadoBadgeSoporte` (`lib/support/badgeSoporte.ts`, tests en `__tests__/support/badgeSoporte.test.ts`, 6 casos incl. tope "9+" con el 13 real de Laura) — decide qué pintar a partir de un número plano, sin BD ni React.
  - **Endpoint** `GET /api/support/unread-badge`: cuenta `notification_logs` (`context_data->>'type'='feedback_response'`, `opened_at IS NULL`) + `question_disputes` + `psychometric_question_disputes` (`is_read=false`, resueltas/rechazadas/alegadas, 30 días) del usuario del TOKEN — **las MISMAS tablas y ventanas** que ya usaban `/api/v2/notifications/system` y `/api/v2/disputes/notifications`, no se inventó ni una columna ni un criterio nuevo. El «leído» YA vivía en BD (`opened_at`/`is_read`), así que el segundo defecto de la ficha (el de localStorage) **no aplica a este badge** — sigue sin tocar para los avisos de estudio genéricos, que son un problema aparte (motivacionales/logros, no respuestas de soporte).
  - **Hook** `useSupportUnreadBadge` (mismo patrón que `useReferralEarningsBadge`) + **componente** `SupportUnreadBadge` (mismo patrón que `RewardsIcon`: "qué decide qué: nada aquí"), montado en `app/Header.tsx` sobre las DOS instancias de `<SupportButton>` (móvil icono-solo y escritorio icono+texto).
  - **Salen de la campana, de verdad, no solo del número:** `useIntelligentNotifications.loadSystemNotifications` filtra fuera `type==='feedback_response'` (ya no entra en `allNotifications` → ni cuenta ni aparece en el dropdown), y `NotificationBell.getAllNotifications()` deja de fusionar `disputeNotifications.notifications` (antes se mezclaban con prioridad 70, por encima de casi todo). `getTotalUnreadCount()` ahora es solo avisos de estudio + avisos de oposición. `handleDismiss`/`handleMarkAsRead`/la alegación inline se dejan intactos (los sigue exigiendo `__tests__/components/NotificationBellDismiss.test.ts`) por si algún día se vuelve a alimentar una lista de disputas ahí — hoy son rutas muertas pero inofensivas, no se borraron para no tocar más superficie de la necesaria.
  - **`/soporte` ya tenía TODO lo necesario para "verlas en Soporte, con su cuenta propia"** sin construir nada nuevo: pestañas `conversations`/`disputes`, deep-link `?conversation_id=…`/`?tab=impugnaciones&dispute_id=…`, y sus propios mark-read contra las mismas tablas. No fue necesario tocar esa página.
  - **Verificado:** 1579 tests de `__tests__/hooks`, `__tests__/components`, `__tests__/support`, `__tests__/notifications`, `__tests__/referrals` + los 2 endpoints de disputes en verde; guardarraíl C2 (user-scoping) en verde (18/18, el endpoint nuevo autentica con `verifyAuth`); `tsc --noEmit` sin errores en los ficheros tocados; `eslint` sin errores (2 warnings preexistentes en `useIntelligentNotifications.ts`, no de esta rama).
  - **NO verificado (no se puede sin deploy + sesión real, este worker no tiene ninguna de las dos):** que el badge se vea y cuente bien en un navegador real con un usuario con impugnaciones/feedback sin leer; que dejar de fusionar `disputeNotifs` en el dropdown no produzca ningún efecto visual raro (huecos, animaciones) que solo se vea en vivo.
  - **💶 Recordatorio del pendiente de arriba, sigue en pie:** al verificar en vivo, si el arreglo funciona, falta que Manuel conceda a mano la recompensa por sugerencia a Laura (`opoauxilaradminitrativo21junio@gmail.com`) — este worker no puede hacerlo (no escribe en BD de negocio ni contacta usuarios).

