### [T-159] 🟠 [ABIERTO 27/07] La cola de PDFs del temario no tiene consumidor automático (Fase D de T-086)

> **🖥️ Qué corre en producción, con el origen de cada pieza:** `docs/ARCHITECTURE_ROADMAP.md` §"QUÉ CORRE EN PRODUCCIÓN" — inventario verificado (2 servicios siempre encendidos + 3 tareas programadas). Enlaza de vuelta a esta ficha.

> **📐 ANÁLISIS 30/07 — qué haría falta para que el PDF bajo demanda pudiera usar esta cola (Fase 2 de [T-270]).**
> Salió de preguntarse si la Fase 2 se podía adelantar. La respuesta es que **el obstáculo no es la
> cadencia del worker, y descubrirlo cambia el diseño.**
>
> **Medido (7 días, `observable_events`):** 44 ejecuciones — **29 en vacío** (<10 s) y 15 con trabajo;
> cuando trabaja tarda **20 min de media y hasta 31**. Y la cola está **completamente drenada**:
> 514 trabajos, todos `done`. O sea: para la pre-generación nocturna el worker va sobrado y **no hay
> nada que arreglar ahí**.
>
> **🎯 El hallazgo:** son **dos cargas distintas en una sola cola**. Si la petición de un opositor
> entra en la misma cola que un lote de pre-generación, espera detrás de esos 20 minutos — y eso pasa
> **igual aunque se despierte al worker al instante**. Por tanto:
>
> **El disparo bajo demanda NO arregla el problema por sí solo.** Lo que hace falta primero es que la
> petición de usuario tenga **prioridad o carril propio**. Es una decisión de diseño de la cola, es
> más importante que la cadencia, y **se puede tomar ya, sin esperar ninguna medición**.
>
> **Las otras dos piezas, para que no se confundan de orden:**
> 1. **¿Qué se encola y qué no?** → un umbral por coste de render. Depende de los datos de `renderMs`
>    que [T-270] Fase 1a instrumentó; hasta que haya un pico con tráfico de PDF, elegirlo es ponerlo a ojo.
> 2. **¿Carril/prioridad para el usuario?** → decidible YA (esta nota).
> 3. **¿Despertar al worker?** → solo tiene sentido DESPUÉS de (2). Y no es gratis: la ruta necesitaría
>    `ecs:RunTask` + `iam:PassRole` (cambio de IAM) y abriría un **vector de abuso** —un premium podría
>    provocar el arranque de tareas Fargate—, así que exigiría deduplicación y límite.
>
> **⚠️ Sobre el coste, deliberadamente sin cifras:** bajar la cadencia NO escala con el número de
> disparos, porque las ejecuciones con trabajo duran 20 min y una cadencia corta **solaparía tareas
> concurrentes**. Antes de decidir gasto hay que mirar Cost Explorer de verdad, no estimar. Contexto:
> la factura AWS son ~$491/mes y [T-206] tiene ~$285/mes de suelo Fargate pendientes de decisión.
>
> **Cómo se dispara hoy, verificado:** `aws scheduler get-schedule --name vence-temario-pdf-worker` →
> `rate(30 minutes)`, `Europe/Madrid`, `FARGATE`, clúster `vence-backend`. Detalle menor: reutiliza el
> rol `vence-instagram-daily-scheduler-role`, compartido con otro job.
>

> **✅ DECIDIDO POR MANUEL (30/07) — el diseño es OTRO, y la propuesta de prioridad de abajo QUEDA DESCARTADA.**
>
> Manuel lo planteó así: *«esos PDF deben estar descargables al instante, y se deben generar de fondo
> en backend; el usuario al clicar ya estaba generado»*, y al ver que eso es lo que ya hace el caché,
> lo remató con *«el primer usuario pincha y espera a que se genere, pero una vez generado ya lo
> tenemos disponible para otros»*. Tiene razón en las dos cosas, y la segunda **ya está implementada**:
> la ruta busca en S3 con una clave que incluye el hash del contenido y, si acierta, sirve al instante.
>
> **Entonces, ¿por qué alguien espera? Tres huecos medidos el 30/07, y ninguno es la cadencia del worker:**
>
> 1. **La pre-generación cubre el 7,1%: 253 temas de 3.547.** Nada siembra el catálogo — la cola SOLO
>    se alimenta del hook de scope, así que hay PDF de lo que alguien tocó, no de lo que se estudia.
>    Medido por oposición: **Aux. Admin. del Estado tiene 2 de 28 temas con PDF y 2.207 alumnos**;
>    Madrid 3 de 21 con 1.465; **CARM 0 de 21 con 551**. Con esa cobertura, «el primer usuario» no es
>    un caso raro: es casi cada clic.
> 2. **El render ocurre EN EL CONTENEDOR QUE SIRVE**, y es lo que convierte la espera de uno en el
>    problema de todos ([T-270], 29/07: 36 renders frescos → CPU 98,5%, event loop bloqueado 215 s y
>    `answer-and-save` a p95 25 s; o sea, gente que estaba respondiendo tests dejó de poder guardar).
>    El worker existe justo para esto —imagen propia, 2 vCPU, fuera del ALB— y se blindó el camino por
>    LOTES dejando sin blindar el de BAJO DEMANDA.
> 3. **No hay «última versión»**: la clave de caché incluye el `content_hash`, así que en cuanto cambia
>    el temario el PDF anterior deja de ser alcanzable aunque siga en S3. Servir el último mientras se
>    regenera exige clave estable por tema + puntero a la vigente (y conviene imprimir la fecha de
>    generación: es contenido legal).
>
> **El diseño decidido, en este orden:**
> - **(a)** El caché perezoso se queda como está — es correcto.
> - **(b)** **Sacar el render de la ruta**: lo hace el worker. El primer usuario espera lo mismo, pero
>   sin arrastrar a nadie. Esto es lo que cierra [T-270].
> - **(c)** **Sembrar solo donde hay alumnos**, no los 3.547: los **212 temas** que faltan en las 8
>   oposiciones más pobladas son **~6 h de render** y cubren a **6.270 alumnos**. Las 96 h de sembrarlo
>   todo NO hacen falta — la cola larga la resuelve el caché perezoso.
> - **(d)** Servir la última versión mientras se regenera (el punto 3).
>
> **Lo que esto DESCARTA:** la columna `priority` y el segundo worker. Con el catálogo sembrado donde
> hay gente y el render fuera del contenedor, no queda cola en la que adelantar a nadie. El disparo
> bajo demanda (IAM + vector de abuso) tampoco hace falta.

> **🧭 PROPUESTA DE DISEÑO (30/07) — el carril/prioridad. ⚠️ DESCARTADA por la decisión de arriba; se conserva por el análisis del claim FIFO, que sigue siendo cierto.**
>
> **El estado actual, leído del código:** `claimNextPdfJob` hace `ORDER BY created_at` — **FIFO puro**,
> con `FOR UPDATE SKIP LOCKED`. Y `enqueuePdfJob` hace `ON CONFLICT DO NOTHING`. O sea: una petición
> de usuario entra **detrás de todo lo pendiente**, y si el tema ya estaba encolado por la
> pre-generación, la petición **no hace nada en absoluto**.
>
> **Opción recomendada — una columna `priority`, no una cola aparte.** Es el cambio más pequeño que
> resuelve el problema:
> - `ALTER TABLE temario_pdf_jobs ADD COLUMN priority smallint NOT NULL DEFAULT 0` (+ índice para el claim).
> - `claimNextPdfJob`: `ORDER BY priority DESC, created_at`. Una línea.
> - **La parte fina, y la que más valor tiene:** `enqueuePdfJob` pasa de `ON CONFLICT DO NOTHING` a
>   `ON CONFLICT DO UPDATE SET priority = GREATEST(temario_pdf_jobs.priority, EXCLUDED.priority)`. Así
>   la petición de un usuario **NO crea un job duplicado: PROMOCIONA el que ya existía**. Hoy ese caso
>   —tema ya encolado por la pre-generación— deja al usuario sin nada, en silencio.
>
> **⚠️ Lo que la prioridad NO resuelve, y hay que decirlo antes de venderla:** el worker reclama de uno
> en uno y un render puede durar **hasta 30 min** (`DEFAULT_RENDER_TIMEOUT_MS`). Con prioridad, el
> usuario deja de esperar *todo el backlog* y pasa a esperar *el render que esté en vuelo*. Es una
> mejora grande, pero **no es una cota**: sigue siendo «hasta 30 minutos» en el peor caso.
>
> Para acotarlo de verdad harían falta **dos workers** (uno de lote, otro de demanda), y eso ya es
> otra tarea Fargate + su disparo + su coste. **No lo recomiendo todavía:** combinado con el umbral
> de [T-270], quien espera es exactamente el que pide un tema enorme, y a ese se le puede decir la
> verdad («tu tema son 651 páginas, lo estamos preparando»). Si tras medir resulta que la espera
> típica es de minutos y no de decenas, el segundo worker no hace falta.
>
> **Orden de implementación cuando se decida:** (1) columna + `ORDER BY` + el `ON CONFLICT DO UPDATE`
> (los tres van juntos o el promocionar no funciona); (2) la ruta encola con `priority=1` solo por
> encima del umbral de [T-270]; (3) medir la espera REAL antes de plantearse el segundo worker o el
> disparo bajo demanda. El disparo (IAM + vector de abuso) es lo ÚLTIMO, no lo primero.
>
> **Para el que retome esto:** lo que NO hay que hacer es cablear `enqueuePdfJob` en la ruta del 413.
> Ese caso lo resuelve mejor el piloto de [T-273] (descarga por partes, contenido **al instante**);
> encolar le haría esperar hasta 30 minutos por lo mismo. La cola sirve para el otro caso: temas que
> **sí caben** pero cuyo render bloquea la task.
> **⚠️ CORRECCIÓN 29/07 — la premisa de abajo es FALSA y desvía el diagnóstico.** Sí hay tarea
> programada: `vence-temario-pdf-worker`, **EventBridge Scheduler** (`rate(30 minutes)`, creada el
> 23/07, ENABLED). No aparece con `aws events list-rules` —que sale vacío— porque **Scheduler y
> Events son servicios distintos**: hay que mirar `aws scheduler list-schedules`. Comprobarlo con
> el comando equivocado fue lo que hizo concluir "no hay consumidor automático". El consumidor
> existe desde el 23/07 (T-086 lo dejó desplegado y corriendo).

- **Qué:** el productor es automático (el hook `hook:scope` encoló **33 jobs solo el 26/07**) y el consumidor es **una persona** ejecutando `tsx scripts/pdf-worker.ts drain`. No hay workflow en `.github/workflows/` ni tarea programada en ECS. Entre el último drenado manual (26/07 00:50) y el barrido de salud del 27/07 se acumularon **36 pendientes, el más viejo de 29 h**.
- **Por qué importa:** el canary `canary-pdf-queue` avisa cada 15 min → **96 CRITICAL/día** al correo de Manuel por algo que no tiene a nadie al otro lado (alert-fatigue puro). Y de los pendientes, los que **no caben en generación síncrona** (>400k caracteres o artículo >60k) devuelven **413 `tema_demasiado_grande`** a un usuario **premium**: el 27/07 eran 4 (`auxiliar_administrativo_andalucia` T11, `policia_nacional` T21, `guardia_civil` T9 y T22).
- **Cómo:** empaquetar el worker como tarea Fargate programada. Ya está preparado para ello — `drain` arranca con el reconciliador `enqueue-big` (estado deseado: re-encola lo que cambió de firma y deduplica lo que está al día), así que un ciclo programado se auto-cura. Ojo al dimensionado: media **101 s/job**, p90 **266 s**, máximo **910 s**, y `RENDER_TIMEOUT_MS` = 18 min por render.
- **Cabo suelto medido:** `oposicion_desconocida` quema los 3 intentos y manda el job al DLQ, cuando es un fallo de **registro viejo en la imagen**, no del tema (7 jobs de `ujieres_cortes_generales` cayeron así el 24-25/07; se recuperaron re-encolando el 27/07 sin tocar nada más). Debería ser reintentable/`skipped`, no DLQ.
- **Y un agujero aparte, barato:** el **413 no encola nada**. Es la única señal de que un premium REAL quería ese PDF concreto y se tira; la cola solo se alimenta del hook de scope (ciego y masivo) y de barridos manuales. Son ~3 líneas en `app/api/temario/[oposicion]/[topic]/pdf/route.ts`.
- **Origen:** barrido de salud del 27/07.

**🔵 ESTADO 27/07 08:20 — la cola está VACÍA y el canary VERDE; lo que queda es el diseño, no el incendio.**
- **Drenado manual hecho:** 47 jobs procesados, 47 done, 0 retry, 0 DLQ. Cola final **383 done / 0 pending / 0 failed**, y el canary lo confirmó por sí mismo: `08:15 [info] canary_pdf_queue_ok`. Se acabaron los ~24 CRITICAL diarios. Los 7 de `ujieres_cortes_generales` se recuperaron re-encolando (ya resuelve el registro) y los 4 temas grandes quedaron en caché S3, así que esos premium dejaron de recibir 413.
- **🔴 EL DLQ TENÍA DENTRO UN TEMA PERFECTAMENTE RENDERIZABLE — el diagnóstico "irreparable" era falso.** `auxiliar_administrativo_diputacion_segovia` T29 se renderizó a mano **sin el techo: 20 min 1 s y 2,9 MB, `outcome=uploaded`**. El techo del worker es `RENDER_TIMEOUT_MS` = **18 min exactos**: el tema se pasaba **2 minutos**. Al re-encolarlo con el PDF ya en S3, el worker lo cerró en **3,7 s** reconociéndolo desde caché.
- **Consecuencias para el arreglo (cambian la ficha):**
  1. **El techo está mal calibrado, no el tema.** Se fijó con el peor caso conocido de entonces (récord: 15,2 min) y sin holgura; el primer tema que lo superó se quedó en el DLQ **para siempre** generando 24 CRITICAL/día. Subirlo con margen real (el peor caso medido es 20 min) es parte del empaquetado del worker.
  2. **Refuerza el acuse en el DLQ**: un job que cae por timeout **no es irreparable por definición**, así que tratar `dlq > 0` como fuego permanente estaba doblemente mal.
  3. **Partir el Access sigue mereciendo la pena, pero YA NO es el arreglo del PDF.** El bloque de 91k es `Access 365` art. 1 *"Fundamentos de Access 365"* —contenido editorial de ofimática en un solo contenedor, con otros de 75k, 70k y 51k detrás: ~290k de manual de Access en 4 bloques—. Partirlo en secciones mejora navegación, TTS y progreso por artículo. Es una mejora aparte, no un apaño de render.
- **🔴 Y la asimetría de fondo, comprobada: la cola está sobre-alertada y el usuario no está alertado en absoluto.** `canary_pdf_queue_failed` es `critical` con `cooldownMin: 60` y dispara con `dlq > 0` solo → ~24 emails/día indefinidos. Y el `413` (`temario_pdf_served` con `served='too_large'`) se emite con severidad **`info`** y **no hay NINGUNA regla de alerta que lo mire** (verificado con grep sobre `backend/src/alerts/`): si un premium se queda sin su PDF, no salta nada. Que el 24/07 pasara 5 veces con `auxiliar-administrativo-estado` T109 se supo solo buscándolo a mano en la BD.
- **Diseño acordado — las tres piezas van juntas, separadas no funcionan:** (1) el **413 encola** el tema que falta (auto-curación; solo tiene sentido con consumidor automático, así que va CON el worker, no antes); (2) la **alerta cambia de sujeto**: de *"hay algo en el DLQ"* a *"este tema volvió a dar 413 DESPUÉS de encolarse"* = reincidencia, que sí es accionable y trae el tema en el payload; (3) **acuse en el DLQ** y el canary cuenta solo lo no reconocido — legítimo **solo si existe (2)**, porque si no se apaga la luz roja dejando al usuario a oscuras. **NO añadir (2) sola antes de la auto-curación**: sería un canal de emails nuevo para algo que debe curarse solo, que es la enfermedad que estamos tratando.

**🟢 ESTADO 29/07 — causa raíz del fuego ENCONTRADA Y ARREGLADA (código en `main`); queda el diseño de las 3 piezas.**
- **El worker llevaba 2 días muerto, y no por la cola. DOS fallos encadenados:**
  1. **Nunca tuvo camino de despliegue.** El Dockerfile ya traía su stage `worker` y `deploy-frontend.sh` documentaba que *"el worker se despliega por su propio camino"*… pero **ese camino no existía**. Su imagen se construyó **a mano** una vez (23-24/07) y se subió al repo **`vence-frontend`**, con lifecycle `"conservar solo las últimas 10 imágenes"` (`tagStatus: any`) y ~6 pushes al día → **estaba condenada a purgarse en ~2 días**. El 27/07 se purgó y cada tick empezó a morir con `CannotPullContainerError`, **antes del entrypoint**: sin logs, sin eventos, sin alerta. Evidencia: `stopCode=TaskFailedToStart`, `ImageNotFoundException` sobre el digest, y `temario_pdf_pregenerated` a diario del 22 al 27/07 y **cero** después.
  2. **Y NO vale re-pinearla a la imagen del frontend** (error que cometí en el primer intento de arreglo y que cazó la verificación): el frontend es el stage `runner`, **sin devDependencies**, y el worker arranca con `tsx` → `Cannot find module '/app/node_modules/.bin/tsx'`. Es literalmente el error del **primer** intento del 23/07, que los logs conservan. Se distingue del (1) porque la tarea SÍ arranca y deja logs.
- **Es la REGRESIÓN de un postmortem ya cerrado (#115, 26/05).** El pineado por digest se introdujo para el SERVICIO, y lo que lo hace seguro no es el digest sino que **el deploy lo refresca cada vez**. Las tareas programadas copiaron el pineado sin el refresco… y sin imagen propia.
- **`vence-content-radar` y `vence-instagram-daily` hacían las 3 cosas bien** (repo propio, sin retención agresiva) y son las únicas que siguen vivas. El worker era el único de prestado en el repo del frontend.
- **Segunda víctima encontrada:** `vence-health-digest` (mismo patrón, digest también purgado). Es un **duplicado muerto** — el barrido ya corre in-process (`ContentHealthSweepCron`, 07:30 UTC) y `content_health_findings` está fresco. Decisión: **borrarla**, no revivirla.
- **Arreglado (2 capas):**
  1. **Prevención** — `scripts/deploy/repin-derived-taskdefs.sh`: fuente única `DERIVED_WORKERS` (`familia|stage|repo ECR`) que por cada tarea **construye su stage → lo sube a SU repo → re-pinea por digest del push**. Invocado por **los dos** caminos de deploy (script manual + workflow GHA). Repo ECR nuevo `vence-temario-pdf-worker`, siguiendo el patrón de las dos tareas que sí sobrevivieron. Guardrail en `deploy-scripts.test.ts`: los 2 caminos lo llaman, el stage existe en el Dockerfile, **ninguna usa el repo del frontend**, se pinea por digest, y toda derivada tiene liveness declarada. Bonus de corrección: el worker corre `scripts/pdf-worker.ts` del mismo árbol y comparte `PDF_TEMPLATE_VERSION`, así que **debe** ir en lockstep con el frontend.
  2. **Detección, AGNÓSTICA de proveedor** — `backend/src/cron-schedule/external-jobs.registry.ts`: catálogo declarativo (nombre + cadencia cron portable, **nunca** `rate()` ni ARNs) de los jobs que corren fuera del proceso. `CronScheduleService` los fusiona en el calendario con `origin: 'external'` y **`cron_overdue` los juzga con la misma regla y el mismo email**. El worker emite ya `cron_tick`/`cron_run`. Detalle de diseño: el guard de "tick anterior al arranque del proceso" **no** se aplica a externos (su vida no depende de nuestros reinicios), o cada deploy los silenciaría.
- **Por qué la detección va así y no con un canary que mire ECR/ECS:** eso sería un silo que muere el día de koigrid. Comparar cadencia declarada contra señal emitida no toca la nube: sobrevive intacto al cambio de proveedor.
- **Tests:** 5 unitarios del catálogo + 4 de comportamiento + **2 simulaciones del incidente real** (última señal 27/07 08:09 + cadencia 30 min → dispara; drenando normal → no dispara) + guardarraíl de paridad `externalScheduledJobs.test.ts` (el nombre vive en 2 ficheros que no pueden importarse) + 5 en `deploy-scripts.test.ts`. Backend 1081/1081 verde.
- **Runbooks:** `health-check.md` §1.bis.b (nuevo) + causa (4) de cron overdue; `pusheo-revision-despliegue.md` sección de tareas programadas.
- **✅ RECALIBRADO EL TECHO DE RENDER (29/07).** Al revivir el worker, el drenado topó justo con `auxiliar_administrativo_diputacion_segovia` T29: quemó intentos contra el techo de **18 min exactos** cuando el tema mide **20 min 1 s**. `DEFAULT_RENDER_TIMEOUT_MS` (nuevo, junto a `DEFAULT_STALE_SECONDS` en `lib/temario/pdf/pdfJobQueue.ts`) = **30 min**, 50% de margen sobre el peor caso medido; el worker lo importa en vez de declarar su literal. Y apareció un **acoplamiento sin declarar**: el techo y el rescate de `running` colgados tienen relación de ORDEN obligatoria (si el rescate llega antes, re-encola un render en curso → dos workers con el mismo tema), pero vivían en ficheros distintos sin nada que lo vigilara — así se descalibró. `DEFAULT_STALE_SECONDS` 30 → **45 min**, el canary del backend espeja el valor, y hay guardarraíles PUROS (corren sin BD) que fijan: techo ≥ peor caso medido +20%, rescate ≥ techo +10 min, el worker usa la constante compartida y el canary espeja. **Validados por mutación.**
- **SIGUE ABIERTO:** las 3 piezas del diseño acordado (413 encola / alerta por reincidencia / acuse en DLQ). Eso no se ha tocado.

**🔴 BLOQUEO TÉCNICO ENCONTRADO (06/08, w3) al intentar empezar la pieza (b) — «sacar el render de la ruta: lo hace el worker».**
- **MEDIDO, no supuesto:** `tsx` está en `devDependencies` de `package.json` (no en `dependencies`); `@react-pdf/renderer` y `pdf-lib` sí están en `dependencies`. El propio Dockerfile lo dice explícito para el stage `runner` (el que sirve tráfico): *"No incluye devDependencies"*. Es el MISMO hecho que ya cazó esta ficha el 29/07 al re-pinear el worker a la imagen del frontend (línea de arriba, `Cannot find module '/app/node_modules/.bin/tsx'`) — aquí aplica en el sentido inverso: la ruta servida (stage `runner`) **no puede** spawnear `tsx scripts/pdf-local.ts` como hace `childRender` del worker (`scripts/pdf-worker.ts`), porque `tsx` no existe en su imagen. Reusar tal cual el patrón del worker desde la ruta **no compila en producción**, aunque en local (con devDependencies instaladas) parecería funcionar — la trampa exacta de probar contra el entorno equivocado.
- **Confirmado además:** no hay ningún uso de `worker_threads` en el repo (`grep` sobre todo el árbol, cero resultados) ni ninguna herramienta registrada para "renderizar sin bloquear el hilo dentro del mismo contenedor" (`npm run tools:buscar -- "worker thread"` y `"pdf render"`, sin match). No es «ya existe y no lo vi»: es terreno nuevo para este codebase.
- **Por qué esto importa para decidir el CÓMO de (b), no solo el qué:** el guardarraíl `cpuBoundRoutes` exige que la ruta servida no alcance `@react-pdf/renderer`/`pdf-lib` **por import estático** (los sigue en profundidad). Eso dejaría pasar tanto "spawnear un proceso hijo" como "un `worker_threads.Worker` apuntando a un fichero aparte" (ninguno de los dos es un `import` estático de esos paquetes desde la ruta) — pero **spawnear un proceso hijo con `tsx` está descartado por el hallazgo de arriba**, y `worker_threads` con un fichero YA COMPILADO (sin tsx) es técnicamente viable en teoría —`@react-pdf/renderer`/`pdf-lib` sí están en `dependencies` y por tanto sí viajan al `runner`— pero exige que ese fichero-worker quede en el output de `next build --standalone` (hoy Next.js solo empaqueta las rutas/páginas que él mismo traza; un script aparte no sale ahí sin tocar `outputFileTracingIncludes` o un paso de build nuevo). Es una pieza de infraestructura de build que este repo no tiene hoy, y no se puede verificar sin un `next build` real + un despliegue — cosas que este trabajador no puede hacer ni comprobar.
- **La alternativa que SÍ es de bajo riesgo (cero infraestructura nueva):** en vez de aislar el render dentro del contenedor, quitarlo del todo de la ruta y reusar el patrón que YA EXISTE y YA ESTÁ PROBADO para los temas demasiado grandes (`encolarParaElWorker` + degradar a `window.print()` en el cliente ante un no-200, `components/TopicPrintButton.tsx`): CUALQUIER miss de caché encola el job para el worker y responde de inmediato (sin renderizar nada en el proceso que sirve), en vez de solo los que no caben. Esto SÍ cierra el guardarraíl al 100% (el `import` de `@react-pdf/renderer` desaparece de la ruta entera) y no añade nada nuevo al sistema. **El coste real:** hoy el usuario que pide un tema no cacheado espera unos segundos y se descarga el PDF de verdad; con esto, mientras no esté pre-generado (pieza (c) cubre los 212 temas de más demanda, no todos) recibiría la impresión del navegador en su primer intento, y el PDF de verdad quedaría listo para la SIGUIENTE vez que lo pida — con el disparo bajo demanda descartado, "la siguiente vez" puede tardar hasta el próximo ciclo del worker (cadencia 30 min). Es justo lo contrario de lo que Manuel describió como objetivo (*"el primer usuario pincha y espera a que se genere"*).
- **No he implementado ninguna de las dos** porque son dos caminos legítimos con consecuencias de producto distintas (uno cambia la UX del primer usuario en el caso no-cacheado; el otro exige construir y desplegar infraestructura de bundling nueva que no puedo verificar desde aquí) y la ruta en juego es la que ya causó el incidente de esta ficha — adivinar mal aquí es reproducirlo. Lancé la pregunta a Manuel (`node scripts/backlog.cjs preguntas`) con las dos opciones y sus costes. Piezas (c) (sembrar 212 temas, ~6h de render) y (d) (puntero a última versión) no dependen de esta decisión y quedan libres para quien retome esto — (d) es codeable/testeable ya (cambia solo la clave de caché + un puntero en BD/S3, sin tocar el mecanismo de render); (c) es un job de horas contra producción, no cabe en un turno de este trabajador.

**✅ RESPONDIDO Y PIEZA (b) IMPLEMENTADA (06/08, w3) — OPCIÓN B, decidido por Manuel en la pregunta #82.**
- **Manuel confirmó la medición y el criterio:** *"Tu premisa la he verificado y es correcta... el criterio de la casa decide solo: 'nada de silos, engánchalo al sistema que ya lo cubre'. La opción B extiende un mecanismo que YA existe, YA está desplegado y YA se usa... A construye un segundo camino de render DENTRO del contenedor que sirve tráfico... es exactamente el silo que la regla prohíbe... Sobre la UX: sí, con B el primer usuario que pida un PDF no cacheado se lleva impresión del navegador en vez del PDF real. No es una degradación nueva — es la que ya viven hoy los temas grandes. Extiéndelo, no lo reinventes."*
- **Implementado y con capas, commit `4aa9b1b54` en `flota/w3`:** `app/api/temario/[oposicion]/[topic]/pdf/route.ts` deja de importar `@react-pdf/renderer`/`TopicPdfDocument`/`stampTopicPdfChrome`/`buildTopicPdfModel` por completo. CUALQUIER miss de caché (no solo los que no caben síncronos) ahora ENCOLA para el worker (`encolarParaElWorker`, ya existente, dedup por `content_hash`) y responde `503 pdf_en_preparacion` al instante — el cliente (`TopicPrintButton.tsx`) ya degradaba un 503 a `window.print()` desde el 30/07, así que **cero cambios de cliente**. `Retry-After` corregido a 1800 s (la cadencia real del worker) en vez de los 30 s heredados del semáforo (que asumían un render en curso que ya no existe).
- **El semáforo de contención (T-270 Fase 1) se ELIMINÓ, no se dejó apagado:** `lib/temario/pdf/renderSemaphore.ts` + sus 2 tests. Su propia cabecera ya avisaba de que sobraría en la Fase 2 («la Fase 2 saca el render del camino servido... y entonces esto sobra»). Sin render que contener, un semáforo sin llamador es código muerto que alguien puede encender por error.
- **El guardarraíl `cpuBoundRoutes` (el que cazó el incidente del 29/07) ya NO tiene esta ruta en `EXCEPCIONES`**, y lleva un test POSITIVO nuevo que confirma —con el MISMO detector, no con prosa— que la ruta ya no alcanza `@react-pdf/renderer` ni `pdf-lib`. `pdfCpuInstrumentation.guardrail.test.ts` se reescribió para vigilar solo `pregenerate.ts` (quien renderiza de verdad) más un test inverso que bloquearía si alguien reintroduce el render en la ruta servida — la regresión exacta de este incidente.
- **Capas:** `topicPdfCacheServing.test.ts` reescrito para el contrato nuevo (encola+503, no genera+200) · guardarraíles arriba · sin nuevo canario (el `X-Pdf-Source`/evento `temario_pdf_served` con `served='encolado'` ya da la señal para uno futuro si hiciera falta).
- **⏳ NO desplegado ni verificado en producción — un trabajador de la flota no puede hacer ninguna de las dos.** Cierra el guardarraíl EN CÓDIGO; falta comprobar en vivo que un miss real encola y sirve 503, y que el worker recoge el job y puebla S3 para la siguiente visita. Ver [T-270] para el cierre formal (esta ficha, T-159, sigue teniendo pendientes (c) sembrar 212 temas y (d) puntero a última versión).

