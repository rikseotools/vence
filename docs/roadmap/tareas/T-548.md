### [T-548] 🟠 [ABIERTO 04/08] Paso 2 de administrativo_asturias: 29 temas stale tras el literal, y un recorte de 208 preguntas esperando decisión

- **Sale de [T-547]**, que puso los 38 epígrafes en `verified_literal` contra el BOPA. Reescribir el epígrafe dispara el trigger, así que su scope quedó **`stale`**: el veredicto anterior se emitió contra una paráfrasis y ya no vale. Estado hoy: **29 stale · 8 verified_correct · 1 verified_issues**.
- **Los 8 `verified_correct` NO son de fiar tampoco**, y conviene saberlo antes de empezar: son los temas cuyo epígrafe ya era literal, así que conservan el veredicto viejo — y ese veredicto lleva escrito *«Revisor único (agentes rate-limited)… **PENDIENTE confirmar con 2 agentes tras reset**»*. Es exactamente el sellado fuera del pipeline que mide [T-518]. Al re-verificar, entran los 38, no los 29.
- **🔴 LA DECISIÓN QUE HAY QUE TOMAR — T215, y no es mecánica.** El programa acota la LO 3/2007 a Título Preliminar + Título I (arts 1-13; estructura verificada contra `BOE-A-2007-6115`). El scope sirve además 17 artículos de los Títulos II-V (14-19, 21, 39, 45, 49, 51, 55, 62, 64, 71, 72, 76): **208 preguntas activas fuera de programa**, el 42% de lo que el tema sirve de esa ley.
  - **Por qué no se aplicó ya:** 208 > 150 → puerta de juicio. Quitar 208 preguntas del tema de igualdad de una oposición viva es una decisión de programa, no un recorte de higiene. Las preguntas **no se borran**, dejan de servirse en ese tema.
  - **Antes de recortar, mirar los temas hermanos:** parte de esos artículos (empleo público, planes de igualdad) puede pertenecer legítimamente a otro tema del mismo programa, y entonces el movimiento es REPARTIR, no quitar.
- **Cómo se hace** (pipeline, no a mano): `verify:scope dump administrativo_asturias` → Workflow `verify-scope-oposicion` (2 agentes + juez anclados a BOE) → `plan` → `apply` (los `auto_safe`; T215 exigirá `--include-gate` tras decidir).
- **Y hay una segunda sospecha que mirar con el literal delante:** T305, cuyo epígrafe ahora delimita la LGSS a *«(Capítulo I, Capítulo II, Secciones 1 y 2 del Capítulo III y Secciones 1 y 2 del Capítulo IV del Título I)»* mientras el scope sirve 52 artículos. No se ha medido: hace falta mapear esas secciones a sus artículos en el BOE, que es trabajo de la adjudicación, no de un `includes()`.
- **NO cerrar esto sellando los 29 como `correct` sin pasar por los 2 agentes.** Es el error que este trabajo acaba de destapar, y hacerlo aquí sería repetirlo con la tinta fresca.

- **📊 AVANCE 07/08 (worker w1) — pipeline corrido, PARADO a mitad por el límite semanal de agentes, dos recortes medidos y listos para decidir/aplicar.**
  - **`verify:scope dump` + Workflow `verify-scope-oposicion` corridos contra los 38 temas.** Solo **6 completaron consenso de verdad antes de que el proveedor cortara por cuota semanal** (mensaje literal de los 65 agentes restantes: *"You've hit your weekly limit · resets 11pm (UTC)"*, medido, no supuesto — 12/77 agentes acabaron): **T1, T2, T3, T4, T5 en consenso `alta` confianza, sin cambios** (T5 en particular SALE del stale: era uno de los 29 y ahora queda re-verificado limpio). **T6 tuvo un falso "discrepancia"**: analista y escéptico coinciden en sustancia (quitar=[]/anadir=[] en las dos leyes) pero nombraron la ley con formato de texto distinto, y el comparador del workflow compara por igualdad de string → lo mandó a juez, que también murió por la cuota. Resuelto a mano leyendo el JSON crudo de los dos veredictos: no hay discrepancia real, T6 queda `correct` sin cambios.
  - **Quedan SIN verificar por el pipeline los otros 32 temas** (T7, T201-T214, T216, T301-T304, T306, T401-T405, T501-T504) — de los 29 originalmente `stale`, solo T5 quedó re-verificado; de los 8 `verified_correct` (single-reviewer, no fiables), solo T1/T2/T3/T4/T6 quedaron confirmados con 2 agentes — **T202, T205, T210 siguen con el sello de un solo revisor sin confirmar**. Retomar corriendo el mismo pipeline (dump ya generado, reusar) en cuanto la cuota semanal se restablezca (11pm UTC).
  - **✅ T215 MEDIDO A MANO, con cifra exacta.** ⚠️ **Corrección de método (aviso de Manuel en #104): distinguir la fuente con precisión** — lo consultado fue `law_sections`, que es **NUESTRA COPIA** de la estructura de la ley (poblada en el Paso 1 de [T-547]), no el BOE directamente; si alguien discute el recorte, lo que lo sostiene de verdad es el **índice oficial del BOE** (`BOE-A-2007-6115`), que no se volvió a abrir aquí porque `law_sections` ya se había verificado contra él en el Paso 1 — pero conviene decirlo así de claro, no "medido contra el BOE" a secas. Consultado `law_sections WHERE law_id='6e59eacd...' AND section_type='titulo'`: Título Preliminar = arts 1-2, Título I = arts 3-13 — exacto a lo que decía la ficha. Las **208 preguntas activas** en los 17 artículos fuera de programa (14-19,21,39,45,49,51,55,62,64,71,72,76) se cuentan exactas por SQL directo contra `questions`/`articles` — 42,3% de las 492 que sirve el tema de esa ley (492 también contado, no de la ficha). **Revisados los temas hermanos** (T301 TREBEP, T302 Ley 2/2023 Asturias Empleo Público): ninguno de los dos menciona en su epígrafe "igualdad" ni "planes de igualdad" — no hay tema hermano al que repartir, es recorte puro, no reparto. Corrido `plan` (con el propuesto quitar) contra el clasificador real (`scripts/lib/scope-classifier.cjs`, no un juicio propio): confirma `judgment_gate` por `impacto_alto` (208>150), exactamente como predecía la ficha. `apply --dry-run --include-gate` (de verdad dry-run, "nada escrito" confirmado) previsualiza 29→12 artículos. **Lanzada pregunta #104 a Manuel** con las dos opciones y mi recomendación (recortar, mismo patrón que [T-522]/Parque Móvil).
  - **✅ APROBADO por Manuel (07/08, respuesta a #104): opción (a) para T215, y confirma T305 "de aplicación directa".** Su nota, íntegra porque importa para la próxima vez: *"la puerta de juicio salta por IMPACTO (>150 preguntas), no porque el caso sea dudoso... un tema sirve lo que su epígrafe pide, y servir 208 preguntas fuera de programa es el defecto, no el statu quo"*. Pide **el plan empujado en la rama** para aplicarlo él con credencial de escritura y verificar en BD que el scope cambió antes de darlo por hecho — **GOTCHA que avisa él mismo**: `verify:scope apply <pt> --include-gate` **sin el jsonPath falla EN SILENCIO, sin COMMIT**, dando la sensación de que se aplicó. Y recuerda invalidar caché de temario/test-counts después (per-instancia, hay que repetir).
  - **📦 Plan + propuestas empujados: `scratchpad/t548/propuestas.json` (el veredicto de los 6 temas consenso + T215/T305) y `scratchpad/t548/plan.json`** (regenerado 07/08 contra el clasificador real, mismas cifras: 208/42,3% y 66/28,8%, sin drift). **Para aplicar:** `node scripts/verify-topic-scope.cjs apply administrativo_asturias scratchpad/t548/plan.json --include-gate` (con el jsonPath explícito, por el gotcha de arriba) — y comprobar después en BD que `topic_scope` de T215/T305 cambió antes de cerrar.
  - **✅ T305 MEDIDO A MANO, segunda sospecha de la ficha cerrada con cifra — y la fuente aquí SÍ es el BOE directo, no nuestra copia** (a diferencia de T215: `law_sections` no tiene granularidad de Sección para esta ley, solo Título/Capítulo, así que hizo falta consultar el **índice oficial del BOE** de verdad — WebFetch a `boe.es/buscar/act.php?id=BOE-A-2015-11724`, no un intermediario nuestro). El epígrafe pide "Secciones 1 y 2 del Capítulo III" y "Secciones 1 y 2 del Capítulo IV" de RDL 8/2015: Capítulo III Sección 1 "Afiliación al sistema..." (15-17) + Sección 2 "Cotización..." (18-20) = **15-20**; Capítulo IV Sección 1 "Disposiciones generales" (42-46) + Sección 2 "Prestaciones contributivas Régimen General" (47-52) = **42-52**. El Capítulo IV YA está bien (el scope para exacto en 52). El Capítulo III **sí sobra**: el scope llega hasta el 41, incluyendo la Sección 3 "Liquidación y recaudación de las cuotas" (21-41, NO pedida por el epígrafe) — **66 preguntas activas exactas** en esos 21 artículos (contadas por SQL), 28,8% de las 229 que sirve el tema de esa ley. `plan` lo clasifica `judgment_gate` pero por `epigrafe_no_localizable` (66<150, no es impacto — el clasificador no puede casar "Secciones 1 y 2" contra el texto del epígrafe sin la estructura de Secciones, que solo estaba en el BOE, no en BD), no por impacto alto: es un recorte **mecánico**, no una decisión de programa. **Confirmado por Manuel en #104: "de aplicación directa, tienes razón".**
  - **✅ CERRADO — nada aplicado por este worker (sin credencial de escritura, regla dura), pero el plan queda EMPUJADO y listo:** `scratchpad/t548/plan.json` + `scratchpad/t548/propuestas.json`, rama `flota/T-548-paso2-administrativo-asturias`. Comando para quien lo aplique: `node scripts/verify-topic-scope.cjs apply administrativo_asturias scratchpad/t548/plan.json --include-gate` (jsonPath EXPLÍCITO — el gotcha que avisa el propio Manuel: sin él, falla en silencio y sin commit). Verificar después en BD que `topic_scope` de T215 y T305 cambió, y purgar/revalidar caché de temario + test-counts (per-instancia, repetir en cada una).
  - **Siguiente paso para quien retome esto, en este orden:** (1) aplicar T215+T305 con credencial de escritura (arriba); (2) re-lanzar `verify-scope-oposicion` para los 32 temas que la cuota dejó sin tocar (dump reusable: `verify:scope dump administrativo_asturias`); (3) solo entonces `record` el consenso completo y cerrar el Paso 2 de esta oposición.

