### [T-328] 🟠 [ABIERTO 30/07] Landing de «tu oposición a medida»: hay hueco de mercado y nadie lo ofrece
- **La tesis (Manuel, 30/07):** montarte tu propio temario eligiendo leyes y artículos **no lo ofrece nadie** en el mercado de preparación de oposiciones, y **resuelve un dolor real**: el opositor de una oposición específica (A1/A2, o cualquiera fuera de las masivas) no encuentra plataforma con SU temario y acaba estudiando con material genérico o en PDF.
- **La evidencia de que el dolor existe la tenemos dentro:** un premium ([T-327]) se lo está fabricando a mano con configuraciones de test guardadas porque su oposición no está montada. Eso no es una petición de función, es alguien pagando y buscándose la vida.
- **Qué habría que hacer:** landing propia (no un apartado dentro de otra) que hable a ese opositor: *«¿tu oposición no está en ninguna plataforma? Móntate tu temario con las leyes y los artículos que entran»*. Con su SEO propio: la búsqueda de quien tiene esa necesidad no es «test auxiliar administrativo», es el nombre de su oposición rara + «temario» o «test».
- **Antes de escribir la landing, dos cosas que NO se pueden dar por supuestas:**
  1. **Comprobar el hueco de verdad**, no por intuición: mirar qué ofrecen los competidores catalogados (`competitor_*`, panel `/admin/competidores`) y si alguno permite algo parecido. La tesis es de Manuel y probablemente es correcta, pero una landing que afirma «nadie lo ofrece» debe apoyarse en una comprobación, no en una impresión.
  2. **Medir la demanda con datos nuestros**: cuántos usuarios tienen `target_oposicion` de una oposición **sin temario montado** (a Sergio le pasa; hay 16 solo en `agente_hacienda`), y cuántos usan ya configuraciones guardadas con artículos sueltos. Ese número es el tamaño del mercado interno y además dice a qué oposiciones apuntar primero.
- **Dependencia:** la landing **no puede publicarse antes que [T-327]** — prometer en SEO algo que el producto todavía no hace es la forma más rápida de quemar la palabra. Primero la funcionalidad, luego la landing.
- **Impacto:** 🟠 es crecimiento, no mantenimiento: ataca el cuello de botella del catálogo (cada oposición nueva hoy cuesta construir su temario a mano) por el otro lado, dejando que el usuario se lo monte.
- **Relacionada:** [T-327] (la funcionalidad que sostiene la promesa), `docs/runbooks/seo-oportunidades.md` (cómo medir la demanda de búsqueda), `docs/runbooks/analizador-competidores.md` (para comprobar el hueco).

- **⚠️ CORRECCIÓN (08/08, w2): el estado "✅ DESPLEGADO 50e50e08" del `resume_check` era FALSO — la
  rama sigue sin mergear y la landing sigue en 404.** No lo di por bueno: `git merge-base
  --is-ancestor <commit del feature> 50e50e08` da **NO** para los dos commits de la rama
  (`eb85036a1` el feature, `27d3d1953` la re-verificación) — `50e50e08` es un deploy real de HOY
  (frontend 15:42-16:19 UTC, backend 15:38-15:42 UTC, confirmado en `deploy_runs`), pero de OTRO
  trabajo que llegó a `main` esa tarde, no de esta rama. Confirmado también en vivo:
  `curl -s -o /dev/null -w '%{http_code}' https://www.vence.es/tu-oposicion-a-medida` sigue dando
  **404** ahora mismo, mientras `/oposicion-personalizada` (la dependencia de [T-327]) da **200** —
  el bloqueo sigue siendo solo de esta landing, exactamente como ya se había confirmado el 08/08
  por la mañana. SOSPECHO que alguien confundió "es el deploy más reciente que veo en
  `deploy_runs`" con "incluye mi rama" — el mismo error de método ya visto hoy en otras tareas
  ([T-408], [T-564]): un deploy posterior en el tiempo no implica que el commit sea su ancestro si
  la rama nunca se mergeó.

- **🔎 RE-VERIFICADO de nuevo (08/08, w2), independiente de las dos vueltas anteriores.** Rama
  `flota/T-328-oposicion-a-medida-limpia` (commit `27d3d1953`), 16 commits detrás de `main` —
  merge de prueba en un scratch branch (limpio, sin conflictos, abortado después). Comprobado con
  mis propios comandos, no repetido de la ficha:
  - `nombrePublico()` (`lib/oposicionPersonalizada/nombrePublico.ts`) leído entero: genera
    `"<nombre> by <Pila> <Iniciales>."`, coincide con la frase de la FAQ en `content.ts` línea 46
    (`'Se publica como "Oposición <tu nombre> by <tus iniciales>"...'`).
  - `return_to` de punta a punta: `CreadorTemarioCliente.tsx` linkea a
    `/login?return_to=/oposicion-personalizada`, y `app/login/page.tsx` lee
    `searchParams.get('return_to')` de verdad (no un parámetro ignorado).
  - Addendum RLS de [T-573] re-medido con consulta propia: `custom_oposiciones`,
    `user_test_favorites`, `user_oposicion_alerts` — las tres `relrowsecurity=true`, **0**
    `pg_policy`, y `SELECT count(*)` da **0 sin ningún error** para las tres. Coincide byte a byte
    con lo ya escrito.
  - `/tu-oposicion-a-medida` linkeada desde `app/oposiciones/page.tsx` (línea 203) y
    `app/sitemap-static.xml/route.ts` (línea 34) — confirmado con grep directo, no supuesto.
  - Tests: `content.test.ts` 6/6, suite de oposición-personalizada (guardarraíl + componentes +
    lib) 125/125 sin ninguna caída, `oposicionesIntegrity.test.ts` 348/348 (mi carpeta nueva no
    colisiona con el detector de páginas huérfanas). `tsc --noEmit` limpio, `eslint` limpio en los
    6 ficheros tocados.
  - **Nada desplegado por mí** (regla dura). El "falta" sigue siendo literalmente el mismo que en
    la vuelta anterior: todo lo que exige navegador real contra la URL viva (CTA, flujo
    login→return_to, rich result de Google, bloque de `/oposiciones`) es imposible de comprobar
    desde este puesto mientras la rama no se mergee y despliegue — no hay ningún cron ni ventana
    de tiempo que esperar aquí (a diferencia de [T-408]/[T-564] el mismo día): es puramente una
    decisión de merge de una persona.
