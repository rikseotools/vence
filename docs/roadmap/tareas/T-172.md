### [T-172] 🟡 [ABIERTO 27/07 · 35 catalogadas ya repuntadas] Fuentes de seguimiento SIN destino estático posible: 8 activas + 37 fichas del SES que solo se pueden vigilar con adapter (o no vigilar)
- **De dónde sale:** al revisar las 123 activas una a una en [T-165] quedaron 8 oposiciones en las que **no existe URL estática que sirva el listado**. No es una URL mal puesta —eso ya se repuntó—, es que el portal no publica el listado en HTML. Repuntar no las arregla; ahora al menos ya no fingen estar sanas.
- **Los 8 casos, medidos (27/07):**
  - **Junta de Extremadura** (`auxiliar-administrativo-extremadura`, 297 usuarios) — el buscador de empleo devuelve los resultados por **POST con token `p_auth` de sesión**; comprobado con headless: rellenar el formulario SÍ devuelve las convocatorias del Cuerpo Auxiliar, pero por GET no hay forma. El portal público (`portalempleado.juntaex.es`) es el área privada del empleado.
  - **SES Extremadura** (`tcae-extremadura`) — `/web/oposiciones` es un **404 servido con 200** y todo lo demás está tras el Portal del Candidato (login). Es la ÚNICA activa que pinga hoy el badge.
  - **Universidad de Murcia** ×2 (`administrativa-universidad-de-murcia`, `tecnico-auxiliar-universidad-de-murcia`) — ConvocUM es una app JSF; `um.es/web/ptgas` es el índice general del área (3.201 chars, cero convocatorias).
  - **Diputación de Huelva** (`auxiliar-administrativo-diputacion-huelva`) — ni la sección de empleo ni "Convocatorias Turno Libre" traen el listado: 4.8-7 KB de navegación y **0 fechas**, también con headless.
  - **Diputación de Cuenca** (`auxiliar-administrativo-diputacion-cuenca`) y **Diputación de Girona** — SPA (`convoca.online`) e índice sin contenido.
  - **SESCAM** (`tcae-sescam`) — el hub de OEP sirve 958 chars por headless; su hermano `celador-sescam-clm` usa la MISMA URL y sí registra cambios, así que se deja hasta decidir el adapter.
- **Opciones (decidir, no improvisar):** (a) **adapter por entidad** que sepa hacer el POST/render y extraer el listado —encaja con el `fetcher_type`/Lambda headless que ya existe, pero hoy solo hace *render*, no *interacción*—; (b) aceptar que su única vigilancia es la capa de **BOLETÍN** — y para Extremadura esa vía **está viva y comprobada el 27/07**: el adapter `doe` de `ccaa-boletines.ts` devuelve sumarios reales (5.500-6.600 chars con 6-11 menciones de convocatoria/oposición). Ojo con la trampa que casi me lleva a decir lo contrario: las filas `DOE` de `detection_sources` tienen `last_success_at` en 2026-06-01, **pero esa es la capa VIEJA** (portales de entidad) y no es de donde se lee el boletín. Ver `salud-radar.md` §2; (c) dejarlas visibles como hallazgo y no fingir cobertura. Hoy estamos en (c) por defecto.

#### 📌 SESIÓN 07/08 (w4) — re-medición de las 8 activas: 6 confirman la ficha, 1 es RUIDOSA (no muda), 1 estaba MAL caracterizada — corregido con lo medido

- **Método:** para cada una de las 8, leí `convocatoria_seguimiento_checks` (últimos 5-10 registros reales del cron, `content_preview` incluido) contra RDS (`VENCE_LECTOR_URL`) — no repetí el fetch a mano, usé la MISMA evidencia que ya persiste el cron cada día.
- **✅ 5 confirmadas EXACTAS, blindas de verdad, contenido leído hoy:** `tecnico-auxiliar-universidad-de-murcia` (ConvocUM: *"Espere, por favor... Cargando..."*, shell JSF sin renderizar), `administrativa-universidad-de-murcia` (menú general de la universidad, cero mención a convocatorias), `auxiliar-administrativo-diputacion-huelva` (chrome de accesibilidad + menú institucional, 0 términos de empleo), `auxiliar-administrativo-diputacion-cuenca` (*"Este navegador no es soportado por Convoca"* + plantillas Vue `{{ }}` sin renderizar), `auxiliar-administrativo-diputacion-girona` (portal general de la Diputació, sin sección de oferta). Las 5 siguen exactamente como las describió la ficha.
- **⚠️ `tcae-extremadura` — SIGUE BLIND, pero por una razón más precisa que "404 corto": el 404 tiene 81.935 caracteres, no unos pocos cientos.** El `content_preview` de hoy empieza literalmente *"Extremadura Salud - Error No se encontr[ó] la p[á]gina [oposiciones] solicitada"* — es la página de error de TODO el portal de salud, que arrastra el megamenú del sitio entero (vacunas, cita previa, tabaquismo…). Por eso el hash es **estable** (mismo error, mismo menú, cada día) y por eso el detector de longitud (`seguimientoVigilable.cjs`, umbral 1.500 chars) NO lo caza — tiene de sobra. Es exactamente el punto ciego que ese mismo fichero documenta ("SPA con mucho armazón estático pasa el umbral y se da por vigilable aunque esté igual de ciega"), aplicado a una página de error en vez de una SPA. Sigue siendo el ÚNICO de los 8 que pinga el badge hoy, y con razón.
- **⚠️ `auxiliar-administrativo-extremadura` — la ficha decía "blind"; MEDIDO es distinto: es RUIDOSA, no muda.** El `content_hash` de `portalempleado.juntaex.es` cambia case a diario (**3 de los 8 checks más recientes con dato marcan `has_changed=true`: 07/08, 06/08 y 31/07** — corregido 08/08, w3: la cifra original decía "4 de 8, incluyendo el 01/08" pero el cron es L-V y ese sábado no corrió; de los 8 checks reales con dato, `07/08,06/08,05/08,04/08,03/08,31/07,30/07,29/07`, solo 3 tienen `has_changed=true`, no 4) sobre contenido que sigue siendo el MISMO shell del portal del empleado (*"Comisiones de servicio... Situaciones administrativas..."*, cero mención a oposiciones) — algo en la página (probablemente un token de sesión o timestamp embebido) hace que el hash nunca se estabilice. Confirmado que esto NO se propaga como ruido aguas abajo: leí `seguimiento-queries.ts` — desde el recalibrado del 26/06 un `has_changed` NO emite señal a `oep_detection_signals` (ese cambio ya se hizo justo para parar el mismo patrón: *"130+ señales por re-baseline, 0 con dato extraído"*), solo marca `seguimiento_change_status='changed'` para revisión humana en `/admin/seguimiento-convocatorias`. O sea: no espamea alertas, pero SÍ ensucia esa cola de revisión con un "cambio" falso cada 2-3 días, indefinidamente, para una fuente que de todas formas no sirve el listado.
- **🔴 `tcae-sescam` (y su hermana `celador-sescam-clm`) — LA FICHA SE EQUIVOCABA, no algo que se arreglara después.** La ficha decía "958 chars por headless". Medido hoy: el historial del cron desde el **27/07** (el mismo día que se escribió la ficha) hasta hoy da **contenido ESTABLE de 66.073-66.091 caracteres**, con el MISMO hash 9 de los últimos 10 checks, y el `content_preview` incluye literalmente *"Oposiciones Ofertas de Empleo Público SESCAM"* y una lista real de *"Procesos Selectivos OEP 2023/2024, OEP Estabilización, OEP 2017/2018, OEP 2016"* — contenido temáticamente relevante, no boilerplate. `fetcher_type='http'` (no headless) en la fila `oposiciones` de hoy. **No sé POR QUÉ la medición original dio 958 chars** (¿otra URL en ese momento? ¿medición manual con curl sin las cabeceras del cron?) — no tengo forma de reconstruirlo sin el detalle exacto de esa comprobación, así que lo dejo como pregunta abierta, no como afirmación. Lo que SÍ puedo afirmar, medido: **`tcae-sescam` y `celador-sescam-clm` NO son un caso "sin destino" — ya tienen uno que funciona, y llevan así desde antes de que se escribiera esta ficha.** Recomendación: sacarlas de la lista de "8 activas" — quedan 7.
- **Resumen tras la re-medición: de las "8 activas", 5 confirmadas ciegas de verdad, 1 ciega por una causa más precisa (404 grande), 1 ruidosa-no-muda (haría falta silenciar `has_changed` para este caso o aceptar el ruido en la cola de revisión), y 1 (SESCAM) que NUNCA debió estar en la lista — ya vigila bien.**
- **Las "37 fichas del SES" NO están en `oposiciones` ni en `detection_sources` con esa cardinalidad** (comprobado: solo 1 fila en cada tabla menciona SES/Extremadura-salud) — son catalogadas fuera de las tablas que puedo leer con `VENCE_LECTOR_URL`, o el término "fichas" se refiere a otro sitio (posible backlog externo de discovery). No las localicé; no afirmo nada sobre su estado actual — **SOSPECHO que siguen igual que el 27/07 (nadie las ha tocado, no hay rastro de escritura reciente), pero no lo he verificado** porque no encontré dónde viven.
- **No implementado ninguna de las 3 opciones** (sigue siendo decisión de Manuel, no mecánica): el adapter de interacción (a) es una pieza de infraestructura nueva no trivial; el boletín-como-cobertura (b) para Extremadura tiene el adapter `doe` vivo (9 pasadas, última hoy 06:31 UTC) pero **0 señales nuevas generadas nunca** — dato neutro, no prueba ni que funcione ni que esté roto, solo que no ha habido nada que reportar en ese periodo o que el matching no está enganchando; no lo investigué más a fondo por ser alcance de otra ficha (el radar en sí, no esta). Sigue en (c) por defecto, ahora con 7 en vez de 8, y con la nota `tcae-extremadura`/`auxiliar-administrativo-extremadura` mejor caracterizada.
- **✅ 35 CATALOGADAS REPUNTADAS 27/07 — y la mitad "mecánica" resultó ser mecánica solo a medias.** De las 72 con 404:
  - **Rioja Salud (33) → ARREGLADAS.** La URL vieja (`riojasalud.es/ciudadanos/oferta-de-empleo-publico`) redirige a `/error-404`. La sección viva está en otra rama del portal: `…/profesionales/rrhh/seleccion-y-provision/ofertas-de-empleo-publico`, que lista las OEP por año (2022→2026) más "Próximas convocatorias y borradores de temarios" — ahí SÍ aparecería una nueva. Las 33 repuntadas con `repuntar-url.cjs --anclas` y hash reseteado. **GOTCHA del lote:** el `while read` de bash **se come la última línea** si el fichero no acaba en salto — 32 de 33 aplicadas y la que faltaba solo se vio comprobando el recuento en BD. Verifica el residuo, no el contador del script.
  - **SERGAS (2) → ARREGLADAS.** `auxiliar-administrativo-sergas` y `enfermero-sergas` vigilaban `fides.sergas.es` (la pantalla de login) igual que sus hermanas activas; repuntadas al índice de OPE del SERGAS. Ya no queda ninguna ficha apuntando a `fides.sergas.es`.
  - **SES Extremadura (37) → SIN DESTINO, se quedan como hallazgo.** Comprobado con render: el portal solo ofrece dos enlaces de empleo y **ninguno sirve**: `/seleccionpersonal/` es el Portal del Candidato (muro de login) y `/bolsa` devuelve **0 caracteres** de texto. No hay sección pública que vigilar. Se suman al bucket de "adapter o boletín" de esta misma ficha.
  - **Efecto medido:** la simulación bank-wide baja de **81 fuentes ciegas a 46** (las 35 repuntadas pasan a "evidencia no atribuible" hasta el recheck del cron, que es el transitorio normal). Las 38 `pagina_no_encontrada` que quedan son las 37 de SES + `tcae-extremadura`.
- **Doc:** `docs/maintenance/oeps-convocatorias-seguimiento.md` · `docs/runbooks/salud-radar.md`.

