### [T-541] ✅ [HECHA 04/08] Dentro de una oposición personalizada, cuatro enlaces te sacaban a otra oposición sin dar ningún error

- **Lo encontró un usuario, no una prueba.** Sergio (`pcsergio0@gmail.com`, premium, feedback `bd8b92d0`, 04/08): *«NO PUEDO PRACTICAR TEST SOBRE MI TEMA, ME LLEVA AL TEMA DE LAS OPOSICIONES DEL ESTADO QUE ELEGÍ CUANDO NO PODÍA PERSONALIZARLO»*. En su journey se ve clavado: **09:45:49** pulsa «Practicar este tema» en `/oposicion-personalizada/a92faef…/temario/tema-10` y aterriza en `/administrativo-estado/test/tema/10`. Repetido a las 09:52:15.
- **CAUSA — un valor por defecto que es una oposición REAL.** La página del tema de una personalizada reutiliza `TopicContentView` de `administrativo-estado` y no le pasaba nada; ese componente tiene `oposicion = 'administrativo-estado'` por defecto, así que `basePath` salía `/administrativo-estado` y con él sus **cuatro** enlaces: tema anterior, tema siguiente, «Volver al índice» y «Practicar este tema». **No falla: teletransporta.** Es la misma familia que ya mordió dos veces —el fallback `auxiliar_administrativo_estado` de `TemaTestPage` y el default de `ExamReviewLayout` (flor/MariSol, 13/07)— y en las tres el destino carga perfectamente, que es lo que lo hace invisible.
- **SEGUNDO FOCO, encontrado por la capa nueva y no por mí:** al terminar un test propio, «Volver al Tema» y «Ver Otros Temas» llevaban a `/auxiliar-administrativo-estado/…`. `resolveOposicionSlugForNav` resuelve contra el catálogo ESTÁTICO: dentro de una personalizada no casa ningún segmento de la URL, el `target_oposicion` tampoco está en sus mapas, y **devuelve la flagship**. Arreglado en el punto donde se decide la raíz (una vez, no en cada botón), con la RUTA mandando sobre el perfil: las personalizadas son públicas y puedes estar viendo una que no es tu objetivo.
- **ARREGLO.** Tres primitivas puras en `lib/oposicion/objetivoPersonalizado.ts` —el módulo que ya era dueño del concepto, ninguna herramienta nueva—: `raizPersonalizada(id)`, `raizPersonalizadaEnRuta(pathname)` y `enlaceSaleAOtraOposicion(href, raiz)`. `raizPersonalizada` **es la primitiva que faltaba**: el Header la sacaba con `rutaTestPersonalizada(...).replace(/\/test$/, '')`, y ese atajo es justo lo que dejó el concepto sin nombre reutilizable — la segunda pantalla que lo necesitó no pudo, y acabó heredando el slug de otra oposición. Ahora `rutaTestPersonalizada` se construye sobre ella y el Header la usa.
- **CAPAS, y las tres se han visto en ROJO antes de darlas por buenas:**
  1. **29 unitarios** del núcleo puro (raíz, ruta, fuga, y los enlaces que un temario tiene legítimamente hacia fuera).
  2. **Guardarraíl** — no un fichero nuevo: el **quinto eslabón** de `testsDeOposicionPersonalizada.guardrail.test.ts`, que ya vigilaba este mismo fallo para la ruta del TEST y no miraba la del TEMARIO. Incluye la regla de CLASE: ninguna página puede montar el `TopicContentView` de otra oposición sin pasarle `basePath`. Comprobado quitando el arreglo: 2 rojos; restaurado: verde.
  3. **Navegador** — `sim:rutas-oposicion-personalizada` (el rastreador de T-327). **Su punto ciego era de construcción:** seguía solo los enlaces `a[href^="${raiz}"]`, o sea que **un enlace que se ESCAPA le era invisible por definición**. Ahora lee todos los del CONTENIDO (el Header es cromo compartido y enlaza fuera con toda legitimidad; ya se comprueba aparte) y marca las fugas. Corrido contra **producción antes de tocar nada**: 5 fugas, los dos focos de arriba. Ese es el control rojo que faltaba en [T-521].
- **GOTCHA de medición:** la primera versión del detector daba **17** señales y **14 eran `/psicotecnicos/test`**, que es un enlace común del Header y no una oposición. Un detector que grita en el caso normal se deja de mirar, así que la forma `/<algo>/test|temario` lleva una lista corta de SECCIONES de la app que no son oposiciones. Se mide, no se supone: las 3 restantes eran el fallo de verdad.
- **NO se añade evento de observabilidad**, y es a propósito: esto no es una condición de ejecución sino un enlace mal construido, que es estático y se caza en CI. Un evento aquí sería ruido que nadie mira; el guardarraíl lo impide antes de nacer.
- **VERIFICADO EN PRODUCCIÓN (04/08, dos vueltas).** La primera no bastó y eso es el hallazgo: con el arreglo ya vivo, el rastreador seguía dando **2 fugas** — eran de `TestPageWrapper` («🎛️ Cambiar configuración»), que resuelve la ruta igual que `TestLayout` y por tanto caía en la flagship. Y destapó un **404**: el pie del tema ofrecía «Tema siguiente» con `topicNumber + 1` a ciegas, y mientras los enlaces se escapaban ese tema existía… en la otra oposición. **El fallo estaba tapado por el otro fallo.** Segunda vuelta con los dos arreglados: **0 fugas y 0 rutas rotas**. Queda un rojo del sim que NO es de esto y ya salía antes de tocar nada (el botón de empezar test con la cuenta efímera, documentado como no concluyente en el propio sim).
- **Mejora del rastreador que salió de aquí:** ahora dice el **texto** del enlace además de la ruta. Una fuga sin saber qué botón es obliga a buscar a ciegas en el código, y costó una vuelta entera localizar «Cambiar configuración».
- **La respuesta al usuario la mandó OTRA sesión** (`imp-04ago-e`, 15:33) cubriendo las dos quejas, incluida esta. La puerta de reserva de [T-474] impidió el correo duplicado: el arreglo viajó por la ficha, no por el mensaje.
- Relacionadas: [T-327] (el creador de temario propio), [T-521] (las migas y el selector en personalizada — mismo origen: el catálogo estático no conoce estas oposiciones) y [T-508].

