### [T-026] 🟡 [SISTEMA HECHO + LOOP VIVO — drena como mantenimiento] Completitud de leyes vs fuente oficial
- **Qué:** sistema para que ninguna ley del temario quede importada a medias / falso verde / sin fuente. **Las 4 capas CONSTRUIDAS y el loop CERRADO Y VIVO (verificado 20/07):** detección (`lib/laws/completeness.ts` + `scripts/audit-law-completeness.cjs` + sweep) · tabla+guard anti-falso-verde+vista (`20260718_law_source_verification.sql`, en RDS) · extractor no-BOE (`scripts/verify-law-source.cjs`) · gate publicación (`trg_topics_gate_incomplete_laws`) · **cron semanal de regresión DESPLEGADO** (`backend/src/law-completeness/`, emite `law_completeness_swept`, última hoy 20/07 10:18).
- **Estado (20/07, re-medido a última hora):** **64 leyes actionable sirviendo en temas vivos** (45 false_green, 12 no_source, 6 never_verified, 1 issues) — bajó de 92 con el drenaje del día (bloque BOA, etc.). **Desglose por fuente (útil para planificar):** **40 SIN url** (editoriales/regionales marcadas "actualizada" sin evidencia — el bucket mayor y el que more reincide), **23 non-BOE con gaceta propia** (aquí vive el ex-"cabo de las 129", ya absorbido; extractor reutilizable `scripts/verify-law-boa.cjs` + 18 tests, y los gotchas por gaceta en esa ficha), **1 BOE**. **OJO — el backlog REINCIDE al alza:** cada build de oposición nueva mete leyes regionales marcadas `actualizada` SIN evidencia (false_green) o sin fuente; el gate solo bloquea `incomplete`, no never_verified/no_source/false_green → **el número sube con cada build** (subió ~83→95 con los builds de esta semana). El cron lo DETECTA (regresión) pero no lo PREVIENE.
- **📏 Cobertura total medida (20/07, desde `/api/verify-articles/stats-by-law`):** de **1.381 leyes**, **573 no tienen
  NINGÚN resumen de verificación** (nunca se han comparado con su fuente). OJO con confundir cifras: las **64
  "actionable"** de arriba son las que **sirven en temas vivos** — ese es el número que importa para priorizar. Las 573
  son el universo entero e incluyen leyes que hoy no sirve nadie. **No encienden el badge** (el endpoint solo marca
  discrepancia cuando HAY resumen y falla), así que es deuda silenciosa, no alarma.
- **Los "easy wins" (URL de BOE) YA se drenaron** → el residuo es todo regional/universitario/EU = **research per-ley finicky** (ver "MAPA DE RAREZAS POR FUENTE" en la memoria `project-completitud-leyes-vs-fuente`: cada boletín tiene su quirk; BORM bloquea el fetch del extractor por UA; SPAs universitarias; EUR-Lex sin separador). Drena bajo garantía, sin urgencia.
- **Drenaje 20/07:** Decreto 152/2021 Murcia (era `incomplete` miss=110 = artefacto URL-boletín; corregida a la disposición única anuncio 5111, 8/8 arts verbatim vía pdftotext → `verified`) + 2 exenciones editoriales (`no_consolidated_text`). **✅ Convenio Budapest RESUELTO (20/07): decisión tomada = verbatim-re-import.** Los 48 artículos eran resúmenes
  (similitud media con el BOE **13%**, máx 34%). Reimportados por `sync-all`: de **5,7 KB a 152,6 KB**, `matching` 0→48.
  **Efecto colateral que hubo que reparar:** 1 de las 14 preguntas (`7ee381d0`) citaba en su blockquote el resumen que
  desapareció → corregida con cita literal verificada del art. 9.1. **Lección para futuros re-imports verbatim: revisar
  SIEMPRE las explicaciones que citen el texto viejo**, porque quedan citando algo que ya no existe en ninguna norma.
- **✅ CORRECCIÓN 20/07 (medido, no estimado) — el vector `false_green` YA ESTÁ CERRADO:** los **44 false_green** vivos
  son **todos anteriores al trigger** `trg_laws_block_false_green` (el más reciente se tocó el 10/07; el trigger entró
  el 18/07). **Cero nuevos desde entonces**, incluidos los builds de esta semana. La frase de arriba ("REINCIDE al
  alza… sube con cada build") describe la situación PREVIA al trigger: para este bucket ya no es cierta, y seguirla
  llevaría a resolver un problema ya resuelto.
- **El vector que SÍ sigue abierto es otro, y NO es "el build olvida la fuente":** de las 46 leyes nuevas que sirven en
  temas vivos en 9 días, **7 entraron sin fuente y las 7 son de hoy**. Al mirarlas una a una **no son leyes**: son
  **contenedores institucionales** (FMI, OMS, OTAN, FAO, EUROJUST, UE, TEDH/TJUE), de 1 artículo, sin articulado que
  comparar. El patrón correcto YA existe: sus equivalentes de abril (`Consejo DDHH ONU`, `Principios Fuerza ONU`,
  `OTAN GC`, `EUROJUST GC`) están marcados **`is_virtual=true`** — justo la exención que el clasificador contempla
  ("editoriales por diseño, no tienen fuente oficial de N artículos"). Los de hoy están `is_virtual=false` → caen en
  `no_source` y engordan el bucket **sin ser un defecto real**.
  → **Arreglo estructural correcto: que el build marque `is_virtual=true` al crear un contenedor institucional**, en vez
  de "registrar una fuente" que no existe. **NO aplicado**: esas 7 son trabajo EN VUELO de otra sesión (builds de hoy) y
  tocarlas a media construcción sería pisarla.
- **MEJORA ESTRUCTURAL pendiente (la de verdad):** que el **build de oposición registre/verifique la fuente** de sus leyes regionales (o al menos no las marque `actualizada` sin summary) → cortar la reincidencia en origen, en vez de drenar a posteriori. Alternativa mínima: endurecer el gate a `never_verified`/`no_source` (con grandfathering) para que un tema nuevo no se publique con leyes sin fuente.
- **Cómo/diseño:** `docs/roadmap/verificacion-completitud-leyes.md` + memoria `project-completitud-leyes-vs-fuente`. Regla dura: NUNCA `verification_status='actualizada'` sin escribir `last_verification_summary` con evidencia real.

- **📌 SESIÓN 31/07/2026 — re-medición, un cierre grande y el diseño de vigilancia acordado con Manuel.**
  - **Estado hoy:** 101 actionable (52 `no_source`, 27 `never_verified`, 21 `false_green`, 1 `incomplete`), **65 sirviendo en temas vivos**. El 20/07 eran 64: **no baja**, drena y entra a la vez.
  - **✅ TUE CERRADO — el de más impacto de todo el catálogo: 807 preguntas, 56 artículos, 39 oposiciones.** Estaba `pendiente · SIN FUENTE` teniendo `boe_id='DOUE-Z-2010-70002'`. Verificados **55/55 artículos contra el espejo del BOE del DOUE**, 0 divergencias, y registrada la evidencia con `aplicar-evidencia-completitud.cjs`.
    - **GOTCHA de método (costó un falso negativo TOTAL):** ese documento contiene **TUE + TFUE + protocolos**, así que trocear por «Artículo N» sin acotar antes el bloque del TUE hace que los números se pisen con los del TFUE → **0% de coincidencia en los 55**, que parece «la ley entera está mal» y es solo un bug del comparador. Regla: **antes de creerse un 0% masivo, sospechar del método, no del banco.**
  - **El cuello de botella NO es comparar ni vigilar, es SABER DE DÓNDE se descarga.** `scripts/verify-law-source.cjs` (capa 3) ya hace todo lo demás —descarga, detecta PDF por magic-bytes, `pdftotext`, extrae inventario de artículos, compara con BD y escribe evidencia en `law_source_verification` con su URL y su hash, sin falsear cuando no parsea— pero **arranca con `if (!law.boe_url) return`**. Por eso esa tabla tiene **21 filas para 876 leyes reales**: solo pudo correr donde ya había fuente registrada. El trabajo pendiente es **research por-ley**, y es manual por naturaleza (boletines autonómicos, sedes universitarias, tratados).
  - **160 leyes reales sirviendo 4.893 preguntas están FUERA del cron de vigilancia**, por tres exclusiones deliberadas de `boe-changes.service.ts`: sin `boe_url`, URL `doc.php` (documento puntual sin consolidar, el extractor siempre falla) y `scope='eu'`. **El TUE, recién verificado, es de los excluidos** (su URL es `doc.php`): tiene línea base y nadie la vigila.
  - **🎯 DISEÑO ACORDADO CON MANUEL (31/07) para esas 160 — clonar + hash + aviso, SIN LLM:**
    1. **Clonar el documento** (PDF o boletín) y **guardar la URL de descarga**, que es dato de primera: sin ella no se puede re-comprobar ni re-clonar. `law_source_verification` **ya tiene `source_url` + `verified_source_hash`**, e historial append-only en `law_source_verification_history`. **No hace falta tabla nueva.**
    2. **Vigilar por hash**, que es determinista, gratis y no alucina. El patrón vivo es `generic_source_checks` (`last_hash`, `last_checked_at`, `last_changed_at`, `fetcher_type` http/**headless** para los boletines con WAF), corriendo a diario.
    3. **Si el hash cambia → alerta**, y la revisa Claude a petición de Manuel («revisa los cambios»), igual que las señales OEP o la salud de contenido.
    - **Manuel descartó expresamente usar un LLM para resumir el cambio**, y es la decisión correcta: comparar dos textos es exactamente lo que hace un hash. El precedente está medido — el LLM que pre-masticaba documentos de convocatoria produjo 6.886 extracciones con **cero** triadas (~17 USD) y se eliminó, además de dejar el cron a merced del saldo del proveedor (10 h sin servicio el 26/07).
  - **Los contenedores institucionales siguen contaminando el bucket** (OMS, OTAN, «Unión Europea (instituciones)»: 1 artículo, 238 preguntas entre los tres, `actualizada` sin fuente). Su propio texto dice *«Contenido institucional (preguntas de hecho, no de articulado)»* → **`is_virtual=true`**, que es la exención que el clasificador ya contempla. Es el arreglo estructural que esta ficha identificó el 20/07 y sigue sin aplicarse.
  - **Ojo al leer cifras de este bucket (falso pánico de esta sesión):** medido sobre TODAS las leyes salen «482 sin fuente», pero la mayoría son **contenedores editoriales** (`is_virtual`: Inglés PN, Biblioteconomía, temario de enfermería, Word 365) que no tienen BOE que consultar y que el clasificador **ya exime**. Filtrando `is_virtual=false` quedan **165**, y de esas las accionables son 65. **Siempre filtrar por `is_virtual` antes de dar un número.**

- **📌 SESIÓN 07/08/2026 (w2) — re-medición (flat) + un caso `false_green` investigado a fondo, sin resolver del todo (research per-ley, tal como esta ficha ya avisa que es).**
  - **Estado hoy, vía `DATABASE_URL="$VENCE_LECTOR_URL" node scripts/audit-law-completeness.cjs`:** **101 actionable** (52 `no_source`, 27 `never_verified`, 21 `false_green`, 1 `incomplete`), **65 sirviendo en temas vivos** — **idéntico byte a byte** al snapshot del 31/07. Ni ha drenado ni ha reincidido en una semana: el bucket está genuinamente estable, no es que nadie lo mire.
  - **`scripts/verify-law-source.cjs --dry` probado contra los 11 `no_source`/`never_verified` de más impacto** (los que SÍ tienen `boe_url`, ordenados por preguntas activas: Estatutos UGR, Bases Ejecución Presupuesto UAL, Normas Presupuesto UC3M, Regl. Admón. Electrónica UAL, Regl. Provisión Puestos PTGAS UAL, Normativa Permanencia UAL, Decreto 69/2012 JCCM, Reglamento 852/2004, Decreto 302/2011 CARM, Estrategia Salud Comunitaria Ext, Plan Seguridad Pacientes SES Ext): **los 11 dan `unparseable`.** Confirma lo que esta ficha ya dice — SPAs universitarias, Drive que devuelve 500 sin navegador, PDFs con maquetación que el regex genérico `Artículo\s+(\d+)` no reconoce. No es un fallo del extractor: es research manual por naturaleza.
  - **Caso trabajado a fondo: `Estatutos UGR` (`3d1fce62-f6dd-422d-be0f-8e2a1b947ec5`, `false_green`, 62 preguntas activas, único tema `escala_administrativa_universidad_de_granada` T102 "Estatutos de la Universidad de Granada").**
    - **CONFIRMADO: el `boe_url` guardado no es una fuente verificable.** Apunta a `secretariageneral.ugr.es/informacion/servicios/bougr/326` — el ÍNDICE del boletín interno nº326, no el documento. Descargado y leído el HTML completo: su único contenido relacionado es una entrada de una línea, *«ACU326/1: Aprobación del Proyecto de Estatutos de la Universidad de Granada»*, sin ningún enlace a PDF/texto adjunto (los 3 PDFs que sí enlaza esa página son dos ediciones previas del boletín y una convocatoria de ayudas lingüísticas — nada de Estatutos). Por eso el extractor da `unparseable`: no hay texto de artículos que extraer en esa URL, ni lo habrá nunca.
    - **CONFIRMADO: el epígrafe del propio T102 pide EXPRESAMENTE el "Proyecto de Estatutos… aprobado por el Claustro (BOUGR núm. 326, de 27 de mayo de 2026)"** — o sea que NO es un despiste de import: el temario oficial de esta oposición quiere el **borrador de la reforma 2026**, no los Estatutos hoy vigentes. Comprobado también que existen unos Estatutos DISTINTOS y actualmente en vigor en `ugr.es/universidad/normativa/estatutos` (preliminar + títulos I-VIII, 9 páginas), con **237 artículos** correlativos 1-237 sin huecos — pero esa NO es la fuente que el epígrafe pide, así que **no sirve de comparación** ni se debe usar para "corregir" el scope. La BD tiene hoy **280 artículos** correlativos 1-280 para esta ley: **no coincide con los 237 del texto vigente, pero eso es exactamente lo esperable si de verdad viene del Proyecto de reforma** (una reforma estatutaria típicamente añade artículos).
    - ~~**NO CONFIRMADO — y aquí me paro, no invento**: no localicé el texto completo del "Proyecto de Estatutos" 2026...~~ **CORREGIDO el 08/08 (ver bloque de abajo): el documento SÍ estaba localizable — un enlace directo en el HTML crudo de la misma página que se dice haber mirado.** Se dejó escrito como sospecha abierta por no leer el HTML sin resumir; queda documentado el error de método, no se borra.
    - **Qué NO hacer con este hallazgo (sigue vigente):** no cambiar el `boe_url` al texto "vigente" de 237 artículos — sería sustituir la fuente que el epígrafe pide por otra que no pide, y dejaría el scope objetivamente mal aunque el detector se calle. El arreglo correcto es el del bloque de abajo: el PDF real del Proyecto BOUGR 326.
  - **No aplicado nada en BD** (rol `trabajador`, sin `DATABASE_URL` de escritura de negocio) — ni siquiera el `--dry` de `verify-law-source.cjs` escribe.
  - **Colisión de ficheros con otras 3 sesiones vivas** (`w3`, `movil`, `w1`) sobre `lib/admin/toolRegistry.ts`, `lib/admin/runbookRegistry.ts`, `scripts/laws/marcar-contenedor-institucional.cjs` y `scripts/laws/vigilar-fuentes-legales.cjs` — parecen estar en el lado ESTRUCTURAL (el arreglo de `is_virtual` para contenedores institucionales, y/o la vigilancia por hash del diseño del 31/07). Por eso esta sesión fue por el lado de **research per-ley** en vez de tocar esos ficheros, para no pisar trabajo en vuelo.

- **📌 SESIÓN 08/08/2026 (w2, review-fix de la revisión de w4) — Estatutos UGR: el documento SÍ existe (confirmado independientemente), PERO no cerrar el `false_green` todavía: tiene un hueco de contenido real en BD que hay que arreglar primero.**
  - **Re-verifiqué la re-medición flat, YO MISMO, sin fiarme del número anterior ni del de la revisión:** `DATABASE_URL="$VENCE_LECTOR_URL" node scripts/audit-law-completeness.cjs` da **110 actionable / 71 en temas vivos (52 no_source, 36 never_verified, 21 false_green, 1 incomplete)** — coincide EXACTO con lo que midió la revisión, no con el 101/65 de la entrega anterior. Confirma la lectura de la revisión: el bucket se movió por trabajo concurrente entre el 08:58 UTC de la medición anterior y ahora, no es un error de quien midió entonces.
  - **Reproducido el hallazgo serio de la revisión, de forma independiente (no me fié de su palabra, repetí el trabajo desde cero):**
    1. `curl` crudo (no WebFetch) a `secretariageneral.ugr.es/informacion/servicios/bougr/326` → HTTP 200, y en el HTML sin resumir aparece el enlace directo `<a href=".../BOUGR/326/Boletín Oficial de la Universidad de Granada nº 325_2.pdf">Aprobación del Proyecto de Estatutos de la Universidad de Granada.</a>` — el mismo que encontró la revisión.
    2. Descargado ese PDF: HTTP 200, 1.739.875 bytes (1,74 MB, coincide con la revisión). Extraído con `pdfjs-dist` (mismo método usado en otras tareas de esta sesión, porque esta máquina no tiene `pdftotext` instalado): **130 páginas**, título *"PROYECTO DE ESTATUTOS DE LA UNIVERSIDAD DE GRANADA — CLAUSTRO UNIVERSITARIO — SESIÓN EXTRAORDINARIA 25 DE MAYO DE 2026"*, texto real (no escaneado).
    3. **Artículo 1 del PDF = artículo 1 de BD, palabra por palabra** (comprobado por mí, no solo citado de la revisión). Máximo artículo del PDF = **280**, exacto a los 280 de BD.
  - **⚠️ PERO fui más allá de lo que comprobó la revisión (que solo miró el artículo 1 y dio el 280 por bueno "temáticamente") y encontré un defecto real que cambia la conclusión:**
    - **El artículo 280 de BD tiene un TROZO DE TEXTO REALMENTE FALTANTE**, no solo "encaja temáticamente". Comparación directa, punto 3 del artículo:
      - **PDF (fuente real):** *"...se procederá a su publicación tanto en el **Boletín Oficial de la Universidad de Granada, como en el Boletín Oficial** de la Junta de Andalucía y en el Boletín Oficial del Estado."*
      - **BD (lo que se sirve hoy):** *"...se procederá a su publicación tanto en el de la Junta de Andalucía y en el Boletín Oficial del Estado."*
      - Falta el tramo en negrita — BD dice literalmente **"tanto en el de la Junta de Andalucía"**, una frase gramaticalmente rota (le falta el sustantivo) que además omite que la reforma también se publica en el propio boletín de la UGR. Confirmado con `pdfjs-dist` sobre el PDF real, acotando el artículo 280 hasta el encabezado "DISPOSICIONES ADICIONALES" que le sigue (para no arrastrar contenido de después).
    - **Intenté un diff automático de los 280 artículos para saber si el hueco es un caso aislado o un patrón — y el intento merece constar como lección, no solo el resultado:** una primera pasada con expresión regular ingenua dio 27 "mismatches" + 12 "no encontrados"; investigando por qué, resultó ser un **bug de mi propio script**: el PDF inserta espacios de kerning DENTRO de la palabra "Artículo" (p.ej. *"Artí culo 31."*), lo que rompía el corte de límites y hacía que el contenido de un artículo se comiera el siguiente. Corregido el regex (280/280 límites encontrados, 0 duplicados), la comparación por CONJUNTO de palabras (no por orden, para no confundir con el defecto ya conocido de "tablas aplanadas") seguía dando ~23 "mismatches" — pero al inspeccionar varios A MANO (arts. 2, 9, 27, 124, 125) resultaron ser **falsos positivos del propio comparador**: mi trozo de PDF incluye el TÍTULO del artículo (que en BD vive en una columna aparte, no en `content`) y a veces cabecera de la sección siguiente, y eso basta para hundir la métrica de similitud sin que haya ningún hueco real de contenido.
    - **Dado que mi comparador automático no es fiable a esta escala sin más ingeniería, no afirmo "hay 23 defectos más" — sería una sospecha vestida de certeza.** En su lugar hice **inspección manual de 9 artículos adicionales repartidos por todo el rango** (2, 9, 24, 27, 50, 100, 124, 125, 150, 155, 200, 219, 250 — trece en total contando los del comparador) leyendo el texto crudo del PDF junto al de BD: **todos coinciden**, salvo el 280. No es una cobertura exhaustiva de los 280, pero sí una muestra amplia y repartida sin sesgo hacia ningún tramo del documento.
  - **Conclusión — CORRIJO la recomendación de la revisión, con la prueba encima:** la revisión decía que esto "probablemente se podía haber resuelto (mover de `false_green` a verificado)" en la misma sesión. **No es correcto hacerlo todavía**: registrar evidencia de verificación ahora, con el artículo 280 roto, sería crear un `false_green` NUEVO — justo el defecto que `trg_laws_block_false_green` existe para impedir. El orden correcto es: (1) alguien con `DATABASE_URL` de escritura de negocio corrige el contenido del artículo 280 (texto exacto de arriba, verbatim del PDF), (2) SOLO ENTONCES se registra la evidencia con `aplicar-evidencia-completitud.cjs` y se cierra el `false_green` (62 preguntas de impacto). Yo no puedo hacer (1): rol `trabajador`, sin escritura de negocio.
  - **Sobre el punto 2 de la revisión (¿el mismo patrón de "no encontrado" en los otros 10 casos merece una segunda pasada con HTML crudo?):** no re-verifiqué los 10 uno a uno (igual que la propia revisión no lo hizo — está fuera de alcance razonable repetir dos veces el mismo trabajo sin nueva información). Pero la LECCIÓN de método sí se puede escribir ya, sin necesidad de repetir el research: **un fetch que resume (tipo WebFetch) puede pasar por alto UN enlace concreto entre varios en una página de listado** — aquí había 3 enlaces y el bueno no destacaba por nada especial en el texto plano. La forma robusta de comprobar "¿de verdad no hay documento?" es `curl` + grep del HTML crudo por `<a href`, no un resumen. Vale la pena que quien retome los otros 10 casos lo haga así antes de darlos por perdidos.
  - **Ni siquiera el `--dry` de `verify-law-source.cjs`, ni ningún `aplicar-evidencia-*`, se ha ejecutado con `--apply`/escritura** (rol `trabajador`, sin `DATABASE_URL` de negocio).

