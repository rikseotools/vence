### [T-191] ✅ [CERRADA 27/07] Dos cifras de plazas siguen publicadas sin ningún documento que las pruebe (Aragón 139, Ayto. Madrid 111)
- **Qué:** el detector `plazas_afirmadas_sin_documento` (estrenado el 27/07, badge de Salud del contenido, severidad `error`) deja 2 casos vivos de los 4 que encontró. Las landings afirman una cifra de plazas como HECHO y **ningún documento de su corpus la contiene**, ni en dígitos ni en letra.
- **Runbook:** `docs/runbooks/provenance-convocatorias.md` §6 (orden de resolución + la válvula firmada `cifra_derivada`). Comando: `npm run audit:convocatorias`.
- **Los 2 casos, con lo ya medido (no repetir el trabajo):**
  - **`administrativo-aragon` — 139 plazas (ciclo 2025).** Tiene 9 documentos, entre ellos la convocatoria del BOA (40 KB, `boa.aragon.es/…MLKOB=1427868650404`). Ese texto **no contiene ni una sola expresión «N plazas»** — se buscó con regex sobre el corpus entero. O sea: el documento que cuantifica las plazas NO está clonado, y el clonado no sirve de prueba. Su `boe_reference` cita «Resolución de 19/12/2025, Dir. Gral. Función Pública (BOA 247, 23/12/2025) — proceso 25/0102»: **empezar por ahí**, es el documento que debería probarlo. Ojo: `plazas_otros_turnos` ya trae 3 turnos reservados con cita y documento (`BOA-247-2025`), así que ese boletín SÍ se leyó en su día — probablemente basta con clonarlo entero.
  - **`auxiliar-administrativo-ayuntamiento-madrid` — 111 plazas (ciclo 2025, `oep_aprobada`).** Sus 2 documentos son **de 2024 y de 2022**: ninguno puede probar una OEP de 2025. El documento correcto está identificado en su propio `boe_reference`: **Acuerdo de 27/11/2025 de la Junta de Gobierno, BOAM nº 10.017 de 01/12/2025** (OEP 2025 del Ayuntamiento, 1.275 plazas en total).
- **⚠️ Dos vías ya intentadas que NO funcionan (no repetirlas a ciegas):**
  1. **`sede.madrid.es` da 403 a curl/WebFetch** (WAF). Con `--headless` (Lambda) SÍ responde… pero devuelve **el menú del portal**: 11 KB de «Acceso al módulo», «Lo más visto», sin el documento. Se clonó por error y **se borró** (`72b275b1`). Hay que llegar al PDF del BOAM en sí, no a la ficha del trámite.
  2. **El espejo en el BOCM tampoco vale tal cual:** `bocm.es/…/2025/12/10/BOCM-20251210-49.PDF` **es** la OEP 2025 de Madrid y trae el anexo con el desglose por categorías, pero el PDF lleva la **fuente rota** y `pdftotext` devuelve mojibake (`EKD/E/MEd'KZ1` = «DENOMINACIÓN CATEGORÍA»). El desglose está ahí, pero hay que decodificarlo (CMap del PDF) o conseguir otra copia. Con `-layout` se ven las tablas, ilegibles.
- **Cómo cerrarlo:** clonar el documento que prueba cada cifra (`backend/scripts/clonar-documento.ts`) y **verificar que el hallazgo desaparece** con `npm run audit:convocatorias` — no dar por bueno el ✅ del clonador sin mirar (ver T-189). Si la cifra resulta no estar sostenida por ningún boletín, **NO inventarla**: `plazas_prevision=true` con motivo, como se hizo con Alcalá de Henares el 27/07.
- **Origen:** sesión del 27/07 que llevó `audit:convocatorias` al badge. De los 4 hallazgos: `policia-municipal-madrid` resuelto de raíz (re-clonado, el BOE prueba el 561 en su ficha de análisis) y `alcala-henares` cerrado como previsión (su OEP oficial más reciente es la de 2024 con 11 plazas, ya convocadas; no existe OEP 2025/2026 publicada, y el 29 no lo sostiene ningún boletín).
- **✅ CERRADA 27/07 — los dos casos resueltos, cada uno por un motivo distinto:**
  - **Ayto. Madrid (111): la cifra era CORRECTA y está impresa.** Anexo I del BOCM-20251210-49, fila «Auxiliar Administrativo/a»: cupo general **100** + reserva discapacidad **11** = **111** (y el total de la sección cuadra, 158+18+0=176). **La vía que faltaba probar era renderizar la página y leerla** (`pdftoppm`), después de que `pdftotext`, `-layout` y `gs -sDEVICE=txtwrite` devolvieran mojibake por el CMap roto. Documento clonado (`oep_decreto`) + verificación firmada con `cifra_derivada`.
  - **⚠️ Y el propio sistema me cazó**: el detector `cita_no_prueba_nada` saltó porque firmé sin `source_snippet`. Relleno con una descripción en prosa de la tabla, **validada antes de escribirla contra el mismo criterio que usa el detector** (≥5 palabras en minúscula). Buena señal: la válvula tiene su propio guardarraíl y funciona.
  - **Aragón (139): la cifra estaba MAL — el BOA convoca 144.** `250102 Escala General Administrativa. Administrativos **144** (3 reservadas a víctimas de violencia de género, 1 reservada a víctimas de terrorismo y 1 reservada a personas transexuales)`. El 139 salía de restar esas 5 reservas: **una resta que no aparece escrita**, el patrón del 2.163 de Policía Nacional. Las reservadas son plazas del turno libre CON reserva, no descontadas — y así se cuentan en Madrid (100+11=111). **La landing anunciaba 5 plazas MENOS de las convocadas.** Corregido a 144 en las dos filas del dual-write, con guarda (abortar si ya no estaba en 139) y traza `plazas_corregidas_contra_boletin`.
  - **Verificado**: `audit:convocatorias` deja `plazas_afirmadas_sin_documento` y `cita_no_prueba_nada` a **cero**. Aprendizajes bajados al runbook `provenance-convocatorias.md`.
  - **🕳️ HUECO QUE DEJA AL DESCUBIERTO (no cerrado):** de las cuatro salidas del §6 del runbook, «**corregir la cifra contra el boletín**» —la única que cambia un dato público— **es la que NO tiene herramienta**. La guarda, el dual-write transaccional y la traza los puse a mano; la próxima sesión puede no ponerlos. Lo suyo es un escritor registrado al estilo de `repuntar-url.cjs`: dry-run por defecto, cita y URL obligatorias y, como guarda clave, que la cifra nueva pase el MISMO `cifraEnTexto` que usa el detector — así es imposible escribir una cifra que el detector no daría por probada.
