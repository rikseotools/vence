### [T-316] ✅ [HECHA 30/07] Las 6 explicaciones `safe` con la letra clavada de verdad que T-262 dejó sin dueño, y la vigilancia que faltaba
- **De dónde sale:** al cerrar [T-306] el barrido de drift reportaba **14 narrativas** con letra clavada. [T-262] se había cerrado el 29/07 diciendo *«1.211 → 6 falsos positivos»*, así que el residuo estaba dado por bueno y nadie lo miraba. Leídas una a una: **8 eran falsos positivos** (letras del articulado, grupos del EBEP, referencias de celda de Excel) y **6 eran REALES** — `intro` diciendo literalmente «La respuesta correcta es **C) De "habeas corpus".**», «**B) son opcionales.**», «**A) Sí, menú Formato**». Son la familia que el script de reparación manda a criterio humano (nombran la opción entera, y podar de más mutila lo que el opositor lee), y ahí se habían quedado.
- **✅ Las 6 reparadas y REVISADAS con el manual (v2.1), no solo editadas:** se podó de la narrativa el anuncio de la clave —la letra la pone el render, que sí sigue al barajado— conservando el razonamiento, con `aplicar-explicacion.ts` (dry-run y revisión del render antes de escribir). En `641979e0` (examen OFICIAL, enunciado intacto) hubo que reformular «En su letra b) exige…» como «El artículo 1.b) … exige…»: decía lo mismo y el guard la rechazaba.
- **Y salieron DOS defectos que nadie buscaba, que es el argumento de leerlas en vez de barrerlas:**
  - `6d59e0ae`: la **opción CORRECTA** decía «fuera conveniente **suscitarla**» donde el art. 95.4 de la Ley 39/2015 dice «**sustanciarla**». Verificado con `curl` contra el BOE (no contra nuestra copia) y corregido; el trigger la marcó `stale` al cambiar el texto y se re-selló con `record_shuffle_safety`, como debe ser.
  - `5ac458b1`: al enunciado le faltaba una tilde («La ley *regulara*»). Lo cazó la auditoría ciega.
- **Revisión trazada, con las dos pasadas del manual:** verificación + **auditoría ciega independiente**, ambas Sonnet, sobre las preguntas VIVAS después de aplicar (paso 7). **Coinciden en las 6 y en los cuatro ejes** (`article_ok`/`answer_ok`/`options_ok`/`explanation_ok`) → nada que adjudicar. Registradas 12 filas en `ai_verification_results` con `review_method_version='v2.1'` (`claude_code_recheck_t316` + `claude_code_audit`); la etiqueta de campaña evita pisar las filas de junio, porque la tabla tiene `UNIQUE (question_id, ai_provider)`. Las claves se verificaron contra el BOE por `curl` en las tres legales; las dos de LibreOffice quedan con confianza `medium` declarada (conocimiento de producto, sin fuente citable).
- **Un aviso de método que se pagó aquí:** el dump que se les pasó a los agentes traía el render roto («Por qué **(vacío)** es correcta») por llamar a `renderStructuredExplanation` con la firma antigua. **Las dos pasadas lo detectaron por su cuenta, ejecutaron el render de verdad y avisaron de que era artefacto del extractor, no defecto de producción.** Es exactamente para eso que la revisión la hace un agente independiente y no el que aplicó el cambio.
- **La vigilancia que faltaba (el cabo de fondo):** nada avisaba de que una mejora del detector deja veredictos viejos. Ahora `sweep-shuffle-safety-drift.ts` cuenta los que el detector de HOY contradice y los emite como **`shuffle_veredicto_criterio_viejo`** (`info`: no es defecto de la pregunta, es trabajo de una línea), con su entrada en `runbookRegistry`, su frase-gatillo (c) en CLAUDE.md y el comando `npm run shuffle:recriterio`. Así la próxima calibración no depende de que alguien se acuerde — que es lo que falló dos veces seguidas.
- **Estado final medido:** barrido de drift **en verde entero** (0 regresiones · 0 narrativa · 0 hash desincronizado · 0 criterio viejo) y las 6 en `approved`/`safe`.

