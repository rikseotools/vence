### [T-173] ✅ [CERRADA 27/07 — arreglada, desplegada y verificada en vivo] `traffic_drop` llevaba >24 h caída: si el tráfico se desplomaba, NADIE avisaba
- **Qué:** la regla que vigila caídas de tráfico **no llega a evaluarse**: su query supera el `statement_timeout: 20000` del pool de la réplica. Medido el 27/07: **255 fallos en 24 h** (la nº1 del motor), y sigue cayéndose después de arreglar las de cron (6 fallos en 40 min ya con el índice puesto). `materialized_stats_stale` está igual (110 fallos/24 h).
- **Por qué importa más que las otras:** una regla de cron caída retrasa una detección interna. Esta es **la red que avisa de que la web ha perdido tráfico** — el tipo de incidente que si no se caza en horas, se caza en la factura del mes.
- **Por qué el índice de T-162 NO la arregla (medido, no supuesto):** aquel es PARCIAL sobre los eventos de cron (605k filas). Esta regla consulta `event_type='request_completed'`: **4.085.645 filas en 30 días**, 7× más y el 38% de la tabla. Un índice cubridor equivalente sería enorme y habría que mantener el mapa de visibilidad de esa porción entera.
- **Cómo (la salida NO es un índice):** la regla solo necesita **contar por hora** (compara la última hora cerrada con la misma hora-del-día y día-de-semana de los últimos 28 días). Eso es un caso de libro de **tabla de rollup horaria** — y ya está contemplado como paso 1 de la escalera del runbook `contencion-rds-paneles-admin.md` («si se necesita histórico >14 d → rollup diario, tabla resumen chica»). Con el rollup la query pasa a ser trivial y de paso sirve a cualquier otra regla que cuente tráfico.
- **Antes de tocar nada:** reproducir el timeout contra la RÉPLICA (no contra el primario — la app corre en `DRIZZLE_READ`; medir en el primario fue el error que me costó media tarde el 27/07) y mirar el plan. Y recordar el **GOTCHA del mapa de visibilidad**: en esta tabla de inserción continua, un index-only scan sin `VACUUM` previo va al heap igual y puede salir PEOR que el barrido (ver `20260727_observable_events_cron_covering_idx.sql`).
- **Ya no es invisible:** desde el 27/07 el motor emite `alert_rule_failed` a `observable_events` y la regla `alert_rule_failing` avisa con ≥3 fallos de la misma regla en 1 h. Consulta: `SELECT metadata->>'rule', count(*), max(error_message) FROM observable_events WHERE event_type='alert_rule_failed' AND ts > NOW() - INTERVAL '24 hours' GROUP BY 1 ORDER BY 2 DESC;`
- **Origen:** sesión del 27/07 (T-162). Se arreglaron las reglas de cron y quedaron estas dos con causa idéntica pero solución distinta.
- **✅ RESUELTO (27/07):** reescrita la query, desplegada (`vence-backend:114`, SHA `a0973872`) y **verificada en producción: 0 reglas fallando** en los 10 min posteriores (2 ticks del motor). Con ella se curaron también `materialized_stats_stale` y las demás que arrastraban el mismo timeout.
  - **60.718 ms → 64 ms** (mediana de 5; peor caso en frío 1.636 ms) = 12× de margen sobre el presupuesto de 20 s del pool.
  - **La causa no era el volumen, era la FORMA:** para 4 franjas horarias pedía 29 días de `request_completed` (4.085.645 filas) y se quedaba con las que casaban por `EXTRACT(HOUR/DOW)` — funciones no indexables → barrido completo. Ahora se piden las 4 ventanas **por rango sobre `ts`**: hace 7/14/21/28 días son *por definición* el mismo día de la semana y la misma hora, así que la semántica es idéntica y usa `idx_observable_events_ts_desc`.
  - **Equivalencia comprobada con datos reales**, no razonada: ambas versiones devuelven las mismas 4 franjas con los mismos conteos (1638, 1199, 1132, 953).
  - **NO hizo falta la tabla de rollup** que esta misma ficha daba por salida probable, ni un índice como el de las reglas de cron. Medir antes de construir ahorró una migración entera. **Lección para la próxima:** dos reglas con el MISMO síntoma (timeout sobre `observable_events`) tenían causas distintas y arreglos distintos — no generalizar el remedio del primero.
  - De paso cae el *floor* de régimen del sampling, que su propio comentario pedía eliminar pasado finales de junio.
  - 8 tests que fijan la FORMA de la query (que no vuelva `EXTRACT`, que estén las 4 ventanas, que sigan exclusiones y umbrales), **validados por mutación** y juzgando el SQL ejecutable y no los comentarios. 289/289.
  - **GOTCHA que me costó rehacer el trabajo:** los acentos graves dentro del template literal de SQL lo cierran y rompen el fichero (mismo patrón que el `$` en el script de deploy). Y NUNCA usar `git checkout <fichero>` para revertir una mutación de prueba si el fichero tiene trabajo sin commitear: se lleva la reescritura entera. Usar copia de seguridad.


