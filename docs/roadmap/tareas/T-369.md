### [T-369] ✅ [HECHA 01/08] «Desactivar TODOS los emails» apaga también las respuestas a lo que el usuario nos escribe

- **El defecto en una línea:** el botón rojo de `/unsubscribe` pone `email_soporte_disabled = true` (`lib/emails/emailService.server.ts:252`, rama *«Nuclear: disable everything»*), y esa columna es la que gobierna la **entrega de nuestras respuestas** a impugnaciones y feedback. Quien se da de baja de la publicidad deja de recibir, sin saberlo, la contestación a lo que él mismo ha preguntado.
- **Su propio texto promete lo contrario.** Bajo el botón se lee *«No recibirás ningún email **automático** de Vence.es»*. Una respuesta a tu impugnación no es un email automático: es la contestación a algo que tú escribiste. La palabra «automático» es exactamente la promesa que se incumple.
- **Y el código ya declaraba la intención correcta**, en `lib/api/emails/queries.ts`: *«Transactional: only blocked by soporte toggle, NOT by unsubscribed_all»*. Es decir, el diseño SÍ quería proteger las transaccionales de la baja masiva — y el unsubscribe masivo lo sortea apagando el interruptor directamente en vez de fijar `unsubscribed_all`. El guardarraíl estaba puesto y se entra por la ventana.
- **Nadie eligió esto (el número que decide la ficha):** de los **80** usuarios con el soporte apagado, **79 llegaron por el botón rojo** y **1 solo** marcó la categoría «Soporte y transaccional» a propósito. Cuando el 99% de los que están en un estado no lo eligieron, no es una preferencia: es un efecto colateral.
- **Lo que ya ha costado, medido el 31/07:** **29 respuestas a impugnaciones** y **10 a feedbacks** escritas y no entregadas por email. Concentradas en 3 personas: 22 a `marta_benitopadilla@hotmail.com` (la última, del 30/07 — **sigue impugnando activamente y lleva desde mayo sin recibir una sola respuesta por correo**), 6 a `luciacn1984@gmail.com` y 1 a `mbelen177@gmail.com`. Les llegó la campana; el correo, no.
- **HECHO (31/07) — casilla explícita, no apagado automático.** Decisión de Manuel sobre la propuesta inicial: el botón rojo **sigue pudiendo cortarlo todo**, pero cortar las respuestas pasa a ser un acto marcado. Se descartó a propósito la variante de «que el botón no pueda tocar el soporte y quien quiera use la categoría»: el usuario no pierde poder, solo deja de perder algo que no pidió.
  - `processUnsubscribeByToken` acepta un 5.º argumento `includeSoporte` (**por defecto `false`**). La rama nuclear solo escribe `email_soporte_disabled` si viene marcado. Un caller que no lo mande —cliente viejo, enlace cacheado— cae del lado que no hace daño.
  - `/api/unsubscribe` lo pasa (`includeSoporte === true`, nada de *truthy*), y el modal de `/unsubscribe` tiene la casilla **desmarcada** con su explicación (*«tendrás que entrar en la web para leer la respuesta»*). Se olvida al cancelar. Los textos de fuera ya no prometen lo que no hacen.
  - **Capas:** 4 tests nuevos en `__tests__/emails/unsubscribeFlow.test.ts` (incluida la firma vieja) + **simulación contra la BD REAL** `npm run sim:baja-emails` (17 comprobaciones, usuarios efímeros que se limpian solos). La simulación es la que importa: los unit tests mockean Drizzle y el UPDATE se construye con columnas dinámicas. Comprueba lo que el usuario recibiría de verdad vía `canSendEmail` — respuesta a impugnación, soporte y **aviso de renovación** siguen llegando; marketing y newsletter, no.
  - **`recordatorio_renovacion` también es categoría `soporte`** (ver `EMAIL_TYPE_TO_CATEGORY`): el botón rojo estaba dejando además sin aviso de cobro a quien paga. No estaba en el diagnóstico inicial.
  - **El camino de la newsletter NO se ha tocado** (comprobado a petición de Manuel): su enlace lleva al mismo `/unsubscribe?token=…` y sigue yendo por la rama de categorías. `/api/email-unsubscribe` es un redirect legacy y no toca preferencias.
- **~~PENDIENTE~~ → ✅ RESUELTO (01/08).** Los 79 ya se restauraron en [T-373]. Y sobre re-entregar lo perdido: **decisión de Manuel — NO se le escribe a nadie.** Las respuestas están en la campana y en `/soporte`, la persona sigue impugnando con normalidad, y lo que estaba roto —que las PRÓXIMAS no llegaran— ya está arreglado. **Esto no se vuelve a plantear**: mandar 20 correos antiguos de golpe se lee como spam y desentierra un fallo que la usuaria no ha notado.
- **DESCARTADA — la señal de «respuesta escrita y no entregada».** `resolveDispute` y `respondFeedback` devuelven `emailSkipReason:'user_preferences'` y nadie lo mira; se llegó a escribir la señal + su regla de alerta y **se retiró por decisión de Manuel**: a partir de ahora quien esté en ese estado lo habrá elegido marcando la casilla, y la campana dentro de la app ya se lo comunica. Queda escrito aquí por si el criterio cambia — el trabajo era pequeño (un módulo, dos cableados y una regla).
- **✅ VERIFICADO EN PRODUCCIÓN (01/08, deploy `56aea323`).** El deploy que faltaba **ya había ocurrido sin que nadie lo pidiera**: el commit del arreglo (`83dc6a1e1`, en `main` desde el 31/07) entró vivo a las 08:47 UTC dentro del acumulado de otra sesión, porque los deploys son cumulativos. La ficha llevaba desde entonces en «esperando decisión de Manuel» por una condición que ya se había cumplido sola.
  - **El endpoint DESPLEGADO, en las dos direcciones** (`npm run sim:baja-emails-vivo`): baja masiva **sin** marcar la casilla → `email_soporte_disabled` se queda en `false` · **marcándola** → pasa a `true`. En los dos casos se comprueba además que la baja surtió efecto (`unsubscribed_all = true`), porque con un token inválido el servidor no escribiría nada y el primer caso saldría verde por el motivo equivocado.
  - **El modal, en el navegador contra producción:** la casilla sale **desmarcada**, con su explicación (*«tendrás que entrar en la web para leer la respuesta»*), y **sigue desmarcada tras cancelar y reabrir**.
  - **Estado de los datos:** quedan **3** usuarios con el soporte apagado (de los 80 iniciales; los 79 del botón rojo se restauraron en [T-373]) y **0 bajas masivas** desde que el arreglo está vivo — o sea, aún no ha pasado por el caso real, pero el camino está comprobado de extremo a extremo.
- **🕯️ CAPA NUEVA `npm run sim:baja-emails-vivo`, y no duplica a `sim:baja-emails`.** Aquélla llama a `processUnsubscribeByToken` **en proceso**: ejercita la lógica con el código de TU árbol, que es la capa correcta para lo que vigila (el UPDATE con columnas dinámicas, invisible para los tests porque mockean Drizzle). Ésta pregunta otra cosa: **si lo que hay VIVO se comporta así**. La distancia entre las dos no es teórica — esta misma tarea estuvo un día con el arreglo en verde en local y el defecto vivo en producción, y el mismo 01/08 [T-450] enseñó la versión cara: un arreglo desplegado, con tests y guardarraíl en verde, que en producción no hacía absolutamente nada. Usuarios efímeros que se limpian solos; seguro contra producción.
- **Origen:** apareció comprobando, a raíz de una pregunta de Manuel, si un usuario con los emails desactivados recibe la respuesta a su impugnación (31/07, tras cerrar `ab3b9e43`).


