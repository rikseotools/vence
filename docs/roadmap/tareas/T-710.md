### [T-710] 🟡 [ABIERTO 08/08] Construir Auxiliar de Archivos/Bibliotecas/Museos de Madrid (C1): mapear topic_scope de 50 temas + config + gates

- **Esfuerzo: sesión_propia.** Mapeo de scope tema→(ley, artículos) para 50 temas + config + activación — trabajo de contenido real, no medición.
- **De dónde sale:** separada de [T-393] a petición explícita de Manuel (embudo, pregunta `#100`, 08/08): *"construirla es trabajo de CONTENIDO real (mapear el `topic_scope` de los 50 temas, entrada de config, gates `audit:oposicion` y `verify:scope`), así que va a ficha propia con su esfuerzo declarado, no dentro de T-393"*. T-393 se queda como el diagnóstico (ya corregido); esta ficha es la construcción.
- **Decisión de producto YA TOMADA por Manuel (08/08), no pendiente:** regla textual del día — *"A1 y A2 no las vamos a hacer por el momento; las C1, C2, D etc. esas sí, todas"*. `auxiliar_archivos_bibliotecas_museos_madrid` es **grupo C, subgrupo C1** (confirmado por Manuel al corregir T-393) → cae exactamente en lo que SÍ se construye. **Se termina, no se retira.**
- **Estado real medido (08/08, vía `VENCE_LECTOR_URL` + confirmado por Manuel con su credencial):**
  - `oposiciones` tiene fila (`slug='auxiliar-archivos-bibliotecas-museos-madrid'`, nombre *"Auxiliar de Archivos, Bibliotecas y Museos de la Comunidad de Madrid"*, `is_active=false`, grupo C, subgrupo C1) — **existe**, contra lo que decía la ficha original de T-393 tras su remedición del 07/08.
  - **NO está publicada:** `is_active=false` y los 50 `topics` tienen `disponible=false` (`is_active=true` en el topic, pero no servible). No hay nadie aterrizando en un temario vacío hoy — la urgencia original de T-393 estaba sobrestimada.
  - `topic_scope`: **0 filas** (confirmado 08/08). Esto SÍ sigue siendo el hueco real: sin scope, ningún tema puede servir preguntas aunque se active.
  - Sin entrada en `lib/config/oposiciones.ts` y sin rutas `app/auxiliar-archivos-bibliotecas-museos-madrid/` (confirmado, grep + `find`) — el scaffold de frontend no existe.
  - 3 usuarios FREE la tienen como `target_oposicion` (dato de la ficha original de T-393, no re-verificado hoy — `user_profiles` fuera del alcance de `VENCE_LECTOR_URL` por diseño, PII).
- **Qué hacer, en orden (manual `docs/maintenance/crear-nueva-oposicion.md` + `verificar-epigrafes-scope.md`):**
  1. **Primero, lo que pidió Manuel como paso 1 de T-393:** comprobar qué ven hoy esos 3 usuarios con la oposición `is_active=false`. El mecanismo que podía borrarles la oposición en silencio ([T-569], el limpiador de `/perfil` que vaciaba `target_oposicion`) **está confirmado ya en `main`** (commit `b54cd6934`, mergeado vía `0444aee0b`) — pero confirmar el efecto EN VIVO para estos 3 usuarios concretos requiere una sesión con más alcance que un trabajador de lectura.
  2. Mapear el `topic_scope` de los 50 temas (tema→ley/artículos) según cada epígrafe — el temario ya está escrito con epígrafes (lo caro ya está hecho), falta el juicio del mapeo.
  3. Añadir la entrada de config en `lib/config/oposiciones.ts` + rutas + `OnboardingModal`/perfil/mapeo CCAA.
  4. Gates `audit:oposicion` + `audit:served` + `verify:scope` en verde antes de `is_active=true`.
- **⚠️ Cabo suelto encontrado de paso (no arreglado, fuera de alcance de esta ficha):** la ficha de [T-569] sigue con cabecera `🟠 [ABIERTO 05/08]` en el markdown pese a que su fix está confirmado en `main` desde el 07/08 — parece la misma clase de divergencia DB/markdown que [T-393] tenía (ver abajo), aunque no comprobé su estado en `backlog_tasks`.
- **Relacionadas:** [T-393] (el diagnóstico, ahora corregido), [T-643] (el patrón RLS que hizo parecer estas filas ausentes), [T-045] (mismo patrón, otra oposición), [T-569] (el fix que protege a los 3 usuarios mientras tanto).

> **🔍 RECONNAISSANCE (08/08, w1) — el plan de mapeo, con lo verificado y lo que falta. NO escribo
> `topic_scope` porque un trabajador solo tiene `VENCE_LECTOR_URL` (lectura); esto es la PLANIFICACIÓN
> que quien tenga escritura puede aplicar directamente, no una promesa de "está mapeado".**
>
> - **Los 50 temas se dividen en CUATRO grupos, por tipo de fuente — esto es lo primero que faltaba
>   decidir, y cambia el método de cada uno:**
>   1. **7 temas (1-7) generales/legales**, con equivalente cercano en `auxiliar_administrativo_madrid`
>      — reutilizables **CON AJUSTE**, no copia ciega. Comprobado comparando epígrafe contra epígrafe,
>      no solo el título:
>      - **Tema 2 (Estatuto de Autonomía) y Tema 3 (Gobierno y Administración CM): candidatos de
>        alta confianza**, epígrafes prácticamente calcados a `auxiliar_administrativo_madrid` T2/T3
>        (LO 3/1983 + Ley 6/1986 CM ILP + Ley 11/1986 CM + Reglamento Asamblea; Ley 1/1983 CM + Ley
>        1/1984 CM + Decreto 38/2023 CM respectivamente).
>      - **Tema 1 (CE) NO es copiable tal cual**: el epígrafe de ABM incluye explícitamente *"La
>        organización territorial del Estado. Las Comunidades Autónomas"* (Título VIII, arts.
>        137-158), que el T1 de `auxiliar_administrativo_madrid` (solo derechos/deberes/valores) NO
>        cubre — haría falta AMPLIAR el rango, no reutilizarlo.
>      - **Tema 4 (Ley 39/2015) tampoco es copiable tal cual**: el epígrafe de ABM menciona
>        explícitamente *"Los interesados"*, *"actos administrativos: requisitos y eficacia"*,
>        *"notificación"* y *"recursos administrativos"* — el scope de `auxiliar_administrativo_madrid`
>        T6 llega hasta el art. 105 y NO incluye el Título V de recursos (arts. 112-126), que el
>        epígrafe de ABM sí pide.
>      - **Tema 5 (Transparencia) es AL REVÉS: hay que RECORTAR, no ampliar.** El epígrafe de ABM es
>        solo Ley 19/2013 + *"especial referencia... Portal de Transparencia"* — NO menciona LOPD ni
>        RGPD. El T8 de `auxiliar_administrativo_madrid` mezcla Ley 19/2013 + LO 3/2018 + RGPD + Ley
>        10/2019 CM: para ABM solo tocaría Ley 19/2013 (+ quizá Ley 10/2019 CM por la "especial
>        referencia a la CM"), sin LOPD/RGPD.
>      - **Tema 6 (personal AAPP) NO tiene equivalente directo**: `auxiliar_administrativo_madrid`
>        T10 es específicamente TREBEP, más estrecho que el epígrafe de ABM (*"tipología y sistemas
>        de acceso... procesos de selección... derechos y deberes e incompatibilidades"*, que
>        sugiere TREBEP + quizá Ley 53/1984 de incompatibilidades) — necesita verificación propia.
>      - **Tema 7 (Igualdad) probablemente un SUBCONJUNTO** del rico scope de T13 de
>        `auxiliar_administrativo_madrid` (8 leyes) — el epígrafe de ABM no menciona
>        específicamente Ley 15/2022 (igualdad trato) ni Ley 2/2016 CM Trans, así que copiar las 8
>        leyes sin criterio sería sobre-inclusión (el defecto que `verify:scope`/`scope_over_inclusion`
>        vigila). Necesita juicio artículo por artículo del epígrafe, no una lista importada.
>   2. **6 temas con LEY MADRILEÑA propia, algunas YA importadas:**
>      - **Tema 101 (Ley 6/2023 Archivos CM): LEY YA IMPORTADA COMPLETA** (`law_id
>        3ac86532-19ed-4a47-8248-e984252138d6`, **115 artículos activos**, verificado con `count(*)`).
>        El epígrafe de ABM nombra la ley entera sin restringir subtítulos (*"La legislación
>        archivística en España. La ley 6/2023... de Archivos y Documentos de la CM"*) — mismo
>        patrón que otros temas de "ley entera" en el banco (p. ej. `Ley 11/1986 CM` con
>        `include_full_title:true`). **Candidato firme: scope = ley completa, arts. 1-115.**
>      - **Tema 117 (Depósito legal): dos leyes YA IMPORTADAS** — `Ley 23/2011` (depósito legal) y
>        `RD 635/2015` (depósito legal en línea, nombrado literalmente en el título del tema:
>        *"Depósito legal y depósito legal en línea"*). Candidato de alta confianza, falta decidir
>        si es la ley entera o un subconjunto de artículos.
>      - **Tema 138 (Ley 10/2007 lectura/libro/bibliotecas): LEY YA IMPORTADA** (`law_id
>        453e4c89-b300-4c4f-989c-99a2061a05c3`) — no medí cuántos artículos activos tiene ni si
>        cubre toda la ley; falta ese conteo antes de fijar el rango.
>      - **Tema 139 (propiedad intelectual): LEY YA IMPORTADA, pero POR DUPLICADO** — hay DOS
>        entradas de la misma norma (`RDL 1/1996`): `LPI` (`77cec287-...`) y `RDL 1/1996 (TRLPI)`
>        (`67728696-...`). **No es hallazgo de esta ficha resolverlo** (podría ser una duplicación de
>        contenido real, o dos versiones con distinto alcance de artículos) — quien mapee este tema
>        tiene que decidir CUÁL usar (o investigar si son iguales) antes de escribir el scope, para
>        no repetir el patrón de "dos escritores del mismo hecho" ([T-130]).
>      - **Temas 105 (Ley 7/2023 Libro CM) y 140 (Ley 8/2023 Patrimonio Cultural CM): NO
>        IMPORTADAS.** Comprobado con `SELECT` sobre `laws` por nombre — 0 filas para ninguna de
>        las dos. Esto es TRABAJO NUEVO real (clonar del BOCM, importar artículo por artículo,
>        doble verificación) antes de poder escribir ningún scope — el paso más caro de los 50 temas.
>   3. **~37 temas técnicos/conceptuales (102-104, 106-116, 118-137, 141-143) — SIN ley real
>      detrás, y ESO ES CORRECTO, no un hueco.** Son bibliotecología/archivística/museología pura
>      (*"El formato MARC 21"*, *"OPAC y herramientas de descubrimiento"*, *"Preservación y
>      conservación del fondo"*…) — no hay artículo de BOE/BOCM que responda a "¿qué es un OPAC?".
>      **Hay PRECEDENTE ya construido y reutilizable**: `auxiliar_archivos_estado`,
>      `auxiliar_biblioteca_estado` y `auxiliar_museos_estado` (oposiciones estatales del mismo
>      campo) ya tienen temas casi idénticos escopados contra leyes "editoriales" (contenido
>      redactado por la casa, no BOE) creadas para justo este propósito:
>      **`Archivística`** (`c13d45a0-...`, 30 arts.), **`Biblioteconomía`** (`d2cea377-...`, 36
>      arts.), **`Museología (editorial)`** (`c53b8551-...`, 37 arts.), **`Historia Cultural de
>      España`** (`5dfb3dcf-...`, 7 arts.), **`Conceptos Archivo`** (`97201714-...`, 2 arts.),
>      **`Reglamento Museos (editorial)`** (`e96516dc-...`, 1 art.) — **113 artículos en total ya
>      escritos y verificados** para otras oposiciones del mismo campo profesional. Verificado con
>      un caso real: `auxiliar_biblioteca_estado` tiene un tema **"El formato MARC 21"** escopado a
>      `Biblioteconomía` art. 217. **Falta el trabajo de EMPAREJAR cada uno de los ~37 temas de ABM
>      contra el artículo editorial que le corresponde** (por CONTENIDO, no solo título — la
>      exigencia de literalidad no aplica igual a contenido editorial propio, pero sí hay que
>      comprobar que el artículo responde de verdad al epígrafe del tema, no solo que el título se
>      parece) y, donde no haya artículo editorial que encaje, escribir uno nuevo siguiendo el
>      mismo patrón que ya usan esas tres oposiciones (no un cuarto sistema para el mismo contenido
>      — sería el error de [T-130] otra vez).
>   4. **Lo que NO hice**: no escribí ni un solo `topic_scope`, ni construí la config, ni toqué
>      código — todo esto es lectura (`VENCE_LECTOR_URL`) y no tengo escritura de negocio. La
>      cifra "50 temas" del título sigue en 0 filas de `topic_scope`, exactamente igual que antes de
>      este reconocimiento.
> - **Próximo paso, en orden de coste creciente:** (a) aplicar los 2 candidatos de "ley entera"
>   de alta confianza (temas 101 y 117) — son los más baratos y ya están importados; (b) resolver
>   el duplicado LPI/TRLPI del tema 139 y aplicar; (c) hacer el emparejamiento tema↔artículo
>   editorial de los ~37 temas técnicos (el bloque más grande, pero el más barato por tema porque
>   el contenido YA EXISTE); (d) verificar y ajustar (no copiar) los 7 temas generales contra sus
>   equivalentes de `auxiliar_administrativo_madrid`, uno a uno; (e) importar Ley 7/2023 y Ley
>   8/2023 de Madrid desde el BOCM (los 2 temas sin ley, el trabajo más caro) — dejarlo para el
>   final tiene sentido porque es contenido 100% nuevo, sin nada que reutilizar.
