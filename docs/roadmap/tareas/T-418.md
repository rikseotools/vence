### [T-418] 🔴 [ABIERTO 31/07] El usuario free agotado sigue respondiendo preguntas que el servidor RECHAZA una por una: 386 personas en 7 días

- **Esfuerzo: ~2 h** el diagnóstico de producto (¿qué debe ver?); lo que se decida arreglar, aparte.
- **⚠️ No abras esto sin leer [T-271] primero.** Aquel cubo ya había detectado el mismo 403 desde el lado de la observabilidad (*«el muro de pago registrado como error infla la señal y con ella el umbral del catch-all»*) y decidió **no perseguirlo**. Esta ficha NO lo contradice: aporta el ángulo que allí no se miró, que es **el del usuario** —responde y no se le guarda nada, 2.879 rechazos en 386 personas en 7 días, hasta 9 preguntas seguidas— y por eso es 🔴 aquí y «no perseguir» allí. Las dos cosas son ciertas a la vez: como señal es ruido, como experiencia es un agujero. Coordinad el arreglo: **dejar de emitirlo como `error`** (lo de T-271) y **avisar al usuario antes de que conteste** (lo de aquí) son el mismo cambio bien hecho.
- **Qué pasa, en una frase:** alguien con el cupo diario agotado **sigue pudiendo abrir un test y contestar**; ve su respuesta corregida al instante (la corrección es client-side, por diseño) y **el servidor rechaza el guardado de cada una** con `403 · «Has alcanzado el límite diario de preguntas del plan gratuito»`. No cuenta para su progreso, ni para su score, ni para sus estadísticas. Él no tiene forma de saberlo mirando la pantalla.
- **Escala medida el 31/07 (7 días, `observable_events`):** **2.879 rechazos en 386 usuarios distintos.** Por pantalla: `/api/v2/answer-and-save` 2.160 (367 usuarios), `/api/exam/answer` 326 (8), `/api/answer/psychometric` 68 (17). El `questionOrder` más alto observado es **9**: hay quien encadena nueve preguntas seguidas en balde.
- **Caso concreto para reproducir:** `javiergalinanesvarela@gmail.com` (free), 71 rechazos el 31/07 en `/tramitacion-procesal/test/test-aleatorio-examen`, con `questionOrder` 1, 2, 3, 4, 5… — es decir, **desde la PRIMERA pregunta del examen**: abrió un examen que ya nacía sin cupo.
  ```sql
  SELECT user_id, endpoint, error_message, created_at FROM observable_events
  WHERE error_message LIKE '%límite diario%' AND created_at > now() - interval '7 days'
  ORDER BY created_at DESC;
  ```
- **Lo que NO está comprobado y hay que mirar primero (no lo des por hecho):** si la UI le enseña el banner de límite en ese momento. Existe uno en el modo examen (`ExamLayout`, ver los tests de «Banner de límite no debe mostrarse en resultados»), así que el defecto puede ser (a) que no se pinte en esta ruta, (b) que se pinte y aun así se le deje contestar, o (c) que el examen se sirva completo y el gate solo actúe al guardar. **Cada una lleva a un arreglo distinto**, así que la primera media hora es reproducirlo con una cuenta free agotada, no tocar código.
- **Por qué es 🔴 y no una curiosidad:** es el momento exacto en que un free decide si paga o se va, y hoy lo vive como *«contesté veinte preguntas y no me aparecen»*. Enlaza con lo que ya sabemos del churn por el límite diario. Si la decisión es que el cupo se acabó, hay que **decírselo antes de que conteste**, no rechazarle en silencio cada respuesta.
- **Y un defecto de observabilidad de propina:** ese 403 se registra como `console_error` con `severity='error'` y el texto *«Error guardando respuesta en API (permanente)»*. **Una regla de negocio funcionando no es un error**: ensucia el conteo de errores del panel de salud (182 de los ~1.900 `console_error` de 24 h) y confunde a quien triaje. Al arreglarlo, que deje de contarse como error — ver [T-420].
- **De dónde sale:** triaje de errores de cliente del 31/07 (sesión `central-izquierdo`), buscando por qué había 2.173 eventos `severity='error'` en 24 h.
- **✅ DIAGNÓSTICO HECHO (01/08) — y lo primero que cambia es el marco: NO es una población, son DOS, con causas distintas y arreglos distintos.** Medido sobre **14 días** (la ficha de arriba contaba 7, así que los totales no son comparables de forma directa; lo que cambia es el REPARTO, no la escala). Ese volumen son 607 usuarios perdiendo **una** respuesta cada uno más 27 que chocan **1.471 veces** contra otra puerta.
  - **De las tres hipótesis de arriba, (a) y (c) son CIERTAS pero cada una en un sitio distinto del que la ficha suponía, y (b) no se sostiene.** En `TestLayout` el muro existe y funciona: `handleAnswerClick` (`components/TestLayout.tsx:1308`) impide contestar y `:1827` sustituye el test entero por la pantalla de límite. La (a) —«no se pinta en esa ruta»— es cierta en `ExamLayout`/`PsychometricTestLayout`/`OfficialExamLayout`, que no cablean el puente. La (c) —«se sirve completo y el gate solo actúa al guardar»— es cierta para la puerta de DISPOSITIVO, que el cliente no puede consultar. La clave de fondo: **el cliente solo conoce el contador de la CUENTA** (`useDailyQuestionLimit`) y el servidor rechaza por **dos** motivos distintos con el **mismo texto**.
  - **① EL GOTEO — 607 usuarios, 1.317 rechazos, y la forma es inequívoca: 1.139 de 1.188 casos usuario/día son EXACTAMENTE 1 rechazo.** No pierden veinte respuestas: pierden **la respuesta con la que topan el muro**, siempre esa. Cuadra con el contador de su propia cuenta (1.187 de 1.317) y la cuenta está al tope.
    - **✅ REPRODUCIDO (01/08) CON NAVEGADOR REAL, Y LA CAUSA ES OTRA: son DOS PESTAÑAS.** `scratchpad/t418/sim-goteo-2pestanas.ts` (usuario free efímero, contador sembrado en 24/25, sesión forjada con `lib/sim/session.ts`). Se abren dos pestañas del mismo test ANTES de gastar nada: **A contesta → HTTP 200** y el contador pasa a 25; **B, que sigue abierta y no se ha enterado, NO enseña ningún muro, deja contestar, y el servidor devuelve 403.** Esa respuesta se pierde.
      - **Por qué pasa:** el muro lo levanta un contador que vive **en la pestaña** (`useDailyQuestionLimit`: estado de React optimista + reconciliación cada 2,5 s). No hay nada que sincronice pestañas — el `CustomEvent('dailyLimitUpdated')` viaja por `window`, o sea **dentro de una sola pestaña**. Dos pestañas = dos contadores que se creen buenos, y estudiar con varias abiertas es lo normal.
      - **Y lo que ve la persona lo empeora:** en la pestaña B la pregunta **se corrige igual, con su explicación** (la corrección es client-side, por diseño), así que *parece* registrada; encima aparece el modal de Premium (el puente de [T-304] disparando con el 403). **En ningún sitio se le dice que esa respuesta no se ha guardado.** Captura: `scratchpad/t418/goteo-2pestanas.png`.
    - **❌ FALSADO por experimento — no volváis a esta hipótesis, que era la mía:** que el goteo fuera la carrera entre el cobro asíncrono del servidor y la reconciliación optimista del cliente. Con UNA pestaña **no se reproduce** (`scratchpad/t418/sim-goteo.ts`): el muro funciona, y sigue funcionando **retrasando el guardado 5 s** (el doble de los 2.500 ms de la reconciliación) y **cerrando el modal a mano**. Tres variantes, ningún 403.
    - **❌ FALSADO con datos:** que fueran reintentos de la cola persistente (`answerSaveQueue`, 7 días en `localStorage`). Es EN VIVO: **634 de 1.185 rechazos ocurren a menos de 30 s** de la última respuesta guardada del usuario (solo 10 pasada 1 h) y **465 de 468 tienen actividad suya alrededor** (`scratchpad/t418/medir10.cjs`).
    - **⚠️ Dos trampas del ARNÉS que dieron lecturas falsas antes de acertar, por si alguien rehace la sim:** (1) el modal de onboarding (edad/género/ciudad) es `fixed inset-0` y **se come los clics** → el usuario efímero necesita `age`/`gender`/`ciudad`/`onboarding_completed_at` rellenos (y `gender` solo admite `male|female|other|prefer_not_say`); (2) buscar «Hazte Premium» en el `body` para detectar el muro **da bloqueo SIEMPRE**, porque ese botón está en la CABECERA de todas las páginas — hay que buscar lo que solo existe con el muro puesto (`Limite alcanzado`, `Has completado tus`).
  - **② LA COLA LARGA — 27 usuarios, 1.471 rechazos: el 53% de TODO el volumen.** Aquí solo 9 rechazos cuadran con el contador de la cuenta; **749 traen un `questionsToday` MAYOR** que ese contador (que satura en el tope — por eso se ven 31, 41, 45) y **533 son de días en los que esa cuenta no tiene ni fila en `daily_question_usage`**. Solo lo produce un conteo que **suma entre cuentas**: la puerta de **DISPOSITIVO** (`checkDeviceDailyUsage`), que el cliente **no consulta jamás** y por tanto no puede prever ni pintar. **13 de los 27 ya tienen señal de fraude** (`multi_account_device`, `device_daily_farming`), o sea que en buena parte es el antifraude haciendo su trabajo — pero haciéndolo de la peor manera: rechazo mudo respuesta a respuesta en vez de un muro. Los otros 14 no tienen ninguna señal y hay que mirarlos uno a uno antes de asumir nada.
    - **⚠️ Y OJO CON EL EVENTO, que casi me hace concluir lo contrario: `device_daily_limit_blocked` solo tiene 9 eventos (2 usuarios, 30-31/07), y eso NO significa que la puerta apenas actúe.** `checkDeviceDailyUsage` está en `answer-and-save` desde el **17/04** (`e8f1724ed`); el evento no nació hasta el **30/07** con [T-304] (`f15c26657`). O sea que esa puerta lleva **tres meses y medio rechazando en silencio, sin emitir nada**. Contar el evento mide la edad del evento, no la actividad del gate — y leerlo al revés lleva a descartar justo la causa que explica el 53% del volumen.
  - **El puente de [T-304] existe pero no llega:** `useDailyLimitEvent` (que abre el modal cuando el servidor rechaza) **lo cablea SOLO `TestLayout`**. Ojo con la lectura fácil: `ExamLayout` y `PsychometricTestLayout` **sí** tienen muro de cuenta (banner, bloqueo en el clic y modal — `ExamLayout.tsx:873`, `PsychometricTestLayout.tsx:315`); lo único que les falta es **el puente del rechazo del SERVIDOR**, que es justo el que hace falta cuando el que corta es el dispositivo. Son las superficies donde la ficha ya había contado rechazos (326 y 68 en 7 días). Y **aun en `TestLayout` solo abre un modal**: no toca `isLimitReached`, así que al cerrarlo la puerta de `handleAnswerClick` sigue abierta y el usuario puede seguir contestando en balde.
  - **⚠️ TRAMPA DE MEDICIÓN, que costó una conclusión falsa: NO se pueden separar las dos puertas por el TEXTO del mensaje.** Lo intenté y dio un resultado limpio y del todo equivocado (2.693 «dispositivo» contra 97 «cuenta»). Dos motivos: el `errorMessage` se guarda **cortado a 200 caracteres** y el texto del límite ocupa ~120, así que el campo que distinguía (`dailyLimit`) cae fuera del corte; y el mensaje **se unificó el 30/07** (`lib/api/daily-limit/mensajes.ts`), así que en una ventana de 14 días el texto separa **épocas de deploy, no puertas**. El discriminador que SÍ vale es numérico: `body.questionsToday` contra `daily_question_usage.questions_answered` de ese usuario y ese día. Scripts en `scratchpad/t418/medir7.cjs` y `medir8.cjs`.
  - **⚠️ SEGUNDA TRAMPA en ese mismo discriminador, y muerde a cualquiera que cruce `observable_events` con contadores diarios:** `daily_question_usage.usage_date` lo escribe la función SQL como `(NOW() AT TIME ZONE 'Europe/Madrid')::DATE`, pero la sesión de `pg` va en **UTC**, así que unir por `ts::date` manda los rechazos de 22:00-24:00 UTC al día **equivocado**. Medido el desfase (`medir9.cjs`): aquí movía poco —los «cuadra» del goteo pasan de 1.164 a 1.187 y los «sin fila» de 111 a 95, y la cola larga no se mueve **nada**—, así que las conclusiones aguantan; pero el sesgo va **siempre** contra el mismo grupo (quien estudia de noche) y en otra pregunta puede decidir el resultado. Unir por `(ts AT TIME ZONE 'Europe/Madrid')::date`.
  - **La observabilidad de propina YA está medio arreglada:** `utils/answerSaveQueue.ts` emite estos 403 como **`usage_limit_hit` (`severity:'info'`)**, no como `console_error`: en 14 días hay **2.785 `usage_limit_hit`** y los `console_error` con esa firma solo aparecen el 29 y el 30/07 (192 y 108), cero el resto de días. Lo que la ficha describía sigue vivo solo en las colas que no pasan por ahí. Coordinar con [T-271]/[T-420] antes de tocarlo.
  - **🔎 HALLAZGO SUELTO encontrado al reproducir, NO es la causa del goteo pero es un defecto real:** `invalidateDailyLimitCache` (`lib/api/dailyLimit.ts:348`) invalida la clave **`daily_limit:${userId}`**, pero `getDailyLimitStatus` cachea bajo **`daily_limit_status:${userId}`** (línea 301). La invalidación **no toca la clave que existe**, así que la puerta del SERVIDOR puede ir hasta 30 s por detrás de la verdad. El desajuste nace del propio arreglo del incidente del 07/07 —se separó el namespace y no se actualizó al invalidador—, y el comentario de la línea 295 avisa de la separación sin darse cuenta de esto. Va en la dirección PERMISIVA (regala preguntas, no las quita), por eso no explica el goteo; pero conviene arreglarlo con su capa. Nótese la asimetría: el estado que lee el CLIENTE (`/api/v2/daily-question/status`) va sin caché, directo a la BD.
- **📌 ESTADO AL CERRAR LA SESIÓN (01/08) — dónde retomar, en orden.** El diagnóstico está ENTREGADO y la causa REPRODUCIDA; el defecto sigue vivo en producción, por eso la ficha NO se cierra. Tres trozos, y solo uno necesita a Manuel:
  1. **El goteo (607 usuarios) — no necesita decidir nada, se puede atacar ya.** Dos cosas: (a) sincronizar el contador del cupo **entre pestañas** (hoy el `CustomEvent('dailyLimitUpdated')` viaja por `window`, o sea dentro de una sola pestaña — sirve `BroadcastChannel` o el evento `storage`), y (b) que al llegar el 403 se le **diga que esa respuesta no se ha guardado**, en vez de enseñársela corregida con su explicación como si sí. Reproducción lista para usar como prueba: `npx tsx --env-file=.env.local scratchpad/t418/sim-goteo-2pestanas.ts` (hoy termina en ✅ REPRODUCIDO; con el arreglo tiene que dejar de reproducirse — ahí está la capa).
  2. **La clave de caché equivocada** (`invalidateDailyLimitCache` invalida `daily_limit:` y `getDailyLimitStatus` cachea en `daily_limit_status:`). Minutos + su test. Independiente de todo lo demás y NO es la causa del goteo — va en dirección permisiva.
  3. **✅ DECIDIDO POR MANUEL (01/08): se le AVISA de que tiene varias cuentas, que va contra los términos y que se le está limitando.** Motivo suyo: *«a dar recursos gratis se aprovecha creando multicuentas»*. Regla elegida: **una cuenta por persona y por dispositivo**.
    - **PRIMER PASO YA HECHO — los términos lo dicen ahora, antes NO.** Se añadió la cláusula a `app/terminos/page.js` §5 «Uso Aceptable» (una cuenta por persona y por dispositivo · prohibido alternar cuentas para superar el límite del plan gratuito · consecuencia: limitar el acceso conforme al §8) y se actualizó la fecha a Agosto 2026. **Era imprescindible antes del aviso:** los términos solo prohibían *«compartir tu cuenta con terceros»*, que es casi lo contrario (una cuenta usada por varias personas), así que el mensaje habría afirmado un incumplimiento inexistente y el usuario que fuera a leerlos lo habría visto. Guardarraíl: `__tests__/guardrails/terminosUnaCuentaPorDispositivo.guardrail.test.ts` (5 casos, validado por mutación: contra los términos anteriores falla entero) — si alguien recorta la cláusula, el aviso pasa a acusar en falso.
    - **Los datos con los que se decidió:** los 27 tienen **≥2 cuentas** en el aparato (ninguno tiene una sola, o sea que la afirmación «tienes varias cuentas» es CIERTA en los 27). Reparto: **2 cuentas → 8 usuarios · 3-5 → 14 · 8-11 → 3 · 23, 29 y 66 → 3**.
    - **⚠️ LO QUE QUEDA POR DECIDIR AL IMPLEMENTARLO, y no es menor:** con la regla nueva, los **8 usuarios de 2 cuentas** son infractores igual que el de 66, y a ojo son indistinguibles de una familia que comparte portátil (dos personas, una cuenta cada una). Conviene **graduar el mensaje por número de cuentas** en vez de mandar el mismo a todos, y **no** empezar por la banda de 2.
    - **✅ DECIDIDO (01/08): el cambio de términos NO se anuncia.** Decisión de Manuel: *«no lo vamos a anunciar, que lo lean ellos si quieren»*. Se planteó porque el §7 de los propios términos promete avisar de los cambios significativos, y este lo es. **Queda cubierto por el propio modal**, que es mejor aviso que un correo masivo: la persona afectada ve la regla en el momento en que le afecta y tiene que pulsar Aceptar (llega solo a quien le importa y deja constancia). **No volver a proponer el anuncio**: está decidido y sale del inventario de pendientes.
    - Contexto original de la disyuntiva (sigue siendo válido para redactar el texto): avisar revela que contamos por dispositivo, que es justo lo que `lib/api/daily-limit/mensajes.ts` evita a propósito por criterio de Manuel (*«cogerá el móvil de su pareja»*). El aviso nuevo asume ese coste **a cambio de poder invocar los términos**, que es lo que antes no se podía.
  - *(histórico de la disyuntiva, ya resuelta arriba)* qué ve quien topa por **DISPOSITIVO**. Avisarle antes es mejor experiencia, pero **13 de esos 27 tienen señal de multicuenta** y explicarle cuándo cortamos es enseñarle el mecanismo — justo lo que `lib/api/daily-limit/mensajes.ts` evita por criterio explícito de Manuel (*«cogerá el móvil de su pareja»*). Opciones: **(a)** avisar igual · **(b)** no avisar pero **no perder la respuesta** (se guarda aunque tope) · **(c)** no tocarlo y quedarse solo con el goteo. Recomendación de la sesión: **(b)** — conserva el trabajo del usuario legítimo, no regala información al que multicuentea, y deja el debate del mensaje para cuando haya datos de los 14 sin señal.
  - **NO rehacer** (ya está hecho y comprobado): la reproducción con navegador, el reparto entre las dos puertas, y las tres trampas de medición anotadas arriba. Los scripts están en `scratchpad/t418/`.
  - **Y ojo con el entorno:** para correr las simulaciones hace falta `node_modules` REAL en el worktree (`rm node_modules && cp -al …`) y las 4 claves de SSM en `.env.local` (`AUTH_SECRET` + las 3 RS256) — ver `reference-dev-local-login-env-rs256`.
  - **Lo que queda es DECISIÓN DE PRODUCTO, no código:** ① ya no es «la carrera del borde» sino **la sincronía entre pestañas**, y pide decidir qué se hace cuando llega el 403 (guardar igual la que ya escribió, o al menos DECIRLE que no se ha guardado en vez de enseñarla corregida) y ② pide decidir **qué se le enseña a quien topa por dispositivo** — que es gente que en la mitad de los casos está multicuenteando a propósito, así que «avisarle antes» es también decirle cómo contamos. Ver la nota de `mensajes.ts` sobre por qué los dos textos son iguales a propósito.
- **✅ HECHO (05/08, trabajador w1) — puntos 1 y 2 de la lista de arriba, en rama `flota/T-418-goteo-multicuenta-403` (NO en main, pendiente de revisión):**
  - **(1a) Sincronía entre pestañas.** `hooks/useDailyQuestionLimit.ts`: el `CustomEvent('dailyLimitUpdated')` viajaba solo por `window` (una pestaña); ahora se añade un `BroadcastChannel('vence-daily-limit')` a nivel de módulo que retransmite el mismo `newStatus` a cualquier otra pestaña del mismo origen, en los DOS sitios donde se actualiza el estado (`fetchStatus` y el incremento optimista de `recordAnswer` — este último es el que importa: dispara AL CLICAR, sin esperar red, así que cierra la ventana de la carrera). El mensaje lleva `userId` y el listener lo descarta si no coincide con el usuario de ESTA pestaña (evita pisar el estado si hay otra cuenta abierta en el mismo origen — perfil/incógnito aparte). Fail-open si el navegador no soporta `BroadcastChannel` (no rompe nada, simplemente no sincroniza, igual que antes). Tests nuevos en `__tests__/hooks/useDailyQuestionLimit.test.ts` (2, usando el `BroadcastChannel` REAL de Node como si fuera "la otra pestaña" — jsdom 20 no lo trae, se polyfilla en `jest.setup.js` con `worker_threads.BroadcastChannel`, con `.unref()` en el hook para que el handle no cuelgue el proceso).
  - **(1, bonus no pedido explícitamente pero cierra un hueco real que el diagnóstico ya había apuntado):** en `TestLayout.tsx`, el puente `useDailyLimitEvent` (el que abre el modal cuando el SERVIDOR rechaza por dispositivo) solo hacía `setShowUpgradeModal(true)` y no tocaba `isLimitReached` — al cerrar el modal, `handleAnswerClick` seguía dejando contestar. Ahora también llama a `refreshStatus()` (fuerza una lectura fresca del servidor, se apoya en el fix de caché de abajo), así que la puerta se cierra con el conteo real sea cual sea la causa del 403.
  - **(2) Clave de caché equivocada, confirmada y arreglada.** `lib/api/dailyLimit.ts`: `invalidateDailyLimitCache` solo invalidaba `daily_limit:${userId}` (la clave de `/api/daily-limit`); `getDailyLimitStatus` — la fuente real de `questionsToday` — cachea aparte en `daily_limit_status:${userId}` desde el incidente del 07/07. Ahora invalida las DOS (`invalidateMany`). Test nuevo en `__tests__/api/daily-limit-enforcement.test.ts` que verifica ambas claves.
  - **Verificado:** las suites directamente relacionadas (`dailyLimitFlow`, `dailyLimitAllowance`, `dailyQuotaConsumePolicy`, `daily-limit-enforcement`, `useDailyQuestionLimit`, `multicuenta/avisoMultiCuenta`, `simulacroPaywall`, guardarraíles de cupo) — 240 tests, todos verdes. `npx eslint` sobre los ficheros tocados — 0 errores (solo warnings preexistentes de `exhaustive-deps` no relacionados). **NO se pudo correr `npm run typecheck` completo** — revienta por OOM en este entorno (heap de Node agotado con el proyecto entero, no es cosa de este cambio: ya pasaba antes de tocar nada). Tampoco se ha probado en navegador real (dos pestañas de verdad) — la prueba es la simulación con `BroadcastChannel` de Node en el test, que ejercita el mecanismo real, no una réplica.
  - **NO se ha tocado (b): decirle al usuario que SU respuesta concreta no se guardó.** Es distinto de (a): (a) evita que seas TÚ MISMO en la pestaña B quien siga perdiendo respuestas nuevas; (b) es sobre la respuesta que YA disparó el 403 — hoy se sigue viendo corregida en pantalla como si se hubiera guardado, porque el guardado es async y desacoplado del feedback visual (por diseño, ver "Sistema de Respuestas" en CLAUDE.md). Implementarlo bien exige decidir el enfoque (¿reintentar guardado cuando baja el cupo? ¿toast retroactivo? ¿marcar la tarjeta de esa pregunta?) y tocaría además `ExamLayout`/`PsychometricTestLayout`, que ni siquiera tienen cableado el puente `useDailyLimitEvent` (solo `TestLayout` lo tiene) — gap más pequeño (326 y 68 rechazos/7d respectivamente) pero real, sin tocar en esta sesión. Lanzada `preguntar` para la decisión de producto.


