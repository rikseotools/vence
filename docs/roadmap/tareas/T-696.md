### [T-696] 🟠 [ABIERTO 08/08] Cabos sueltos de la sesión movil4 del 07-08/08 (para que no se pierdan al compactar)

Índice de lo que quedó a medias o esperando decisión, con dónde está cada cosa. **Lo que ya está
cerrado NO se repite aquí**: [T-657] (cupo/huella), [T-237], [T-671] (los 401) y las doce revisadas
están cerradas y desplegadas.

> **📌 AL DÍA — 08/08 por la tarde.** Lo cerrado desde que se escribió esto:
> - **[T-712] cerrada y verificada EN EL VPS.** La sonda de salud de máquina llevaba 439 pasadas
>   sin medir nada (se le pasaba el objeto entero donde va el nombre, y el `continue` lo tapaba).
>   La hipótesis del ssh que traía su ficha era FALSA y queda descartada por escrito.
> - **[T-647] con el veredicto LEÍDO** y en «hecho, falta desplegar»: cero OOM en 20 h con más
>   carga que cuando caían. Corrige una conclusión de su propia ficha (`MemoryHigh` NO es inerte)
>   y cierra el hueco de que su capa 4 emitía sin que ninguna regla la mirase. **Su regla de
>   alerta necesita OTRO deploy de backend**: el de las 14:30 salió con un sha anterior.
> - **Backend desplegado** (`50a89878`): despertó [T-613] y [T-405], listas para verificar.
> - **Merges del día: 19 ramas revisadas** juntadas en `main` (las 14 de la mañana + T-694, T-339
>   y la grande de T-378, que arrastraba T-467, T-435, T-636, T-408 y T-410).
> - **Sigue abierto:** el feedback de la cola lleva **21 h** reservado por una sesión de Manuel
>   viva pero parada (checkout principal); ni movil4 ni movil2 lo tocan. Y [T-691] no se puede
>   cerrar hasta el barrido de esta noche.

**1. ESPERAN DECISIÓN DE MANUEL — nadie más las va a mover**
- **Las dos aportaciones de Laura Simar del 06/08.** Ella reclamó que no le generaron recompensa y
  **tiene razón en los hechos**: la impugnación `a1a6e998` es `explicacion_confusa` (motivo
  subjetivo, no paga sola — evento `reward_skipped_subjective_type`) y el bug `7847ff3e` de una
  premium **sí puede pagarse pero exige orden explícita** que nadie dio. Ya tiene 2 recompensas
  cobradas (04 y 05/08), no es alguien que reclame sin fundamento. **Si se conceden, se crean en
  SILENCIO**: el mensaje ya se le envió y no las menciona, como manda la regla.

**2. ESPERAN EL DESPLIEGUE (se despiertan solas)**
- **[T-677]** — que el supervisor del VPS emita `flota_maquina_salud` de forma PERIÓDICA.
- **[T-691]** — que el barrido nocturno emita el kind con el patrón nuevo de figuras rotuladas.

**3. LA CAUSA DEL VPS AHOGADO NO ES MÍA — ya tiene ficha, NO abrir otra**
[T-682] (crítica, «cuatro `tsc --noEmit` a la vez no caben en el VPS») y [T-647] (20 OOM en 6 h).
Lo medido, para quien las coja: los cuatro Claude Code ocupan **menos de 1 GB entre todos**; el peso
son sus **builds** (1.574 + 1.383 + 1.295 + 1.213 MB). Carga 17-19 en 4 núcleos **con la CPU al
91-98 % ociosa** = esperando disco, no calculando. **Bajar trabajadores NO es el arreglo**
(conclusión que yo mismo propuse y retiré al medir quién consumía la memoria); serializar los builds
sí.

**4. ERRATA CONOCIDA**
El commit del validador de mensajes está pusheado como `feat(T-678)` y **T-678 es una ficha ajena ya
cerrada**: se eligió el id sin `reserve`. El código y el manual ya citan **[T-695]**, que es la ficha
buena. Quien busque el origen por el mensaje de commit llegará a la ficha equivocada.

**5. CABO MENOR, SIN FICHA PROPIA**
El payload de `/api/v2/daily-question/status` se contradice consigo mismo: devuelve `questions_today`
ya corregido con el cupo del aparato, pero `questions_remaining` e `is_limit_reached` siguen
calculados solo con las respuestas propias (visto en vivo: `today:25`, `remaining:25`,
`limit_reached:false`). Viene de [T-418], **no se introdujo aquí**, y la interfaz funciona porque
decide con `questions_today`. Cerrarlo es barato.

**6. CABO NUEVO (08/08, tras el deploy de frontend): UN JOURNEY QUE FALLA SOLO**
El deploy de frontend (task-def 638, `c5b0af94`) cerró con **«DEPLOY OK» teniendo 3/4 journeys**: falló
`examen-fallo-de-sesion-no-culpa-a-la-conexion` con *«no se llegó a pulsar Corregir Examen (0 opciones
respondidas)»*. **NO es un fallo de producción** — comprobado en el momento: 228 respuestas de 23
usuarios en 20 min y **cero 5xx** —, así que es el journey el que no llegó a interactuar (tiempos o
selectores). Es de otra sesión (lo creó para [T-669]/[T-315]) y conviene que lo sepa: **un journey que
falla por su cuenta es una alarma que se aprende a ignorar**, y encima el deploy no gatea por él.

**7. HACE FALTA OTRO DEPLOY DE FRONTEND**
[T-677] y [T-691] **siguen esperando con razón**: el deploy subió el último commit VERDE EN CI
(`c5b0af94`) y el que ellas esperan (`29a2d7d1b`, el que mueve la sonda de la máquina al BUCLE del
supervisor) es posterior — quedaron 5 commits fuera. El mecanismo funciona; su código aún no está
vivo. Hasta ese deploy, la medición de la máquina solo ocurre cuando alguien ejecuta `npm run flota`
a mano, o sea que **la alerta todavía no es proactiva del todo**.

**8. SOPORTE DE ESTA TANDA (todo cerrado, nada pendiente)**
- María José (`d0a3ce35`, racha de días): ya estaba respondida y dio las gracias → **cerrada en
  silencio**, sin generarle otro aviso.
- Ricardo Novoa ×2 (`2e705c38` y `ea163bd5`, art. 117 CE): **ambas rechazadas**, verificadas contra el
  BOE (117.5 dice «estado de sitio», no «excepción»). Ninguna procedía, pero las dos preguntas tenían
  la explicación apelotonada y **anclada a las letras** (una empezaba con «La respuesta correcta es
  D»), así que **ninguna podía barajar**: reescritas por opción con cita literal y ahora **las dos
  nacen barajables**.

**8.bis ⚠️ COLISIÓN DE ID AJENA, SIN RESOLVER: DOS FICHAS DISTINTAS SON [T-692]**
Al ordenar esto saltó el guardarraíl de ids únicos. Son **dos tareas diferentes de otras sesiones**
compartiendo id (las dos del 08/08, sobre los 401 del token):
  · «La petición sale SIN el token y muere en un 401 invisible: 44 % en una ruta que llevaba nueve
    días a cero»
  · «El token se pide antes de existir: el 63 % de los 401 cae en los 10 primeros segundos…»
**NO se toca desde aquí**: no es trabajo mío y quedarme con un lado borraría el del otro, que es
justo lo que persigue el guardarraíl anti-pérdida. Lo tiene que resolver quien las escribió, dando a
una un id nuevo con `reserve`. **Mientras tanto el guardarraíl de ids únicos está EN ROJO**, así que
tapa cualquier colisión nueva que aparezca detrás.

**8.ter LA VIGILANCIA DE LA MÁQUINA NO MIDE SOLA — ficha [T-712]**
Se despertaron [T-677] y [T-691] con el deploy, pero al verificar apareció que el supervisor **no
emite ni una lectura**: vive DENTRO de `flota-1` y el registro la declara remota, así que se hace
`ssh` a sí mismo y recibe `Permission denied`. Falla en `continue`, sin rastro. **Desde el portátil
funciona** (yo tengo la llave), o sea que el mismo código da verde en una máquina y silencio en
otra. Se reinició el servicio (era otra hipótesis razonable y no era la causa). Detalle y arreglo
propuesto en [T-712].

**8.quater ESTADO FINAL DE LA VIGILANCIA DE LA FLOTA (08/08, 12:30)**
- **[T-677] figura como `done` pero su última mitad NO está verificada.** Se cerró sola al
  despertarla el deploy, con el `resume_check` diciendo «falta SOLO verificar». Lo verifiqué y
  **falla**: la sonda no mide desde el VPS ([T-712]). Lo que SÍ está comprobado: las dos reglas
  desplegadas (backend task-def 181), el cruce proceso-vivo/latido-congelado emitiendo
  (`flota_turno_sin_progreso`, 2 eventos) y el panel midiendo bien a mano. **No leer ese `done`
  como «funciona entero».**
- **Se reinició `vence-flota-supervisor`** (08/08 10:19 UTC) porque el proceso corría con código
  anterior al clon. Era cierto y **no era la causa**: siguió sin emitir. Queda hecho, no hay que
  repetirlo.
- **[T-691] está en «falta solo verificar» y su verde de hoy NO prueba nada todavía**: el barrido
  no emite el kind porque las 2 preguntas ya están jubiladas (ausencia real), pero **el último
  barrido es de las 07:33, anterior al deploy del patrón nuevo**. Lo confirmará el barrido
  nocturno siguiente: si emite con el patrón ampliado, verde de verdad.

**9. COLAS**
Las tres contestadas por esta sesión (Diego, y Laura ×2: feedback e impugnación). Lo demás está en
otras sesiones: los dos bugs de `rbsc87` en `movil-colas`, dos `other` y dos bajas de cuenta en
`136e28c4` y `movil2`. **No reabrir sin mirar el claim.**


**10. TARDE-NOCHE DEL 08/08 — LO QUE SE APRENDIÓ VACIANDO LA COLA DE MERGES (esto NO estaba escrito en ningún sitio)**

Se mergearon **11 ramas** más, se cerraron **3** tareas (T-532, T-679, T-367) y se vació el embudo
(las **6** preguntas contestadas). Lo que importa conservar no son esos números, son estas cuatro
cosas, que costaron tiempo real y ninguna estaba documentada:

- **⚠️ `docs/roadmap/tareas-pendientes.md` es GENERADO, y resolver sus conflictos a mano BORRA el
  trabajo sin avisar.** Desde [T-532] el fichero lo produce `generarIndice()` a partir de las 692
  fichas de `docs/roadmap/tareas/`. Resolví dos conflictos a mano —con criterio, conservando los dos
  lados— y `indiceEstaAlDia()` dio **`false`**: ese texto habría muerto en la siguiente
  regeneración. **La receta correcta, y con ella los 6 merges siguientes entraron SIN UN SOLO
  conflicto:** resolver las **fichas** (que sí son fuente) y ejecutar
  `node -e "require('./lib/backlog/fichasDir.cjs').regenerarIndice()"` para el índice. Comprobar
  siempre con `indiceEstaAlDia()` antes de commitear.
- **Las ramas anteriores a [T-532] escriben su texto DENTRO del fichero generado**, así que su
  contenido no sobrevive a una regeneración. Al mergear una de ésas hay que **portar su texto a la
  ficha** `docs/roadmap/tareas/T-nnn.md`. Pasó con `T-608-modales-parte2` y
  `T-312-desglose-ruta-auth-antifraude`; en la segunda, lo único que no estaba ya en ninguna ficha
  era una corrección que pertenecía a **T-315** (que `fuera_de_fases` de `answer_save_lento` NO ve
  el antifraude) y cuya ficha seguía afirmando lo contrario. Sin portarla se habría perdido.
- **🔴 Un veredicto `ok` NO garantiza que la rama siga siendo la buena.** De las ramas revisadas en
  verde, **tres estaban OBSOLETAS** y mergearlas habría hecho RETROCEDER cifras ya corregidas en
  `main`: `T-634` (la rama dice 1.650 preguntas y «al menos 10» con «365»; la ficha ya mide **1.905
  y 894**, y **45** oposiciones en vez de 36), `T-196` (la rama acusa al T3 por «President», que la
  pregunta #121 ya había desmentido contra el PDF castellano oficial: el alcance real es **1**
  epígrafe de 24, no 2) y `T-699` (cabecera anterior a su propia revisión). **Antes de mergear una
  rama docs-only, comparar sus líneas contra la ficha que hay en `main`**: si la ficha es más nueva,
  la rama sobra. Se detecta en segundos con `grep -vxFf docs/roadmap/tareas/T-nnn.md <lineas-que-añade>`.
- **El grupo «ya SIN RAMA PENDIENTE (se cierran con `done`)» de `backlog.cjs list` ENGAÑA.** Significa
  *no hay rama que mergear*, no *está terminada*. De las 10 que ofrecía, **8 confesaban trabajo
  pendiente o eran decisión de Manuel** (T-681 llevaba 10 de 43 artículos, T-683 tenía tabla de
  pendientes, T-713 esperaba una cuenta desechable, T-644 esperaba la pregunta #123, T-465 tenía 7
  preguntas colgando, T-513 y T-530 son decisiones). **Solo 2 eran cerrables.** Cerrarlas en bloque
  habría metido 8 falsos verdes de golpe.

**10.bis TRES CHOQUES QUE NINGUNA RAMA VE POR SEPARADO** (verde en `origin/main`, verde en su rama,
rojo al juntarlas). Es el argumento de por qué el merge NO se delega:

1. **[T-613] contra [T-733]:** los dos diagnosticaron el MISMO incidente de producción el mismo día
   y lo arreglaron por separado. Me quedé con T-733 (guarda el mensaje del error y hace que el cron
   reporte `partial`, no `success`) y **rescaté lo único que solo tenía T-613: el
   `ORDER BY tsColumn`**, medido con `EXPLAIN` (Seq Scan 8.124 ms / 145.058 buffers → Index Scan).
   No es cosmético: sin él, 2-3 lotes se pasan del `statement_timeout` de 30 s que documenta T-733
   — **son la misma avería vista dos veces**. Además la auto-fusión dejó `cron.ts` llamando a
   `result.purgeFailed`, renombrado a `purgaFallida`: **referencia rota que compila y revienta en
   ejecución**.
2. **El guardarraíl del podador único se puso rojo por su propia prueba.** El spec de regresión de
   T-613 cita el mensaje de error real (`Failed query: DELETE FROM observable_events…`) palabra por
   palabra, y el guardarraíl buscaba ese literal por `grep` en todo el árbol. Acotado **solo a esa
   comprobación** (las otras cuatro siguen mirando los spec) y **verificado por MUTACIÓN**: un
   `DELETE` literal metido a mano en `telemetry-retention.cron.ts` lo sigue poniendo rojo.
3. **El kind `seguimiento_fuente_error` de [T-564] entró sin superficie.** La rama lo dio de alta en
   `runbookRegistry` pero no lo asignó en `lib/admin/landingSurfaces.ts`, y el guardarraíl de
   cobertura («todo detector de landing está asignado a una superficie») lo cazó al juntar. Va con
   sus dos hermanos en `enlace_seguimiento`.

**10.ter LAS 6 DECISIONES DEL EMBUDO, CONTESTADAS — con el porqué, que es lo que no cabe en la respuesta**
- **#113 [T-080] barajado:** ampliar a **Córdoba + Murcia**, no a `all`. El piloto valida el
  mecanismo pero solo contra contenido valenciano; dos oposiciones más dan otro temario y otra
  semana por el mismo esfuerzo. Las tres comprobaciones salieron limpias (0 impugnaciones por letra
  descuadrada sobre 21 que tocan barajadas, `shuffle_safe_regressed` 0, y los 13 avisos de orden
  inválido son todos del bug que se arregló el 01/08, cero desde el 02/08).
- **#109/#121 [T-196] valencianas:** opción **(c)** — literal en `epigrafe`, traducción aparte en el
  campo que ve el usuario. **Motivo, para que no se implemente a medias:** el epígrafe es la VARA
  con la que se decide el `topic_scope` (Paso 2) y se verifica la literalidad (Paso 1); traducirlo
  significa verificar nuestra prosa contra el boletín, y eso solo puede acabar en «sin verificar
  para siempre» o en un **falso verde por construcción**. Y se repite: **16 oposiciones activas**
  (301 catalogadas) están en territorios con lengua cooficial, medido hoy. Tres condiciones: el
  campo mostrado **cae al `epigrafe`** si no hay traducción (los otros 3.799 temas no cambian), la
  traducción se marca **como nuestra** (nunca `verified_literal`), y la verificación sigue apuntando
  al original. Mismo patrón que sacar las notas internas de la landing a la tabla de verificación.
- **#116 [T-339] personalizadas:** opción **(A) MÁS arreglar el onboarding**. La (B) revierte de
  hecho [T-508]; pero la (A) sola tampoco vale, porque hoy se ofrecen 10 personalizadas «más
  populares» y **las 10 tienen cero temas**, o sea que las 10 fallan. Lo que falta: que el
  onboarding **no ofrezca** las de cero temas, y que «Crear oposición personalizada» lleve a
  CONSTRUIR el temario en vez de a un error.
- **#119 [T-530] huella:** instrumentar **sí**, pero la pregunta mezclaba dos cosas con coste de
  privacidad muy distinto. **Guardar las señales sueltas (canvas/WebGL) es una escalada real;
  guardar un HASH POR SEÑAL dice cuál de las 12 derivó y no permite reconstruir nada.** Y antes de
  recoger nada, **paso 0 gratis**: `user_devices` solo tiene `device_id`, `device_label`,
  `first_seen_at`, `last_seen_at` y `hw_fingerprint` (comprobado), pero los user agents están en
  `observable_events` — mirar si el navegador cambió de versión entre las dos filas de esos 17
  relevos. Si lo explica, no se recoge nada. Retención de 60 días y solo para diagnóstico.
- **#125 [T-152] fechas de Madrid:** **ya estaba aplicado**, no hacía falta decidir. Las 3 filas
  están en `2026-08-25` (la convocatoria se actualizó a las 17:04 UTC del 08/08, después de que el
  trabajador midiera). **GOTCHA que se lo hizo ver mal:** `inscription_deadline` y
  `convocatoria_hitos.fecha` son columnas `DATE`, y pintadas con `toISOString()` salen como
  `2026-08-24T22:00:00.000Z` (Madrid es UTC+2) — parece el 24, es el 25. Leerlas con `::text`.
