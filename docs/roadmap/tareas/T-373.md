### [T-373] ✅ 🟢 [HECHA 31/07 · abierta 31/07] Los 79 usuarios que perdieron las respuestas por email sin haberlo pedido

- **Cabo de [T-369].** Arreglar el código no deshace el estado que dejó: el botón «Desactivar TODOS los emails» llevaba meses apagando además `email_soporte_disabled`, que es la puerta de nuestra contestación a lo que el usuario escribe **y del aviso de renovación de quien paga**.
- **El número que decide el criterio:** de los 80 con el soporte apagado, **79 tenían la firma del botón rojo** (`email_soporte_disabled AND unsubscribed_all`) y **1 sola** lo eligió por la categoría «Soporte y transaccional» (`unsubscribed_all=false`). Esa 1 **no se ha tocado**: su preferencia es real y es suya.
- **Lo que costó, medido antes de tocar nada:** 3 personas con **27 respuestas escritas que nunca les llegaron** — 20 a `marta_benitopadilla`, 6 a `luciacn1984`, 1 a `mbelen177`. Les llegó la campana; el correo, no.
- **Ejecutado el 31/07:** `scripts/emails/restaurar-soporte-baja-masiva.cjs --apply` → **79 filas restauradas**, verificado con consulta independiente (queda 1 con el soporte apagado, y es la que lo eligió). **No se reactivó nada más**: `unsubscribed_all` y la newsletter siguen como estaban — se dieron de baja de la publicidad y siguen de baja. Lo único que se les devuelve es la respuesta a lo que ellos escriban.
- **Reversible:** el estado previo se volcó a `~/vence-backups/t373-estado-previo-*.json`. Va **fuera del repo** a propósito: lleva 79 correos reales y `scratchpad/` no está gitignoreado, pero un fichero ignorado dentro del worktree se pierde al borrarlo (lección del 20/07 con los `out-NN.json`).
- **Si esto vuelve a dar filas, el defecto ha reaparecido**, porque la causa ya está cerrada en `processUnsubscribeByToken`. Herramienta registrada en `lib/admin/toolRegistry.ts` como `historico` para que nadie la reconstruya.
- **NO se re-enviaron los correos perdidos, y ya NO es una decisión pendiente: ✅ Manuel decidió el 01/08 que no se hace nada.** Reenviar 20 respuestas antiguas de golpe a una misma persona es spam, y las tiene disponibles en la campana y en `/soporte`. Lo que se ha arreglado es que **las próximas sí le lleguen**. **Cerrado: no reabrir.**

