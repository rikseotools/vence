### [T-367] 🟡 [ABIERTO 31/07] Llegar al filtro por artículos desde el temario (prometido por escrito a un usuario)

- **Es un compromiso adquirido, no una idea.** El 31/07, en el hilo `6df1e69a`, se le escribió a un usuario premium: *«Vamos a hacer que se pueda llegar a ese filtro también desde tu temario, que es por donde entras habitualmente.»* Esa frase no estaba anotada en ningún sitio hasta esta ficha. Una promesa que solo vive dentro de un hilo cerrado es una promesa que no se va a cumplir.
- **Lo que pidió:** hacer test de una parte de una ley larga («llevo estudiados 50 de los 100 artículos, quiero test solo de esos») sin tener que tirar del test completo. Se puede: el filtro está en el configurador (`components/TestConfigurator.tsx`, apartado «📄 Filtrar por Artículos», plegado por defecto).
- **Por qué no lo encontraba, que es el fondo del asunto:** desde su temario el botón le lleva **directo a las preguntas** y se salta la pantalla donde vive el filtro. Se le dio la ruta larga (Teoría → la ley → «🎯 Test» → «Filtrar por Artículos») y **se desvió**: en `/teoria/[law]` hay un botón **«📚 Filtrar por Títulos»** (`LawArticlesClient.tsx:526`) que se llama casi igual y filtra **la lectura**, no el test. Su journey lo enseña pulsando títulos y «Aplicar filtro» en esa pantalla, convencido de que estaba configurando el test.
- **Dos cosas, entonces:**
  1. **Un camino al filtro desde el temario** (que es por donde entra la gente), sin obligar a pasar por Teoría.
  2. **Desambiguar los dos botones**, que hoy compiten por el mismo nombre. Uno filtra lo que LEES y otro lo que te PREGUNTAN, y la única pista es el emoji (📚 vs 📄).
- **Cuidado al medirlo:** no basta con que el filtro exista y funcione (existe y funciona). Lo que falla es **encontrarlo**, así que la comprobación es de recorrido: entrar por el temario y llegar a un test de artículos escogidos sin pasar por Teoría.
- **Origen:** feedback `6df1e69a` (cerrado en silencio: el usuario ya no esperaba respuesta). Tercer feedback suyo sobre lo mismo en dos semanas.
- **Relacionada:** [T-313] (la ficha madre, de donde salió esta idea como compromiso 5), [T-711] (por qué esto lleva mergeado días sin desplegarse y sin que nadie lo notara).

#### 🔧 Implementado y MERGEADO en `main` — pero SIN DESPLEGAR, y sin `pause --tras-deploy` que lo recuerde (08/08, w1)

- **Las dos cosas que pedía la ficha, hechas — verificado leyendo el código real, no la ficha:**
  1. **Camino desde el temario:** `components/temario/LawTestCTA.tsx` (el ÚNICO punto que arma enlaces de ley desde el temario, centralizado en [T-073] y usado por las 131 copias de `TopicContentView`) tiene ahora un SEGUNDO enlace, «🔧 Elegir qué artículos», junto al «Hacer test de {ley} →» de siempre. Usa `buildLawFilterLink()` (`lib/navigation/backToArticleLink.ts`) que a propósito NO lleva `selected_articles` (así no dispara el auto-arranque de `LawTestConfigurator`) y sí lleva `abrir_filtro=1`.
  2. **El panel ya no nace plegado en ese camino:** confirmado `grep` en `app/leyes/[law]/LawTestConfigurator.tsx:29` — lee `searchParams.get('abrir_filtro') === '1'` y lo pasa como `initialShowLawsFilter` (línea 189) a `TestConfigurator.tsx`, que lo usa para inicializar `showLawsFilter` (línea 148). Cadena completa verificada de punta a punta, no solo el componente hoja.
  3. **Desambiguados los dos botones:** `app/teoria/[law]/LawArticlesClient.tsx:530` — confirmado que ya NO dice «Filtrar por Títulos» (el nombre que competía con el del test), dice **«Ver por Títulos (lectura)»**.
  - Tests: corrí yo mismo (no me fío del recuento de la ficha) `__tests__/navigation`, `__tests__/components/LawTestCTA.test.tsx`, `__tests__/components/temarioVueltaAlArticulo.test.tsx`, `__tests__/guardrails/filtroArticulosDescubrible.test.ts`, `__tests__/guardrails/lawTestCtaNoBareLink.test.ts` → **7 suites, 61/61 tests verdes**. `tsc --noEmit` limpio. `eslint` sobre los 5 ficheros tocados: 6 avisos `react-hooks/exhaustive-deps`, los mismos 5 (uno se desplazó una línea) que ya existían ANTES de este commit — lo comprobé corriendo eslint sobre `TestConfigurator.tsx` en un worktree del commit padre (`52f092878^`, o sea antes de T-367): mismos 5 avisos. Cero regresión de lint.
- **⚠️ MISMO PATRÓN QUE [T-313]: mergeado, pero NO desplegado, y NADIE lo estaba rastreando.** El commit `f9293e33a` (autor 06/08 23:19 UTC, vivía en la rama compartida `flota/w3`) está en `origin/main` — confirmado `git merge-base --is-ancestor f9293e33a origin/main` → sí. Pero el último deploy de frontend (`deploy_runs id=105`, sha `7a4d8481`, terminado 2026-08-08T14:59:33Z) es **anterior** a esa fusión: `git merge-base --is-ancestor f9293e33a 7a4d8481` → no. Confirmado además en el contenido: `git show 7a4d8481:components/temario/LawTestCTA.tsx` no tiene `buildLawFilterLink` ni el segundo enlace. **Producción, ahora mismo, sigue sin el camino desde el temario** — la promesa a Manolo sigue sin cumplirse en lo que él ve, aunque el código ya esté listo.
  - Esta ficha estaba `open`/sin reclamar en el backlog pese a tener trabajo terminado — no había ningún `pause --tras-deploy` que la sacara de la cola de "por hacer" y la metiera en la de "esperando deploy". Es exactamente el hueco que documenta [T-711].
- **El segundo bypass que menciona el `falta` anterior** (`leyes-de-oposiciones/page.js:328`) **ya está resuelto, por [T-313]:** esa ruta usa el mismo `LawTestPageWrapper`/`TestLayout.tsx` que arregló el botón "Volver a la ley" — hereda el fix sin tocar ese fichero. No hace falta trabajo aparte.
- **Repunto la espera de deploy** con el sha real ya confirmado en `origin/main` (`f9293e33a`), `--superficie frontend`, para que el próximo deploy la despierte sola y dé rastro en `list`/`parte` en vez de quedarse invisible como hasta ahora.

