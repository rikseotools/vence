### [T-352] 🟠 [ABIERTO 31/07] El navegador de un usuario se cree otra cuenta: manda un `userId` que ya no existe

- **CÓMO SE VIO (31/07, revisando las señales de [T-340]):** `rdiazprados@gmail.com` intentó comprar premium **17 veces entre las 05:54 y las 06:04** y recibió 403 en todas. Su sesión era válida y el token decía correctamente quién era. Lo que fallaba: **su navegador mandaba en el cuerpo el id `140ef91a-2d5a-4f36-a38a-c872467763a8`, que NO existe en `user_profiles`.** Acabó pagando a las 06:10, probablemente tras recargar.
- **NO es solo el pago.** A las 05:50, antes de intentar comprar, sus llamadas a `/api/v2/user-stats` ya daban 401 **con ese mismo id fantasma**. O sea que la aplicación entera le estaba respondiendo como a otra persona: lo que vio en pantalla durante esos minutos no era su cuenta.
- **Alcance medido:** 1 usuario en 30 días (16 rechazos). Poco — pero es un usuario que estaba pagando, y el patrón es silencioso: sin las señales de T-340 no habría constancia de ninguno.
- **YA MITIGADO, que no arreglado ([T-340], 31/07):** en la ruta de pago el contraste de identidad dejó de cortar (`alDiscrepar: 'seguir-con-el-token'`), así que un cliente en este estado ya puede pagar. La alerta `cobro_bloqueado_auth` avisa si vuelve a pasar. **Pero la causa sigue ahí:** ese navegador continúa creyéndose otra cuenta hasta que recarga.
- **LO QUE FALTA — la pregunta antes que el parche:** de dónde sale ese id. Las tres hipótesis, en orden de probabilidad: (a) identidad cacheada en `localStorage`/`AuthContext` de una cuenta anterior en ese mismo dispositivo, que no se limpia al cambiar de sesión; (b) una cuenta borrada cuyo id sobrevive en el cliente; (c) un id que nunca existió, escrito por un bug de hidratación. **Comprobar primero si `140ef91a` aparece en `auth.users`, en backups o en alguna tabla huérfana**: si nunca existió, la hipótesis cambia entera.
- **El arreglo, cuando se sepa la causa:** al acuñar el token, si la identidad que el cliente tiene cacheada no es la del token, limpiarla y recargar el estado — con su señal, para poder medir cuántos estaban así sin saberlo.
- **Relacionadas:** [T-340] (el contraste que lo destapó), y los **38 endpoints fuera de `/api/stripe`** con el mismo patrón de `userId` sin verificar, que siguen sin auditar.

- **🔬 CAUSA REPRODUCIDA (07/08), no supuesta — y descarta la hipótesis (a) para ESTE caso.** Medido en `observable_events` sobre el id real `140ef91a…`: **247 eventos en 3 días** (29-31/07), **44 acuñados de `/api/auth/token` con `http_status=200`**, y en NINGUNO de ellos `metadata->>'identityMismatch'` es `true` — el token está `verified:true` como ESE id, no hay un id "reclamado" distinto del "verificado". Esto descarta que sea el mismo mecanismo que [T-434] arregló después (`identidad_ajena_no_drena`, id legado de Supabase en `?userId=` junto a un token sano): aquí hay UNA sola identidad, autoconsistente, y esa identidad no tiene fila en `user_profiles` desde el primer evento medido.
  - **El mecanismo, leído en el código, no adivinado:** `decidirReintentoPerfil` ([T-434], `lib/auth/reintentoPerfil.ts`) es lo único que revisa `token.appUserId` en cada rotación de sesión (`jwt` callback, `lib/auth/authjs.ts`) — pero su PRIMERA comprobación es `if (cadenaNoVacia(t.appUserId)) return 'ya_resuelto'`. Solo repara cuando está VACÍO. En cuanto tiene CUALQUIER valor, lo da por bueno **para siempre**: nunca vuelve a comprobar que esa fila SIGA existiendo en `user_profiles`.
  - **`canonicalSubForToken` ([T-245]) SÍ hace esa comprobación** (existencia + reconciliación por email) — pero vive en `/api/auth/token`, un endpoint DISTINTO sin forma de escribir de vuelta en la cookie de sesión de Auth.js. Cura el ACCESS TOKEN de esa llamada concreta; dos llamadas después, con el mismo token de sesión sin tocar, hay que curarlo desde cero. **Medido en producción (06/08):** un usuario (`cd6af90f…`) reconciliado a `989c434d…` **5 veces en 2 días** — la cura nunca se quedaba pegada porque no había dónde pegarla.
  - **Y el mecanismo SIGUE naciendo huérfanos HOY, no solo el 31/07:** `auth_sub_reconciliado` con `resultado='huerfano'` tiene eventos el **30/07, 05/08 y 06/08** (id `621a755a…`, `f9238416…`, entre otros) — 6 disparos seguidos de `f9238416…` el mismo 06/08 a las 17:48, un burst típico de varios componentes acuñando token a la vez.
  - **SOSPECHO, sin poder confirmarlo con los datos a los que tengo acceso:** que la causa de FONDO es el borrado de cuentas (hipótesis b). Verificado leyendo `app/api/admin/delete-user/route.ts`: **no revoca ni invalida la sesión del usuario** (sin `signOut`, sin cookie clearing, sin lista de revocación) — y las sesiones Auth.js son JWT sin estado en servidor, así que un navegador que ya estaba logueado seguiría presentando el id borrado hasta que el token expira o hace logout explícito. No pude confirmarlo para `140ef91a` en concreto: no hay tabla de bajas accesible con `VENCE_LECTOR_URL` (`deleted_users_log` no existe con ese nombre ni aparece en `information_schema` para este rol). Queda para quien tenga el acceso de revisarlo.
- **✅ ARREGLADO (07/08) — mismo patrón que [T-434]/[T-245], no arquitectura nueva.** Núcleo puro `lib/auth/revalidacionPerfil.ts` (`decidirRevalidacionPerfil`): con `appUserId` puesto, revalida su existencia como mucho una vez por hora (ventana igual de barata que la de T-434 — lookup por PK, microsegundos, mismo coste que ya paga `/api/auth/token` en cada mint). Cableado en el callback `jwt` de `authjs.ts`, en el MISMO `try/catch` que el reintento (una reparación no puede tumbar aquello que repara):
  - Reconciliado por email → `token.appUserId` se ACTUALIZA con el id bueno (esta vez sí se queda pegado, porque escribe en la cookie).
  - Huérfano (ni el id ni el email resuelven) → se LIMPIA `token.appUserId`, así que el reintento normal de T-434 lo recoge en la siguiente rotación — sin duplicar esa lógica.
  - Evento `auth_perfil_revalidado` (`resultado: reconciliado|huerfano`) + regla de alerta nueva `RULE_PERFIL_REVALIDADO_HUERFANO` (dispara con 1 solo huérfano en 24h, igual que `alta_sin_perfil` — no hay volumen mínimo aceptable para alguien que no puede pagar).
  - **Capas:** 10 tests del núcleo puro (`__tests__/lib/auth/revalidacionPerfil.test.ts`) + 11 de guardarraíl de cableado (`__tests__/guardrails/perfilSeRevalida.guardrail.test.ts`, mismo patrón de inspección estática que `perfilSeReintenta.guardrail.test.ts` — anti-mutación: si alguien borra la llamada, el guardarraíl lo caza aunque el núcleo puro siga en verde) + 6 tests de la regla de alerta. 290 tests de la familia auth en verde (sin regresión), 338 del fichero de reglas de alerta en verde. `tsc` y `eslint` limpios en frontend y backend.
  - **NO PUDE probar de extremo a extremo** (crear una cuenta, borrarla, comprobar que la sesión se autocura) — exige escritura en `user_profiles`/Auth.js real que este worker no tiene (`VENCE_LECTOR_URL` es solo SELECT). Queda para quien despliegue: mirar `auth_perfil_revalidado` a las 24-48h — si `huerfano` sigue apareciendo a diario es la confirmación de que la causa de fondo (borrado sin revocar sesión) sigue viva y hay que ir a por ella directamente, no solo mitigar el síntoma.
  - Cambio de **frontend** (el callback `jwt` corre en Next.js) + **backend** (la regla de alerta): no se activa hasta el próximo deploy de los dos.

- **✅ RE-VERIFICADO (08/08, w3) — rama extraída limpia + cada afirmación re-comprobada contra
  datos reales, no dada por buena; una cifra corregida.**
  - **Rama extraída a `flota/T-352-perfil-revalidado-limpia` (commit `bf5a63d90`), pusheada.** El
    commit `19cce5770` vivía apilado sobre la rama de otra ficha (T-328), sin extraer contra
    `origin/main` actual — cherry-pick limpio, con un conflicto real (no cosmético) en
    `backend/src/alerts/alert-rules.spec.ts`: dos ramas habían añadido sendos bloques `describe`
    nuevos al final del fichero (T-295/T-607/T-401 en `main`, T-352 en esta rama) y git no pudo
    intercalarlos solo. Resuelto a mano conservando los tres — ninguno se pierde.
  - **Re-medido `140ef91a…` contra `VENCE_LECTOR_URL` en vivo:** 247 eventos, rango
    29/07→31/07, 44 acuñados de `/api/auth/token` con 200, 0 con `identityMismatch=true` — los
    cuatro números coinciden EXACTOS con lo ya escrito. `user_profiles` sigue devolviendo
    `permission denied` para este rol, confirmando que la comprobación directa contra esa tabla
    sigue sin ser posible (no es que se me olvidara intentarlo).
  - **Corrección de una cifra, no de la conclusión: `cd6af90f…` fue reconciliado 7 veces, no 5.**
    Re-consultado el evento completo (no solo `subOriginal`, también `subAcunado` y `resultado`):
    7 filas `auth_sub_reconciliado`/`reconciliado`, todas `cd6af90f… → 989c434d…`, entre 05/08
    21:11 y 07/08 10:40 (~37,5 h). El punto que sostenía la cifra ("la cura nunca se queda
    pegada") queda IGUAL de fuerte — más, si acaso: son 7 fallos de persistencia, no 5.
  - **`auth_sub_reconciliado` con `resultado='huerfano'` re-confirmado por día:** 30/07 (1),
    05/08 (2), 06/08 (6) — coincide con "sigue naciendo huérfanos HOY, no solo el 31/07".
  - **La regla `RULE_PERFIL_REVALIDADO_HUERFANO` releída en `alert-rules.ts`:** consulta
    correcta contra `auth_perfil_revalidado`/`resultado='huerfano'`/24h, `shouldFire` con
    cualquier `veces > 0`, registrada en `ALERT_RULES`. Con los huérfanos ya medidos arriba
    (30/07, 05/08, 06/08), esta regla habría disparado ya varias veces si hubiera estado viva —
    confirma que el hueco de vigilancia era real, no hipotético.
  - **`delete-user/route.ts` re-grepeado:** cero menciones de `signOut`/`revoke`/`invalidat`/
    `session` — sostiene la sospecha (declarada como sospecha, no como certeza) de que el borrado
    de cuenta no revoca la sesión.
  - **Lint del bloque insertado, aislado del ruido del fichero:** `alert-rules.ts` tiene **79
    errores de eslint YA EN `origin/main`, sin tocar nada de esta ficha** (medido con `git
    stash` + eslint sobre el HEAD limpio) — deuda preexistente en un fichero de 7.000+ líneas.
    Las líneas 6433-6467 (el bloque `RULE_PERFIL_REVALIDADO_HUERFANO` que aporta esta ficha):
    **cero errores**, aislado explícitamente por rango de línea. No se ha tocado la deuda
    preexistente — está fuera de alcance de esta tarea.
  - **Tests re-corridos:** 21/21 del núcleo+guardarraíl propios, 648/648 de `backend/src/alerts`,
    1.460/1.463 del sweep `__tests__/auth` + `__tests__/lib/auth` + `__tests__/guardrails` (los 3
    fallos son `temarioPdfQueue.integration.test.ts`, `permission denied for table
    temario_pdf_jobs`, mismo bloqueo de escritura de siempre, no relacionado). `tsc --noEmit`
    limpio en frontend y backend.
  - **Nada desplegado por mí** (regla dura del turno). El "falta" sigue siendo el mismo: mirar
    `auth_perfil_revalidado` 24-48h tras el deploy, y la prueba de extremo a extremo (crear →
    borrar → ver autocurar) que ningún worker de lectura puede hacer.


