### [T-127] ✅ [CERRADA 26/07] Leyes DUPLICADAS: dos filas de `laws` para la misma norma, y el trabajo de una no sirve a la otra
- **Qué era:** conviven varias parejas de filas en `laws` que son la MISMA norma (mismo id del BOE). Como la pregunta cuelga del `article_id`, **las preguntas de una fila no se sirven en los temas escopados a la otra**: el banco queda partido en dos mitades que no se ven y todo lo que se haga sobre esa norma (generar, completitud, incisos TC, monitoreo BOE) cuesta el doble.
- **Gotcha que casi lo tapa:** una de las filas tenía `laws.is_active=false` **y eso NO la desactivaba**: sus artículos estaban activos, tenía 56 preguntas vivas y 6 temas escopándola. Lo que se sirve lo gobiernan `topic_scope` + `articles.is_active`, **no el flag de la ley**. Nunca dar por muerta una ley por su `is_active`.
- **Censo fiable (por id del BOE, no por nombre ni por número de norma):** **18 grupos duplicados**, de los que solo **6 tenían las DOS filas escopadas** (los que parten trabajo de verdad); los otros 12 tienen la segunda fila inerte (0 temas, 0 preguntas) y **no urgen**. ⚠️ Censar por *número* de norma da falsos positivos: «10/2025» agrupa la Instrucción de Detención Policial con el Reglamento del Parlamento de Andalucía, que no tienen nada que ver.
- **✅ CONSOLIDADOS LOS 6** (censo posterior: **0 grupos partiendo trabajo**) con `node scripts/consolidar-ley-duplicada.cjs <slug-muerta> <slug-viva> [--apply]`, y el efecto es grande porque los temas pasan a ver el banco entero:
  - **LPRL** (56 preguntas re-ancladas, 6 temas): SERMAS T13 **52 → 1.092** · País Vasco T11 ×2 **30 → 752** · UMU T308 **30 → 713** · Almería T8 **32 → 578**.
  - **LO 6/1984 Habeas Corpus** (25 preguntas, 2 temas): IIPP T110 y Ejecución Penal PV T106, **3 → 106** cada uno.
  - **RD 364/1995** (15 preguntas, 1 tema): IIPP T9 **2 → 12**. · **RD 488/1997 Pantallas** (2 temas): CyL T28 y Castilla y León T511 **0 → 10**. · **IV Convenio AGE** (1 pregunta, rama de FUSIÓN de listados): admin-estado T408 **0 → 45**.
  - **RDL 670/1987** (Clases Pasivas): CARM T21 **0 → 40**. Este abortó al principio y **destapó dos clases de problema que valía la pena resolver en el núcleo, no a mano**:
    1. **Nomenclatura divergente del mismo artículo.** Una fila lo llama `37 quater` y la otra `37 quáter` — un acento, el mismo precepto, los mismos 1.227 caracteres. La igualdad exacta lo daba por inexistente. Núcleo puro `lib/salud/emparejarArticulos.js` (10 tests): normaliza acentos, mayúsculas y espacios, **pero NO ordinales ni números** (`37 bis` ≠ `37 ter` ≠ `37`), y **se planta si hay ambigüedad** en vez de adivinar. Los emparejados por normalización se IMPRIMEN, para que un humano los mire.
    2. **Artículo que solo existe en la fila muerta** (el `preámbulo`): antes abortaba; ahora se **re-parenta** (`articles.law_id` a la superviviente) conservando contenido y referencias. No se copia ni se inventa nada.
    3. **Y un fallo silencioso que habría introducido yo:** al mover el `topic_scope`, el número `37 quater` habría quedado apuntando a un artículo que la superviviente llama `37 quáter` → `scope_phantom_article`, un tema sirviendo 0 preguntas de ese artículo **en silencio**. Ahora los números se **reescriben a la nomenclatura de la superviviente** al mover el scope.
- **Preguntas varadas rescatadas:** 2 grupos tenían preguntas en una fila SIN temas (nadie las veía). Consolidada la que tenía destino servido (`Orden 01/02/1996`). La otra (`BOE-A-2014-879`) tiene **las dos filas sin escopar**: consolidar no cambiaría nada para el usuario, se deja.
- **La herramienta, con sus capas de seguridad** (`scripts/consolidar-ley-duplicada.cjs`): exige que ambas filas compartan **id del BOE** (un slug parecido no basta); **aborta** si algún artículo con preguntas o escopado no existe en la superviviente; **compara el texto artículo por artículo** y exige `--acepta-diferencias` por encima del 2% (mover una pregunta a un texto distinto puede invalidar su clave); sin `--apply` es SIMULACIÓN; con `--apply` va en **una transacción** y verifica al final que **ningún tema sirve menos que antes**. **No borra nada:** las tablas históricas y de analítica siguen apuntando a los artículos viejos a propósito.
- **Caso real que justifica la capa del 2%:** en la LPRL el art. 7 difería un 2,5%. Verificado contra el BOE, la superviviente era la **vigente** (incluye *«y respecto de las personas trabajadoras autónomas»*, Ley 1/2026), y la única pregunta afectada citaba el **encabezado del 7.1**, idéntico en ambas → seguro mover. Consolidar, además, **actualizó el texto** que veían esos 6 temas.
- **Al terminar cada consolidación:** `refresh_topic_question_summary()` + invalidar tags `test-counts`/`questions`/`temario`/`laws`/`test-config`. Hecho para las 5.
- **Origen:** T-112, sesión `sesion-26jul-d` (26/07), midiendo el universo de `article_no_coverage`: los arts de la LPRL salían huérfanos **por duplicado**, uno por cada fila de ley.


