### [T-304] ✅ [HECHA 01/08] El límite por dispositivo llevaba TRES MESES construido y sin cortar ni una vez

> **⚠️ Se le QUITÓ el `✅` el 31/07 en el triaje de [T-429]: la cabecera decía «HECHA» y en `backlog_tasks` sigue `open`, esperando el deploy del frontend — y ahí manda la BD.** Comprobado, no supuesto: la versión viva es `a933bd3e` (`/api/version`) y el commit que la despierta, `96c00e254`, es **19 commits POSTERIOR**, así que el frontend no lo lleva. Mientras solo esté vivo el backend el corte está a medias y el agujero sigue abierto por la otra superficie. Lo que falta al desplegar está en su `pause`: (1) desplegar frontend, (2) verificar que aparecen `device_daily_limit_blocked` con `mode:enforce` —el único evento de los 7 días previos salía con `mode:shadow`, midiendo y dejando pasar—, (3) rollback poniendo los dos parámetros SSM en `shadow` si algo va mal. **Que el deploy termine NO es la verificación.**
- **✅ VERIFICADO EL 01/08 QUE EL CORTE ESTÁ VIVO (sesión `t115-huerfanos`), y DECISIÓN DE MANUEL sobre el único caso afectado.**
  - **Las cuatro comprobaciones, hechas contra producción y no supuestas:** el frontend vivo (`ed51de37`) **contiene** el commit que la despierta, 115 commits atrás · las dos task defs vivas (`vence-frontend:593`, `vence-backend:153`) **referencian** el parámetro —el cableado idempotente aguantó— · los dos SSM están en **`enforce`** desde el 31/07 16:17 · y las tareas vivas **arrancaron el 01/08 entre las 01:19 y las 08:37**, posteriores al cambio, así que lo leyeron. ECS lee los secretos al ARRANCAR la tarea: cambiar el parámetro sin redeploy no habría servido de nada, y por eso se comprobó la hora de arranque y no solo el valor.
  - **Cobertura de huellas v2 creciendo sola con el tráfico:** 543 dispositivos, con picos de 283 y 159 al día. Ya no es un corpus vacío.
  - **⏳ SIGUE SIN PODER CERRARSE:** no ha aparecido **ni un solo** `device_daily_limit_blocked` con `mode:enforce`. El único evento de toda la historia es de `shadow` (31/07 09:44). El criterio de cierre de esta ficha es ver el primer bloqueo real, y que el deploy esté vivo NO es la verificación.
  - **El único caso que el corte afecta, investigado a fondo a petición de Manuel — son DOS PERSONAS REALES compartiendo ordenador:** `trabajospilarfreire@gmail.com` (23, Nigrán, alta marzo, *auxiliar administrativo Galicia*) y `javiergalinanesvarela@gmail.com` (31, Villanueva de la Serena, alta 27/07, *tramitación procesal*). Misma huella v2 y solo ellos dos; **se turnan en el mismo equipo con 2-16 minutos de diferencia**, tres mañanas seguidas sobre las 09:40. **Lo que descarta la segunda cuenta para evadir: preparan oposiciones DISTINTAS** — saltarse tu tope para estudiar el temario de otra oposición no tiene sentido. Y descarta la colisión de huella: dos máquinas distintas no se alternan cada mañana a la misma hora.
  - **DECISIÓN DE MANUEL (01/08): se les corta.** Es coherente con la regla que ya fijó —*«las cuentas son individuales, no por familias»*, dos personas en casa = dos dispositivos—. Se deja en `enforce`, no se sube el tope y no se vuelve a `shadow`. Queda anotado que el caso se miró uno a uno antes de decidir, que es lo que el script pedía.
  - **⚠️ Y AL INVESTIGARLO APARECIÓ [T-450], que deja esta ficha en entredicho mientras siga abierta:** el **modo examen no cuenta para el límite diario** — 10.181 respuestas en 7 días de 136 usuarios sin pasar por el contador. El propio Javier contestó **489 preguntas en 4 días** con el contador marcando ~65, y 432 de ellas eran de exámenes. Es decir: **el tope se rebasa 7× sin necesidad de segunda cuenta ni de compartir equipo**. Mientras eso esté abierto, el límite por dispositivo corta a quien comparte ordenador y deja pasar a quien abre un examen.
- **Cómo salió:** Manuel lo planteó de frente — *"tenemos fiabilidad al 100% de que un free cierra sesión y abre con otro email en el mismo dispositivo para saltarse el límite, y no lo cortamos"*. Y añadió el dato que lo cambiaba todo: *"ya hay mucho hecho de antes y les salía una pantalla de bloqueo"*.
- **Tenía razón las dos veces.** El enforcement existe desde el **17/04/2026** (commit `e8f1724ed`): función SQL, `checkDeviceDailyUsage`, cableado en 4 endpoints del frontend **y** en el backend, con tests y pantalla de bloqueo. Y en 30 días **0 bloqueos**, frente a **1.095** del límite por cuenta. Mientras tanto, 3-11 dispositivos al día pasándose del tope.
- **La causa: el ANCLA, no el bloqueo.** Se agrupaba por el `device_id` de `localStorage`, que se borra en dos clics. Prueba medida: el trío `suusyyr`/`susanaborgesr`/`susistrawberryy` aparece bajo **tres `device_id` distintos**; el 29/07 rotaron en 40 minutos (20:40→20:54, 21:03→21:11, 21:14→21:20), 25 preguntas cada una.
- **Por qué no valía la solución obvia:** `hw_fingerprint` (v1) sí sobrevive al borrado, pero **colisiona hasta 83 cuentas** — hash casero de 32 bits y, sobre todo, el canvas recortado con `toDataURL().slice(-50)`, que se queda con el CIERRE del PNG, casi idéntico entre equipos. Se estaba tirando la señal de >99% de unicidad y guardando su parte constante. Agrupar por ahí habría apagado a decenas de usuarias que solo comparten modelo de móvil.
- **Qué se hizo:**
  - **Huella v2** (`lib/security/fingerprint/`): SHA-256 sobre canvas COMPLETO + WebGL vendor/renderer + AudioContext + `deviceMemory` + `hardwareConcurrency` + pantalla **normalizada por orientación** + zona horaria/idioma/plataforma. Estado del arte 2026: son las señales dominantes.
  - **La estabilidad como criterio rector:** una huella que cambia entre visitas es PEOR que no tener huella (el contador se reinicia solo y el enforcement vuelve a ser mudo creyéndose vivo). Por eso: girar el móvil no cambia la identidad, una señal ausente ocupa su sitio con `na` en vez de desplazar al resto, el audio se redondea, y **borrar `localStorage` devuelve la MISMA huella** — invariante fijado por test.
  - **`get_device_daily_usage_v2(device_id, fingerprint)`**: unión de ambas señales, **ignorando las huellas v1 a propósito**. Aditiva: nunca cuenta menos que la anterior (verificado sobre 200 dispositivos reales, 0 regresiones). Nombre nuevo → desplegar y revertir sin tocar la base.
  - **Cableado en los DOS caminos** (frontend y backend), porque `answer-and-save` hace proxy al backend y en ese caso el antifraude local no se ejecuta: arreglar solo uno dejaba el agujero abierto por el otro.
  - **Cada bloqueo deja rastro**: evento `device_daily_limit_blocked` (con `anchor`, para saber si lo cazó la huella o el `device_id`) **y anotación en el perfil** vía `fraud_watch_list` —tabla que YA existía con 894 filas y llevaba sin escribirse desde el 17/04—, idempotente por usuario y respetando el motivo si ya estaba fichada por otro detector. `confirmed_fraud` lo sigue poniendo una persona.
  - **Alerta `device_limit_mudo`**: dispara si hay farmeo sostenido y CERO bloqueos en 3 días. Es la que habría avisado en abril. Contra producción hoy diría: **18 device-días, 0 bloqueos → dispara** (correcto, el arreglo aún no está vivo); se callará sola cuando empiece a cortar.
- **Decisión de producto (Manuel):** tope por dispositivo **25, el mismo que por cuenta** — *"las cuentas son individuales, no por familias"*, y *"hay que ser muy riguroso con las cuentas free"*. Propuse 50 para dejar sitio a una pareja real y lo descartó. La medición respalda que el daño es nulo: **0 device-días con UNA sola cuenta se ven afectados**, porque a quien usa su equipo solo ya lo topa su propio límite.
- **Capas:** 25 tests del núcleo de la huella (estabilidad, discriminación, vector conocido de SHA-256, degradación sin Web Crypto → `null` y nunca una huella inventada, y el invariante de que borrar el almacén no cambia la identidad) · 7 tests de la alerta · verificación del upsert **contra la BD real** con sonda creada y borrada (crea, acumula al reincidir, respeta el motivo previo) · paridad vieja↔nueva sobre 200 dispositivos · la query de la alerta ejecutada contra producción.
- **🕶️ ENCENDIDO EN DOS TIEMPOS (añadido a petición de Manuel: *"se tiene que quedar trackeando para asegurarnos a ciencia cierta que funciona y no queremos cortar el servicio a usuarios legítimos"*).** Tenía razón y era un fallo de mi entrega: tal como la dejé, al desplegar cortaba. Ahora hay `DEVICE_LIMIT_MODE` con **`shadow` por defecto** — evalúa y registra lo que habría pasado, sin bloquear a nadie. Un despliegue sin decidir MIDE, no corta; y un typo en la variable tampoco se lee como `enforce`. En sombra **no se marca el perfil**: fichar a alguien por un bloqueo que no ha ocurrido sería ensuciar su ficha con una sospecha sin validar. Espejo en el backend + guardarraíl de paridad (26 tests), porque `answer-and-save` reparte tráfico entre los dos caminos.
- **La decisión de activar se toma con datos, no a ojo:** `npm run fraude:sombra-device -- --dias 2` lista **a quién se habría cortado**, uno a uno, con su actividad real y **con qué otras cuentas comparte equipo** — el dato que separa una granja de una coincidencia. Si dice "sin registros", comprueba antes la cobertura de huellas v2: cero datos puede ser "no hay farmeo" o "no estamos midiendo", y el script distingue las dos cosas.
- **⏳ NO surte efecto hasta desplegar Y hasta que los usuarios visiten la web** con el código nuevo: la huella v2 se calcula en el navegador, así que el corpus se llena solo con el tráfico. Mientras tanto la función ignora las huellas v1 y el comportamiento es el de hoy (fail-open). **Verificación tras el deploy:** (1) `SELECT count(*) FROM user_devices WHERE hw_fingerprint LIKE 'fp2\_%'` creciendo; (2) aparecen eventos `device_daily_limit_blocked`; (3) la alerta `device_limit_mudo` deja de disparar.
- **Lo que queda fuera a propósito:** la prueba E2E con `vence-sim` (Playwright). Simular dos cuentas en el mismo navegador es el escenario natural para ella, pero sin huellas v2 en el corpus la prueba mediría el estado viejo. Tiene sentido en cuanto haya cobertura, no antes.
- **Relacionado:** [T-095] (por qué el enforcement de uso se descartó en su día: sigue siendo cierto que el upside de ingresos es 0 — esto se hace por integridad del límite, no por caja), [T-308] (mismo patrón para premium compartido), `docs/runbooks/revisar-fraudes.md` §límite por dispositivo.


#### 🚢 ESTADO FINAL DE LA SESIÓN DEL 30/07 — DESPLEGADO Y VIVO (`67ae7f56`, frontend + backend)

**Todo lo de abajo está EN PRODUCCIÓN desde el 30/07.** Lo que falta es medir, no construir.

**1. El diagnóstico que lo abrió todo.** El enforcement por dispositivo existía desde el **17/04**
(commit `e8f1724ed`: función SQL, `checkDeviceDailyUsage`, cableado en 4 endpoints del frontend Y
en el backend, tests y pantalla de bloqueo) y **en 30 días no cortó ni una vez** — frente a 1.095
bloqueos del límite por cuenta. No fallaba el bloqueo: fallaba el **ancla**. Se agrupaba por el
`device_id` de `localStorage`, que se borra en dos clics. Prueba: el trío
`suusyyr`/`susanaborgesr`/`susistrawberryy` aparece bajo **tres `device_id` distintos** y el 29/07
rotó en 40 minutos (20:40→20:54, 21:03→21:11, 21:14→21:20), 25 respuestas cada cuenta.

**2. Huella v2** (`lib/security/fingerprint/`): SHA-256 sobre canvas COMPLETO + WebGL
vendor/renderer + AudioContext + `deviceMemory` + `hardwareConcurrency` + pantalla normalizada por
orientación. La v1 colisionaba **hasta 83 cuentas** porque recortaba el canvas con
`toDataURL().slice(-50)` —el cierre del PNG, casi idéntico entre equipos— y hasheaba con 32 bits.
**Criterio rector: ESTABILIDAD.** Borrar `localStorage` devuelve la MISMA huella (invariante fijado
por test); girar el móvil no cambia la identidad; una señal ausente ocupa su sitio con `na`.

**3. `get_device_daily_usage_v2(device_id, fingerprint)`** — unión de ambas señales, **ignorando a
propósito las huellas v1** (agrupar por ellas apagaría a decenas de usuarias que solo comparten
modelo de móvil). Aditiva: 0 regresiones sobre 200 dispositivos reales.

**4. Encendido en DOS TIEMPOS** (`DEVICE_LIMIT_MODE`, defecto **`shadow`**): mide sin cortar. Un
despliegue sin decidir MIDE; un typo en la variable tampoco activa el corte. Espejo en el backend
con guardarraíl de paridad (26 casos), porque `answer-and-save` reparte tráfico entre los dos
caminos.

**5. CORTE DIRIGIDO YA ACTIVO** a los **8 dispositivos / 25 cuentas** confirmados por revisión
manual (`esFraudeConfirmado`, caché 5 min, fail-open). Se les aplica **aunque el modo global siga
en `shadow`**: son casos verificados uno a uno, ahí no hay duda que resolver con más datos. El trío
pasa de **75 a 25** preguntas/día; el de 4 cuentas, de 100 a 25; las cinco «Rocío», de 60 a 25.
**No es bloqueo de cuenta**: las cuentas del mismo equipo comparten los 25 del día.

**6. El mensaje NO delata el mecanismo.** Había DOS textos (cuenta vs dispositivo) y bastaba
compararlos para deducir cómo contamos — y de ahí, coger otro móvil. Ahora es **uno solo e
idéntico** en las 5 rutas, con guardarraíl. La evasión que queda no es gratis: exige cuenta nueva
**y** dispositivo distinto, y rotar equipos deja **más** rastro (lo vigila `evasion_multidispositivo`).

**7. La marca SOBREVIVE al borrado de cuenta.** `fraud_watch_list.user_id` y `user_devices.user_id`
van con `ON DELETE CASCADE`: pedir la baja borraba el historial. La marca persistente vive en
`fraud_confirmations` —tabla que YA existía con la forma correcta y 0 filas—, anclada a la huella y
al **hash SHA-256 de los correos** (nunca el correo), con **retención de 2 años** (RGPD art. 17.3;
«para siempre» es indefendible). Reincidir reinicia la cuenta atrás. Al crear cuenta nueva se
comprueba y se emite `alta_con_marca_previa`: **solo detecta**, bloquear un alta es decisión de Manuel.

**8. El rechazo abre el modal correcto.** El 403 por cupo de dispositivo moría en la cola asíncrona
y el usuario seguía respondiendo mientras sus respuestas se perdían. Ahora dispara
`vence:dailyLimitReached` → modal de Premium. **NO se reutilizó** `deviceLimitReached`, que abre el
de «desconecta un dispositivo»: sería mandarle a arreglar lo que no falla.

**9. Alertas nuevas** (todas validadas contra producción): `device_limit_mudo`,
`session_ip_coverage_drop`, `fraude_confirmado_sin_accion` (**renombrada a `fraude_sin_triar` el
31/07 por [T-426]**: pedía resolver lo confirmado, que no tenía remediación), `evasion_multidispositivo`.

**10. Herramientas:** `npm run fraude:dossier` (expediente de cada confirmada),
`fraude:sombra-device` (a quién cortaría el modo global), `fraude:marcar-confirmadas` (idempotente).

#### ⏳ QUÉ VERIFICAR (nadie ha hecho esto todavía)
1. **Que el corte MUERDE**: `observable_events WHERE event_type='device_daily_limit_blocked'` con
   `metadata->>'dirigido'='true'`. Debería aparecer en cuanto alguna de las 25 cuentas pase del cupo
   compartido. **Si en 48 h no hay ninguno, algo no está cortando** — mirar por qué.
2. **Que crece el corpus**: `SELECT count(*) FROM user_devices WHERE hw_fingerprint LIKE 'fp2\_%'`
   (era 0 antes del deploy). Sin esto, el modo global seguiría ciego.
3. **Que la alerta `device_limit_mudo` deja de disparar** (antes del deploy diría 18 device-días / 0
   bloqueos).
4. **Antes de pasar a `enforce` global**: `npm run fraude:sombra-device -- --dias 2` y revisar
   **uno a uno** a quién cortaría. Si aparece un solo caso que parezca legítimo, NO activar.

#### 📌 CONTEXTO QUE NO ESTÁ EN EL CÓDIGO (decisiones de Manuel, 30/07)
- **Tope por dispositivo = 25, el mismo que por cuenta.** Propuse 50 para dejar sitio a una pareja
  real y lo descartó: *«las cuentas son individuales, no por familias»*, *«hay que ser muy riguroso
  con las cuentas free»*. Los datos lo respaldan: **0 device-días con UNA sola cuenta se ven
  afectados** — a quien usa su equipo solo ya lo topa su propio límite.
- **Cortar YA a los confirmados**, sin esperar a la sombra.
- **Marcar dispositivo Y cuentas**, para que borrar la cuenta y volver a crearla no limpie el rastro.
- Descartado tras medirlo: que `/api/v2/daily-question/status` consultara el dispositivo. Lo usan 8
  componentes, el banner pinta ese número (habría mostrado «75/25 preguntas hoy») y solo ahorraba
  UNA pregunta antes del muro. Lo frenó Manuel: *«igual lo estás rompiendo»*.
- **[T-095] sigue siendo válida** en lo suyo: el upside de ingresos del enforcement es ~0 € (los
  farmers no pagan). Esto se hace por **integridad del límite**, no por caja. Dato que lo matiza:
  quien topa el límite convierte al **4,8 %**, el doble de la media (2,37 %) — el muro es la palanca
  de conversión más fuerte medida, así que conviene que funcione.

- **📊 MEDIDO EN VIVO EL 31/07 (24 h después del despliegue) — el corte dirigido MUERDE, y el hueco que queda es el global.**
  - **El peor equipo está contenido.** `fe135d4a…` (3 cuentas, pico de **75/día**, 1.827 preguntas en 30 días) lleva **dos días seguidos clavado en 25**, que es exactamente el tope. Los otros confirmados, entre 0 y 10 al día.
  - **Pero el modo sombra dejó pasar un equipo a 42 preguntas hoy** (`dirigido:false`, `mode:shadow`, ancla `fingerprint_v2`): un equipo NO confirmado por encima del tope al que no se le cortó porque el modo global sigue en `shadow`. Es el único evento de corte en 7 días, y no era de los confirmados.
  - **Alcance de un `enforce` global, medido:** **35 device-días por encima de 25 en 7 días, en 15 equipos**. De esos, **solo 8 device-días son de los 8 confirmados** → el **77 % del exceso viene de equipos que nadie ha mirado**. Son ~5 device-días al día: el radio de acción es pequeño y acotado.
  - **Y hay cola nueva que el triaje no ha tocado.** Equipos multi-cuenta con >100 preguntas en 7 días: **6, y solo uno está confirmado**. Los otros cinco (`2bbb2177…` 298 preg/2 cuentas, `d60ca3e4…` 297/2, `bff26373…` 238/3, `002999b0…` 175/2, `00677a19…` 149/2) están farmeando ahora mismo con el badge 🚨 **a cero**: el detector confirmó 8 equipos el 21/07 y desde entonces no ha vuelto a levantar ninguno. Confirmar saca del badge, pero **no detectar tampoco lo llena** — son dos silencios distintos y este es el segundo.
  - **La decisión que queda es de Manuel y es una sola:** poner `DEVICE_LIMIT_MODE=enforce` cubre a los 15 equipos de golpe, sin bloquear cuentas a mano y sin ir confirmando de uno en uno. La alternativa —seguir con el corte dirigido— exige un triaje continuo que ya se ha demostrado que no ocurre solo.
  - **✅ REGLA DE PRODUCTO, dicha por Manuel el 31/07 — y no estaba escrita en ninguna parte:** *«el límite free son 25 preguntas/día **por cuenta Y por dispositivo**. Si en una familia hay dos personas, deberán usar dos dispositivos, cada uno con su cuenta.»*
    - **Con eso, la única objeción al `enforce` global desaparece.** Lo que yo apuntaba como *coste* —una familia compartiendo ordenador limitada a 25 entre todos— **es el comportamiento QUERIDO**, no un daño colateral. Queda escrito aquí porque el que venga detrás se va a hacer la misma pregunta al ver el caso `bff26373…` de [T-372], y sin esta línea volvería a frenar.
    - Corolario para el triaje: dos cuentas en un mismo equipo **no necesitan ser la misma persona** para que el tope aplique. La marca de FRAUDE sí exige evidencia (mismo nombre, alta el mismo día, IP compartida); el LÍMITE, no.
  - **🟢 ESTADO AL CERRAR LA SESIÓN DEL 31/07: DECIDIDO Y EN ACTIVACIÓN — ya no espera a nadie, espera a un deploy.** Manuel dijo que sí (ver la regla de producto arriba). Lo hecho, verificado pieza a pieza:
    - **Parámetros SSM creados y comprobados leyéndolos de vuelta:** `/vence-frontend/DEVICE_LIMIT_MODE` y `/vence-backend/DEVICE_LIMIT_MODE` = **`enforce`**.
    - **Cableado idempotente en los DOS scripts de deploy** (commit `202b78cc6`), para que el flag no desaparezca cuando el deploy clona la task def viva. **Ya se ha probado solo:** otra sesión desplegó el backend sin saber nada de esto y su **task def 150 salió con el secreto dentro** — la lista idempotente se propaga sola, que era justo el modo de fallo que había que evitar.
    - **Runbook `revisar-fraudes.md` actualizado**, porque decía *«el límite free es 25/día POR CUENTA → N cuentas = N×25»* y eso pasa a ser **falso**. Es lo que lee quien llega a triar.
  - **⚠️ EL ESTADO INTERMEDIO ES SEGURO, y por eso se pudo cerrar la sesión aquí:** los parámetros ya valen `enforce` pero **el código tiene `shadow` como defecto**, así que mientras el flag no esté en las task defs el sistema **mide y no corta**. Una activación a medias no deja a nadie sin servicio.
  - **LO QUE FALTA, en orden, y ninguna es opcional:**
    1. **Desplegar el FRONTEND.** El backend ya rodaba con la task def 150 al cerrar. Hasta que las **dos** superficies estén vivas el corte NO está completo: `answer-and-save` proxya al backend cuando el canary está activo, y con una sola el agujero sigue abierto por la otra.
    2. **Verificar que corta de verdad:** que aparezcan eventos `device_daily_limit_blocked` con **`mode:enforce`**. Antes de esto, el único evento en 7 días salía con `mode:shadow` — midiendo y dejando pasar. **Que el deploy termine NO es la verificación.**
    3. **Rollback si algo va mal:** poner los dos parámetros SSM en `shadow` y forzar un new deployment. ~5 min, sin build ni task def nueva. Por eso va por SSM.
  - *(Histórico: hasta la decisión, esto era lo que quedaba abierto y por qué)* Lo técnico estaba entero y vivo desde el 30/07; lo que faltaba no lo podía hacer una sesión. Las dos entradas posibles eran:
    1. **ACTIVAR (la regla de arriba ya lo decide) — por SSM, que es el patrón de esta casa, no por `environment`:** un valor en `environment` va **horneado** en la task def y cambiarlo obliga a registrar otra; por **SSM** se resuelve al arrancar la tarea, así que encender o apagar pasa a ser *cambiar el parámetro + forzar new deployment* (~5 min, sin build). Es exactamente cómo van `FEATURE_SHUFFLE_OPTIONS` y `FEATURE_TEMARIO_PDF_PARTES`.
       - Crear `/vence-frontend/DEVICE_LIMIT_MODE` y `/vence-backend/DEVICE_LIMIT_MODE` = `enforce`.
       - Añadir el nombre a la lista idempotente de secretos de **`deploy-frontend.sh`** y **`deploy-backend.sh`** — si no, el siguiente deploy clona la task def viva y el flag desaparece en silencio.
       - **Las DOS superficies, sin excepción:** `answer-and-save` hace proxy al backend cuando el flag de canary está activo, y en ese caso el antifraude local no corre. Con una sola superficie, el agujero sigue abierto por la otra.
       - **La primera vez SÍ hace falta deploy** (hay que meter el secreto en la task def); a partir de ahí, los flips son SSM + new deployment.
       - **Verificar:** que aparecen `device_daily_limit_blocked` con `mode:enforce` (hoy el único evento en 7 días salió con `mode:shadow`, o sea midiendo y dejando pasar).
    2. **Si dice que no:** entonces el triaje manual es la única red, y hay que asumir que **no se sostiene sola** — el detector no levantó ni un equipo entre el 21 y el 31/07 mientras 14 farmeaban en el punto ciego de [T-372].
  - **Lo hecho el 31/07 sobre esta ficha, para que no se repita:** se midió el corte en vivo (muerde), se triaron 5 equipos nuevos a mano, se confirmaron 3 con evidencia y se abrió [T-372] con el punto ciego que explica por qué el detector estaba mudo.

- **✅ CERRADA EL 01/08 — CORTA POR PRIMERA VEZ, y el primer corte es el caso para el que se hizo.** Tras desplegar el frontend (`56aea323`, task def 594) aparecen **6 `device_daily_limit_blocked` con `mode:enforce`** (07:31 y 08:30-08:31), **los seis anclados en `fingerprint_v2`** — que era exactamente el objetivo: el `device_id` de `localStorage` no podía, porque se borra en dos clics.
  - **A quién cortó: `elenapb71@gmail.com` y `helenpb71@gmail.com`.** Variantes del mismo nombre con el mismo sufijo, compartiendo equipo. Es el patrón de granja que el propio `fraude:sombra-device` describe (*«correos que son variantes del mismo nombre → activar»*), **no** una familia. El estreno del enforcement no se lo ha llevado un usuario legítimo.
  - **El rastro está completo:** el evento lleva el `user_id` en su columna (no en `metadata`; ojo al consultarlo, que induce a un join equivocado) más `anchor`, `deviceTotal` y `hasFingerprintV2`.
  - **El otro caso, el de la pareja real, se investigó a fondo y lo decidió Manuel** (ver arriba): se les corta, coherente con su regla de que las cuentas son individuales.
  - **⚠️ AL LEER ESTA FICHA CERRADA:** [T-450] sigue abierta, y mientras lo esté **el tope se rebasa sin necesidad de segunda cuenta ni de compartir equipo**. Este límite cierra una puerta; la otra sigue abierta.
