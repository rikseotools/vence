### [T-089] 🟡 [APARCADA 31/07 — decisión de Manuel] Migración a Koigrid — probada y a favor, pero NO se retoma hasta cerrar T-357 y la periferia

> **⛔ APARCADA POR DECISIÓN DE MANUEL (31/07): «aparca migrar hasta que esté todo esto solventado».** En la tabla queda `status='blocked'`, `blocked_by=['T-357']` y **sin claim**, así que `list` la enseña bloqueada y `next` no la sugiere. **No es un no:** lo técnico está probado y lo económico también ($35/mes contra $619 medidos). Es un **orden**: primero se cierra lo que hay que hacer igual, migremos o no.
>
> **Qué la desbloquea, en concreto:**
> 1. **[T-357]** — cerrar el origen (hoy el ALB acepta `0.0.0.0/0` y la IP `trusted` es falsificable). Es prerrequisito literal del §7-pre: mover el borde sin esto **traslada** el problema en vez de arreglarlo.
> 2. **La periferia auditada en §3-bis** del manual: purga de CDN, propagación de la purga ISR entre réplicas, SLOs/alarmas/canary, y decidir destino del pooler HA y de la Lambda del fetcher.
> 3. **`TRUSTED_EDGE` + el código de IP ya commiteado** (`67e60daff`), pendientes de desplegar.
>
> **🧹 LIMPIEZA AL CERRAR SESION (31/07).** Recursos de koigrid dejados en mínimos: **BD `vence-poc` running**
> (512MB, con `articles`+`laws` reales) y **las 5 apps pausadas** (coste cero, `/resume` en ~40s sin rebuild).
> **Borradas** las dos BD de ensayo: `vence-rehearsal2` (basura de los intentos fallidos, sin snapshot) y
> `vence-cutover-test` **CON snapshot final** — o sea que el ensayo de 24GB verificado es **recuperable** por
> `POST /databases/:id/restore` con el id de la borrada, si algún día hace falta.
>
> **⚠️ Y un cambio que afecta a quien retome:** los volcados locales (~12GB en `~/.cache/koigrid-mig`) se
> **BORRARON a propósito**. Motivo: producción crece a diario, así que en cuanto pasen días el volcado miente,
> y guardarlo da falsa sensación de "ya está hecho". **Re-volcar son ~50 min** y el procedimiento está en §3-ter.
>
> **Lo que NO hay que rehacer al retomarla** (está medido y no caduca): capacidad (615 rps con 1 réplica = 37× el pico), paridad de latencia en página completa (1,02-1,15×), precio (Pro $35), y la **ventana de siembra: ~3h10m** — de ahí sale que el cutover tiene que ser por replicación lógica, no en frío.

> **🖥️ Qué corre en producción, con el origen de cada pieza:** `docs/ARCHITECTURE_ROADMAP.md` §"QUÉ CORRE EN PRODUCCIÓN" — inventario verificado (2 servicios siempre encendidos + 3 tareas programadas). Enlaza de vuelta a esta ficha.

- **💰 SEGUNDA RELEASE del 29/07 (`llms.txt` 810→837, mismas 189 rutas): PUBLICARON LOS PRECIOS.** Era lo único que quedaba abierto. Hecha la cuenta con NUESTROS números medidos:
  - **Lo que necesitamos (medido, no estimado):** app **2 GB × 1 réplica** (el load-test de hoy da 615 rps = 37× nuestro pico, CPU 0%); BD **31 GB de datos sobre 4 GB de RAM** (nuestra RDS es una `db.t4g.medium` Multi-AZ, 100 GB gp3); salida **~370 GB/28 días (~400 GB/mes)**; **43,3 M peticiones/mes**; ~56 GB de vídeo en object storage (ya está en koigrid).
  - **Encaja en Pro ($35/mes):** 4 GB/2 vCPU por app (pedimos 2 GB), 6 GB de RAM total de apps, **BD 4 GB RAM / 50 GB disco (paridad EXACTA de RAM con nuestra RDS**, y 31 GB caben), 100 GB de storage, **2 TB de tráfico (5× lo nuestro)**, 5 réplicas por app.
  - **Starter ($12) NO vale**, y no por los totales sino por el **tope POR APP**: 1 GB de RAM y nuestro contenedor Next.js necesita 2 GB. (Su tabla de costes del `llms.txt` solo lista totales → quien presupueste leyendo solo los docs se equivoca de plan. Reportado.)
  - **📊 La comparación, con nuestra factura real:** AWS **$491/mes** (run-rate medido en Cost Explorer) vs **Pro $35 → 14× más barato**; Scale $89 (8 GB BD / 200 GB disco) → 5,5×.
  - **🔍 Hallazgo que cambia el argumento de venta:** **$40 de nuestro CloudFront son PETICIONES** (43,3 M Tier-2), no bytes — la salida real (~400 GB) es casi gratis. **Koigrid no mide peticiones en absoluto.** O sea, para nosotros el ahorro NO es la historia del egress que ellos cuentan, es que el contador de peticiones no existe.
  - **🔴 Hueco del modelo publicado: el REDIS no está tarifado en ninguna parte** (ni fila en la tabla, ni en el modelo de costes, ni deducible de `/usage`). Nosotros corremos uno (~$8/mes en AWS). Sin eso no se cierra el presupuesto al céntimo. Reportado.
  - **Bueno (menor):** la **retención del registry** que añaden en la misma release resuelve el "¿seguirá ahí mi objetivo de rollback?": guardan N por plan **pero nunca borran una imagen que esté sirviendo o siga siendo rollback-alcanzable**, calculado por ellos. Mejor que las lifecycle rules de ECR que mantenemos a mano.
- **💰 30/07 — LA FACTURA DE AWS HA SUBIDO Y CAMBIA LOS NÚMEROS DE ESTA FICHA: ya no son $491, son $619/mes.** Cost Explorer 01→29/07 = $598,38 en 29 días → **run-rate $619/mes**. Desglose: **ECS $229** (¡la mayor línea, antes era $98!) · RDS $142 · impuestos $107 · CloudFront $46 · VPC $37 · ELB $26 · CloudWatch $16 · ElastiCache $10 · resto $5. **Contra Koigrid Pro ($35) la diferencia es ~$584/mes ≈ $7.000/año.**
  - **⚠️ CORRECCIÓN (30/07, investigado a fondo): "recortar `min-capacity`" NO es la vía rápida al dinero que dije. Me equivoqué en el orden de magnitud.** Medido hoy con la métrica por la que realmente escala el servicio (`ALBRequestCountPerTarget`, objetivo **250/min**): el **pico horario son 12.866 req/target/hora ≈ 214/min → el 86% del objetivo con 8 tareas**. O sea que **de día las 8 tareas son las que pide la política**; bajar `min` no ahorra nada porque el autoescalado las repone. La CPU media del 2,5% engaña: el cuello de este servicio no es CPU media, es event-loop por tarea (ver postmortem abajo).
  - **Dónde SÍ sobra: de noche.** 01:00-05:00 son **391-999 req/target/hora = 6-16/min = un 3-6% del objetivo** con las mismas 8 tareas. Y **ahora mismo no hay ninguna `scheduled-action`** (el pre-warm del postmortem ya no está). Un suelo nocturno programado ahorra ~25 de 192 task-horas/día ≈ **13% de la línea de frontend ≈ $25-30/mes**. **Real, pero un orden de magnitud por debajo de los ~$584/mes de la migración.** La palanca AWS grande sería **subir el objetivo de 250** (con tareas de 2 vCPU cada una aguanta más que cuando se calibró con 1 vCPU en caliente), y la telemetría lo respalda — pero es el ajuste que se tocó durante una caída, así que se mueve por pasos mirando el lag.
  - **📖 DE DÓNDE VIENEN LOS PICOS (postmortem `docs/architecture/incidente-frontend-healthcheck-cascade-21jul.md`): causa raíz conocida.** No es la BD ni la memoria: es el **event-loop de Node (un solo hilo) saturándose en tareas INDIVIDUALES** con trabajo CPU-bound, dominado por **`/api/auth/token`** (el nº1 por goleada, 4,5× el siguiente; firma **RS256** = crypto). Firma inequívoca: `CPUUtilization max=100% / avg=40%` = **una tarea clavada mientras el resto está ociosa** — exactamente los picos del 53-98% que veo hoy. Lo que lo convertía en caída era el **health check de contenedor**: si el loop está bloqueado el handler no llega a ejecutarse, ECS mataba la tarea (que servía, solo iba lenta), su carga caía en las demás → **cascada hasta 0 tareas y 504 (pico 1.698/min)**.
  - **✅ ¿ESTÁN ARREGLADOS? 4 de las 5 capas del plan, SÍ — y la telemetría dice que el loop está sano:** (1) **tareas de 2 vCPU** (eran 1) ✔ vivo; (3) **escalado por `ALBRequestCountPerTarget`** ✔ vivo (250) junto a CPU 55%, memoria 75% y un step-out de ráfaga; (4) **health check tolerante** ✔ (timeout 5→10, retries 3→5, startPeriod 60→180); (5) **`event_loop_lag` medido** ✔ (`lib/observability/eventLoopLag.ts`, recalibrado el 28/07 en T-160) — y **el dato es el que decide: p99 mediano de 22-24 ms sobre un suelo de resolución de 20 ms, y en 7 días el p99 pasó de 100 ms solo 3 veces.** El loop NO se está saturando: los picos de CPU son stalls aislados (arranque en frío, GC), no saturación sostenida.
  - **❌ La capa que FALTA (causa raíz de verdad): sacar el minteo RS256 del hot path.** `lib/api/auth/rs256.ts` sigue firmando en el frontend (la clave sí se cachea). El postmortem lo llama **T-071** y **esa ficha NO existe en el backlog** — hueco a reponer.
  - **🔴 Y EL CABO MÁS SERIO, que no es de dinero: el DRIFT de Terraform sigue abierto.** `backend/infra/frontend.tf` dice **`min_capacity = 2`** y producción está en **8** (toda la contención del 21/07 se aplicó por CLI). **Un `terraform apply` bajaría el suelo de 8 a 2**, que es justo la condición que el postmortem señala para que el incidente vuelva. Eso es más urgente que los $25-30/mes.
  - **Veredicto sobre "¿migramos ya para no pagar tanto?": NO todavía, y el argumento del dinero apunta primero a AWS.** Lo que bloquea no es el precio ni el rendimiento (ambos a favor de Koigrid, ver arriba) sino que **el ensayo de 33 GB NUNCA se ha completado**: todo lo probado es a 624 MB, 1/40 del tamaño, así que **no sabemos cuánto dura la restauración → no se puede fijar una ventana de cutover**, y sin ventana no se planifica una migración. Y ahora mismo está bloqueado del lado de ellos (D3).
  - **Orden recomendado:** (1) recortar el suelo de ECS en AWS (reversible, dinero ya, riesgo cero) — **requiere OK de Manuel, es config de producción**; (2) terminar el ensayo cuando liberen espacio → de ahí sale la ventana; (3) el fix `getClientIp()`/`CF-Connecting-IP` **antes** de mover tráfico; (4) el cambio atómico del storage de vídeos; (5) decidir fecha. **NO tocar DNS ni storage de producción hasta que el ensayo pase.**
- **🔻 30/07 — el ensayo sigue bloqueado y el fallo es MENOS diagnosticable:** usando su changelog como toca (guardar `latest` y preguntar `?since=`) → **`count: 0`**, nada nuevo (contrastado: 876 líneas, 190 rutas). Pero `POST /databases/:id/restore-dump/upload-url` **ahora devuelve `{"error":"internal","errorRef":…}`** — ayer esa llamada iba bien y era la SUBIDA la que fallaba con un `XMinioStorageFull` claro. Regresión de diagnosticabilidad del mismo problema. Nuestro dump de 4,09 GB está hecho y verificado: reintentar son 3 minutos de subida.
- **🐛 30/07 17:39 — ARREGLARON TODO (12 entradas en su changelog) Y EL ENSAYO VUELVE A FALLAR, por un bug de una línea: el plazo se calcula sobre el tamaño COMPRIMIDO.**
  - **Lo suyo funciona:** con el volcado ORIGINAL sin parchear y `preSeed`, pasó de largo las líneas 32, 5.920 y 18.287 → **D4 y D4-bis cerrados por medición**. Y el error nuevo es ejemplar: `restore_timeout` con el tiempo consumido, el tope aplicado y qué pasa con la base. **Ese mensaje me dio el bug en una sola pasada** (ayer, con `restore_failed` pelado, hizo falta una corazonada).
  - **El bug:** sus docs dicen *"~170 MB de SQL EN CLARO por minuto… un volcado de 25 GB en claro recibe ~2,5 h"*. El nuestro **es** un volcado de ~25 GB en claro y recibió **30 minutos**. La cuenta: 25.293 MB/170 = **149 min** (su propio ejemplo) vs 4.092 MB (el gzip) /170 = 24 min → por debajo del suelo → **30 min = lo que nos dieron**. **Miden el fichero subido, no el SQL que expande.**
  - **Afecta a todos:** su API pide gzip, su CLI comprime sola, y su nota dice que un volcado comprime ~8× → **el plazo sale 6-8× corto en toda subida comprimida**. El suelo de 30 min lo tapa en los volcados pequeños (terminan igual) y es fatal en los grandes, que son para los que existe la función. Con el techo nuevo de 20 GB es peor: 20 GB de gzip ≈ 150 GB de SQL y recibirían ~2 h.
  - **Matiz medido (con cuidado, porque ayer me pasé de alarmista y me corrigieron con razón):** el rollback lógico SÍ ocurre (las tablas desaparecen), pero **el espacio NO vuelve "en unos segundos"** como promete el mensaje — 10 GB → 11 GB a los 2 minutos; ayer acabó volviendo horas después. La base está bien; lo optimista es el plazo del mensaje.
  - **➡️ Se cambia de método para medir la ventana:** `pg_dump | psql`, que no tiene tope y es el que usaremos en el cutover. Seguimos prefiriendo su restore gestionado (fail-fast, `tableCounts`, atomicidad son mejores) y se re-probará el día que arreglen la aritmética; el volcado está hecho y guardado.
  - **Dato nuevo para dimensionar:** la subida de 4,09 GB tardó **10 min 47 s** en la flota nueva (~6,3 MB/s) frente a **3 min 09 s** ayer en el disco compartido. A ese ritmo, 20 GB son ~53 min solo de subida.
- **⛔ 30/07 01:38 — ENSAYO DE 33 GB COMPLETADO EN CUANTO A DIAGNÓSTICO: el restore gestionado NO SIRVE a nuestro tamaño. Hay un tope de ~30 min sin documentar.** Y koigrid lo predijo: dijeron que a tamaño real «aparecen los tiempos de espera».
  - **Los 3 intentos, cada uno más lejos:** con `preSeed` → línea **32** (`schema extensions already exists`); sin `preSeed` → línea **5920** (`type extensions.vector does not exist`); con el `CREATE SCHEMA` hecho idempotente + `preSeed` de **vector Y pg_stat_statements** → pasa las tres trampas, carga datos de verdad y **muere a los 1.802,2 s = 30 min 02 s exactos** con `error: "restore_failed"` y `logs: ""`.
  - **D5 (fatal para nosotros):** había cargado **~9,9 GB** de los 24,7 GB → a ese ritmo el volcado completo necesita **~75 min, 2,5× el tope**. **Arreglar D4 no cambia nada: una BD de 33 GB no cabe en ese endpoint hoy.** No está documentado en ninguna parte (grep en `llms.txt`: cero menciones).
  - **Y dos cosas más que salieron ahí:** (a) **el diagnóstico se invierte con la escala** — los fallos pequeños dan `psql:<stdin>:18287: ERROR: …` con fichero, línea y error crudo; el de 30 min da `restore_failed` y logs vacíos. Solo supe la causa porque puse hora a mi bucle de sondeo y el número salió redondo. (b) **la atomicidad (B2) NO cubre que maten el trabajo**: la BD queda con **9.874 MB** ocupados, **2 tablas en `public` donde el clúster virgen tenía 4**, y el esquema `extensions` que el rol `app` no puede borrar (D2) → **un restore que caduca QUEMA el clúster**.
  - **➡️ DECISIÓN PARA EL CUTOVER: se usa `pg_dump | psql` directo al leader, que no tiene tope** (es como se cargaron `articles`+`laws` en 2m39s y como se hizo el clon de 31 GB en julio). Se pierde el fail-fast, el `tableCounts` y la atomicidad, pero **termina**.
  - **⚠️ SIGUE SIN MEDIRSE LA VENTANA DE CUTOVER**, que es para lo que servía el ensayo: el endpoint mata el trabajo antes de poder decir cuánto tarda. **Próximo paso: `pg_dump | psql` de punta a punta, cronometrado.** Ese número es el que decide la fecha.
- **🧪 29/07 19:15 — ENSAYO DE 33 GB: hecho el volcado, y BLOQUEADO por un problema de ELLOS. Dos bugs reales que la prueba pequeña no podía enseñar.**
  - **Lo que sí midió:** `pg_dump` de la RDS (33 GB, 204 tablas) → **24,7 GB de SQL / 4,09 GB gzip**, íntegro; **subida 2m56s**; la restauración falla en **6 min** (fail-fast bueno).
  - **🔴 D1 — `preSeed` y un volcado COMPLETO son incompatibles:** `psql:<stdin>:32: ERROR: schema "extensions" already exists`. `preSeed` crea la extensión **y de paso su esquema**; un `pg_dump` real trae su propio `CREATE SCHEMA extensions;` **sin `IF NOT EXISTS`** (sus `CREATE EXTENSION` sí lo llevan). Es justo el escenario para el que existe `preSeed`. **En la prueba de 624 MB no salió porque un dump `-t tabla` no tiene `CREATE SCHEMA`** — exactamente lo que koigrid avisaba de probar a tamaño real.
  - **🔴 D2 — un `preSeed` fallido deja un resto que NO se puede quitar:** `DROP SCHEMA extensions` → *"must be owner of schema extensions"* (lo crea el superusuario; `app` es el único login que dan). Como el esquema ya existe, **todo reintento del volcado completo muere en la misma línea** → la base queda inservible y hay que borrarla y volver a subir GB. (Lo bueno: **B2 funcionó**, la transacción revirtió todo lo demás.)
  - **⛔ D3 — SU ALMACÉN DE VOLCADOS ESTÁ LLENO y no podemos limpiarlo:** `XMinioStorageFull` en `koi-db-dumps` al resubir. **No es cuota nuestra** (`/usage` a 0 en todas las métricas). Y **no hay endpoint para borrar volcados** (`DELETE` → 405; solo `upload-url`/`restore-dump`/`GET job`), ni borrar la base parece liberar el suyo. O sea: quien siga su consejo de ensayar a tamaño real sube varios GB y **no tiene forma de recuperarlos**. **El ensayo queda bloqueado por esto, sin workaround.**
  - **Detalle de dimensionado:** su `maxBytes` de subida son **5 GB** y nuestro gzip son **4,29 GB** → entramos con un 20% de margen. Una BD del doble que la nuestra **no podría usar el restore gestionado**.
  - **Sigue sin medir (que es el objetivo del ensayo):** si aguanta toda la restauración sin caducar, si el `maintenance_work_mem` llega para el ivfflat **dentro de un restore de 24,7 GB**, si `tableCounts` viene relleno con 204 tablas, y **cuánto dura la ventana real de cutover**. El volcado ya está hecho y verificado: reintentar es una subida.
- **🎯 29/07 19:00 — LA RELEASE QUE CIERRA EL BUCLE: sacan changelog, `/audit` y arreglan casi todo lo que habíamos reportado.** Verificado contra la API, no leído del changelog.
  - **`GET /api/v1/changelog`** (público, sin token; `?since=` filtra; `kind` + `endpoints` por entrada; feed Atom). **Se acabó comparar md5 de `llms.txt`** — y con él se acaba el fallo que provocaba: 2 de nuestros 7 intentos de `scale-out` fueron re-pruebas de una build sin cambios, porque desde fuera una release vacía y una real eran idénticas. Además nos avisan de que `llms.txt` anunciaba **un año de caché** por error suyo (ahora 5 min), lo que explica que algún diff nuestro saliera congelado.
  - **✅ Arreglo 18:40 (referencia `${{}}` sin resolver) VERIFICADO en nuestra propia app.** `web7` tenía `DATABASE_URL=${{db.vence-mig2.DATABASE_URL}}` (BD borrada) y ahora **el despliegue se PARA** nombrando la referencia y explicando por qué. **Efecto colateral bueno:** la apunté a la BD real del POC y **el clon funciona entero por primera vez** — `/api/health` → 200, `overall: ok`, `database: ok` en 38 ms (antes solo servía HTML pre-renderizado).
  - **🔎 Arreglo 16:50 (diagnóstico de réplica): FUNCIONA y RESUELVE LA PREGUNTA DE SIETE INTENTOS.** El registro ya dice: `runner=167.233.84.5`, `image=…vence-koigrid-mig:latest`, y **`no existe: no se llegó a crear` / `No such container: koi-vence-web7-23f37d-r1`**. O sea: **el contenedor de la réplica NUNCA se crea.** Se asigna máquina e imagen, pero nada llega a estar sano ni enfermo → `replica_unhealthy` sigue siendo el código equivocado (les propongo `replica_never_created`).
  - **⚠️ Y una doble corrección NUESTRA, anotada en el journey:** por la mañana dijimos "`runner: null` ⇒ nunca se planificó"; por la tarde **nos retractamos** al aparecer el campo; esta noche los logs muestran que **el fondo de la primera lectura era correcto** (el contenedor no se crea) aunque el mecanismo es más fino (la máquina **sí** se asigna). Inferimos de un campo vacío y luego sobre-corregimos en cuanto apareció: ninguna de las dos merecía la seguridad con que se dijo.
  - **✅ `/audit` (14:00)** — el registro visible para el cliente que pedimos por la tarde (S4), el mismo día. Categorías + `actorType`/`actor`/`ip`; nuestras acciones salen como `token:vence-api`.
- **✅✅ 30/07 — CERRADO: PRODUCCIÓN LEE DE `storage.koigrid.com` y Manuel ha CONFIRMADO que los vídeos se ven bien en la app.** (Autorizado por Manuel; paso 3 de koigrid cumplido.) Koigrid puede borrar la copia vieja.
  - **🛡️ EL PUNTO DE NO RETORNO ESTÁ CUBIERTO — y esto cambia una nota de memoria que era peligrosa.** El rollback rápido (restaurar SSM + volver a `:570`) muere cuando borren la copia vieja, así que se verificó el plan B: **los backups locales `~/vence-video-{faststart,hls}` (41 GB + 52 GB) cubren los 56.691 objetos del bucket con el mismo tamaño** (diff: **0 objetos solo en el bucket**; los 2 extras en local son `_progress.log`). O sea que el peor caso deja de ser "se pierden los vídeos" y pasa a ser "se resuben desde local". **⚠️ Esos dos directorios YA NO son borrables** (la memoria `project-video-cursos-mobile-faststart` los daba por tal — corregida el 30/07): son la ÚNICA copia fuera de koigrid.
  - **Paridad verificada ANTES de tocar nada**, porque el siguiente paso es un borrado irreversible: **56.691 objetos y 99.137.723.146 bytes IDÉNTICOS** en los dos sitios, y **diff objeto por objeto (clave + tamaño) sin una sola diferencia**. Además leídos desde el sitio nuevo los artefactos que pide el player: master `index.m3u8` (200, `application/vnd.apple.mpegurl`), segmento `.ts` (200, `video/mp2t`) y un MP4 de 1,6 GB (206 con Range). 56.541 de los objetos son segmentos `.ts`, así que el camino HLS era el que importaba.
  - **Cómo se hizo (clave):** **deploy SOLO de variables** — task def `:570` → **`:571` con el MISMO digest de imagen** y una variable más. **No subió código, así que no arrastró los 9 commits pendientes de `main`.** Rolling en-sitio (100/80) con circuit breaker; **8/8 tareas convergieron y `www.vence.es` dio 200 durante todo el rollout** (lo fui midiendo). ~28 min, de una en una.
  - **Los tres valores:** SSM `KOIGRID_VIDEO_ACCESS_KEY` + `KOIGRID_VIDEO_SECRET_KEY` (llave nueva, verificada que lee el bucket **antes** de escribirla) y `KOIGRID_VIDEO_ENDPOINT=https://storage.koigrid.com` en el task def. **Bucket sin cambios: `vence-videos`.**
  - **Verificación E2E sin poder darle al play:** ejecuté **nuestra propia `presignKoigridKey`** con el entorno exacto que ahora tienen los contenedores → firma contra `storage.koigrid.com` y **el MP4 y el segmento HLS bajan con 206 y el content-type correcto**. Cadena completa: SSM v2 → `:571` con la dirección → 8/8 tareas en `:571` → nuestro firmador resuelve el host nuevo → bytes reales. `/api/health` en `ok` en los 5 checks.
  - **⚠️ ROLLBACK — ventana limitada:** restaurar los 2 valores de SSM (guardados en el scratchpad de la sesión) + `update-service` a `:570`. **Deja de funcionar en cuanto koigrid borre la copia vieja.** Se asume a sabiendas porque la paridad se verificó objeto por objeto.
  - **➡️ Esto DESBLOQUEA D3:** el borrado de la copia vieja libera el espacio que impedía subir el volcado → **el ensayo de 33 GB se puede reintentar** (nuestro dump de 4,09 GB está hecho y verificado: son 3 min de subida).
  - **Cabo nuestro, anotado:** `lib/api/video-courses/videoSignedUrl.ts` sigue con `https://s3.koigrid.com` como **valor por defecto**. Es inerte (gana la variable), pero debería pasar a variable obligatoria. **NO se tocó a propósito:** con deploys cumulativos, cambiar el defecto antes de rotar credenciales permitía que otra sesión lo desplegara y tirara los vídeos.
- **✅ 29/07 (tarde-noche) — STORAGE DESBLOQUEADO: S1 y S2 CERRADOS, verificado contra el bucket real.** Engancharon `vence-videos` a nuestra organización y emitimos llave nueva: `GET /buckets` ya lo lista (`region: eu-madrid`, `backend: cluster`), la llave nueva **lista los 8 prefijos**, lee un MP4 real (`ContentLength: 1.643.315.283`, `video/mp4`) y el **presigned GET devuelve 206**. O sea: el cambio de producción ya es POSIBLE.
  - **🔴 PERO SIGUE SIENDO ATÓMICO — probadas las dos combinaciones cruzadas y los fleets son ESTANCOS:** llave vieja en `storage.koigrid.com` → `InvalidAccessKeyId`; **llave nueva en `s3.koigrid.com` → `InvalidAccessKeyId` también**. **No hay estado intermedio seguro**: no se puede rotar la llave primero y mover la dirección después (ni al revés) — cualquier mitad suelta deja **todos los vídeos en 403**.
  - **📋 PROCEDIMIENTO (un solo cambio, con rollback):** (1) escribir en SSM `KOIGRID_VIDEO_ACCESS_KEY` + `KOIGRID_VIDEO_SECRET_KEY` con la llave nueva — **las tareas en marcha NO se enteran** (ECS inyecta los secretos al arrancar la tarea), así que producción sigue sirviendo con la vieja; (2) añadir `KOIGRID_VIDEO_ENDPOINT=https://storage.koigrid.com` al task def (hoy NO existe → el código usa su constante `s3.koigrid.com`); (3) desplegar: las tareas nuevas cogen llave y endpoint **a la vez**. Rollback = restaurar los 3 valores y redesplegar.
  - **⚠️ RIESGO DE SESIONES PARALELAS — por eso NO se ha tocado el código:** si se cambia la constante por defecto de `videoSignedUrl.ts` y se pushea, **otra sesión puede desplegar `main` antes de rotar SSM** (el deploy es cumulativo) → endpoint nuevo + llave vieja = vídeos caídos. **El cambio de código y la rotación de SSM tienen que ir en la misma ventana**, no adelantar el push.
  - **Estado:** `s3.koigrid.com` sigue sirviendo producción con normalidad → esto es una migración, no una avería. Sin fecha de apagado conocida (se la hemos pedido).
- **🗄️ 29/07 15:20 — [HISTÓRICO, ya desbloqueado arriba] MUEVEN EL STORAGE a `storage.koigrid.com` y NOS AFECTA EN PRODUCCIÓN (los vídeos de los cursos). NO se ha tocado nada: no se puede completar todavía.** (`llms.txt` 837→865, todo el diff es la sección de storage; openapi igual.)
  - **Qué piden:** cambiar la dirección a `storage.koigrid.com` y sacar llave nueva desde koigrid. El nombre del bucket (`vence-videos`) no cambia. Sus docs ahora dicen **"nunca fijes el endpoint a mano, tómalo de `POST /storage/keys`"** — y nos pilla en falta: `lib/api/video-courses/videoSignedUrl.ts` tiene `https://s3.koigrid.com` como **valor por defecto fijo** y el task def de ECS (`vence-frontend:567`) **no define `KOIGRID_VIDEO_ENDPOINT`** (solo BUCKET/ACCESS_KEY/SECRET_KEY por SSM). O sea, producción va con la constante del código. **Ese arreglo es NUESTRO.**
  - **🔴 BLOQUEADO — la llave que podemos emitir NO ve nuestro bucket.** Medido: llave de producción en `s3.koigrid.com` **funciona** (lista los 8 prefijos; presigned GET de un MP4 real → **206 en 44 ms**); la MISMA llave en `storage.koigrid.com` → **`InvalidAccessKeyId`**; `POST /storage/keys` devuelve bien `endpoint: https://storage.koigrid.com` pero esa llave nueva da **`AccessDenied`** al listar `vence-videos` y **403** en el `HeadObject`; y `GET /buckets` devuelve **`{"buckets":[]}`** pese a que servimos ~56 GB de vídeo desde ahí. Encaja con su propia regla ("la llave alcanza los buckets que posee tu organización"): `vence-videos` está fuera del registro de buckets de esta org.
  - **⚠️ EL RIESGO A NO OLVIDAR: los dos cambios van ACOPLADOS.** Cambiar solo el endpoint (con la llave vieja) = `InvalidAccessKeyId` = **todos los vídeos a 403**. Es la lectura natural del encargo ("cambia la dirección") y es una caída dura de los cursos premium. **No tocar SSM ni el task def hasta tener llave que lea el bucket.**
  - **Qué hace falta de ellos:** que enganchen `vence-videos` a nuestra organización (y aparezca en `GET /buckets`) **o** que nos den una llave autorizada. Pedido en el journey (S1-S4).
  - **Proceso:** apareció en NUESTRO `/tokens` un token `migracion videos (borrar)` (scopes `storage:*`) creado hoy 12:43 y revocado 13:14 — ellos moviendo los bytes. Limpio y revocado, nada se rompió, pero **nos enteramos por casualidad**; les pedimos un evento visible para el cliente.
  - **Estado:** producción intacta y sirviendo vídeo por `s3.koigrid.com`. La llave temporal que emití para la prueba, borrada.
- **✅ 29/07 17:55 — A4 ARREGLADO y PRIMERA RESTAURACIÓN GESTIONADA COMPLETA.** Los dos arreglos que anunciaron llegaron **sin tocar docs** (`llms.txt` byte-idéntico), así que solo se ven probando.
  - **El índice ivfflat ya se construye:** la línea de fallo saltó de la **61219** (`maintenance_work_mem`) a la **61290**. Los dos fallos siguientes fueron **NUESTROS** (el POC es un cascarón: a `laws` le faltaba la PK, y habíamos volcado `articles` sin las filas de `laws` que referencia). Con el volcado completo: **`done` en 267 s**.
  - **Verificado, no supuesto:** `articles`=**61.123** y `laws`=**1.404**, EXACTOS vs RDS; el índice `articles_embedding_idx … USING ivfflat` presente; `owner = app`; 624 MB desde 242 MB gzip. **B1** (reintento con el mismo `dumpKey`, sin resubir) y **B2** (atómico: tras cada fallo `articles` no quedaba) confirmados otra vez. **Es el camino que usaríamos para los 31 GB reales.**
  - **🟡 Pero `tableCounts` vuelve VACÍO en el job que SÍ funciona** (`{"status":"done","tableCounts":[],"logs":"__KOIOK__"}`). Es justo lo que hace fiable una migración sin segunda herramienta; tuvimos que contar por `/query`. Reportado como A5.
  - **🟡 N1 medio arreglado (6.ª reproducción):** ahora sí guarda `runner` (`167.233.84.5`) y `logs` deja de estar vacío — **pero** el log es solo el centinela `KOI_SO_FAIL`, el `error` sigue siendo el mismo `replica_unhealthy` (o sea, **NO** distingue todavía "no llegamos a la máquina" de "el contenedor no arrancó", que era lo anunciado) y el deploy fallido trae `image: null`. Falla en **14 s** mientras el log de la app muestra `✓ Ready in 0ms`.
  - **⚠️ Y una corrección NUESTRA:** por la mañana afirmamos que `runner: null` significaba "la réplica nunca se planificó". Ahora que guardan el runner se ve que **sí se planificó** → aquel diagnóstico era **erróneo**, lo dedujimos de un campo que ellos no rellenaban. Corregido en el journey.
- **⭐ 29/07 16:10 — EL DATO QUE CAMBIA LA COMPARATIVA: en PÁGINA COMPLETA estamos en PARIDAD con AWS.** Todo lo que habíamos medido esta semana era **TTFB**; midiendo `time_total` (el documento entero, que es lo que espera el usuario) y comprobando que los tamaños cuadran (±2-3%, los de Koigrid incluso algo menores): `/` **105ms AWS vs 107ms Koigrid (1,02×)**, `/leyes` 136 vs 157 (1,15×), `/constitucion` 91 vs 100 (1,10×). **Una réplica de 2 GB en plan gratis contra CloudFront + ALB + 8 tareas Fargate.** El 25/07, antes de A3, esto mismo era 523ms vs 78ms = **5-6× peor**. Y explica la discrepancia: el TTFB parte de una base pequeña (40 vs 60ms), así que 20ms de rutado al edge se leen como "1,5× más lento" y son invisibles al lado de 100ms de transferencia. **Para decidir el cutover manda esta tabla, no la de TTFB.**
- **⚠️ Y una corrección de método nuestra:** el ratio de TTFB tiene **más varianza entre pasadas que la propia diferencia que mide** — hoy, mismo método y misma app: ~06:00 → 1,37-1,97×; 15:20 → 2,11-2,28×; 16:10 → **1,35-1,88×**. Lo honesto es decir "~1,4-1,9× en TTFB de documentos ligeros y empate en la pesada", no citar una pasada suelta (que es lo que veníamos haciendo). Capacidad estable todo el día: 615 → 519 → **523 rps** (conc 15) con 0% errores y CPU 0%. A3 verificado otra vez (3/3 `HIT`).
- **🔴 S1 (storage) re-verificado 16:10 y NO era propagación:** segunda llave nueva, mismo `AccessDenied` sobre `vence-videos`, `GET /buckets` sigue vacío. Llave borrada tras la prueba. **Producción sigue en `s3.koigrid.com` y sirviendo bien.**
- **📊 Re-medición 29/07 tarde (15 muestras/fila, alternando host; ojo: la de la mañana fue a las ~06:00, hora tranquila — leerlas juntas, no como tendencia):** AWS p50 `/`=38ms, `/leyes`=50, `/constitucion`=45, `/aux-admin-estado`=167 vs Koigrid 87/111/95/**188** → **2,11-2,28× y 1,12× en la página pesada**. **A3 sigue vivo** (3/3 `HIT` en las tres). Capacidad con 1 réplica: **519 rps** (conc 15) y **712 rps** (conc 50), 0% errores, sin saturar, CPU 0% — un ~15% por debajo de la mañana (hora más cargada, no regresión) y aun así **31× nuestro pico**.
- **🔒 31/07 — BLOQUEANTE NUEVO del cambio de borde, con ficha propia: [T-357].** Al ir a verificar la precondición del módulo de IP de cliente (`lib/api/clientIp.ts` dice que `trusted` solo vale si el origen NO es alcanzable directamente) resultó que **NO se cumple**: el ALB acepta `0.0.0.0/0` y sirve la app con `Host: www.vence.es` (200 real). O sea que hoy la IP marcada `trusted` **es falsificable** y encima corre el antifraude. **Cerrar el origen es prerrequisito de §7-pre**, no trabajo posterior: detrás de Cloudflare el problema sería idéntico con `CF-Connecting-IP`. Detalle, evidencias y las dos vías de arreglo en T-357.
- **⏭️ EL GATILLO YA NO ES TÉCNICO NI DE PRECIO — es la DECISIÓN de Manuel.** Si dice adelante, lo que falta es NUESTRO y está acotado: (1) **re-clonar la BD** (el clúster del POC es un cascarón de esquema, ver abajo), (2) el fix de **`getClientIp()`/`CF-Connecting-IP`** (obligatorio ANTES de mover tráfico), (3) poner **nuestro Cloudflare** delante, (4) ensayo de cutover con AWS de rollback. A4 (su `maintenance_work_mem`) solo importa si queremos usar su restore gestionado en vez de `pg_dump | psql`.

- **🎉 REVISIÓN 29/07 (versión nueva: `llms.txt` 790→810 líneas, `openapi.json` 178→189 rutas). Cambia el veredicto de la ficha: se acabaron los bloqueos técnicos.** Todo medido contra la API viva y el clon fiel (`vence-web7`, 1 réplica de 2GB, plan gratis) en una sola sesión. Detalle completo y evidencias en `docs/roadmap/koigrid-migration-journey.md` §RE-TEST 2026-07-29.
  - **✅ A3 (caché de HTML en el edge) FUNCIONA — el bloqueo nº1 desde el 24/07, cerrado.** `/`, `/leyes` y `/leyes/constitucion-espanola` van `MISS → HIT` con `age` creciendo monótono (17→34 en 18s). Y honran el **`s-maxage` a secas**, que era justo el matiz que esta ficha señaló el 25/07 como posible invalidante (Next.js nunca emite el token `public`).
  - **💥 La consecuencia es de CAPACIDAD, y es enorme: ~60×.** Mismo load-test que veníamos usando (30s, conc 15), misma app, **1 réplica**: **615 rps** en `/leyes/constitucion-espanola` (p50 19ms, p95 50ms, 0% errores, **sin saturar**, CPU 0%) y 462 rps en la home; a conc 50, **838 rps**. El 25/07 eran **8,8-10,5 rps SATURADO**. **Nuestro pico es ~16,6 rps → una sola réplica aguanta 37× el pico.** El dimensionado pasa de "4-6 réplicas + plan por averiguar" a "1 réplica con margen absurdo".
  - **📊 Head-to-head vs AWS (mediana de 7, misma ventana, alternando host):** AWS `/`=36ms, `/leyes`=41ms, `/constitucion`=49ms, `/aux-admin-estado`=202ms vs Koigrid 72/63/67/**165**ms → **1,37-1,97×, y Koigrid GANA la página más pesada**. Veníamos de 2,5-3,5× (25/07 con CDN) y 4,2-5,6× (sin CDN). Una caja de 2GB en plan gratis a menos de 2× de CloudFront + 8 tareas Fargate.
  - **⚠️ La conclusión del 27/07 "A3 depende de N1" era FALSA.** A3 funciona con `scale-out` apagado; lo que necesita el edge central son las *reglas personalizadas*, no la caché de documentos. Nos lo hizo concluir mal el propio `/rules`, que **hoy sigue diciendo** `enforced:false, servedBy:"legacy_runner"` con remedio `scale-out`… en una app que está cacheando. Reportado.
  - **🔴 N1 (`scale-out`) NO funciona: 5.ª reproducción** (25/07, 27/07 y 29/07 ×3 — el bug abierto más antiguo del informe), siempre `replica_unhealthy` en ~24-30s con **logs vacíos** y **`runner: null`**. **Dato nuevo del 29/07 tarde: lo probamos en una SEGUNDA app distinta (`vence-web9`) y falla igual** → **es de plataforma, no de nuestra app** (ambas despliegan bien por la vía normal minutos antes y después). Sin runner, sin logs y sin salida de contenedor = la réplica **nunca se planificó**, así que `replica_unhealthy` apunta a la capa equivocada (perdimos dos sesiones buscando un `healthPath` que no existe). **Ya no nos bloquea** (A3 va sin él, y en `vence.es` el CDN sería el nuestro). Revertido en las dos apps; **sin downtime en ninguno de los 4 intentos** (el último-bueno siguió sirviendo, 200 en 0,05s).
  - **🟡 Restore gestionado RE-PROBADO E2E con dump REAL de RDS** (`pg_dump` 17.10, 709MB → 241MB gzip): **A1 ✅** (`\restrict` ya no rompe), **B1 ✅** (el dump se conserva al fallar y se reintenta con el MISMO `dumpKey`, sin resubir), **B2 ✅** (**atómico de verdad**: tras 2 jobs fallidos, CERO objetos a medias), **`preSeed` ✅** (`extensions.vector` pre-creado). Subida de 241MB en 11-23s; diagnóstico fail-fast con `fichero:línea` exacto en ~100s.
  - **🔴 A4 (NUEVO, bloqueante para el restore gestionado):** no puede construir el índice **ivfflat** de pgvector → `memory required is 65 MB, maintenance_work_mem is 64 MB`. **Un mega corto.** Y no es culpa de nuestro dimensionado: `maintenance_work_mem` es el **default de fábrica de PostgreSQL** (`source: "default"`, NO escalado a la RAM del clúster), `PUT /databases/{id}/resources` da **404**, `apply-config` es para recrear nodos (no tunear GUCs) y su pooler rechaza el escape del cliente (`PGOPTIONS=-c maintenance_work_mem` → `unsupported startup parameter in options`). **El arreglo es una línea en SU sesión de restore** y lo demostramos: el rol `app` (sin privilegios) **sí** puede `SET maintenance_work_mem='256MB'`. Afecta a **cualquier origen Supabase/pgvector**, que es su ICP declarado.
  - **Workaround válido (el de siempre):** `pg_dump | psql` con el `SET` por delante restauró `public.articles` (**61.123 filas = EXACTO vs RDS**, 606MB, 8 índices incl. ivfflat) en **2m39s**, owner `app`. Extrapolado, el 31GB real ≈ 2-3h.
  - **⚠️ HALLAZGO NUESTRO — la BD del POC estaba VACÍA y la ficha decía lo contrario.** `vence-poc` seguía `running`, sí, pero es un **cascarón de esquema**: 0 preguntas, 0 usuarios, 0 `test_questions`, y le faltaban **11 tablas** (entre ellas `articles`). El clon de 31GB vivía en `vence-mig2`, **borrada**. Las páginas del clon se veían bien porque el HTML ISR venía horneado en la imagen del 22/07. **"BD 1:1 sigue running" ≠ "BD poblada": para el ensayo de cutover hay que RE-CLONAR.** (Hoy se le devolvió `articles`.)
  - **🟡 `env/verify` (endpoint nuevo) da verde sobre un valor roto:** el contenedor recibía el literal `${{db.vence-mig2.DATABASE_URL}}` (interpolación sin resolver a una BD borrada) → **todas las rutas de API a 500**, y `env/verify` respondía `present:true, matchesConfigured:true`. Es justo la clase de fallo que ese endpoint existe para cazar. Pedido: que una `${{…}}` sin resolver **tumbe el deploy** y que `env/verify` la marque `unresolved_reference`.
  - **Otros:** `pause`/`resume` excelentes (POC pausado → sirviendo HTML de producción en **~45s**, sin rebuild, coste cero mientras tanto); `GET /databases/{id}` **no** devuelve el `postgres:{running,available,behind}` que documenta (corre **17.2**, sus docs dicen 17.5, nuestro origen es 17.6); `GET /apps/{id}/deployments/{id}` y `/events` dan 404; `concurrency:150` en el loadtest da `bad_request` pelado (el techo parece 100).
- **⏭️ GATILLO NUEVO (el anterior, "reintentar scale-out", ya no aplica):** **preguntar el PRECIO del plan que necesitamos** (1-2 réplicas × 2GB + ~35GB Postgres con PITR + Redis + object storage) — es **decisión de gasto de Manuel** y ahora es lo ÚNICO entre nosotros y el ensayo de cutover. En paralelo, lo nuestro: (a) el fix de `getClientIp()`/`CF-Connecting-IP` (27/07, obligatorio ANTES de cualquier cutover), (b) re-clonar la BD, (c) A4 solo importa si queremos usar su restore gestionado en vez de `pg_dump | psql`.
- **⚠️ Ojo al leer lo de abajo:** todo lo anterior al 29/07 se conserva como registro histórico, pero **las conclusiones de coste y dimensionado de los días 25-27 quedaron obsoletas** por la medición de hoy (se calcularon con 4-6 réplicas porque no había caché de borde; ahora son 1-2).

<details><summary>Historial 22-27/07 (obsoleto en dimensionado/coste, útil por los gotchas)</summary>

- **Estado 24/07 (tarde):** **BD (31GB 1:1) + frontend + BACKEND + Redis TODOS VIVOS en Koigrid, 1 proyecto (`demo`=`7a9881f4`).** Frontend `vence-web7`=`38864c7c`, `https://vence-web7-23f37d.apps.koigrid.com` (`✅ Cache 1354 leyes`, warm TTFB 0,2-0,77s). **Backend NestJS VIVO** (`vence-backend`=`b49328b5`, `https://vence-backend-261bdf.apps.koigrid.com/health`→200) contra la BD real (ranking cron 1922 filas, insert 27-77ms; AlertsCron 59/59) **con Redis cableado** (`cache`=`b7fcad1a`, `rediss://…rds-cache-d9085a…:44921`, `CACHE_PROVIDER=elasticache` → `[CacheService] Cache configurada`). Desplegado vía **`POST /manifest`** (whole-app, project `demo`, backend+redis). BD/frontend siguen vía imagen ECR.
- **Cómo se desbloqueó:** Koigrid arregló (23-24/07): capacity-accounting bug + `build_oom` classifier + **defecto #2 (pull de registry externo con creds, ECR nativo)** + **sugerencias #1 (`/manifest` whole-app) y #4 (`/apps/{id}/loadtest`)**. El build-OOM del `--dir` se esquiva NO buildeando en Koigrid (modelo ECS: build en CI → imagen → Koigrid la corre). #3 (registry 413) sigue abierto pero moot vía ECR.
- **2 BUGS de plataforma hallados en el apply (en el journey para Koigrid):** (1) **plan≠apply en `project`**: pasar el UUID → plan resuelve por-id (noop/update) pero apply por-NOMBRE → creó un proyecto paralelo VACÍO (incl. BD vacía). Fix: pasar el NOMBRE (`demo`). (2) **`build_export_failed` flake**: TODO deploy de imagen falla al 1er intento (I/O del runner) y un `POST /apps/{id}/deployments` de retry lo aterriza; el fallido deja el app en `error` sin URL aunque el contenedor arranque (+cascada `docker run 'manifest'`). Sin downtime (el último-bueno sigue sirviendo).
- **✅ ÚLTIMA MILLA probada E2E (24/07 noche):** **login/auth** (`GET /api/profile` con Bearer HS256 → 200 perfil real con RLS; sin token 401) y **guardar respuesta** (`POST /api/v2/answer-and-save` → 200 `isCorrect`+explicación+INSERT real en `test_questions`, 0,79s) FUNCIONAN contra la BD copia. **Redis E2E** (SET/GET directo en `rediss://`). **Frontend** renderiza páginas reales de BD. Método: token acuñado con `SUPABASE_JWT_SECRET` real (el `verifyAuth` acepta HS256, forward-compat); secretos cableados por manifiesto.
- **⚠️ POC ≠ cutover. AÚN NO probado / pendiente:** (1) ~~backend~~ ✅ VIVO; (2) ~~Redis~~ ✅ E2E; (3) ~~auth login + guardar respuesta~~ ✅ E2E; (4) **Stripe webhook** — handler OK (rechaza firma no-cuadrada, 400 limpio) pero NO en verde: falta emparejar el webhook secret vivo (SSM posiblemente rotado o matiz dual Manuel/Nila; Koigrid no deja leer secretos → no introspectable en remoto). Config, no fallo de stack; (5) **frontend→backend Koigrid** por red privada (`BACKEND_URL=${{app.vence-backend.INTERNAL_URL}}`, sin cablear); (6) **crons** (GitHub Actions, sin repuntar); (7) **LOAD TEST de pico** — floor 1 réplica 2GB CDN-off: home satura ~8.7 rps / `/leyes` 0 err conc15; pico ~43M req/mes ≈16,6 rps → necesita CDN+~6-10 réplicas = **plan de pago + dominio propio** (gate crítico, decisión de gasto de Manuel; `loadtest.yml`+`e2e-smoke.yml`).
- **Perf vs AWS (medido 24/07):** AWS+CloudFront TTFB ~40-90ms vs Koigrid POC (CDN OFF, 1 box) ~180-320ms static / ~400-890ms DB-page = AWS 2-5× más rápido AHORA, pero no apples-to-apples (CDN off + 1 box). Win Koigrid: BD co-ubicada 6,45ms + coste ~10× menor ($89 vs $800-1200). Camino: front→back privado → E2E funcional (writes/auth/Stripe) → load-test ≥2× pico con CDN+réplicas → shadow/canary → cutover con AWS de rollback.
- **LIMPIEZA POC** (si no se sigue): borrar apps koigrid `vence-web7`+`vence-backend`, redis `cache`, IAM user `koigrid-ecr-pull`(+key, ahora scoped a `vence-koigrid-mig`+`vence-backend`), repos ECR `vence-koigrid-mig`. Proyecto huérfano `1b70c6bc` (del bug #1) a borrar cuando deje de dar 409. **PROD INTACTA en AWS.**
- **🧪 REVISIÓN 25/07 — versión nueva de Koigrid + re-test vs AWS:** la ÚNICA novedad es el **restore gestionado** (`/databases/:id/restore-dump`, 3 endpoints; llms 650→659 líneas). **Probado a fondo con dump REAL de RDS** (4 tablas, 6.477 filas, 5MB, con `COPY`, `extensions.vector(1536)`, índices ivfflat y `extensions.uuid_generate_v4()`), 7 jobs sobre 2 BD nuevas (PG16 y PG17), ambas borradas al terminar. **2 BLOQUEANTES reproducibles:** (1) **rechaza cualquier dump de `pg_dump` ≥17.6/18** — muere en la línea 5 con `invalid command \restrict` (el psql del runner es viejo; pasa IGUAL en cluster PG16 y PG17 → no se arregla eligiendo versión). Workaround: `grep -v '^\restrict \|^\unrestrict '` antes de subir. (2) **tras un restore CORRECTO, el rol `app` no puede leer sus propias tablas** (`permission denied`): todo queda `owner=postgres` y `app` es el único login expuesto; **tampoco pueden la Data API ni `/query` de Koigrid**, y NO hay auto-reparación (`ALTER OWNER`/`GRANT` denegados, `/query` es read-only). Peor que el `pg_dump | psql` a mano. **2 fricciones:** `dumpKey` de un solo uso (cada fallo = re-subir el dump entero → 31GB; error opaco `download_failed`/404) y **sin rollback**: un fallo deja objetos a medias y el intento siguiente reporta la causa EQUIVOCADA (`already exists`). **Lo que SÍ funciona (el núcleo):** fail-fast con causa raíz exacta (fichero:línea + LINE + HINT, en 2,2s), **`preSeed` resuelve el snag ex-Supabase** (vector/uuid-ossp en el esquema `extensions` → columnas vector + ivfflat restauran bien), COPY directo al leader, y `tableCounts` EXACTO (4/4 tablas cuadran con RDS; 5MB en 5,7s). **Para NUESTRO cutover: hoy NO sirve** (los 2 bloqueantes lo invalidan) → seguir con el `pg_dump | psql` que ya funcionó.
- **📊 Re-test de rendimiento 25/07 (~00:00, ambos medidos en la misma ventana, mediana de 7):** AWS `/`=58ms, `/leyes`=39ms, `/constitucion`=45ms, `/aux-admin-estado`=122ms, backend `/health`=113ms — Koigrid 138/188/143/301/154ms. **Ratio 2,4-4,8× (el número más justo, `/health` sin CDN en ninguno: 1,4×) — el ratio NO se ha movido** respecto al 24/07 (ambos midieron más rápido por ser hora tranquila; por eso vale el ratio, no los absolutos). Causa confirmada por cabeceras: AWS `x-cache: Hit from cloudfront, age:6560` vs Koigrid **`cf-cache-status: DYNAMIC`** en todas, aun mandando `s-maxage` y sin cookies. **Y el load-test lo confirma por el otro lado:** con **CDN YA ENCENDIDO**, 30s a concurrencia 15 → `/`=**8,8 rps** y `/constitucion` (¡prerenderizada!)=**9,2 rps**, saturado, **idéntico al 8,7 rps de antes del CDN** → cachear HTML en el edge no es solo latencia, es **el gate de capacidad** (pico necesario ≈16,6 rps). La recomendación #1 del journey (cachear HTML honrando `s-maxage`) **sigue sin implementarse** (0 hits de `cacheEverything`/`cacheTtl`, `/apps/{id}/rules` idéntico).
- **🚨 RELEASE SIGUIENTE (25/07, madrugada) — el #1 llega… y se vuelve inalcanzable:** el `llms.txt` YA documenta **HTML EDGE CACHING** (nuestra recomendación #1: cachear documentos honrando el `Cache-Control` del origen, sin cachear `Set-Cookie`/`private`, RSC pasa de largo) **pero NO se puede comprobar**, y este build **regresa 4 cosas que ayer funcionaban**. Todo reproducido contra la API viva: (**R1**) `PUT /apps/{id}/cdn {enabled:true}` vuelve a fallar en `*.apps.koigrid.com` con el error EXACTO que arreglaron el 24/07 ("needs a valid Cloudflare edge certificate") — en la app existente (5 intentos) **y en una app nueva** (`server: Caddy`, sin Cloudflare delante); **y apagar SÍ funciona → puerta de un solo sentido**: al apagar el CDN para medir un antes/después, **el POC se quedó sin CDN y no se puede volver a encender**. (**R2**) **el restore gestionado DESAPARECIÓ** (404 en las 3 rutas; `openapi.json` byte-idéntico al del 24/07) — coherente con arreglar los 2 bloqueantes que reportamos, pero es una feature documentada que se esfuma. (**R3**) las apps nuevas ya NO traen CDN por defecto. (**R4**) `POST /apps sourceType:image` ya no auto-despliega (10 min en `created` sin error; arranca con un `POST /deployments` a mano). (**R5**) las reglas `header` de `/apps/{id}/rules` se guardan pero **no tienen efecto** (probado con `Cache-Control`). (**R6**) el catálogo de errores de RUNTIME desapareció del `llms.txt`. **Ojo al matiz que puede dejar la feature inútil para Next.js:** los docs piden el token literal `public`, y **Next.js no lo emite** (nuestras páginas mandan `s-maxage=31536000` a secas; CloudFront las cachea igual porque `s-maxage` ya es directiva de caché compartida) → si exigen `public`, todo el ICP Next.js seguirá viendo `DYNAMIC`.
- **📊 Re-test vs AWS 25/07 01:15 (Koigrid CDN OFF a la fuerza por R1 — comparación PEOR que la de ayer, y no por culpa nuestra ni de la app):** AWS `/`=40ms, `/leyes`=38ms, `/constitucion`=41ms, `/aux-admin-estado`=101ms, `/health`=104ms vs Koigrid 199/211/174/291/164ms → **4,2-5,6× (health 1,6×)**, contra 2,4-4,8× de ayer con CDN. Página completa: Koigrid 523ms home / 668ms `/leyes` vs AWS 78/128ms. **Capacidad IGUAL: 10,5 rps saturado** (conc 15, 30s) ≈ los 8,8-9,2 rps de ayer CON CDN → prueba limpia de que **mientras el HTML no se cachee en el edge, encender el CDN no cambia el throughput** (todo cae en el único contenedor de origen). **Para volver a medir en condiciones hace falta que arreglen R1** (o atar un dominio propio, que toca DNS de vence.es → decisión de Manuel).
- **⚖️ ALCANCE HONESTO DE LA COMPARATIVA (medido 25/07, a raíz de la pregunta de Manuel) — NO era apples-to-apples y el coste estaba MAL estimado:** lo que comparamos era **AWS producción = 8 tareas Fargate × 2 vCPU/4GB = 16 vCPU y 32GB, autoscaling 8→12 (hasta 24 vCPU), + CloudFront + ALB + RDS Multi-AZ** contra **Koigrid = 1 réplica de 2GB sin CDN** (Free capa a 1 réplica). Cuota Fargate 30 vCPU (ajustable), en uso 16,25/30. Leído así, Koigrid sale MUY bien parada (la única pata comparable de verdad, `/health`, sin CDN en ninguno: **1,6×**) y **nuestras tablas de latencia son un SUELO, no un veredicto**. Los ~10 rps de saturación son **por réplica**: igualar los 16 vCPU de AWS ≈ **8 réplicas** = plan de pago. **COSTE REAL MEDIDO (Cost Explorer 01→24/07): $376,62 en 23 días → run-rate ≈ $491/mes ($421 sin impuestos)** — RDS 103,6 · ECS 98,3 · impuestos 69,7 · CloudFront 37,1 · VPC 25,8 · ELB 21,1 · CloudWatch 11,5 · ElastiCache 8,0 · R53 1,6. **La ficha decía $800-1.200 → el ratio honesto es ~4-5×, no ~10×** (sigue siendo una ventaja grande, pero medida). Y ojo: el stack Koigrid equivalente (≈8 réplicas + BD HA + CDN) tampoco son los $89 del free.
- **📏 LO QUE FALTA POR MEDIR para una comparativa seria a escala** (agrupado por qué lo desbloquea): **(a) bloqueado por R1/R2 de Koigrid** → latencia y capacidad con HTML cacheado en el edge (todo el sentido de A3, hoy inmedible). **(b) bloqueado por plan de pago/dominio propio** → throughput multi-réplica y p95/p99 **bajo carga sostenida** (no medianas en vacío), autoescalado, si los ~10 rps/réplica escalan lineal hasta nuestro pico ~16,6 rps, failover de BD HA, y el precio real del stack equivalente. **(c) se puede hacer YA y gratis sobre la BD copia** → throughput de ESCRITURA (`answer-and-save` a N rps) vs RDS, latencia de BD **bajo carga** (solo tenemos una muestra de 6,45 ms), tiempo real de restore/PITR, tiempo de deploy+rollback punta a punta, throughput del object storage (ya servimos los 30 vídeos HLS desde ahí), cold-start tras idle, y paridad de logs/alertas. **(d) requiere decisión de Manuel (toca producción)** → replay/shadow de tráfico REAL (única forma de comparar mezcla de rutas, sesiones autenticadas y ratio de escrituras) y lag de replicación lógica desde la RDS viva.
- **🔻 R7 (25/07):** durante esta medición el **plano de control de Koigrid devolvió 500 en TODOS los endpoints** varios minutos (`/apps`, `/databases`, `/usage`, `/metrics`) mientras **el plano de datos seguía sirviendo 200 a ~0,23s**. Segunda vez en la sesión que una ventana de release degrada la API. Por eso quedó a medias la columna de recursos/plan de Koigrid.
- **🧮 ¿LA MIGRACIÓN ARREGLA LA CUOTA DE FARGATE? (medido 25/07) — sí, pero NO es el motivo para migrar, y hoy la cuota YA no bloquea:** (a) **En Koigrid ese límite no existe** (no hay pool de vCPU por cuenta); sus topes son **réplicas por plan** (Free=1) y fair-use **16GB/app, 50 apps** — o sea, cambias "no me caben las tareas" por "cuántas réplicas me paga el plan". (b) **En AWS ya está resuelto:** el servicio está con **deploy en-sitio** (`maximumPercent=100`/`minimumHealthyPercent=80` + circuit breaker) y autoscaling capado a **12** → pico 12×2+0,25 = **24,25 de 30, cabe**; y subir la cuota es un **ticket gratis** (~1 día). Migrar POR la cuota sería desproporcionado. (c) **Utilización REAL del frontend (7 días, CloudWatch):** CPU media diaria **2,4-18,7%**, memoria **~15%** de 4GB, con **picos puntuales al 100%** en tareas sueltas → hay margen para redimensionar en AWS (bajar los ~98$/mes de ECS), pero **con cuidado: las ráfagas son reales**, hay que ver de dónde salen antes de tocar. (d) **EL RIESGO REAL de migrar hoy no es la cuota, es el edge:** en AWS CloudFront absorbe casi todo el HTML y el origen apenas trabaja; **en Koigrid, sin caché de HTML (R1), el origen se comería el 100% del tráfico**. Medido: **~10 rps por réplica de 2GB y satura** (p95 4,1-5,7s) → para el pico ~16,6 rps con p95 decente, estimo **4-6 réplicas**; con edge caching arreglado, **1-2**. **Conclusión: el dimensionado (y el precio) del cutover NO se puede decidir hasta que arreglen R1.**
- **🏆 RELEASE 25/07 (tarde) — EL GATE DE PICO SUPERADO + casi todo lo reportado ARREGLADO (verificado contra la API viva):** Koigrid **quitó el tope de réplicas del plan** (hasta 12 en Free) *"porque medir tu PICO es lo que decide una migración real"* → por fin se pudo correr **el load-test de pico, gate #1 del cutover desde el día 1**. Resultado con **6 réplicas de 2GB (plan gratis)** sobre `/leyes/constitucion-espanola`: **109,3 rps con 0,00% de errores** (p50 81ms, p95 596ms) a conc 20; 143,3 rps a conc 35 (2,8% err); y **216 rps en la home** a conc 50. Con 1 réplica eran ~10 rps → **×11**. **Nuestro pico de producción es ~16,6 rps → 6,5× de margen, y encima sin que la caché de HTML ayude nada** (todo renderizado en origen). Escalar = 1 llamada y ~30s. **ARREGLADOS y verificados:** R1 (el CDN se vuelve a encender, se acabó la puerta de un solo sentido), R3 (apps nuevas con CDN por defecto), R4 (crear app de imagen YA despliega: `running` al 1er intento en ~25s), R5 (`/rules` ahora devuelve `enforcement{enforced,servedBy,note,remedy}` — mejor de lo que pedimos), R6 (códigos de error de runtime de vuelta), y **el restore gestionado VUELVE con TODOS los arreglos documentados**: `\restrict` soportado (A1), **propietario= rol de la app automático + `POST /databases/:id/fix-ownership`** (A2, con el nombre de endpoint que propusimos), dump conservado si falla → reintento con el mismo `dumpKey` (B1), **restore atómico en una transacción** (B2), + subida gzip (31GB → ~3,8GB). *(El restore está documentado; falta re-probarlo E2E — próxima sesión.)*
- **🔴 SOLO QUEDAN DOS COSAS ABIERTAS:** (**A3**) el **HTML edge caching está documentado pero NO funciona**: app nueva con CDN por defecto → 6/6 peticiones `cf-cache-status: DYNAMIC`; aislado en la misma app: `/robots.txt` SÍ cachea (REVALIDATED) y `/sitemap.xml` **con `public, max-age=86400, s-maxage=86400` NO** (DYNAMIC) → solo corre la caché estática por extensión, la regla de documentos no se aplica. (**N1, nuevo**) `PUT /apps/{id}/scale-out {enabled:true}` + deploy —la vía documentada para que apliquen las reglas y, seguramente, la caché— **falla el deploy con `replica_unhealthy`, 2 veces, en ~30s y con los logs VACÍOS** (el código ni está en la lista documentada de errores de runtime; y no hay `healthPath` que tocar). La misma imagen despliega bien por la vía normal. Revertido a `scale-out:false`; **sin downtime** (el último-bueno siguió sirviendo).
- **📊 Head-to-head 25/07 19:11 (web7, clon fiel, 1 réplica, CDN ON; tamaños de página cuadran con AWS ±2%):** AWS `/`=45ms, `/leyes`=54ms, `/constitucion`=38ms, `/health`=102ms vs Koigrid 112/188/127/154ms → **2,5-3,5× (health 1,5×)**, cuando anoche sin CDN eran 4,2-5,6×. **Recuperar el CDN devuelve el gap a 2,5-3,5×; el resto es ÍNTEGRAMENTE A3** (AWS sirve esos documentos desde el edge, Koigrid los renderiza siempre). Con A3 resuelto, esas filas deberían caer a nivel CloudFront — y por capacidad no habría problema (ver 6 réplicas arriba).
- **Refs:** memoria `reference_koigrid_evaluacion_fase_d` (§24/07). Journey para Koigrid `docs/roadmap/koigrid-migration-journey.md` (secciones 24/07 evening=apply backend+redis+2 bugs, later=manifest+load-test, VENCE-RETEST, head-to-head vs AWS). Artefactos `/home/manuel/.cache/koigrid-mig/` (manifest-v2.json). Claim vía sesión `89b90a8a`.

- **🔬 RE-INVESTIGADO A FONDO EL 27/07, a raíz de que la factura de AWS se disparó. Veredicto corto: migrar HOY no arregla el problema de coste, y el bloqueo sigue exactamente donde estaba.**
  - **A3 y N1 NO son dos cosas: son UNA CADENA.** Lo dice la propia API al preguntarle por `/apps/{id}/rules`: `enforced:false`, `servedBy:"legacy_runner"`, *«esta app se sirve por el Caddy heredado de su runner, que no ejecuta reglas»*, con `remedy: PUT /apps/{id}/scale-out`. Es decir: **la caché de HTML (A3) solo puede aplicarse si la app está en el edge central, y para eso hace falta scale-out (N1)**. Arreglar A3 por separado es imposible.
  - **N1 REPRODUCIDO HOY, dos días después y sin cambios:** `scale-out:true` + deploy → **`replica_unhealthy`** el 27/07 a las 17:58, idéntico al fallo del 25/07 a las 17:02. La app quedó en `failed` pero **el último-bueno siguió sirviendo** (`GET /leyes` → 200 en 0,73 s), como la vez anterior. Revertido a `scale-out:false`, redesplegado y devuelto a `paused`, como estaba.
  - **A3: los docs ahora responden por su nombre a nuestra objeción… y sigue sin funcionar.** El `llms.txt` pasó de 659 a **758 líneas** e incluye ahora: *«The bare s-maxage is ENOUGH — the "public" token is NOT required (Next.js ISR never emits it and those pages DO cache; RFC 9111)»*. Era literalmente el matiz que esta ficha señaló el 25/07 como posible invalidante. **Medido en el clon fiel con CDN ON: `/` (`s-maxage=3600`), `/leyes` (`s-maxage=2592000`) y `/leyes/constitucion-espanola` (`s-maxage=31536000`) → las tres, `cf-cache-status: DYNAMIC` en 3 peticiones cada una.** Segunda vez que algo está documentado y no funciona; conviene no volver a fiarse del changelog sin medir.
  - **💰 Y el dato que responde a la pregunta económica: el plan gratis está a ORDEN DE MAGNITUD de nuestra carga.** `GET /usage` con nuestro token: `plan: free`, `maxApps: 1`, y cuotas de **180.000 `cpu_seconds` (= 50 vCPU-hora) y 100 `memory_gb_hours`**. Nuestro frontend en AWS consume hoy **~390 vCPU-hora y ~864 GB-hora AL DÍA** → el free cubre **algo más de un OCTAVO de un solo día**. Las cuotas no son topes duros (se pasa y se factura), pero **el precio del plan equivalente sigue sin publicarse en los docs**, así que la afirmación «Koigrid es más barato» **sigue sin poder verificarse para NUESTRA carga real** — igual que el 25/07.
  - **🧮 Por qué migrar no arregla la factura (que es lo que se preguntaba):** el sobrecoste de AWS son ~$600/mes del **suelo de autoescalado del frontend (8 tareas × 2 vCPU)**, con la CPU al **2-4% de media**. En Koigrid, **sin A3 el origen se come el 100% del HTML**: la propia ficha midió ~10 rps por réplica y nuestro pico es ~16,6 rps → **4-6 réplicas** sin caché, 1-2 con ella. O sea: **lo que haría barata la migración es justo lo que no funciona.** El camino corto para la factura es revisar el suelo en AWS (cambio de configuración), no un cutover.
  - **Lo que SÍ sigue en pie a favor de Koigrid:** el **gate de pico está superado** (109 rps, 0% err, 6 réplicas, p50 81 ms — 6,5× nuestro pico), la BD del POC (`vence-poc`, PG17) sigue **`running`**, y las 5 apps del POC están `paused` (coste cero, recuperables con `/resume` en ~40 s, sin rebuild).

- **🚧 RELEASE NUEVA la misma tarde del 27/07 — y destapa un bloqueo MAYOR que `scale-out`. Esto reordena la ficha.**
  - `llms.txt` **758 → 777 líneas**. Lo decisivo: **⛔ una app con DOMINIO PROPIO activo NO puede usar el CDN.** Sus palabras: *«Choose ONE: (a) serve the app on its `<slug>.apps.koigrid.com` host and get the CDN, or (b) keep your own domain and run without edge caching»*. El motivo es técnico y lo explican bien: hacer proxy de un CNAME cross-user da **error 1014 de Cloudflare = sitio CAÍDO**, no solo sin cachear. Levantarlo exige **Cloudflare for SaaS**, y lo midieron ellos mismos hoy: su zona está en plan Free y la API responde `1404 "No quota has been allocated"`.
  - **Para Vence la opción (a) NO EXISTE:** servimos `www.vence.es`, con 11.171 usuarios, SEO del que dependemos y campañas de Ads apuntando ahí. No podemos servir producción desde `vence-web7-…apps.koigrid.com`. **Nuestra única opción es (b): dominio propio y CERO CDN.**
  - **Y "cero CDN" es peor que "sin caché de HTML":** sin caché de ningún tipo, sin protección DDoS y con la IP de origen expuesta. Con sus propios números: latencia **4,2-5,6×** peor que AWS (la medición con CDN apagado, no la de 2,5-3,5×) y **4-6 réplicas** para nuestro pico en vez de 1-2.
  - **⚠️ Consecuencia para el trabajo de hoy: A3 y N1 quedan MOOT para nuestro caso.** Aunque mañana arreglaran `replica_unhealthy`, en `vence.es` no habría CDN que programar. **El bloqueo dejó de ser un bug que podamos esperar y pasó a ser una decisión de plan/coste de Koigrid** (Cloudflare for SaaS en su zona).
  - **Bueno de la misma release:** `GET /databases/:id/connection` ya devuelve `caCert` + `sslVerifiedUri` con un aviso explícito de NO usar `NODE_TLS_REJECT_UNAUTHORIZED=0` *«desactiva la verificación TLS de TODO el proceso, así que Stripe/S3/toda llamada saliente deja de verificarse»*. **Nuestros scripts del POC tenían exactamente ese footgun.**

- **✅ DESBLOQUEADO la misma noche del 27/07 — TERCERA release del día: BRING-YOUR-OWN CDN.**
  - `llms.txt` **777 → 790**. Un solo cambio y es el que faltaba: *«**YOU CAN BRING YOUR OWN CDN, and it is measured**: koigrid ya emite un certificado Let's Encrypt válido para tu dominio en el origen, así que poner tu propio Cloudflare (plan gratis) delante funciona sin cambios»*. **Medido por ellos hoy: TTFB 0,350 s sin CDN → 0,053 s con el Cloudflare del cliente y `cf-cache-status: HIT` = 6,6× más rápido**, más DDoS, en tu cuenta y con tus reglas.
  - **Retira el bloqueo que declaré hace dos horas.** La restricción de dominio propio es sobre el CDN *de ellos*; nada impide poner **el nuestro** delante de `vence.es`. Consecuencia: volvemos a **1-2 réplicas** en vez de 4-6, con caché de borde y protección DDoS. Y **`scale-out` (N1) deja de bloquearnos**: si el edge es nuestro, no necesitamos el suyo.
  - **⚠️ Y aquí está el hallazgo NUESTRO que hay que arreglar ANTES de cualquier cutover:** `getClientIp()` (`lib/api/rateLimit.ts`) confía en **`CloudFront-Viewer-Address`** —que inyecta CloudFront y **no es falsificable**— y solo cae a `x-forwarded-for[0]`, que el propio comentario del código marca como *spoofable*. Detrás de nuestro Cloudflare **no existe esa cabecera**, así que **todo control por IP pasaría a fiarse en silencio de una cabecera que el cliente puede falsificar**. Y encima de eso corre el antifraude (`multi_account_reg_ip`, `curl_scraping`, IP de registro). No es una función rota: es **un control de seguridad debilitado sin avisar**, que es peor. Arreglo pequeño y obligatorio: que `getClientIp()` confíe en `CF-Connecting-IP` **solo** cuando la petición venga de Cloudflare.
  - Segundo cabo menor: blindar el origen a los rangos de Cloudflare (ellos avisan de que la IP queda alcanzable). Tienen WAF propio (`/waf/rules`), así que parece resoluble en plataforma; sin probar.
  - **Estado tras esto: el bloqueo pasó de ser un BUG (mañana) a una POLÍTICA (tarde) a un NÚMERO (noche).** Lo único grande que falta es **el precio del plan**. Todo lo técnico que objetamos hoy está resuelto o es nuestro.
  - **Estado real de la migración, sin adornos:** capacidad y BD probadas y sobradas; el bloqueo ya no es técnico-de-bug sino de producto de ellos. **No se cierra la ficha** —el POC sigue pausado y la BD `running`, recuperables— pero **el gatillo cambia**: ya no es "reintentar `scale-out`", es **"¿pueden cachear en el edge un dominio de cliente?"**. Hasta entonces no hay cutover posible sin degradar a los usuarios.
  - **⏭️ Gatillo para retomarlo, en una línea:** volver a probar `scale-out` (`PUT /apps/{id}/scale-out` + deploy). **Si deja de dar `replica_unhealthy`, se desbloquean A3 y N1 a la vez** y entonces —y solo entonces— tiene sentido pedir precio del plan y dimensionar el cutover. Mientras siga fallando, no hay nada que decidir. *(29/07: `scale-out` sigue fallando **pero A3 se desbloqueó solo** — la premisa de este gatillo era errónea; ver cabecera.)*

</details>

- **📋 CABOS DEL CUTOVER, al cerrar la sesión del 27/07 (por orden de cuándo hacen falta):**
  1. **`getClientIp` ya es agnóstico del CDN — HECHO y en `main` (`5ae64f208`), pero NO desplegado.** Núcleo `lib/api/clientIp.ts` con veredicto `trusted|untrusted|unknown`, registro `EDGE_PROVIDERS` (cloudfront, cloudflare, fastly, akamai) y la variable **`TRUSTED_EDGE`**, que convierte un cambio de CDN en un cambio de configuración. **No urge desplegar**: en AWS `CloudFront-Viewer-Address` sigue ganando y el comportamiento es idéntico; lo único inactivo es el evento `client_ip_untrusted`. Entrará solo con el próximo deploy de cualquier sesión.
     - **📌 AVISO PARA EL CUTOVER (añadido 31/07 desde [T-314], por si esta ficha la retoma otra sesión): la GEO tenía el mismo agujero y ya está tapada, y ahora hay DOS lectores de `TRUSTED_EDGE`.** La geolocalización del borde vivía dentro de una ruta y **sólo sabía leer `CloudFront-Viewer-*`**: detrás de vuestro Cloudflare habría devuelto `null` **en silencio**, que es exactamente el modo de fallo de T-314 (un escritor deja de escribir y nadie se entera en 27 días). Se ha sacado a **`lib/api/edgeGeo.ts`** con la misma forma que `clientIp.ts` —registro `GEO_PROVIDERS` (cloudfront · cloudflare · vercel legacy), añadir un borde = una fila— y **honra la MISMA variable `TRUSTED_EDGE`**, para que el cutover siga siendo un cambio de configuración y no dos. Al ponerla, cambian a la vez la IP y la geo. ⚠️ **Cloudflare no da lo mismo que CloudFront:** `CF-IPCountry` llega siempre, pero **ciudad, región y coordenadas dependen del plan / de activar las cabeceras de ubicación** — si no se activan, `user_sessions.city` se quedará vacío (el país, que es lo que consulta el antifraude, no). Conviene decidirlo ANTES de mover tráfico, no descubrirlo después. 9 tests en `__tests__/lib/api/edgeGeo.test.ts`, incluido el caso «detrás de Cloudflare NO se lee una cabecera de CloudFront» (que un cliente sí puede falsificar).
  2. **⚠️ CORTAFUEGOS DEL ORIGEN — precondición, no cabo opcional.** Una cabecera de borde solo es fiable si **el origen NO es alcanzable directamente**: con Cloudflare propio, cualquiera que llegue saltándose el CDN puede inventarse `CF-Connecting-IP`. Hay que limitar el origen a los rangos del CDN (ellos tienen `/waf/rules`, sin probar). **Sin esto, `trusted` es mentira** y el antifraude por IP no vale. Va ANTES de mover tráfico real.
  3. **Poner `TRUSTED_EDGE=cloudflare`** en el entorno del frontend al cortar. Sin fijarla se acepta cualquier borde conocido (compatible pero menos estricto).
  4. **Preguntar el PRECIO del plan.** Es la única incógnita grande que queda; el journey está listo para enviarse.
  5. Siguen abiertos del análisis de huecos: **G3** (assets estáticos que sobrevivan al deploy — el `ChunkLoadError` de Next.js) y **G4** (verificar un secreto sin leerlo, que es exactamente por lo que el webhook de Stripe no está en verde).

