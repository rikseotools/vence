### [T-163] 🟡 [ABIERTO 27/07] El chat de IA no tiene fallback de proveedor: si Anthropic cae, el psicotécnico se queda sin respuesta
- **Qué:** `lib/chat/shared/modelRouter.ts` elige proveedor **por contenido y de forma determinista** (psicotécnicos de series/tablas/cálculo → Anthropic; el resto → OpenAI). No hay cadena de respaldo: si el proveedor elegido falla, la petición muere ahí.
- **Medido en el incidente del 26/07** (saldo de Anthropic agotado, 09:38-17:08 UTC): **21 chats enrutados a Anthropic fallaron** y los 30 que funcionaron no fueron un fallback — iban a OpenAI de origen. Es decir: los usuarios afectados fueron **exactamente los de psicotécnico con cálculo**, que es donde el chat más aporta.
- **Cómo:** degradar al otro proveedor ante error de infraestructura del elegido (5xx, error de facturación/cuota, timeout), no ante un error de contenido. Anotar en el evento `llm_call` que la respuesta salió por el proveedor secundario, para no perder la señal de que el primario está caído.
- **Ojo al alcance:** el objetivo NO es igualar calidad — Anthropic se elige ahí a propósito por razonamiento. Es preferible una respuesta peor que ninguna, pero la degradación debe ser visible.
- **Origen:** barrido de salud del 27/07.

#### 📌 SESIÓN 07/08 (w4) — HECHO: cadena de respaldo construida, en las DOS direcciones, con tests que reproducen el incidente

- **Implementado en `lib/chat/domains/psychometric/PsychometricService.ts`** (el único punto donde `selectModel` decide de verdad entre proveedores para el chat de usuario — `VerificationDomain`/`ReanalysisService` son otro caso, admin-facing, no tocados por quedar fuera del alcance de esta ficha). Refactor: las llamadas a Anthropic/OpenAI se extrajeron a dos funciones (`callAnthropic`/`callOpenAI`) que **lanzan** en vez de capturar-y-devolver-error; el bucle exterior intenta el proveedor elegido por `modelSelection` y, si lanza, intenta el OTRO antes de rendirse — en las DOS direcciones (Anthropic→OpenAI, que es el incidente medido, y OpenAI→Anthropic por simetría, casi gratis con el mismo bucle). Solo si los DOS fallan se devuelve el mensaje de error (clasificado con `clasificarErrorProveedor`, ya existente, reutilizado sin tocar).
- **El disparador es "lanzó excepción", no una clasificación nueva — y es la lectura correcta del propio pedido de la ficha ("no ante un error de contenido"):** un error de CONTENIDO (el LLM responde con texto raro) da una respuesta HTTP 200 válida, nunca una excepción del SDK. Todo lo que llega al `catch` es, por construcción, un fallo de infraestructura (5xx, sin saldo, timeout, red). No hacía falta inventar un clasificador nuevo para decidir CUÁNDO reintentar.
- **Señal de que el primario cayó, sin tocar esquema:** el intento de respaldo se etiqueta con `runWithLlmFeature('psychometric_fallback', …)` (módulo `lib/observability/llm.ts`, YA EXISTENTE — instrumenta `getAnthropic`/`getOpenAI` en el punto de creación del cliente, así que cada `.create()` ya emite su propio evento `llm_call` a `observable_events` con provider/ok/error). Con la etiqueta, contar "cuántas veces se cayó el primario" es `SELECT count(*) FROM observable_events WHERE event_type='llm_call' AND endpoint='psychometric_fallback'` — sin necesitar correlacionar dos eventos por sesión/fecha ni añadir una columna nueva.
- **Tests: `__tests__/chat/domains/psychometricFallback.test.ts` (7, todos verdes), REPRODUCEN el incidente, no lo suponen** — mockean `getAnthropic`/`getOpenAI` y verifican: (1) sin fallo, responde con el proveedor elegido y NO llama al otro (cero llamadas de más); (2) **el caso exacto del incidente del 26/07** (Anthropic rechaza con "Your credit balance is too low", status 400, igual que describe `clasificarErrorProveedor`) → responde con OpenAI, no con el mensaje de error; (3) dirección inversa (OpenAI cae → Anthropic); (4) los DOS proveedores caen → mensaje legible (`esRespuestaDeError` lo reconocería), nunca una excepción sin capturar; (5) un 429 (saturado) también dispara el respaldo. Suite completa `__tests__/chat` (295 tests) y `__tests__/observability` (75) sin regresión; typecheck y eslint limpios.
- **NO tocado (fuera del alcance medido):** `ReanalysisService.ts`/`VerificationDomain.ts` (otro flujo, admin, no el chat de usuario del incidente) y el camino OpenAI de `PsychometricService` cuando NO hay excepción explícita capturada más arriba en la cadena de llamada HTTP (`app/api/ai/chat-v2/route.ts`) — ese nivel superior ya existía y no se ha tocado.
- **Listo para desplegar:** es código de aplicación puro, sin migración ni escritura de negocio — no necesita `DATABASE_URL` de escritura para nada de lo construido.

