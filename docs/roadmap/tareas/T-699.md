### [T-699] 🔴 [ABIERTO 08/08 · revisado 08/08 — 2 de 7 confirmados como FALSO POSITIVO de RLS, no gate mentiroso] El gate audit:oposicion se puede declarar en verde sin pasarse — 6 oposiciones más con topics pero sin fila en catálogo

- **Esfuerzo: rato.**
- **ORIGEN.** Al cerrar [T-503] (Agente de la Hacienda Pública), medí que la oposición NO estaba "construida" como decía su ficha: `topics` tenía 32 filas para `position_type='agente_hacienda'` pero `oposiciones` tenía CERO filas para ese slug — y el commit que lo mergeó a `main` (`aa7f8e6f2`) afirmaba en su mensaje *"GATES: audit:oposicion 0❌/0🟡"*, algo que no pudo haber pasado nunca con esa fila ausente. Pregunté a Manuel (#112) si esto era un caso aislado o un patrón; su respuesta: *"mide cuántas oposiciones más tienen topics y rutas pero les falta la fila... si son varias, es ficha propia y vale más que los 6 temas de impuestos: un gate que se puede declarar verde sin haberse pasado deja de proteger para todas las altas siguientes, no solo para esta."* Esta ficha es esa medida.
- **🔬 MEDIDO (08/08), no supuesto — tres pasos, cada uno reproducible:**
  1. **Barrido determinista:** `topics.position_type` vs `oposiciones.slug` (con la transformación `_` → `-`, la convención real del repo, confirmada muestreando slugs existentes como `subalterno-gva`, `auxilio-judicial`). Da **17 `position_type` sin fila** en `oposiciones`.
  2. **Filtro de falsos positivos:** de esos 17, **10 son `personalizada_<uuid>`** — configuraciones de test PERSONALIZADAS de un usuario (per-usuario, no catálogo; **no cuentan**, es su diseño no tener fila). Quedan **7 candidatos reales**.
  3. **Verificación con el gate oficial** (`npx tsx scripts/audit-oposicion-completa.ts <slug>`, con `VENCE_LECTOR_URL` — solo lectura): **los 7 dan `❌ fila oposiciones NO existe`**, el mismo fallo exacto que agente_hacienda.
- **Los 7 candidatos, con lo que cada uno tiene construido alrededor** (medido: `lib/config/oposiciones.ts` + rutas en `app/<slug>/`):

  | `position_type` | temas | config (`oposiciones.ts`) | rutas `app/<slug>/` | Estado |
  |---|---|---|---|---|
  | `etgoa_sanidad_consumo` | 120 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** |
  | `auxiliar_archivos_bibliotecas_museos_madrid` | 50 | ❌ | ❌ | huérfana de verdad (ni config ni rutas) |
  | `auxiliar_museos_estado` | 48 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** |
  | `administrativo_agencia_tributaria_canaria` | 40 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** |
  | `agente_hacienda` | 32 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** — es [T-503], ya en la ficha |
  | `celador_ibsalut` | 20 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** |
  | `celador_ics` | 17 | ✅ | ✅ (3 páginas) | **construida a medias, invisible** |

  - **6 de los 7 replican EXACTAMENTE el patrón de agente_hacienda**: scaffold de frontend completo (config + 3 rutas: temario, test, test/tema/[numero]) + topics reales en BD, pero sin la fila de catálogo — o sea, servibles por la app (el test funcionaría) pero **invisibles para salud-contenido, rollover, OEP, SEO y cualquier detector de los ~70 que este mismo CLAUDE.md documenta, porque TODOS iteran `FROM oposiciones`**.
  - **`auxiliar_archivos_bibliotecas_museos_madrid` es distinto**: no tiene ni config ni rutas, solo 50 filas de `topics` sueltas en la BD sin nada más construido alrededor — probablemente un import de datos abandonado a medio camino, no una "oposición construida sin catalogar". Hay una oposición YA REAL y activa con nombre parecido (`auxiliar-archivos-estado`, nacional) que NO es la misma (esta es de Madrid) — no confundir ni fusionar.
  - **No until encontré el commit de origen para ninguno de los 6** (`git log --all` no tiene ningún commit cuyo mensaje mencione estos `position_type`) — a diferencia de agente_hacienda, cuyo commit `aa7f8e6f2` SÍ existe y SÍ mintió sobre el gate. Sin el commit no puedo repetir la misma demostración de "gate falso" letra por letra en los otros 5, pero el SÍNTOMA final (fila ausente pese a scaffold completo) es idéntico, así que **SOSPECHO** (no demostrado) que nacieron por el mismo camino — falta encontrar quién/qué los generó para confirmarlo.
  - **user_theme_stats da 0 usuarios para los 7** (incluido agente_hacienda, que la ficha de T-503 decía con 16 usuarios/2 premium por otra vía) — esta tabla solo se puebla cuando alguien COMPLETA preguntas, así que 0 aquí no contradice que haya usuarios con la oposición como objetivo; solo dice que nadie ha podido usarla de verdad. No crucé la fuente real de los "16 usuarios" de T-503 para los otros 6 — falta esa medida si se quiere priorizar por impacto.
- **CONSECUENCIA:** el gate `audit-oposicion-completa.ts` (que SÍ falla correctamente cuando se corre — comprobado en los 7) parece no haberse corrido, o haberse corrido y ANUNCIADO en verde sin serlo, en al menos 6 altas de oposición. Es un fallo del PROCEDIMIENTO de alta (el manual `crear-nueva-oposicion.md` exige el gate ANTES de dar por construida una oposición), no de esta oposición en particular — por eso Manuel pide ficha propia.
- **Lo que NO puedo hacer yo:** crear las 6 filas — es `INSERT` en `oposiciones` (+ `convocatorias` SSOT + hitos), escritura de negocio fuera del alcance de un worker (`VENCE_LECTOR_URL` es solo lectura). Tampoco puedo confirmar la causa raíz exacta de cómo nacieron sin el commit (sospecha, no demostrado).
- **Siguiente paso propuesto (para quien tenga escritura):** por cada una de las 6, verificar contra fuente oficial (BOE/boletín correspondiente) los datos de convocatoria igual que se hizo para agente_hacienda en [T-503], y aplicar el INSERT + gate hasta 0❌. `auxiliar_archivos_bibliotecas_museos_madrid` necesita antes decidir si se completa (config+rutas) o se retira (los datos huérfanos no sirven a nadie tal cual).
- **Relacionadas:** [T-503] (el caso que destapó el patrón), [T-397] (usuarios en oposiciones sin temario — puede solaparse).

#### ⚠️ CORRECCIÓN 08/08 (w2, review-fix) — cruzado con [T-643]/[T-393], y eso CAMBIA la conclusión para al menos 2 de los 7 (posiblemente más)

**Los 5 commits de origen SÍ existen — la ficha decía "no until encontré ninguno" y era una búsqueda
incompleta, no un límite real.** Buscando por commit que AÑADIÓ la carpeta de rutas
(`git log --all --diff-filter=A -- 'app/<slug>/'`) en vez de por mensaje que mencione el
`position_type`, aparecen 5 de los 6 (todos menos `auxiliar_archivos_bibliotecas_museos_madrid`,
que efectivamente no tiene ninguno — coherente con "import abandonado"):

| candidato | commit | fecha | cita literal sobre el gate/fila |
|---|---|---|---|
| `etgoa_sanidad_consumo` | `3a6e1c80f` | 11/07 | *"oposición + 2 bloques + 120 topics + convocatoria SSOT + hitos en RDS... FASE 6: audit:oposicion 0 error"* |
| `auxiliar_museos_estado` | `32d7d4acc` | 16/07 | *"Gates verdes (audit:oposicion/served...). is_active=false (pend deploy+flip)"* |
| `administrativo_agencia_tributaria_canaria` | `df8e27db5` | 26/07 | *"La oposición ya tenía BD y scope (fases 1-3)... Sigue con is_active=false: esto NO es go-live... Gate audit:oposicion: 0❌/0🟡"* |
| `celador_ibsalut` | `d5eb9582e` | 03/07 | *"Fila oposiciones completada + convocatoria... Gates: audit:oposicion 0❌/0🟡... BD ya en prod; commit despliega el frontend"* |
| `celador_ics` | `a1d75a839` | 03/07 | *"Fila oposiciones completada... + convocatoria... Gates: audit:oposicion 0❌... BD ya en prod; commit despliega el frontend"* |

**PERO al cruzar con [T-643] (que la entrega debía referenciar y no lo hizo — es el hallazgo 3 de la
revisión) aparece algo que revisa la conclusión, no solo la completa: [T-643] fue corregida HOY MISMO
(08/08, w3) con confirmación DIRECTA de Manuel de que 2 de estos 7 candidatos NO son "gate mintió",
son FALSOS POSITIVOS de RLS:**

- **`etgoa-sanidad-consumo`**: Manuel confirmó en el embudo (#99) que escribió la fila él mismo con
  `is_active=false`. **Existe. No está ausente.**
- **`auxiliar_archivos_bibliotecas_museos_madrid`** (el 7º candidato de esta misma ficha, `T-393`):
  Manuel confirmó (#100) que la fila SÍ existe con `is_active=false`, y que además el
  `position_type` no coincide con el `slug` de esa fila — dos motivos independientes por los que
  `VENCE_LECTOR_URL` no la encontraba.

**Re-verifiqué el MECANISMO yo mismo, no me quedé en la palabra de Manuel — es reproducible y
explica los dos casos de arriba sin necesitar ninguna hipótesis exótica:**
```sql
SELECT rolname, rolbypassrls FROM pg_roles WHERE rolname = current_user;
-- vence_lector | rolbypassrls = false
SELECT polname, polcmd, pg_get_expr(polqual, polrelid) FROM pg_policy WHERE polrelid = 'oposiciones'::regclass;
-- "Public read oposiciones" | r (SELECT) | using: (is_active = true)
```
`vence_lector` (el rol de `VENCE_LECTOR_URL`, el ÚNICO que un trabajador puede usar para leer
`oposiciones`) tiene RLS activo con la política de lectura literalmente `is_active = true`, y **no
tiene bypass**. Postgres reescribe CUALQUIER `SELECT ... WHERE id = X` como `WHERE id = X AND
is_active = true` para este rol — así que una fila que EXISTE con `is_active=false` es indistinguible
de una fila que NO EXISTE, para cualquier query que un trabajador pueda correr. Probé si
`DATABASE_URL` (`vence_coordinacion`) podía servir de vía alternativa: **no** — `permission denied
for table oposiciones`, ni siquiera llega a evaluar RLS.

**Esto no es solo un matiz de los 2 casos que Manuel ya miró — es un defecto del MÉTODO que usa TODA
esta cadena de trabajo (la entrega, la revisión, y esta misma re-verificación):**
`scripts/audit-oposicion-completa.ts` lee `process.env.DATABASE_URL` (confirmado leyendo el código,
línea 29) — en una ejecución NORMAL (con el `DATABASE_URL` real de la app, no el de un trabajador) se
conectaría con un rol que sí ve `is_active=false`. Pero un trabajador de la flota **solo puede
correrlo inyectando `VENCE_LECTOR_URL` en la variable `DATABASE_URL`** (`DATABASE_URL="$VENCE_LECTOR_URL"
npx tsx scripts/audit-oposicion-completa.ts <slug>`, el patrón que toda esta ficha y su revisión
usaron) — y ESO hereda el mismo bloqueo de RLS. El gate en sí no miente: **la herramienta de
verificación de un trabajador no puede confirmar "la fila no existe", solo puede confirmar "la fila
no es visible con este rol"**, y son cosas distintas.

**Qué significa esto para cada uno de los 7, sin inventar certeza donde no la tengo:**
- **`etgoa_sanidad_consumo` y `auxiliar_archivos_bibliotecas_museos_madrid` (T-393): CONFIRMADOS por
  Manuel — NO son "gate mintió", son filas `is_active=false` normales e invisibles por RLS.** Salen
  del marco "6 candidatos con gate falso" de esta ficha.
- **`administrativo_agencia_tributaria_canaria` (T-045): el propio commit dice literalmente "Sigue
  con is_active=false: esto NO es go-live"** — la MISMA firma textual que etgoa (que ya se confirmó
  falso positivo). Esto también **revisa el Hallazgo 2 de la revisión** (la teoría de que la
  convocatoria huérfana con FK `ON DELETE CASCADE` demuestra un borrado por vía anormal): esa
  observación sobre la FK sigue siendo real y verificada por mí (`convocatorias_oposicion_id_fkey1`
  es efectivamente `ON DELETE CASCADE`, y la convocatoria `a2ec6502…` sigue viva apuntando a
  `f6fe6eb1…`), pero **la explicación más simple y ya demostrada dos veces hoy no es "se borró por un
  camino raro", es "nunca se borró: sigue ahí con `is_active=false`, y por eso el CASCADE nunca tuvo
  nada que disparar"**. Las dos teorías explican los mismos síntomas; la de RLS no necesita asumir
  ningún mecanismo de borrado anómalo. **SOSPECHO fuertemente (no confirmado por Manuel para ESTE
  slug en concreto, a diferencia de los otros dos) que T-045 es el mismo patrón — falta la misma
  confirmación directa que resolvió los otros dos.**
- **`auxiliar_museos_estado`: mismo patrón textual** ("is_active=false (pend deploy+flip)") —
  **SOSPECHO** que es el mismo caso, sin confirmar.
- **`celador_ibsalut` y `celador_ics`: AQUÍ LA SEÑAL ES DISTINTA Y MÁS PREOCUPANTE.** Sus commits NO
  dicen "is_active=false, no es go-live" — dicen *"Fila oposiciones completada... BD ya en prod;
  commit despliega el frontend"*, que suena a que la intención era dejarlas SERVIBLES, no en espera.
  Si estos dos también resultan ser simples `is_active=false`, hay que explicar por qué el commit
  afirma "ya en prod" de una fila que no está activa. **No lo sé — no puedo confirmarlo sin RLS
  bypass, y es justo la pregunta que hay que hacerle a Manuel antes de tratarlos como "gate mintió".**

**Con esto, el marco "6 oposiciones con gate declarado en verde sin haberse pasado" pasa de ser el
diagnóstico a ser UNA hipótesis entre dos, y la más simple (RLS, no borrado ni mentira) ya se
confirmó 2 de 2 veces que se comprobó.** Antes de mandar esto a Manuel como "6 casos", el paso que
falta es exactamente el que resolvió `etgoa`/`T-393`: preguntarle directamente (o conseguir una
lectura que haga bypass de RLS) si estas filas existen con `is_active=false`. Podría reducir "6
candidatos" a 2 (`celador_ibsalut`, `celador_ics`) o menos.

- **[T-643]** (`Oposiciones con topics construidos pero SIN fila en oposiciones (T-045, T-393)`):
  **REFERENCIADA** — cubre el mismo patrón para 2 de los 7 candidatos de esta ficha y ya tiene la
  corrección de Manuel de arriba. No duplicar su investigación ni su punto 3 pendiente (el modelo de
  escritura de la flota para tareas de generación de contenido).
- **[T-393]** (`Auxiliar de Archivos, Bibliotecas y Museos de Madrid`): es el MISMO candidato que el
  7º de esta ficha (`auxiliar_archivos_bibliotecas_museos_madrid`) — **NO es un caso "distinto"**
  como decía el texto original de T-699, es el mismo slug con su propia ficha ya viva, ya remedida
  hoy mismo por otra sesión, con su propia decisión pendiente de Manuel (terminar vs retirar). Fundir
  la fila de la tabla de arriba con la ficha de T-393 en vez de tratarlo como hallazgo nuevo.

**Adyacente, medido pero NO investigado a fondo (fuera del alcance de este arreglo, dejado para quien
continúe con más contexto):** buscando TODAS las FK que apuntan a `oposiciones(id)` aparecen miles de
filas huérfanas en tablas satélite — **`convocatorias`: 2.430** (cifra re-verificada por mí, exacta),
y la revisión midió además `convocatoria_notas` 6.847, `convocatoria_seguimiento_checks` 27.901,
`convocatoria_hitos` 604, `oep_detection_signals` 984, `coverage_history` 400 — concentradas sobre
todo en jul-ago 2026 y casi todas `is_current=true`. Podría ser ruido esperado del radar OEP
(candidatos no promovidos a oposición real) o un problema mucho más grande que el de esta ficha. **NO
afirmo causa** — dado lo que se acaba de encontrar con RLS, una parte de estas 2.430 podría ser el
MISMO patrón (`oposiciones` con `is_active=false` invisible) multiplicado por candidatos del radar, y
otra parte podría ser huérfana de verdad; distinguirlas necesita la misma vía de confirmación (Manuel
o RLS bypass), no una nueva medición de solo lectura.

