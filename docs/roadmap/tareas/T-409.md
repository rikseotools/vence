### [T-409] 🟠 [ABIERTO 31/07] Cubo «la explicación es el ARTÍCULO copiado»: 2.137 preguntas servidas que no explican nada

- **Esfuerzo: sesion_propia por tandas.** El detector ya está hecho y en `main`; lo que queda es **cola de reescritura**: ~2.137 preguntas, 16 por lote, **unos 15 min por tanda de cuatro** (una hora larga por lote). Se ataca por exposición y se puede parar en cualquier tanda.
- **ORIGEN — una impugnación que parecía de clave y era de explicación.** Natalia impugnó `e60091bd` («¿en qué artículo establece la CE el principio de autonomía de los municipios?») diciendo que la clave estaba mal. La clave era correcta (art. 137), pero la pregunta llevaba **46% de acierto en 130 exposiciones** y su explicación entera era el artículo 137 copiado, con su encabezado. No decía por qué el 140 —el que uno tiene en la cabeza— no era la respuesta. **La impugnación no era el fallo: era el síntoma.**
- **Qué es el cubo:** pregunta activa cuya explicación (a) **no razona ninguna opción** y (b) su contenido **ya está en el artículo vinculado**. Se exigen LAS DOS: solo (b) marcaría las citas legítimas dentro de una explicación que sí razona; solo (a) marcaría media base de datos.
- **Medido el 31/07** (≥10 impresiones en 90 días): **2.185 preguntas · ~78.700 impresiones** — 1.040 copia literal y 1.145 casi literal. Concentración: Ley 39/2015 (511), CE (439), Ley 40/2015 (225), LPRL (190), LO 3/2018 (131). Calibrado sobre **muestra ciega de 600 preguntas** (8,2% caen en el cubo), no filtrando por su propia salida.
- **Punto ciego de los cubos que ya había.** «Apelotonada» mira la FORMA (>400 caracteres sin un salto), así que una transcripción bien maquetada se le escapa entera y una explicación buena escrita de corrido cae en él sin tener este defecto. «Nota de auditoría» es la explicación que habla de sí misma; aquí no habla: calla y copia.
- **Herramientas (ya en `main`):** núcleo puro `lib/health/explicacionTranscripcion.cjs` (13 tests) + cubo nuevo del extractor de siempre: `node scripts/apelotonadas/extraer-lote.cjs --cubo transcripcion --min-impresiones 10 --out <dir>`. El criterio necesita el artículo, así que el predicado SQL es solo prefiltro y el juicio lo pone el núcleo sobre las filas ya traídas.
- **Cómo se repara (una a una, NUNCA en automático):** verificar la clave contra el artículo → reescribir en formato estructurado con `scripts/aplicar-explicacion.ts` (una razón por opción, referida al CONTENIDO) → la pregunta nace **barajable**. Comprobar la cita con `citaNoLiteral` antes de aplicar. Si la clave no se sostiene, va a `needs_human`; no se auto-corrige.
- **Por qué merece la pena aunque sea larga:** son preguntas que YA se están sirviendo, con explicación inútil justo en el momento en que el opositor la abre porque ha fallado. Y cada una reparada sale del pozo de las que no pueden barajar.
- **AVANCE 31/07 — lotes 01 y 02 cerrados (32/2.185 · 5.609 impresiones, los dos de más exposición de toda la cola).** En el lote 02, **32 de 32 claves correctas** también: el defecto es siempre la explicación, nunca la respuesta. Quedan `unsafe` 3 preguntas del lote 02 y es correcto: son las de «Todas las anteriores» / «A) y B) son correctas», que no se pueden barajar por construcción. **Siguiente tanda: `lote_03.json`.**
- **AVANCE previo — lote 01 (16 preguntas · 3.131 impresiones).** Las 16 verificadas contra su artículo (las 16 claves eran correctas: el defecto era solo la explicación), reescritas estructuradas y **las 16 quedan `safe`** y fuera del cubo al re-medirlas. Caché purgada. Reparto: 9 CE, 4 Ley 39/2015, 1 LO 4/2001 y la impugnada `e60091bd`. Lotes en `scratchpad/t409/` (regenerables con el comando de arriba). 
- **Patrón que se repite y conviene saber antes de empezar una tanda:** casi todas son preguntas de «¿en qué artículo está X?» o de enumeración, con distractores que son artículos VECINOS. La explicación útil no es la cita —esa ya estaba—: es decir **qué hay en el artículo de al lado**, que es lo que el opositor confundió (137 frente a 140, 28.1 frente a 28.2, los valores del 1.1 frente a los fundamentos del 10.1, el ingreso del 31.1 frente al gasto del 31.2).
- **Relacionadas:** T-249 (cubo `nota-auditoria`, mismo pipeline), T-080 Fase 2 (explicación estructurada), `docs/maintenance/revisar-preguntas-con-agente.md`.

#### 📋 RECETA COMPLETA — cómo retomar esto desde cero (worktree nuevo, sin contexto)

1. **Sacar la cola** (los lotes NO están en ningún sitio: se regeneran, y es gratis):
   `node scripts/apelotonadas/extraer-lote.cjs --cubo transcripcion --min-impresiones 10 --tam 16 --out scratchpad/t409`
   Imprime cuántas quedan y las reparte por exposición descendente.
   ⚠️ **La numeración de los lotes NO es estable entre ejecuciones, y es a propósito.** Las ya reparadas salen del cubo (tienen `explanation_data`), así que al regenerar, **el `lote_01` de tu ejecución son las siguientes por atacar**. No busques «el lote 04»: empieza por el 01 de tu propia salida.
2. **Volcar una tanda de 4** con el artículo delante:
   `node -e "const l=require('./scratchpad/t409/lote_01.json'); l.slice(0,4).forEach(q=>{console.log('══',q.id,'|',q.ley,'art',q.article_number); console.log('P:',q.question_text); ['a','b','c','d','e'].forEach((k,i)=>{if(q['option_'+k])console.log(' ',(q.correct_option===i?'✔':' '),String.fromCharCode(65+i)+')',q['option_'+k])}); console.log('ART:',q.article_content.slice(0,900))})"`
3. **Verificar la clave contra el artículo** (viene en el propio JSON, no hace falta ir al BOE salvo duda). Si no se sostiene → `needs_human`, NUNCA auto-corregir la clave.
4. **Escribir el JSON de la explicación** (uno por pregunta, en `scratchpad/t409/exp/<8-primeros-del-id>.json`):
   `{ "cita": {"ref":"Art. X de la Ley Y","texto":"…literal del artículo…"}, "intro":"…", "options": {"0":"…","1":"…","2":"…","3":"…"}, "outro":"**Clave:** …" }`
   - Las razones se refieren al **CONTENIDO** de cada opción, **nunca a su letra ni a su posición** («la primera», «la anterior»): no sobreviven al barajado.
   - `intro` y `outro` se emiten VERBATIM en cualquier orden → **jamás nombres la clave por su letra ahí** (el aplicador rechaza el JSON si lo haces).
   - La `cita` se copia LITERAL del `article_content` del propio lote. Si no casa, el guardarraíl la caza.
5. **Aplicar** (dry-run quitando `--apply`):
   `npx tsx --env-file=.env.local scripts/aplicar-explicacion.ts <question_id> scratchpad/t409/exp/<f>.json --apply`
5.bis. **Gate de citas ANTES de aplicar** (paso 5 del manual, se saltaba):
   `node --env-file=.env.local scratchpad/t409/gate-citas.cjs --pre scratchpad/t409/lote_01.json scratchpad/t409/exp`
   Usa el `citaNoLiteral` de `scripts/impugnaciones/validar-explicacion.cjs` — el criterio ÚNICO de la casa, no una copia.
6. **Verificar la tanda y purgar caché** al cerrar el lote: contar que las 16 tengan `explanation_data`, re-medirlas con `clasificaTranscripcion` (deben salir 0 en el cubo) y `POST /api/admin/revalidate` con `{"tag":"questions"}` y el `x-cron-secret`. **OJO al medir:** `clasificaTranscripcion` devuelve `{clase, cobertura, motivo}` y «fuera del cubo» es `clase === null`; un check contra un `enCubo`/`esTranscripcion` inexistente sale verde SIEMPRE.
6.bis. **PASO 7 DEL MANUAL — re-verificación independiente sobre la pregunta VIVA, y el lote no se cierra sin ella:**
   `node --env-file=.env.local scratchpad/t409/dump-vivas.cjs scratchpad/t409/lote_01.json scratchpad/t409/reverif_lote_01.md`
   y un agente Sonnet aparte que lea ese fichero e intente REFUTAR: clave, verdad de cada razón (sobre todo las que hablan de OTROS artículos), literalidad de la cita, correspondencia enunciado↔artículo vinculado, y si algún DISTRACTOR es también verdadero. Si encuentra algo, se corrige y se vuelve a pasar.
7. **Anotar el avance en esta ficha** y `release` de la tarea. Es lo único que sobrevive a la sesión.

#### ⚠️ Gotchas ya pagados (no los redescubras)

- **`unsafe` al aplicar NO es un fallo tuyo.** Son las de «Todas las anteriores» / «A) y B) son correctas»: no se barajan por construcción. Comprobado además que **`shuffle_mode='no_shuffle'` manda por encima de `shuffle_safety`**, así que una de esas marcada `safe` tampoco se permuta.
- **Un fallo suelto de `aplicar-explicacion.ts` suele ser de CONEXIÓN.** Pasó una vez en mitad de un bucle de cuatro; al reintentarla sola entró sin cambiar nada. Reintenta antes de investigar.
- **La cita del blockquote se comprueba entera**, no solo los primeros caracteres (§5.1.bis del manual de impugnaciones).
- **Medir sin sesgo:** para re-calibrar el detector, muestra ciega con `scripts/apelotonadas/muestra-transcripcion.cjs` (no filtra por la salida del propio detector; imprime también los NEGATIVOS para poder juzgarlos).

#### 📈 Avance

- **02/08 — el bucle de esta ficha se quedaba CORTO respecto al manual, y las dos piezas que faltaban encontraron defectos reales.** La receta de arriba decía «comprobar la cita con `citaNoLiteral`» y el manual `revisar-preguntas-con-agente.md` exige además, en su **paso 7**, una **re-verificación por un agente INDEPENDIENTE sobre la pregunta VIVA en BD** — no sobre el JSON que uno acaba de escribir. Ni una ni otra se estaban ejecutando. Ahora van en el bucle, con dos herramientas de tres líneas en `scratchpad/t409/`: `gate-citas.cjs --pre|--post` (usa el `citaNoLiteral` del validador de impugnaciones, sin copiarlo) y `dump-vivas.cjs`, que vuelca la pregunta servida + su artículo para el agente. **Lo que cazó el paso 7 en los tres primeros lotes:**
  - Una **afirmación de derecho FALSA** escrita por mí en la razón de un distractor: decía que el art. 119 CE reconoce la justicia gratuita *«para quienes acrediten insuficiencia de recursos y en todo caso cuando la ley así lo disponga»*, invirtiendo el «en todo caso» (que va con la insuficiencia de recursos, no con la ley). La clave era correcta y todos los gates deterministas daban verde: **la forma estaba impecable y el contenido era falso**, que es exactamente lo que el manual dice que solo ve un agente independiente.
  - Un **enunciado que citaba el artículo equivocado**: dos preguntas decían «según el artículo 26 del RD 1708/2011» cuando el plazo que preguntan está en el 30 (el 26 regula el acceso restringido). Medido en el banco: **2 de 3** preguntas que citan ese artículo lo citaban mal; corregidas, con evento `enunciado_referencia_corregida`.
  - **DOS preguntas con dos respuestas válidas**, que no son defecto de explicación sino de OPCIONES, y que por eso NO se reescriben: `25d12017` (el art. 3.1 de la Ley 40/2015 enumera como principios tanto «eficiencia» como «servicio efectivo a los ciudadanos», que es justo el distractor) y `7b94e9a4` (el art. 2.2.b) sujeta a las entidades de derecho privado a los principios del art. 3 **y** a la ley cuando ejercen potestades administrativas: las dos cosas son ciertas). Las dos a `needs_human` por `transition_question_state`, con el porqué escrito en su historial. **GOTCHA:** `p_changed_by` de esa función es `uuid`, no texto — la atribución va en `p_notes`.
- **02-03/08 (sesión `preguntas-02ago`) — DOCE lotes cerrados: 192 preguntas**, las de más exposición que quedaban en la cola (la cola arrancó en **2.110 preguntas / 132 lotes** y quedan **1.918**). De las 192: **188 reescritas** con explicación estructurada y **4 retiradas a `needs_human`** (ver abajo). Re-medidas todas: **0 siguen en el cubo**, 0 sin `explanation_data`, **177 `safe` y 11 `unsafe`** —las siete tienen una opción del tipo «Ninguna de las anteriores» o «A) y B) son correctas», que no se barajan por construcción: es el gotcha ya documentado, no un fallo—. Caché de `questions` purgada tras cada lote (4 pasadas, es per-instancia).
  - **188 de 188 claves correctas.** Van 236/236 en este cubo contando los lotes de julio: el defecto sigue sin estar NUNCA en la respuesta.
  - **PERO el defecto que aparece cuando miras de verdad no es el que esta ficha esperaba, y no se arregla reescribiendo:** cuatro preguntas tenían **dos respuestas válidas** y son defecto de OPCIONES, no de explicación. `25d12017` (el art. 3.1 de la Ley 40/2015 enumera como principios tanto «eficiencia» como «servicio efectivo a los ciudadanos», que es el distractor entero), `7b94e9a4` (el art. 2.2.b) sujeta a las entidades de derecho privado a los principios del art. 3 **y** a la ley cuando ejercen potestades administrativas: las dos afirmaciones son ciertas), `3ee77365` (el art. 79.1 CE pide «la mayoría de sus miembros» y el distractor dice «mayoría absoluta de sus miembros», que es el MISMO umbral con otro nombre) y `a9a9a94c` (la clave es «renunciar a derechos» y el distractor «formular instancias» describe el mismo acto que «formular solicitudes» de la letra a)). Las cuatro a `needs_human` con el porqué escrito en `question_lifecycle_history` — se revierten en un comando si el criterio humano discrepa. **Las dos últimas son juicio, no hecho**, y por eso no se decidieron aquí.
  - **Defectos de ENUNCIADO encontrados de paso y corregidos (SIETE):** una servía un placeholder sin sustituir —*«a tenor de lo dispuesto en el artículo **XX** de la Ley…»*—, medido después en el banco entero: era la única (**gotcha de medición**: buscarlo con `LIKE '%artículo __ de la%'` da **6.002 falsos positivos**, porque en SQL `_` es un comodín de un carácter y casa con «artículo 26 de la»; con regex, cero). Otra citaba una ley por siglas sin desarrollar (`LPACAP`, que no está en la allowlist del manual) y otra descuadraba el número entre enunciado y opciones. Además, dos preguntas citaban «el artículo 26 del RD 1708/2011» cuando el plazo que preguntan está en el 30 (medido: 2 de las 3 preguntas del banco que citan ese artículo lo citaban mal), y una del TUE preguntaba por «el objetivo» cuando el objetivo estaba YA en el enunciado y lo que se pedía era la acción (art. 8.1: la Unión *desarrolla relaciones preferentes* CON EL OBJETIVO de establecer ese espacio). Reescrito. **Gotcha propio:** al reescribirlo puse «Tratado de Funcionamiento de la Unión Europea» donde la norma vinculada es el **TUE**; corregido en el momento, pero es el error fácil de esa pareja de siglas.
  - **El defecto que MÁS se repitió es de quien escribe, no del banco: citas LITERALES pero cortadas a mitad de frase** (una cita puede pasar `citaNoLiteral` y aun así dejar fuera justo la palabra que decide: «diez días» sin «naturales», «no se suprime o restringe debidamente» sin «la competencia de estos», «responderán frente al particular, en todo caso, de forma solidaria» sin su condicional). La re-verificación independiente lo cazó **5 veces**; al añadir la comprobación al gate aparecieron **7 más** que nadie había mirado. **Ya no depende de acordarse:** `gate-citas.cjs` avisa cuando la cita no cierra la frase (no bloquea: hay recortes legítimos). Las 20 corregidas; los doce lotes quedan a 0. **Y el gate tenía el mismo punto ciego por el otro extremo:** solo miraba el FINAL de la cita, así que no veía las que arrancan a mitad de oración («propuestas al empresario…» por «Los trabajadores tendrán derecho a efectuar propuestas al empresario…»). Lo señaló la re-verificación del lote 12 y, al añadir la comprobación de arranque en minúscula, aparecieron **5 más**. Moraleja para quien siga: un gate que se escribe a partir de UN caso solo cubre ese caso.
  - **Rendimiento medido**, para quien planifique: un lote de 16 cuesta unas 3 rondas de trabajo (volcado → 8 explicaciones → gate + aplicar) más la re-verificación en paralelo. **A 192 preguntas por sesión, la cola son ~10 sesiones más.** No se cierra de una sentada, y la ficha está pensada para pararse en cualquier tanda.
- **03/08 (segunda sesión) — DIECISÉIS lotes: 256 preguntas, 254 reescritas.** La cola arrancó en **1.916 preguntas / 120 lotes** y quedan **1.660**. Re-medidas las 256 juntas: solo 2 quedan fuera del arreglo y las dos a propósito (una mal vinculada, otra retirada a `needs_human`), y **218 quedan `safe` frente a 38 `unsafe`**, todas estas por tener una opción del tipo «Todas las anteriores» o «A y B son correctas», que no se barajan por construcción. **254 de 254 claves verificadas correctas** — van 490/490 en este cubo, y sigue sin aparecer una sola clave mala. Caché purgada tras cada lote.
  - **Lo más valioso de la sesión NO fueron las explicaciones, sino DOS HUECOS del detector de barajado que el propio bucle destapó al contar.** El recuento de cierre de un lote dio `16 safe / 0 unsafe` con una pregunta cuya opción decía «Son correctas las opciones a) y c)», y eso no cuadra. `optionsReferenceOtherOptions` cubría «las opciones A y C» pero no (a) el plural con letras entre PARÉNTESIS, (b) la letra en minúscula tras «opciones» —que ahí no es cita legal— ni (c) la conjunción «o»; y por separado tampoco veía (d) la meta-opción **sin sustantivo delante**, «B y C son correctas», cuyo patrón gemelo llevaba desde el principio en el detector de EXPLICACIONES y nunca se trajo al de opciones. **Cuatro preguntas se estaban sirviendo BARAJADAS con la clave citando a otras opciones** (`95003f09`, `57ed7965`, `daa99a80`, `97978377`): al permutar, la respuesta correcta señalaba a otras. Medido sobre las 138.106 activas, los patrones nuevos cazan 379 + 233 que antes eran invisibles y de paso dejan de marcar 5 falsos positivos del anterior («opciones **de b**úsqueda» se leía como letra D + conjunción E + letra B). Arreglado con `(?:\)|\b)` tras cada letra —eso es lo que sostiene la precisión— y exigiendo el verbo de veracidad pegado en el segundo patrón; capa en `__tests__/shuffle/opcionesQueCitanOtrasOpciones.test.ts` con los textos reales, los falsos positivos medidos y el caso hermano que NO debe marcarse (la letra del articulado tras «letra»/«apartado»). Re-marcadas las 9 `safe` afectadas por `record_shuffle_safety`. **Moraleja que conviene arrastrar: arreglar el detector NO mueve una sola fila** —el trigger invalida por hash del CONTENIDO—, así que todo arreglo de criterio necesita su pasada de recálculo, como ya avisa `npm run shuffle:recriterio`.
  - **Una QUINTA pregunta con dos respuestas válidas, y la encontró el paso 7: `119b649e`** (58 exposiciones). El enunciado pregunta de forma GENÉRICA cuándo tiene el interesado derecho a no aportar documentos, sin acotar apartado; la clave sale del art. 28.2 (ya en poder de la Administración actuante o elaborados por otra) pero el **28.3, párrafo 2.º** dice que las AAPP no requerirán documentos «aportados anteriormente por el interesado a CUALQUIER Administración», que es lo que afirma un distractor. Materialmente son lo mismo: si no pueden requerirlos, no hay que aportarlos. A `needs_human`, reversible. **Lección de dónde mirar: cuando el enunciado no acota apartado y el artículo tiene varios que dicen cosas parecidas, hay que sospechar de los distractores** — ese aviso ya va en los prompts de verificación.
  - **Una pregunta se deja SIN reescribir a propósito y es un tipo de defecto nuevo aquí: `6ffb3342`.** Clasifica los informes en preceptivos o facultativos y cuelga del art. 28 de la Ley 39/2015, que va de aportación de documentos y no lo dice; lo dicen el 79.1 y el 80.1. No se toca porque **no se puede citar honestamente un artículo que no responde**, y re-vincularla la sacaría de **80 de los 105 temas** en que hoy se sirve: eso es decisión de contenido con impacto de scope, fuera del alcance de este cubo. Queda registrada con evento `vinculo_articulo_no_responde` y el porqué.
  - **Defectos de ENUNCIADO corregidos de paso (20), todos con rastro** (`enunciado_referencia_corregida`, vía `scratchpad/t409/corregir-enunciado.cjs`, que hace el UPDATE y el evento en una transacción). Y **dos erratas en OPCIONES** («Del *Presiente* del Gobierno», «*Si*» sin tilde), que hasta ahora no tenían herramienta: `corregir-opcion.cjs`, deliberadamente limitado a ortografía —**aborta si la longitud varía más de 3 caracteres**—, porque cambiar el CONTENIDO de una opción toca a la clave y a la resolubilidad y no puede hacerse de pasada. El sistémico: **10 preguntas activas de la Ley 39/2015 se encabezaban con el nombre de la Ley 30/1992** («Régimen Jurídico de las Administraciones Públicas y del Procedimiento Administrativo Común»), **derogada en 2016**. Corregidas 6; **las otras 4 son de examen OFICIAL y NO se tocan** (regla del manual). Más tres typos de puntuación, una errata («sevicios públicos») y un **empalme sin limpiar** entre el encabezado de un apartado y su letra: el enunciado decía *«a través de **Cualquier** sistema…»*, con mayúscula a media frase y sin la palabra «otro» que el precepto sí tiene.
  - **La re-verificación independiente encontró defecto en 8 de los 12 lotes, y NINGUNO lo veía ningún gate.** Los seis eran del mismo tipo —una afirmación de derecho falsa sobre OTRO artículo, con la forma impecable—: el plazo de resolver la reposición (un mes, no dos), el de remisión del expediente en alzada (diez días, no quince), «reproduce» el art. 103.1 CE cuando lo amplía, los baremos de muerte o lesiones (Seguros obligatorios y Seguridad Social, no tráfico), el refrendo del art. 64.1 atribuido al Presidente del **Senado** cuando la Constitución lo reserva al del **Congreso**, y decir que el art. 119 CE está «en el mismo Capítulo» que el 24 y el 25 cuando vive en el Título VI y ellos entre los derechos fundamentales. **Conclusión operativa para quien siga: el paso 7 no es un trámite de cierre, es el único filtro que mira el CONTENIDO.** Y el sitio donde mirar está localizado: las frases que dicen qué hace otro órgano o a qué corresponde un plazo que no está en el artículo vinculado. Los prompts de re-verificación de esta sesión ya llevan esa instrucción explícita.
  - **El paso 7 volvió a ser lo único que encontró defectos, y los tres fueron de CONTENIDO con la forma impecable:** dije que dos meses es el plazo «para resolver el recurso de reposición» (es UN mes, art. 124.2), que quince días es el plazo de «remisión del expediente en la alzada» (son DIEZ, art. 121.2 — contrastable con otra pregunta del mismo lote) y que el art. 3.1 de la Ley 40/2015 «reproduce» el art. 103.1 CE, cuando lo AMPLÍA: la Constitución dice «sometimiento pleno a la ley y al Derecho» y la Ley añade «a la Constitución». Los tres pasaron todos los gates deterministas. **Y un aviso sobre el verificador: no siempre acierta.** Dudó del umbral de 4.000 habitantes del art. 13.2 LBRL creyendo recordar 5.000; el BOE consolidado confirma 4.000. Sus dudas se comprueban contra la fuente, no se aplican.
  - **PUNTO CIEGO DEL PROPIO GATE, y es el hallazgo más reutilizable de la sesión: comprobar que la cita ACABE en punto no sirve de nada si el punto lo pone quien escribe.** La cita de `24b8b23e` terminaba en «…devolviéndose los originales al interesado.» y en el artículo ahí hay una COMA: seguía «, sin perjuicio de aquellos supuestos en que la norma determine la custodia por la Administración…». Formalmente cerrada, materialmente cortada — exactamente lo que el gate existe para cazar, y pasaba limpia. Arreglado: ahora localiza el fragmento DENTRO del artículo y mira qué carácter le sigue; si no es un cierre, el punto es añadido. **La moraleja general vale para cualquier gate: comprobar la FORMA del recorte no dice nada si esa forma la produce el propio recortador; hay que comprobarla contra la FUENTE.** El arreglo se validó solo: dos lotes después cazó en vivo otra cita mía cerrada con punto donde el art. 23.2 b) de la Ley 40/2015 seguía con «, así como compartir despacho profesional o estar asociado con éstos…».
  - **Y había un TERCER modo de cita cortada, el peor de los tres porque el gate lo daba por bueno EXPLÍCITAMENTE: la que acaba en DOS PUNTOS.** Cuando el artículo es un encabezado seguido de enumeración —«…adoptará las medidas necesarias con el fin de que:»— y la respuesta depende de esa enumeración, la cita servida al opositor **no contiene nada de lo que prueba la clave**. Lo señaló la re-verificación del último lote sobre una pregunta de «Todas son correctas» cuyo blockquote no mostraba ninguna de las dos medidas. Al medirlo, era SISTEMÁTICO: **12 de las 256 citas de la sesión** estaban así. Once extendidas —la duodécima se deja porque ahí la respuesta sí está en el encabezado («la ley electoral determinará las causas…»), y por eso el gate AVISA en vez de bloquear—. Los tres modos juntos dan la regla: una cita puede estar mal cerrada por el final, por el principio o por quedarse en la puerta de lo que iba a demostrar.
  - **Erratas en OPCIONES: 9 corregidas**, y la herramienta ganó una salida explícita. El margen de 3 caracteres separa la errata del cambio de contenido, pero hay erratas legítimas que lo exceden —una palabra descolocada que deja la frase agramatical («Se deberá enviar un aviso **persona** dirección de correo postal…», en dos opciones a la vez)—. En vez de relajar el umbral, `corregir-opcion.cjs` tiene `--forzar`: el guardarraíl sigue protegiendo por defecto y la excepción queda REGISTRADA en el evento, que es lo que permite revisarla después.
  - **Todo lo tocado deja rastro consultable**, no solo la ficha: `observable_events` con `source='scripts/t409'` acumula **20 `enunciado_referencia_corregida`**, **9 `opcion_errata_corregida`** y **1 `vinculo_articulo_no_responde`**, cada uno con el texto anterior y el nuevo. Es lo que permite revisar o revertir sin depender de que alguien se acordara de anotarlo.
  - **▶ CÓMO RETOMAR (estado al cerrar la sesión del 03/08).** La tarea queda **liberada**, no pausada: no espera a nada ni a nadie, solo a que alguien coja el siguiente turno. Cola restante: **1.660 preguntas**. **Rendimiento medido en esta sesión: 16 lotes = 256 preguntas**, así que la cola son unas 6-7 sesiones más de este tamaño. Los lotes NO se conservan —se regeneran con el comando del paso 1 y **el `lote_01` de tu ejecución son las siguientes por atacar**, porque las 192 ya reparadas han salido del cubo—. **Las herramientas YA NO son locales: viven en `scripts/explicaciones/` con su README** (`gate-citas.cjs`, `dump-preguntas-vivas.cjs`, `corregir-enunciado.cjs`, `corregir-opcion.cjs`, `extender-cita.cjs`), registradas en `toolRegistry` y encontrables con `npm run tools:buscar -- gate citas`. Se subieron al comprobar que la promesa de esta ficha —«se reescriben en cinco minutos»— había dejado de ser cierta: solo el gate de citas acumula TRES modos de fallo que costó encontrar uno a uno. Y NO son de esta tarea: sirven para cualquier reescritura de explicaciones a escala (T-249, T-197, T-424, T-207). Lo único que hay que arrastrar es el BUCLE: verificar clave → escribir → gate de citas → aplicar → purgar caché → **re-verificación independiente sobre la pregunta viva**, y no cerrar el lote hasta que esa última pasada salga limpia.
- **02/08 — cuarto lote cerrado: 16 preguntas · 1.712 impresiones** (el `lote_01` de la regeneración de ese día, es decir, las 16 de más exposición que quedaban). Las 16 con explicación estructurada, **0 siguen en el cubo** al re-medirlas con `clasificaTranscripcion` (las 16 salen ahora por `analiza_opciones`), 15 quedan `safe` y 1 `unsafe` —`63a8ccbc`, que tiene una opción «Ninguna de las anteriores»: es el gotcha ya documentado, no un fallo—. Caché de `questions` purgada 4 veces. **Cola restante al empezar: 2.110 preguntas en 132 lotes.**
  - **16 de 16 claves correctas** otra vez: van 64/64. Sigue sin aparecer una sola clave mala en este cubo.
  - Reparto: 5 de CE (arts. 9, 53, 54, 55, 104, 156), 5 de Ley 39/2015 (arts. 7 ×2, 10 ×2, 18), 3 de Ley 40/2015 (arts. 25, 31, 44), 1 de TREBEP y 1 del RD 1708/2011.
  - **Gotcha de VERIFICACIÓN pagado aquí:** al re-medir, `clasificaTranscripcion` **no devuelve ningún booleano** — devuelve `{clase, cobertura, motivo}`, y estar fuera del cubo es `clase === null`. Un check escrito contra un campo `enCubo`/`esTranscripcion` inexistente sale «0 en el cubo» **siempre**, incluso sin haber tocado nada. Comprobar contra `clase`, no contra un nombre inventado.
- **31/07 — lotes 01, 02 y 03 cerrados: 48 preguntas · 7.656 impresiones**, los tres de más exposición de toda la cola. Las 48 con explicación estructurada, ninguna sigue en el cubo al re-medirla, caché purgada.
- **48 de 48 claves correctas.** Con tres lotes seguidos ya se puede afirmar: en este cubo el defecto es SIEMPRE la explicación, nunca la respuesta. Por eso la reparación es segura y rápida, y no abre `needs_human`.
- Reparto de lo hecho: 26 de CE, 8 de Ley 39/2015, 4 de Ley 40/2015, y una de LO 4/2001, Ley 7/1985, LPRL, TUE y RD 951/2005.
- **Patrón que se repite y conviene saber antes de empezar una tanda:** casi todas son preguntas de «¿en qué artículo está X?» o de enumeración, con distractores que son artículos VECINOS o listas hermanas. La explicación útil no es la cita —esa ya estaba—: es decir **qué hay en el artículo de al lado**, que es lo que el opositor confundió: 137 frente a 140, 28.1 (libertad sindical) frente a 28.2 (huelga), los valores superiores del 1.1 frente a los fundamentos del 10.1, el ingreso del 31.1 frente al gasto del 31.2, las garantías del 9.3 frente a los principios de organización del 103.1, o el decreto legislativo del 85 frente al decreto-ley del 86. Otras se juegan en UNA palabra: «marco general» frente a específico, elecciones «municipales» frente a autonómicas.
- **Relacionadas:** T-249 (cubo `nota-auditoria`, mismo pipeline), T-080 Fase 2 (explicación estructurada), T-425 (las casi duplicadas que aparecieron por el camino), `docs/maintenance/revisar-preguntas-con-agente.md`.

