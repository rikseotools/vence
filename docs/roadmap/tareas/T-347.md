### [T-347] ✅ [HECHA 07/08] Las fuentes del radar siguen igual que en la avería de julio: 101 en error y ninguna revisada

- **MEDIDO EL 31/07:** `detection_sources` tiene **173 fuentes, las 173 marcadas `is_active=true`**. De ellas: **101 con `last_error`**, **99 que no han tenido un solo éxito jamás** (`last_success_at IS NULL`) y **0 revisadas en las últimas 48 horas**.
- **LO GRAVE NO SON LOS NÚMEROS, ES QUE SON LOS MISMOS.** El runbook `docs/runbooks/salud-radar.md` documenta como incidente del 16/07 exactamente esto: *«101 de 173 fuentes (58%) en error, 99 sin funcionar jamás, 45 días sin ejecutarse. Todas marcadas is_active=true. Ninguna alerta.»* Dos semanas después la foto es idéntica → se escribió el diagnóstico y no se arregló (o volvió, que para el catálogo da igual).
- **Por qué importa:** `is_active=true` en una fuente rota significa que el panel la cuenta como vigilada. Un badge de OEPs a cero puede ser «no hay convocatorias» o «nadie está mirando», y hoy no se distingue — que es justo el motivo por el que existe ese runbook.
- **Primeros pasos (sin dar la causa por hecha):** separar las 99 que nunca funcionaron (probablemente mal dadas de alta: URL, selector o fetcher que no aplica) de las que funcionaron y se rompieron. Y comprobar **quién consume `detection_sources`**: si ningún cron las recorre, el problema no son las fuentes sino el proceso que debería visitarlas.

- **✅ RESPUESTA (07/08, w4): la foto nunca va a cambiar, y NO hace falta que cambie — la tabla está DORMIDA a propósito desde hace más de dos meses.**
  - **DEMOSTRADO por ausencia de código, no supuesto.** `detection_sources` solo tenía DOS escritores en todo el repo: `getActiveSources()`/`updateSourceCheckResult()`, uno en el backend (`oep-signals-queries.service.ts`) y su gemelo en el frontend (`lib/api/oep-signals/sources.ts`). Grep exhaustivo de sus nombres contra `backend/src` Y `app`/`lib` completos: **cero llamadores en los dos**. Ninguna otra pieza de código toca esa tabla. Así que da igual cuándo fue el último `last_checked`: no puede haber uno nuevo, porque no existe ningún camino de código que escriba ahí.
  - **Por qué: el sensor al que pertenecía, `detect-regional-oeps`, está RETIRADO desde el 01/06/2026** — y esto YA estaba escrito, solo que en otro documento sin enlazar desde aquí: `docs/maintenance/oeps-convocatorias-seguimiento.md` §10 lo dice con fecha y motivo exactos («56% error + falsos positivos... Código borrado en `backend/src/detect-regional-oeps/`; la tabla `detection_sources` (167 filas) quedó DORMIDA sin cron que la leyera») y §14.4 lo repite: *"El sensor `regional_scan` y su cron `detect-regional-oeps` ya no existen."* El 16/07 y el 31/07 se re-diagnosticó el SÍNTOMA sin conectar con esta causa ya documentada — la ficha original de este mismo hallazgo llevaba la respuesta escrita desde antes de nacer.
  - **Arreglado, en dos sitios, para que la próxima vez que alguien siga `salud-radar.md` no vuelva a perder tiempo en esto:**
    1. **El runbook** (`docs/runbooks/salud-radar.md`, §2 y la tabla de "mudo por decisión ≠ mudo por avería"): aviso explícito de que esta consulta va a dar siempre ~101/~99, con la fecha de retirada, el enlace a la fuente original, y qué hacer en su lugar (decidir si alguna fuente merece darse de alta en `generic_source_checks`, que es el modelo vivo — eso sí es trabajo real).
    2. **Código muerto eliminado** (no dejado tras un flag: "código que nadie llama pero que alguien puede volver a llamar es una factura esperando"): `getActiveSources()`/`updateSourceCheckResult()` del backend (con su tipo `DetectionSourceForScan`, también sin otro uso), y el fichero frontend **completo** `lib/api/oep-signals/sources.ts` (los 4 exports, todos huérfanos). De paso, mismo hallazgo en `lib/api/oep-signals/regional-extractor.ts` (la extracción LLM del sensor retirado, frontend-era, también sin ningún importador) — eliminado igual. La TABLA `detection_sources` no se toca: sigue con sus filas históricas, y el mapping Drizzle (backend y frontend) se conserva por si alguna vez se decide qué hacer con ellas.
  - **Verificado:** `tsc --noEmit` limpio en backend y raíz tras los borrados; `npx jest oep-signals` (backend) 54/54 verde; suite completa de backend 1471/1472 (el único rojo, `canary-registry.spec.ts`/`identidad-pago`, confirmado preexistente y no relacionado — falla igual con `git stash`); ningún test en `__tests__/` del repo referenciaba ninguno de los dos ficheros borrados.
  - **SOSPECHO, sin poder confirmarlo:** no pude releer `detection_sources` en vivo (RLS activo, 0 políticas para `vence_lector` — mismo hueco de T-573/T-038 que otras tablas, `detection_sources` tampoco está catalogada en `scripts/canary-rol-lector.cjs`) para poner una cifra de HOY junto a la del 31/07. No hacía falta para la conclusión (la ausencia de código es más fuerte que cualquier snapshot: no importa el valor, no puede cambiar), pero se deja anotado el hueco del canario por si alguien quiere ampliarlo.
  - **Lo que NO se ha hecho, a propósito:** decidir si alguna de las ~167 fuentes antiguas merece revivirse bajo `generic_source_checks` (el modelo que SÍ funciona). El propio runbook ya avisa de que "no escala" añadir páginas de entidad y que el boletín es la vía que cubre el catálogo — no hay una decisión pendiente urgente aquí, y si alguna vez interesa revisar el listado de 167 fuentes en busca de algo genuinamente insustituible, es trabajo de investigación nuevo, no la continuación de este.
- **Relacionada:** [T-348], `docs/runbooks/salud-radar.md`, `docs/maintenance/oeps-convocatorias-seguimiento.md` §10/§14.4.

