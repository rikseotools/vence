### [T-125] 🟡 [REABIERTA 30/07 — la verificación de la pasada siguiente nunca se hizo] Fuentes de seguimiento que no se pueden vigilar — cerrada: la ficha original apuntaba a una cosa y el problema era otra
- **Resultado en una línea:** de las 3 fuentes que motivaron la ficha, **ninguna necesitaba lo que la ficha decía construir**. Al bajarlo a código apareció un problema **20 veces mayor** (15 fuentes ciegas, 6 en activas → drenadas) y un **desperdicio de 47 invocaciones diarias de Lambda** que nadie había medido.
- **✅ Detección integrada:** núcleo puro `lib/convocatoria/seguimientoVigilable.cjs` (42 tests) + kind `seguimiento_fuente_ciega` en el gemelo CLI y en el `@Cron` + `runbookRegistry` + frase-gatillo + migración `checked_url` (capa de atribución, sin la cual una oposición recién repuntada se juzga con la evidencia de su URL anterior) + 7 tests de integración. **Desplegado y verificado en prod** (`c85c983d`).
- **✅ Drenadas las 6 activas** (`seguimiento_fuente_ciega` 6 → 0, verificado re-corriendo el sweep) con `repuntar-url.cjs`, que rehúsa escribir una URL no vigilable. Salvedad honesta: 5 de las 6 son ÍNDICES, no fichas — esos portales no exponen URL por proceso.
- **❌ El headless NO era la solución, y se midió en vez de suponerlo.** `jgpa.convoca.online` devuelve **«Este navegador no es soportado por Convoca»** ante el Chromium de la Lambda (un Playwright local sí pasa); `interior.gob.es` sigue en **403** — el headless no cura un WAF. Dos pistas falsas descartadas por el camino y documentadas en `salud-radar.md` para que nadie las repita: el `304` era caché del navegador reutilizado (con cache-buster da 200 y **el mismo** contenido), y `wait_for` **sí** funciona (esperó los 10 s porque el selector no iba a aparecer nunca).
- **✅ Cabo mayor que salió de ahí:** de las **67** fuentes marcadas `fetcher_type='headless'` a mano, solo **12 aportan**. Barrido con la sonda nueva y **47 revertidas a `http`** en dos tandas (20 catálogo + 27 activas), cada una **re-midiendo en el momento** y con traza en `observable_events`. Quedan 20 en `headless`: 12 que sí aportan y 8 huecos con nombre (ahí el problema es la URL, y cambiar el fetcher lo enmascararía).
- **⏳ VERIFICAR EN LA PASADA DE MAÑANA (09:30 y 10:00 UTC):** que ningún sensor pierda señal en las 47 revertidas. Las 47 trazas están en `observable_events WHERE event_type='fetcher_type_ajustado'`; revertir una es `node scripts/seguimiento/ajustar-fetcher-type.cjs --slug <slug>` mirando su medición.
- **🔴 DECISIÓN PENDIENTE (de Manuel, no es trabajo):** el cron `check-seguimiento` está **retirado a propósito** desde el 20/07 (T-050, flag `CHECK_SEGUIMIENTO_ENABLED`), así que el detector de fuentes ciegas vive de **evidencia congelada del 20 de julio** y no se refresca. Tres opciones: (a) reactivar el bucle de fetch como pura telemetría —la señal ruidosa que motivó su retirada ya está borrada del código desde el 26/06, así que solo refrescaría `http_status`/`content_preview`—; (b) alimentar el detector desde los fetches del sensor LLM, que ya descarga esas URLs a diario (más limpio, más trabajo); (c) aceptar que fue una auditoría puntual. **Sin esa decisión, la señal de "hash inmóvil" no tiene sentido: mediría un flujo apagado.**
- **Cabos que se van a otras fichas:** el falso positivo de `ordenanza-ayuntamiento-cordoba` en `seguimiento_url_stale` → [T-113]. La discrepancia de plazas de `auxiliar-administrativo-ayuntamiento-murcia` (nuestra `boe_reference` dice 18+2; PAG y MurciaEmplea, 20+2) → verificar contra el BOE y las bases, sin tocar el dato a ciegas.
- **Y de aquí salió [T-130]** (registro de herramientas + guardarraíl anti-duplicado), porque esta ficha es el caso de estudio: mandaba construir algo que ya existía.

- **▸ RE-MEDIDO 30/07 (la re-medición que la ficha pedía y nunca se hizo): 898 preguntas activas en 126 leyes.** Concentradas: **8 leyes = 537** y una cola de **97 leyes con 1-4 preguntas = 182** (que la ficha ya adjudicó como no rentables de escopar). Comando: consulta determinista «ley con preguntas activas + 0 filas de `topic_scope` unidas a un `topic` activo».
- **▸ RECUPERADAS 197 (30/07), y verificadas EN PRODUCCIÓN con el contador, no con el HTML:** `Carta Social Europea` (139) y `Consejo DDHH ONU` (58) enganchadas a **guardia_civil T1**, cuyo epígrafe las nombra **literalmente** («Carta Social Europea (revisada)», «Consejo de Derechos Humanos de la ONU») y que no tenían gemelo escopado. `GET /api/questions/filtered?action=count&topicNumber=1&positionType=guardia_civil` → **425** (era 229). Huérfanas globales **898 → 701**.
  - **Herramienta nueva y registrada: `scripts/scope/escopar-ley-entera.cjs`** (dry-run por defecto). Se niega si el epígrafe NO nombra la ley —salvo `--motivo` escrito, que queda en el evento—, es idempotente, y expresa «toda la ley» por AUSENCIA de `article_numbers` (así no añade un escritor más a esa columna, que tiene trinquete). Nace porque el repo tenía una docena de `scripts/_xxx.cjs` de un solo uso haciendo este `INSERT` sin ninguna guarda.
  - **Comprobado antes de escribir:** `guardia_civil` **no** tiene `questionTag` (solo `policia-nacional` lo tiene), así que aquí escopear SÍ basta — el gotcha que costó un falso «verificado» en la pasada de julio.
- **▸ NO se escopa la Directiva de Eficiencia Energética (106), y el motivo es de programa:** el epígrafe de GC T16 pide la **Directiva 2012/27/UE, capítulos I y II**, pero **91 de las 106 preguntas son de la Directiva 2023/1791** (la refundición posterior). Escoparla serviría materia que el epígrafe no pide. Es decisión de dominio —¿el examen se hace sobre la vigente?— y necesita mirar la convocatoria, no un `INSERT`.
- **🔴 HALLAZGO COLATERAL, y es MÁS GRANDE que la propia ficha: el Tema 1 de Guardia Civil sirve 2 de las 14 normas que enumera su epígrafe.** Al mapear el epígrafe contra el banco salió que DUDH (271), CEDH (144), PIDESC (127), PIDCP (123), Carta DDFF UE (116), Protocolo Tortura (45) y Convención Tortura (33) —**859 preguntas activas, ya escritas y verificadas**— existen y se sirven… **solo en `policia_nacional` T27/T4**. El opositor de Guardia Civil tiene un Tema 1 titulado «Derechos Humanos» donde no practica ninguna de ellas.
  - **No lo ve ningún detector**, y por eso llevaba ahí: el de esta ficha busca leyes con scope CERO (estas tienen scope… en otra oposición) y los de huecos de tema no disparan porque el tema tiene preguntas. Es el patrón `scope_titulo_huerfano` pero a nivel de LEY y cruzando oposiciones.
  - **✅ APLICADO con el OK de Manuel:** las 7 enganchadas a GC T1. **El tema pasa de 229 a 1.146 preguntas**, verificado con el contador de producción (`byLaw` confirma las 11 leyes). Al opositor de Guardia Civil le aparece el Tema 1 que su programa describe.
  - **✅ Y el mismo patrón, aplicado al resto de Guardia Civil (30/07):** el runner señalaba 6 candidatas más en 3 temas; adjudicadas una a una contra su epígrafe, **3 eran reales y 3 ruido**.
    - **Reales:** `Principios Fuerza ONU` (97) → **T19**, cuyo epígrafe la nombra con su título completo («…adoptados por el Octavo Congreso…»); y los **dos** contenedores de CCN-CERT (39+37) → **T17**, cuyo Bloque 3 es literalmente «Principios y recomendaciones básicas en Ciberseguridad del CCN-CERT» y cuyas preguntas empiezan por esa misma frase. Verificado en producción: **T19 107 → 204** y **T17 286 → 342**.
    - **Ruido, y conviene saber por qué:** `LEY PREVENCIÓN DE RIESGOS LABORALES ENF` en T3 (el tema **ya sirve la LPRL**: es el mismo contenido con otro contenedor) y `Sistemas Operativos (técnico)` (486) + `Administración de Sistemas Operativos` (151) en T17 — el epígrafe **no** pide sistemas operativos (casó con «instrumentos operativos» y «Sistemas de Telecomunicaciones») y ese contenido es de nivel Técnico de Informática: servírselo a un opositor de GC sería peor que no darle nada.
    - **Cabo menor anotado:** hay **dos contenedores gemelos de CCN-CERT** (39 y 37 preguntas, contenido distinto, mismo documento). Alguien debería fusionarlos; hoy se escopan los dos porque las preguntas no se solapan.
  - **📊 Total de la sesión: ~1.070 preguntas puestas a servir** sin generar ni una (T1 229→1.146, T19 107→204, T17 286→342).
  - **¿Y esto debería avisar en `/admin/contenido`? SE INTENTÓ, SE MIDIÓ Y LA RESPUESTA ES NO (todavía).** El detector existe —núcleo puro `lib/health/normaDelEpigrafeSinEscopar.cjs` + runner `scripts/scope/audit-normas-del-epigrafe.cjs`— pero **bajo demanda**, porque la precisión no da para una bandeja automática: 891 hallazgos con el criterio inicial → 226 exigiendo 3 palabras significativas y que lo ignore la oposición ENTERA → **150** excluyendo familias ya servidas… y al muestrear los mayores, **la mayoría eran falsos positivos**. Dos causas que un matcher léxico no puede resolver: **contenedores equivalentes con otro nombre** (un tema que escopa `LPRL` acusado de no servir `LEY PREVENCIÓN DE RIESGOS LABORALES ENF`; `Excel 2019` frente a `Excel 365`, donde además la versión **se averigua, no se deduce**) y **epígrafes con un ANEXO del boletín pegado**, que casan palabras que no son del programa. Cablearlo así habría entrenado a ignorar la categoría entera — la lección de [T-047]/[T-113]/[T-179]. Los 3 falsos positivos medidos quedan fijados como casos NEGATIVOS en los tests, que es lo que permitirá subirlo al badge el día que se sepa establecer equivalencia entre contenedores.

- **⚠️ REABIERTA el 30/07 tras una revisión independiente del backlog.** Se había dado por cerrada, pero su propio texto declaraba trabajo vivo: cerrarla la sacaba del pool **y** dejaba el trabajo sin hacer. Lo destapó la revisión de los 70 cierres al mover fichas a «## Hechas» — y lo confirmó un agente leyendo la ficha entera, no el detector automático.
