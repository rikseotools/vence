### [T-446] ✅ [HECHA 31/07] Lo que tenía guardarraíl volvió solo; lo que no, siguió muerto — un commit rancio sobre `scripts/backlog.cjs`

> **Instancia concreta del fenómeno que cubre [T-443]**, abierta y cerrada en el mismo commit para dejar el rastro. No compite con aquella: el análisis del fenómeno vive allí, y su sesión la tenía reclamada mientras esto pasaba.

- **ORIGEN.** Al ir a desplegar el frontend (política de agrupar, 12 tareas esperando), `deploy-cuando-verde.sh` abortó en la vuelta 5: *«❌ CI en ROJO para `bd7d56fb5` — alguien rompió main»*.
- **QUÉ PASÓ.** El commit `6f3e26261` escribió `scripts/backlog.cjs` entero desde una copia **anterior** a dos cambios que ya estaban en `main`, y se llevó por delante trabajo de **dos** sesiones distintas:
  1. la extracción de los hechos de git a `lib/backlog/gitFichas.cjs` ([T-427]) — el módulo **seguía existiendo**, lo que desapareció fue la llamada, así que el arreglo quedó **vivo pero inerte**;
  2. la **segunda puerta del `done`** ([T-392] F1), la que mira los HECHOS en vez del texto (si el `sha` vivo no incluye los commits que declaran la tarea, no puede estar verificada), junto con el emisor de fricción `friccion()`.
- **🔑 EL HALLAZGO, que no es el que yo esperaba.** Los dos trozos se perdieron en el mismo commit y a la misma hora. **El primero volvió en menos de una hora y por dos caminos independientes** —lo restauré yo y lo restauró su propia sesión en `04b79fdcc`, cada uno por su lado— porque tenía un **guardarraíl que puso el CI en rojo**. El segundo **siguió muerto**, y nadie lo echó de menos: no había test que lo vigilara, así que su desaparición fue exactamente igual de silenciosa que su ausencia.
  > **Un guardarraíl no solo impide el fallo: hace que el trabajo sea RECUPERABLE.** Sin él, el código borrado no se distingue del código que nunca existió.
- **RESOLUCIÓN.** Restaurada la segunda puerta sobre la versión viva (no revirtiendo el commit entero: eso habría borrado lo que ese commit sí aportaba, o sea repetir el fallo en la otra dirección). Verificado que `scripts/backlog/verificacion.cjs` y `scripts/friccion-emitir.cjs` siguen en el repo — como con el otro módulo, lo que faltaba era el cable, no la pieza. **1.108 tests verdes** en `__tests__/backlog/` + `__tests__/guardrails/`.
- **Y un tercer defecto encontrado por el camino, del mismo día:** el backend **no compilaba** (`.rows` sobre el `RowList` que ya devuelve `db.execute`, en `content-health-sweep.service.ts`). **El CI no puede verlo**: su job `typecheck` corre el `tsconfig` de la RAÍZ, que excluye `backend/`. Solo lo caza el guard de pre-push, y solo a quien toque ese fichero — o sea que habría bloqueado el siguiente deploy de backend sin que nadie supiera por qué.
- **POR QUÉ NO SE VIO ANTES.** El guardarraíl de [T-427] hizo su trabajo: el CI se puso rojo **al pushear**. Lo que falló fue **cuándo se leyó ese rojo** — nadie lo miró, y el defecto se descubrió al ir a desplegar, cuando ya bloqueaba a las 12 tareas en espera y con ellas a todas las sesiones. **Un rojo de `main` no tiene dueño:** quien lo provoca ya se ha ido y quien lo sufre está haciendo otra cosa.
- **Relacionadas:** [T-443] (el fenómeno), [T-427] y [T-428] (la misma destrucción sobre el markdown, no sobre un script).

