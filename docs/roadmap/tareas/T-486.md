### [T-486] 🟠 [ABIERTO 02/08] Piloto de flota remota en Koigrid: 2 trabajadores + supervisor de EVIDENCIA, con el mismo reparto que las sesiones locales

- **✅ REVISIÓN (08/08, w4): los dos huecos del último checkpoint estaban RESUELTOS por trabajo posterior, y un tercero se QUEDA cuantificado con cifras reales.** El `resume_check` con el que se retomó esta tarea citaba dos `falta`: (1) el VPS sin credenciales de git, rescate manual con bundle+SSH; (2) w1-w4 compartiendo un checkout con un solo índice de git. Los dos son de una entrega ANTERIOR a rondas de trabajo que ya están en `main` y no se habían volcado a esta ficha.
  1. **(1) RESUELTO por [T-628], no manual.** `lib/flota/rescate.cjs` (`necesitaSegundaFase`) + el bloque `rescatar` de `scripts/flota/flota.cjs` (líneas ~999-1026) ya hacen la segunda fase automática: el portátil/supervisor (única máquina con SSH a la vez que credenciales de git) trae por SSH las refs que el VPS identificó pero no pudo empujar y las publica como `rescate/<w>-...`, con el mismo generador de nombres que usó el rescate (nunca dos generadores del mismo hecho). Verificado ejecutando, no leyendo: `npx jest __tests__/flota/rescateSegundaFase.test.ts` → **15/15**, incluida la reproducción del bug real que motivó el fix (`VENCE_FLOTA_AQUI=flota-1`, la config real del supervisor, hacía que `maquinaDe('w1').local` diera `true` y el criterio viejo dijera "no hace falta" con commits confirmados fuera de todo remoto); y `npm run sim:rescate-flota` (8 escenarios contra repos git reales) → **0 fallos**.
  2. **(2) FALSO — refutado empíricamente, no solo leído.** Escribo esto como `w4`, que es EXACTAMENTE uno de los cuatro trabajadores que la ficha decía compartiendo índice. Medido en esta misma máquina:
     ```
     $ git rev-parse --git-dir
     /home/flota/vence/.git/worktrees/w4
     $ git rev-parse --path-format=absolute --git-path index
     /home/flota/vence/.git/worktrees/w4/index
     $ git worktree list   (desde el repo base)
     /home/flota/vence                            f72815597 [main]
     /home/flota/vence-sessions/w1                4297821cc [flota/w1]
     /home/flota/vence-sessions/w2                284c7c4a3 [flota/w2]
     /home/flota/vence-sessions/w3                3f92396ec [flota/w3]
     /home/flota/vence-sessions/w4                3f92396ec [flota/w4]
     ```
     Cada trabajador tiene su PROPIO `git worktree add` (via `crear-worktree.sh`, invocado por `arrancar-trabajador.sh` §5) con su PROPIO índice bajo `.git/worktrees/<w>/index` — estructuralmente idéntico a como funcionan `l1`-`l6` en el portátil, y exactamente lo que [T-592] (06/08) ya arregló (antes `arbolDe` devolvía el clon base para todos; ahora devuelve `~flota/vence-sessions/<w>`). El único sitio compartido es `~flota/vence` (`.git` común + `npm ci`), y ahí solo corren `fetch`/`clone`, nunca `add`/`commit`. El riesgo que describía el `falta` no existe en el código tal como está desplegado hoy.
  3. **(3) CUANTIFICADO, no arreglado — necesita acceso que un trabajador no tiene.** El tercer punto de "LO QUE QUEDA" (línea de abajo, 05/08): *"el consumo de LLM de la flota es invisible para `llm:gasto`"*. Medido de verdad, no supuesto: `node scripts/observabilidad/ingest-claude-code-usage.cjs --dry` **desde esta misma máquina VPS** encuentra **261 filas (día×sesión×modelo) · 45.456 respuestas · 28,9 M tokens de salida · 19.796,9 M tokens de caché leída** en los transcripts locales de `~/.claude/projects` — SOLO de este worker, ni un byte de eso llega hoy a `observable_events`. Intenté escribirlos de verdad (sin `--dry`): **`❌ permission denied for table observable_events`**, porque el script hace `DELETE ... WHERE dedupeKey = ...` antes de re-insertar (para que una sesión que sigue acumulando tokens se actualice, no se duplique) y `vence_coordinacion` es **solo INSERT** sobre esa tabla — por diseño (CLAUDE.md, "observable_events solo INSERT — nunca la lee"), no un descuido. **No hay que ampliar el permiso** ("¿meto una credencial de más para que pase? → no. Se acota el permiso."): un trabajador con DELETE sobre observabilidad podría borrar el rastro de otro. El diseño correcto es el MISMO patrón que [T-628]: la máquina que tiene las dos mitades (SSH a cada worker + `DATABASE_URL` con permiso completo) trae los transcripts remotos y corre la ingesta desde allí — el script ya soporta apuntar a un directorio distinto vía `CLAUDE_TRANSCRIPTS_DIR`, así que solo falta el `rsync`/`ssh cat` que traiga `~/.claude/projects` de cada `w1`-`w4` a un sitio donde el portátil pueda apuntar esa variable. No lo construyo yo: necesita SSH a las OTRAS máquinas de la flota (`w1`-`w3`), que un trabajador no tiene ni debe tener.
  **Nada de esto es código nuevo para T-486 en sí** (es verificación + una cifra), así que no hay rama de producto que revisar — solo esta corrección de ficha.

- **✅ EL CICLO DE UNA TAREA YA GIRA SOLO (06/08, sesión larga).** Al empezar el día la flota entregaba trabajo que se quedaba en su disco; al terminarlo, reparte → trabaja → empuja → entrega → **revisa** → y el rescate recoge lo suelto. Lo que se arregló, en orden de aparición y todo medido:
  1. **El rescate miraba `HEAD`** y daba «nada que salvar» con **22 commits atrapados** en 8 ramas del VPS. Un trabajador entrega en una rama por tarea y vuelve a `main`: lo entregado nunca es `HEAD`.
  2. **`arbolDe` devolvía el clon base**, no el worktree de cada trabajador → la sonda decía SIEMPRE «al día» y nadie actualizaba ni rescataba el árbol real (`w1`: 80 commits por detrás; `w2` llegó a 127). Lo encontró **w2 revisando su propia tarea** [T-592] y lo verifiqué contra el VPS.
  3. **El encargo se contradecía**: regla dura «NO pushees a main» y seis líneas después «git push origin HEAD:main». Por eso el trabajo se quedaba sin empujar.
  4. **Credenciales de git en el VPS** (no las tenían) + **entorno filtrado** (`entornoTrabajador.cjs`): fuera Stripe, Bitrefill, Ads y correo; dentro BD, AWS y el proveedor de IA. Más la puerta `dinero.cjs`, gemela de la de envíos.
  5. **La flota fuera de impugnaciones y el portátil fuera del reparto** (`reparte:false`), los dos por decisión de Manuel.
  6. **La puerta de impugnaciones estaba cerrada A MEDIAS** (se cambió `vigilar` y no `repartir`), y el test que debía cazarlo pasaba en verde porque comprobaba que la variable APARECIERA en el fichero.
  7. **El reparto no recordaba entre invocaciones** (dos `repartir` daban la misma tarea a dos trabajadores, dos veces). La memoria sale ahora del `flota_turno` que ya se emitía.
  8. **El rescate pisaba un commit vivo** (`index.lock`). Un `index.lock` NO es basura por defecto: ahora mira si hay un git corriendo y se aparta.
- **✅ EL ESCALÓN QUE NO TENÍA MOTOR.** De los seis escalones del ciclo, cinco avanzaban solos y `entregada → revisada` esperaba a que **alguien decidiera mirar**: **23 entregas paradas, 15 h de media, la más vieja 41 h**. No era una cola lenta, era una cola sin salida. Ahora lo mueve otro trabajador con `encargoRevision` + `backlog.cjs revisado`, con tres reglas en la TABLA (veredicto cerrado · hallazgos con longitud mínima · **nadie revisa lo suyo**).
  **Y los veredictos salieron buenos, que era la duda:** `w2` verificó T-533 con un **WebFetch en vivo a la página servida** (comprobando `x-cache: Miss`) además del BOE; `w4` dijo **PROBLEMAS** en T-315 y **reprodujo las mediciones del otro 22 h después** (86%/94%/73% frente a 79%/85%/74%) para confirmar que *«w2 realmente ejecutó la query, no estimó los números»*. Dos revisores independientes sobre T-533 coincidieron **por caminos distintos**.
- **✅ EL AVISO LLEGA CUANDO HACE FALTA (`avisosPorMateria.cjs`).** Tres trabajadores verificaron un atajo contra `support.microsoft.com/es-es` —la fuente que el manual desaconseja— y **el aviso llevaba escrito desde dos días antes**: lo tenían, en la línea ~800 de un manual de 2.157 que el encargo manda «leer entero». La idea de bajarles la memoria del proyecto **se descartó tras medirla** (37 de 40 memorias ya estaban en el repo): el problema no era dónde vivía el conocimiento sino CUÁNDO aparecía. Ahora el dossier detecta la materia y avisa **antes de la checklist**, o sea antes de elegir la fuente.
- **EL PATRÓN DE LA JORNADA, y es lo que hay que recordar:** **ocho defectos, y los ocho los encontró EJECUTARLO.** Ninguno lo vio un test. Y en **tres** casos el test que debía cazarlo pasaba en verde con el arreglo a medias (la variable que «aparecía», la orden que «mencionaba» `git push`, y el patrón de atajos que no cogía el enunciado real). De ahí que los guardarraíles nuevos exijan que **las dos puertas pregunten al mismo sitio**, no que exista la palabra.
- **LO QUE QUEDA:** los trabajadores del VPS aún no despliegan (`puedeDesplegar:false`) — se puede levantar en cuanto [T-485] se verifique en un deploy real, que ya está pausada esperándolo. Y **la aprobación de borradores sigue siendo solo de Manuel**: 15 esperando, y ese número CRECE cuanto más trabajan las sesiones, porque el análisis escala y la aprobación no.
- **✅ BALANCE DE LA JORNADA DEL 05-06/08 — y el patrón que la resume.** Seis defectos del andamiaje encontrados en un día, y **los seis los encontró EJECUTARLO, ninguno los tests**:
  1. El rescate miraba `HEAD` y daba *«nada que salvar»* con **22 commits atrapados** en 8 ramas del VPS (entregas de T-418, T-443, T-525, T-529). Un trabajador entrega en una rama por tarea y vuelve a `main`: lo entregado nunca es `HEAD`.
  2. `arbolDe` del registro devolvía el **clon base**, no el worktree del trabajador, así que ni el rescate veía lo sin commitear ni `ponerAlDia` actualizaba el árbol correcto (`w2` llegó a **70 commits por detrás**). → [T-592]
  3. `a_medias` da trabajo y **no actualiza nunca**, así que quien empieza a descolgarse ya no vuelve. Cuanto más trabaja, más viejo es su clon. → [T-592]
  4. La cola repartía impugnaciones **ya trabajadas**: 15 de 16 abiertas tenían borrador esperando aprobación, y el claim no puede verlo porque el trabajador suelta la fila al terminar.
  5. La puerta de impugnaciones se cerró **a medias**: se cambió `vigilar` y no `repartir`, así que el reparto dependía de por dónde entrases.
  6. El candado de deploy no arrancaba desde el árbol de deploy (sin `node_modules`), y lo tumbó el **primer deploy real**.
  **Y en DOS casos el test que debía cazarlo pasaba en verde con el arreglo a medias:** el de las impugnaciones comprobaba que la variable APARECIERA en el fichero (aparecía, con una sola puerta arreglada), y el del rescate comprobaba que la orden MENCIONARA `git push` (la mencionaba, mirando donde no estaba el trabajo). De ahí que los guardarraíles nuevos exijan que **las dos puertas pregunten al mismo sitio**, no que exista la palabra.
- **✅ LO QUE ADEMÁS SE HIZO:** entorno completo y credenciales de git en el VPS (y el filtro `entornoTrabajador.cjs` que deja fuera Stripe/Bitrefill/Ads/correo, con su puerta `dinero.cjs`) · la flota fuera de impugnaciones y el portátil fuera del reparto · las 3 ramas rescatadas revisadas y mergeadas (el merge destapó un guardarraíl de paridad roto, una colisión de migraciones y tres arreglos duplicados: nada de eso lo veía ninguna rama por separado) · 7 impugnaciones cerradas con 6 preguntas pasadas a barajables.
- **LO QUE QUEDA Y ES DE MANUEL:** **15 borradores esperando su OK** — y ese número CRECE cuanto más trabajan las demás sesiones, porque el análisis escala con sesiones y la aprobación no. Es el único punto del sistema que solo pasa por él.

- **✅ OCHO TRABAJADORES, Y LA FLOTA ANALIZA IMPUGNACIONES (05/08 tarde).** Seis en el portátil (`l1`-`l6`) y dos en el VPS. Medido al ponerlos en marcha: **RAM al 49 %** (techo declarado por Manuel: 80 %) y **carga 5 sobre 14 núcleos** — el límite no está en la máquina.
  **Lo nuevo es a QUÉ se dedican:** analizar impugnaciones. `esApta` las descartaba del reparto automático porque acaban en un correo a una persona, y eso sigue impedido POR PERMISO. Lo que cambia es que ya hay dónde dejar el borrador, y el trabajo de verdad —dossier, contraste contra el boletín oficial, «¿es sistémico?»— es técnico y paralelizable. La cola ya repartía sin colisiones (claim atómico), así que N trabajadores cogen N impugnaciones sin coordinarse.
- **✅ EL PANEL DECÍA «TODA LA FLOTA VIVA Y TRABAJANDO» CON EL VPS A CARGA 0,05 Y CERO PROCESOS.** El latido demuestra que un comando del andamiaje corrió; el claim, que alguien cogió la tarea. **Ninguno de los dos dice si AHORA MISMO hay un proceso.** Los turnos habían terminado solos y las tareas se quedaron **cogidas por nadie** — el peor estado: bloquean la tarea para los demás y no avanzan. Ahora el panel mira el proceso real (`pane_current_command`) y ese estado sale en ámbar con la orden para relanzarlo.
  **Causa:** un `claude -p` es de un solo tiro y dos trabajadores acabaron su turno con *«me paro aquí a esperar a que termine la prueba en segundo plano»*. Nadie recoge eso. El encargo ahora lo dice con su porqué.
- **✅ TRES FALLOS MÁS DE LA PUERTA DEL CLON, los tres encontrados usándola:**
  1. Medía contra `origin/main` cuando los trabajadores trabajan en **su propia rama**, así que sus commits salían «adelantados» para siempre aunque estuvieran a salvo. Lo que hay que proteger es el commit que **no está en NINGÚN remoto** (`HEAD --not --remotes`).
  2. Bloqueaba también **retomar la tarea propia** con el árbol a medias, que es exactamente el estado en que se dejó. Ahora bloquea lo nuevo, deja pasar lo suyo, y **se lo dice** — un turno nuevo no recuerda nada del anterior.
  3. Intentaba `pull --ff-only` de main **sobre la rama del trabajador**: nunca es avance directo, fallaba siempre y se reportaba como «no se sabe», dejándole encallado sin recibir nada. Ahora solo actualiza si el avance es directo; lo que el trabajador necesita —`origin/main` al día— ya lo hace el `fetch`.
- **✅ EL EMBUDO ERA RUIDO, y eso lo dijo Manuel:** de cuatro preguntas con la sesión parada, **tres las contestaba el criterio de la casa** (no apagues el guardarraíl, no metas una credencial de escritura en los secrets, manda lo medido sobre lo que diga la ficha) y solo una era suya de verdad. Un embudo con ruido se deja de leer, y entonces se pierde justo la que sí necesitaba a una persona. El encargo lleva ahora el filtro con esos mismos ejemplos.
- **✅ Y el rol de coordinación puede RECLAMAR una impugnación:** dos trabajadores se pararon con la medición hecha (`42501 permission denied for table question_disputes`) porque el claim hace `UPDATE SET claimed_by`. El permiso se dio **por columnas** — `UPDATE (claimed_by, claimed_at)` y nada más, verificado en la BD—: pueden coger sitio en la cola y **no** tocar el estado, el veredicto ni la respuesta.
- **Lo que queda de la flota:** cerrar `main` por protección de rama (bloqueado: falta un token de GitHub válido) · el % de contexto de cada sesión en el panel · el consumo de LLM de la flota es invisible para `llm:gasto` · y que el trabajador SUGIERA la siguiente tarea al entregar.
- **✅ LOS PARADOS, REPARADOS (05/08) — y los tres fallos que destaparon.** Los cuatro trabajadores están en pie y con tarea (l1 T-115 · l2 T-518 · w1 T-418 · w2 T-443).
  **(1) `l1` había terminado su turno a media tarea con 11 ficheros SIN COMMITEAR y ni un commit** — la única copia de ese trabajo. Y la puerta del clon, recién estrenada, se negaba a darle nada: correcto para una tarea NUEVA (empezar otra cosa encima de un trabajo a medias es como se pierde), pero le impedía también **retomar la suya**, donde un árbol sucio es exactamente el estado en que la dejó. Un bloqueo que no se puede satisfacer se acaba rodeando ([T-375]). Arreglado con el estado `a_medias`: bloquea lo nuevo, deja pasar lo suyo, **y se lo DICE al trabajador** (`avisoTrabajoAMedias`) — un turno de `claude -p` no recuerda nada del anterior, así que sin el aviso lo normal es que empiece de cero encima.
  **(2) `l2` no se había lanzado nunca.** Levantado con la cuenta secundaria (reparto entre las dos cuentas: l1/w1 principal, l2/w2 secundaria). Carga del portátil al hacerlo: **3,91 sobre 14 núcleos y 18 GB libres**, o sea que el techo no está aquí. GOTCHA de usabilidad: `lanzar` exige `CLAUDE_CODE_OAUTH_TOKEN` en el entorno aunque el token YA esté persistido en el fichero de otro trabajador de la misma máquina.
  **(3) EL PANEL SE CONTRADECÍA EN LA MISMA PANTALLA:** decía «✅ toda la flota viva y trabajando» y cuatro líneas más arriba marcaba **las CUATRO tareas de esos mismos trabajadores** como «esa sesión nunca ha dado señal». **Cuatro falsos de cuatro.** La causa no era el cruce —que es correcto— sino lo que se le pasaba: TODAS las tareas pero **solo las sesiones de Manuel**, así que la sesión de un trabajador nunca aparecía y su tarea salía huérfana **por construcción**. Una alarma que acierta cero veces se deja de leer, y entonces tampoco se ve la que sí importa. Arreglado pasándole todas las sesiones y filtrando por rol DESPUÉS. Prueba que lo fija: reproduce el fallo (mismo trabajador, misma señal de hace 3 min, «desaparecida» solo por no venir en la lista) y exige que el supervisor pase `todas`.
  **Capas:** 33 tests en `__tests__/flota/actualizacion.test.ts` (incluidos los del estado `a_medias` y los del cruce).
- **✅ NADA SALE HACIA UNA PERSONA SIN QUE MANUEL LO APRUEBE (05/08).** Regla suya, textual: *«no puedo permitir que los trabajadores envíen correos sin mi supervisión; siempre tengo que aprobar lo que se envía, porque ahí se detectan fallos y los usuarios necesitan que haya personas detrás, no la IA»*. Un opositor que impugna una pregunta está discutiendo con alguien, y la respuesta que recibe tiene que haber pasado por ese alguien.
  **Lo que HOY lo impide de verdad es el PERMISO, y se midió antes de escribir nada:** el trabajador no tiene `.env.local`, ni credenciales de AWS (así que no puede sacar `AUTH_SECRET` de SSM), ni clave del proveedor de correo, y su rol de lectura ni siquiera ve la dirección de nadie — **cero variables sensibles en su entorno** en el VPS. Eso es una propiedad del permiso, que es la buena.
  **Pero es un accidente del aprovisionamiento:** el día que un trabajador necesite una credencial para otra cosa, la contención desaparece sin que nadie lo decida. Por eso el criterio se declara en UN sitio (`lib/sessions/aprobacion.cjs`) y se hace cumplir **en el punto de envío**: los **cuatro** scripts que entregan algo a una persona (impugnaciones, feedback y las dos newsletters) se niegan si el rol no es `persona`, con salida 4. **Un rol sin declarar cuenta como trabajador** — su ausencia dentro de algo automatizado no es «hay una persona delante». Verificado EJECUTÁNDOLOS, con su contraste: trabajador → 4, persona → pasa. Un guardarraíl de TEXTO no es un guardarraíl: el encargo ya decía «esto no es para ti», y un encargo se puede ignorar.
  **Y la otra mitad, sin la cual el bloqueo se rodea:** `node scripts/backlog.cjs borrador --para "<a quién>" --texto <fichero.md> [--tarea T-nnn]`. Va por el **mismo embudo** que las preguntas (`session_questions.kind='borrador'`, migración `20260805_embudo_borradores.sql` ya aplicada) porque es el mismo canal —cosas que esperan la palabra de Manuel—; dos colas serían dos sitios donde mirar y una olvidada. Sale **el primero** en `npm run flota`, separado de las preguntas: una pregunta espera una decisión, un borrador espera un permiso. Cuerpo íntegro y destinatario son obligatorios por CHECK, no por buena voluntad.
  **Capas:** guardarraíl `__tests__/guardrails/aprobacionEnvios.guardrail.test.ts` (27 casos) que exige que **ningún** script de envío se quede sin la puerta — así es como se pierden las protecciones, no quitándolas sino añadiendo un quinto script que no la tiene. Registrado en `toolRegistry` como `aprobacion_envios`. Manuales: `sistema-sesiones-paralelas.md` y CLAUDE.md.
- **✅ LA PUERTA DEL CLON (05/08): la flota estaba trabajando con código de hace 30 commits.** El clon del trabajador se quedó congelado en el commit con el que se aprovisionó la máquina. `w1` tenía la credencial del rol de lectura en su entorno desde hacía horas y aun así **se quedó una hora parado preguntando si podía leer `observable_events`**: lo que le faltaba no era el permiso, era el CÓDIGO — ni `canary-rol-lector.cjs` (con el que lo habría comprobado en diez segundos) ni el comando `revision`, que era exactamente la salida que su situación pedía.
  **El daño no es «va con una versión vieja»:** lo que hace segura a la flota son los guardarraíles, y un clon viejo trae los de SU fecha — el push-guard sin el commit parcial, el gate de tests que no corría dentro de un worktree de agente. Un trabajador desactualizado no es uno con menos funciones: es uno **con las protecciones de otra fecha**, trabajando sin nadie delante.
  **Arreglo:** el clon se pone al día ANTES de cada encargo y, si no se puede, **no se le manda trabajo** (falla CERRADO, como [T-539]). Nunca a la brava: ni `reset --hard` ni `clean` — un árbol sucio o con commits sin empujar puede ser el único rastro de un trabajo ([T-431]); se rehúsa, se dice qué hay, y tirarlo lo decide una persona. El `pull` es `--ff-only`.
  **Y de paso, la puerta ÚNICA:** el bloque de envío estaba escrito dos veces (una en `encargar`, otra en `repartir`), que es como una se queda sin el guardarraíl que se añade a la otra ([T-130]). Ahora las dos pasan por `mandarEncargo` y las comprobaciones van DENTRO, no en cada llamador.
  **Segundo fallo, encontrado al estrenarlo:** `repartir` le mandó a `w1` un segundo encargo mientras el primero corría — «libre» se decidía solo por el claim, y **entre mandar el encargo y que el trabajador reclame pasan minutos**. El segundo se tecleó dentro del proceso vivo y se perdió, desperdiciando esa vuelta de reparto. Ahora se mira su panel de tmux (`pane_current_command`): un shell = esperando, cualquier otra cosa = trabajando, y **sin dato no se manda**.
  **GOTCHA que costó el primer intento:** la sonda hay que citarla en comillas SIMPLES. Con `JSON.stringify` (dobles) el shell de la máquina expande los `$(…)` antes de que el script llegue a su sitio, así que `git rev-parse` corre en otro directorio y la sonda informa de «no hay clon» sobre una máquina perfectamente sana.
  **Capas:** 34 tests unitarios (`__tests__/flota/actualizacion.test.ts` + los nuevos de `encargo`) · **`npm run sim:clon-al-dia`** (13 casos con repos git REALES, incluido el citado que rompió) · evento `flota_clon_desactualizado` + regla `RULE_FLOTA_CLON` · registrado en `toolRegistry`. Manuales: `sistema-sesiones-paralelas.md` y CLAUDE.md.
  **Límite conocido:** la puerta mira el clon COMPARTIDO de la máquina, no el worktree de cada trabajador. Un trabajo sin commitear dentro de `vence-sessions/<w>` no lo ve — eso lo cubre `npm run sesiones:huerfanos`.
- **✅ LISTA PARA COGER (03/08). Sus prerequisitos técnicos están HECHOS y desplegados o en `main`:**
  - **[T-484]** identidad de sesión con MÁQUINA — sin ella la flota compartía sid o no podía commitear. Hecha.
  - **[T-493]** el EMBUDO (`backlog.cjs preguntar/preguntas/responder`) — el canal por el que los trabajadores preguntan sin que Manuel entre en cada terminal. Hecho.
  - **[T-494]** el PARTE (`npm run parte`) — quién trabaja, quién está PARADO y qué espera decisión. Hecho.
  - **[T-495]** el recordatorio de método en tres momentos — lo que evita que una flota grande derive en chapuzas. Hecho.
  - **[T-487]** el barrido de rutas — el mejor TRABAJO para esos trabajadores (salida = evidencia determinista). **Ya DESPLEGADO y verificado en producción** (commit `ad49410a6`; backend vivo `b9388330` lo lleva) — esta línea decía «a falta de un deploy de backend» y se quedó atrás.
  - **NO hace falta [T-485]** (el `flock` del deploy es per-máquina) porque el piloto prohíbe a los trabajadores desplegar. El día que eso cambie, sí.
  - **→ No queda NINGÚN prerequisito técnico pendiente** (revisado el 04/08 ejecutando las herramientas, no leyendo las fichas: `npm run parte`, `backlog.cjs preguntas` y `npm run sesiones:friccion` responden).

- **✅ LA FLOTA ESTÁ VIVA Y TRABAJANDO (05/08).** Tres trabajadores con tarea distinta, repartida con un solo comando: `l1` en el portátil y `w1`/`w2` en un VPS de Hetzner (`cx33`, 4 núcleos, **10,27 €/mes** — el `cpx41` elegido no era pedible y `cx33` da lo mismo que `cpx32` por la cuarta parte). Gobierno desde el portátil con **`npm run flota`** (frases-gatillo: *«supervisor de tareas»*, *«supervisor de trabajos»*): `estado` · `lanzar` · `encargar` · `repartir` · `arrancar|parar`. Ve TODO —trabajadores y las sesiones abiertas de Manuel— y gobierna solo a los trabajadores: a la terminal de una persona no se le manda un encargo.
  **Lo que YA ha producido, sin que nadie tocara un servidor:** T-476, T-244 y T-315 entregadas bajo 🙋, y **dos preguntas que desmienten su propia ficha** (T-315 apuntaba a timeouts de Vercel que ya no existen en ese código). Es el criterio de éxito declarado: verifican contra la fuente en vez de hacer de escribas.
  **Credenciales, y son la contención real:** `vence_coordinacion` (4 tablas, ningún DELETE) + `vence_lector` (SELECT sin identificadores directos: se deniega correo/nombre/IP/pago/tokens, se permite la actividad por `user_id`, que es un UUID). Canarios 13/13 contra `vence_coordinacion`. **⚠️ El «19/19» del lector que decía aquí era un FALSO VERDE, y lo destapó la propia revisión de la flota (w3, 06/08):** el canario comprobaba cada tabla con un `SELECT 1 … LIMIT 1` y solo miraba si lanzaba error — con **RLS activo y CERO políticas** ese SELECT **no lanza**, devuelve 0 filas en silencio, así que la tabla ilegible se contaba como leída. Ya está corregido en [T-573] (el canario distingue «0 filas» de «puede leer»); reproducido por w3 contra el rol vivo: **18 de 20**, con `test_questions` y `ai_verification_results` correctamente en ROJO. **El rojo es el arreglo, no una regresión.** Aquí se deja escrito porque la cifra vivía en ESTA ficha como prueba de que los roles estaban verificados, y una prueba falsa es peor que ninguna. Un trabajador **no puede** leer datos de un usuario ni cobrar ni desplegar aunque ignore su encargo entero.
  **Registros que hacen esto escalable — añadir una fila:** `lib/flota/maquinas.cjs` (el portátil es una máquina más, `local:true`) y `lib/flota/cuentas.cjs` (dos cuentas de Claude Code, reparto DETERMINISTA por nombre para que el consumo por cuenta sea comparable consigo mismo).
  **Los siete fallos del primer día, por si vuelven:** (1) el primer «token» era el código de autorización, no el token; (2) **el TUI ignora `CLAUDE_CODE_OAUTH_TOKEN`** — solo lo usa `claude -p`, así que los trabajadores lanzan `-p`; (3) `bypassPermissions` **se niega con root** → usuario `flota` sin sudo; (4) el `git clone` se quedó como root; (5) `/etc/vence-flota` en 700 de root impedía al trabajador leer su credencial; (6) `claude` no está en el PATH de tmux (bash no interactivo) → se guarda `CLAUDE_BIN`; (7) **los secretos NUNCA dentro del script de shell**: al fallar un paso bash imprimió el comando con las dos contraseñas dentro y hubo que rotarlas dos veces — van por `stdin`.
  **Y el punto ciego que costó veinte minutos de flota fantasma:** el latido demuestra que la máquina vive, **no que el trabajador pueda trabajar** — es node hablando con Postgres y funciona igual con Claude Code parado en el login. Ahora el arranque sonda la autenticación y se niega a decir «en marcha», el panel la comprueba y hay regla de alerta (`flota_autenticacion`).

- **⏳ LO QUE QUEDA DE T-486, ordenado (05/08):**
  1. **Cerrar `main` por PERMISO, no por buena voluntad.** El clon del VPS tiene escritura; hoy lo único que lo impide es el push-guard y el encargo. Lo correcto es **protección de rama en GitHub** (server-side, aplica a todas las máquinas presentes y futuras) + credencial por máquina revocable, NO una credencial de solo lectura por servidor (eso deja el trabajo atrapado y no escala). **Bloqueado:** el `GH_ADMIN_TOKEN` de koigrid da `Bad credentials`.
  2. **El CONTEXTO de cada sesión en el panel.** La barra de estado local ya lo calcula y avisa al 90%; el supervisor no lo ve. Si ese dato viajara en el latido, el supervisor podría decir *«esta sesión va al 85%, mándale cerrar»* — que es justo lo que Manuel pidió el 05/08. Es el paso natural de §6.quinquies.
  3. **Distinguir 🔵 libre de 🔴 caído también en el aviso de TAREAS paradas.** El panel de la flota ya lo hace; el bloque de tareas sin señal todavía no, y por eso T-115 sale como parada teniendo dueño vivo.
  4. **El consumo de la flota es invisible para `llm:gasto`**, que lee los transcripts locales del portátil. Los de los trabajadores viven en su máquina. Sin eso no se puede decidir si cabe un tercer trabajador.
  5. **`l2` nunca se ha lanzado** (un comando) y conviene medir la carga con dos trabajadores en 4 núcleos antes de crecer.
  6. **Que el trabajador SUGIERA la siguiente al entregar, aprovechando su contexto** (petición de Manuel, 05/08). Hoy entrega y se queda libre; el supervisor le da la siguiente por prioridad, **tirando a la basura todo lo que acaba de cargar** — la ley que estaba leyendo, el temario que acaba de mapear, el cabo suelto que vio de paso. Es caro dos veces: se paga otra vez el contexto y se pierde lo que solo él sabía.
     **El patrón YA EXISTE para las personas y no hay que inventarlo:** `done` sugiere la siguiente RELACIONADA y `claim` enseña las libres que la propia ficha cita ([T-498], y las memorias `feedback-sugerir-siguiente-tarea-relacionada` / `feedback-sugerir-siguiente-tarea-al-terminar`). Lo que falta es (a) que el encargo pida al trabajador terminar su entrega proponiendo la siguiente **con el motivo**, y (b) que `repartir` PREFIERA esa sugerencia sobre el orden por prioridad cuando sea apta y esté libre.
     **Cuidado con el modo de fallo obvio:** un trabajador que elige siempre su continuación se queda dando vueltas en su rincón del backlog y nadie ataca lo urgente. La sugerencia tiene que **ganarle a la prioridad solo cuando la diferencia de prioridad es pequeña**; una 🔴 sin coger manda siempre.

- **🚦 PUERTA DE ENTRADA que esta ficha NO tenía, y sale de medir su propio criterio de éxito (revisión 04/08).**
  La ficha dice «si el ratio de escape de guardarraíles sube, la flota está erosionando las protecciones»… y **nunca dejó anotada la línea base**. Medida hoy con `npm run sesiones:friccion` (7 días):
  - **154 roces en 26 sesiones: 95 bloqueos y 42 escapes.** Uno de cada tres bloqueos se rodea.
  - **`indice-compartido` se rodea el 65 % de las veces (13 de 20).**
  - **`done-verificacion` el 40 %** (4/10) y **`backlog-push` el 26 %** (25/97).
  - Y ya hay **3 eventos de `identidad_compartida`** (dos máquinas latiendo con el MISMO session-id) **con una sola máquina**, que es justo el fallo que [T-484] venía a cerrar.
  **El termómetro del piloto está en ámbar ANTES de empezar.** El riesgo nº 3 de esta misma ficha («el cuello de botella no es el cómputo, es la integración») ya no es una hipótesis: es el estado actual.
  **Evidencia fresca de una sola noche (03→04/08), una máquina, ~5 sesiones locales:** el `pre-commit` paró a una sesión, esa sesión usó el escape, su commit **se llevó por delante la ficha sin commitear de otra** ([T-511], recuperada de milagro al resolver el conflicto), hubo **dos conflictos en el backlog** en dos pushes seguidos, y el deploy **no se pudo lanzar desde el árbol principal** porque otra sesión lo tenía sucio. Cinco incidentes de integración en cuatro horas, sin un solo trabajador remoto.
  **Propuesta: bajar ese 65 % antes de montar la flota.** No es «mejorar un guardarraíl»: es que el piloto multiplica exactamente el modo de fallo que hoy se está rodeando dos de cada tres veces, y en remoto no hay nadie delante para verlo.

- **✅ PUERTA ATRAVESADA (04/08) — y el 65 % no significaba lo que parecía.** Al partir la serie por la fecha en que entró [T-496] (03/08 05:32), los dos tramos dicen cosas distintas: **antes, 11 escapes y los 11 con el motivo VACÍO** (el `=1`); **después, solo 3 escapes y los tres con motivo escrito y específico**. O sea que el 65 % de 7 días es casi todo el problema que T-496 ya cerró, arrastrado por la ventana de medida.
  **Y los motivos —que existen precisamente porque T-496 los exigió— dieron el caso legítimo sin contemplar:** 2 de los 3 decían *«ya estaba en el índice cuando otra sesión empezó a latir aquí; commiteo con rutas explícitas»*. El guardarraíl estaba **parando a quien ya hacía lo correcto** y obligándole a apagarlo para hacerlo.
  **Arreglo (no relajar el criterio, contemplar el caso):** el commit PARCIAL (`git commit -m "…" -- <rutas>`) deja de bloquear. Git le da un índice temporal propio (`next-index-<pid>`, visible en `GIT_INDEX_FILE`), así que lo preparado por la otra sesión ni entra ni se toca — no es confianza, es que el fallo no puede ocurrir. El mensaje de bloqueo lo ofrece **antes** que el escape.
  **El corte tuvo que ser estrecho, y eso es lo reutilizable:** la regla cómoda («índice distinto del normal → deja pasar») habría abierto el agujero entero, porque **`git commit -a` también trae índice distinto (`index.lock`) y SÍ se lleva lo ajeno** — medido, no supuesto.
  **Capas:** 41 tests unitarios + **`npm run sim:indice-parcial`**, que monta repos git reales, reproduce el fallo de [T-415] y compara el veredicto del detector con lo que git acaba metiendo en el commit (si una versión futura de git cambia el nombre de su índice temporal, se pone rojo ese día). Manuales actualizados: `sistema-sesiones-paralelas.md` §6.bis, `tareas-pendientes.md` y CLAUDE.md.
- **⚠️ PRIMER INTENTO DE TRABAJADOR REMOTO (04/08): NO probó lo que iba a probar, y aun así encontró dos agujeros.** Se lanzó un agente con aislamiento remoto para contestar las cuatro preguntas de go/no-go (¿ve el repo? ¿alcanza RDS? ¿puede reclamar y latir? ¿pasa los push-guards?). **Corrió en LOCAL**: su `pwd` era `/home/manuel/Documentos/github/vence/.claude/worktrees/agent-…`, misma máquina. Así que **la pregunta que motivaba el experimento —si un trabajador remoto alcanza RDS— sigue SIN RESPUESTA**, y con ella la decisión de secretos. Anotarlo así y no como «medido» es el punto: lo que se probó fue *un clon sin `.env.local`*, que es una condición del piloto pero no la que se buscaba.
  **Lo que sí midió, y es la condición NORMAL de cualquier sesión de agente** (un worktree de `.claude/worktrees/` no hereda el `.env.local`, que está en `.gitignore`):
  1. **El guardarraíl del índice compartido callaba al no poder comprobar.** `if (!u) return 0`, sin una línea. Salida idéntica a «he mirado y estás solo», en el único guard que el CLAUDE.md marca como bloqueante-de-verdad porque su alternativa es irreversible. Los otros dos que dependen de la BD sí lo decían. Incumplía el principio *«"no lo sé" tiene que poder decirse»* de su propio runbook. **Arreglado**: ahora lo dice.
  2. **El gate de tests del `pre-commit` no ejecutaba NI UN TEST dentro de un worktree de agente.** `test:unit` llevaba `--testPathIgnorePatterns='.claude/worktrees/'`; como son regex contra la ruta ABSOLUTA, dentro de un worktree de agente el patrón casa con todo. Medido: **1.143 ficheros de test visibles sin el patrón, 0 con él**, y con `--passWithNoTests` el hook imprime `✅ Pre-commit OK` sin haber corrido nada. O sea: el gate estaba apagado justo para las sesiones con menos supervisión humana — las de agente. **Arreglado** anclando a `<rootDir>` (verificado en los dos sentidos: dentro del worktree 0→1.143; desde el principal, sin cambio) + guardarraíl `gateTestsWorktree.guardrail.test.ts`, porque el patrón sin anclar se lee bien y volvería a entrar.
  **Lo que esto dice del piloto:** los guardarraíles que NO dependen de la BD (robustez, contexto, typecheck) bloquean de verdad en un clon pelado; los tres que sí dependen de ella se apagan. Un trabajador sin credencial no es un trabajador con menos permisos: es uno **invisible para el reparto y sin las protecciones que lo hacen seguro**.
  **Siguiente paso REAL, sin cambios:** repetir el go/no-go en un entorno que sea de verdad remoto.

- **Alcance del permiso, ya acotado (04/08).** El andamiaje de coordinación toca **4 tablas y ninguna de negocio**: `backlog_tasks`, `worktree_sessions` y `session_questions` (SELECT/INSERT/UPDATE) y `observable_events` **solo INSERT** — nunca la lee, y tiene 307.224 filas con `user_id` en 7 días, así que darle SELECT regalaría datos de usuarios sin razón. El riesgo nº1 de esta ficha («N copias de `AUTH_SECRET` y claves de Stripe») se reduce a **un rol propio sobre 4 tablas**: con esa credencial un trabajador no puede leer datos de un usuario ni tocar cobros aunque quiera, y eso es una propiedad del permiso, no una promesa de comportamiento.

  **Lo que queda por ver es el EFECTO, y solo lo dice el tiempo:** la línea base a batir es la de arriba (95/42 y 65 %); se re-mide con `npm run sesiones:friccion` cuando haya días de uso con el cambio dentro. Bajar el ratio a base de escapes que ya no hacen falta es el resultado buscado.

- **LO QUE HAY QUE DECIDIR ANTES DE ESCRIBIR CÓDIGO, y no es código** (revisión 04/08: dos de las tres ya se pueden cerrar con datos):
  1. **Secretos → la propia acotación del piloto los hace innecesarios.** Si los trabajadores no despliegan, no escriben en la BD de negocio y no responden a usuarios, **no necesitan `AUTH_SECRET` ni las claves de Stripe**: les basta alcanzar RDS. La decisión es mucho más pequeña de lo que esta ficha temía — y el corte «solo RDS» ya estaba escrito aquí como alternativa.
  2. **Cuota → medida (`npm run llm:gasto`, 30 días).** La suscripción de Claude Code lleva **49.456 respuestas y 20.080 M de tokens** repartidas en **10 sesiones**; la API facturada aparte son **59,29 USD**. O sea: dos trabajadores más **salen de la misma tarta**, no de una nueva. Es el límite duro, como decía la ficha, y ahora tiene número.
  3. **¿Máquina propia o agentes remotos de Claude Code?** Sigue abierta, pero con un dato que cambia el coste de comprobarlo: **los agentes remotos ya están disponibles en el propio Claude Code**, así que la comparación se puede hacer **sin alquilar ni administrar nada**. Probar esa vía primero cuesta cero; montar VPS no.
- **Cómo se sabrá si salió bien** (declarado antes de empezar): tareas cerradas y **verificadas** por trabajador, ratio de escape de guardarraíles (`npm run sesiones:friccion` — **línea base del 04/08 arriba: 95 bloqueos / 42 escapes; índice-compartido 65 %**) y **horas de revisión de Manuel por tarea entregada**. Si esa última sube, el piloto ha fallado aunque produzca más.

- **Esfuerzo: sesion_propia.**
- **ORIGEN.** Petición de Manuel (02/08): varios Claude Code en servidores de Koigrid trabajando en paralelo y **uno central que supervise, resuma y compruebe lo que hacen los demás**. Condición suya, y es la que define el diseño: *«que el sistema de gestión funcione bien tanto con las sesiones que tengo en local en mi portátil como con Koigrid»* → **una sola flota híbrida**, no un sistema para remotos y otro para locales.
- **Qué se pilota (acotado a propósito):** 2 trabajadores remotos + 1 supervisor, durante una semana.
  - **Los trabajadores NO tocan producción**: nada de deploy, nada de escribir en la BD de negocio, nada de responder a usuarios (impugnaciones y feedback siguen siendo humanas y locales). Trabajo apto: barridos de salud en dry-run, auditorías de scope, generación de preguntas, adjudicaciones. Todo lo que ya produce un artefacto revisable.
  - **El supervisor NO verifica leyendo informes.** Un agente que dice *«hecho y verificado»* es indistinguible de uno que se equivocó, y otro LLM leyendo esa frase tampoco los distingue — es el principio *«se observa, no se declara»* de `sistema-sesiones-paralelas.md`. Mira **evidencia determinista**, que ya existe casi entera: exit code de los tres push-guards, `npm run typecheck`, el diff real contra `origin/main`, las puertas de `done` ([T-392]), `npm run sesiones:friccion` (¿cuántos escapes `*_SKIP=1`?), `npm run deploy:pendiente` y `latidos.cjs`. Su producto es **resumen y triaje**, no un sello de calidad.
- **Prerequisito duro: [T-484]** (identidad y ubicación por máquina). Sin él, la flota comparte sid o no puede commitear.
- **Riesgos que hay que decidir ANTES de montar nada, no después:**
  1. **Secretos.** Cada trabajador necesitaría credenciales de RDS, `AUTH_SECRET` y las claves de Stripe en su `.env.local`. N copias de eso en servidores es una decisión de seguridad, no de arquitectura. Alternativa a evaluar: que los trabajadores solo alcancen RDS y **ningún** secreto de pagos.
  2. **Cuota.** N instancias headless consumen la misma suscripción o facturan por token con API key. Esto se mira **antes** que la infraestructura: es el límite duro, no el CPU.
  3. **El cuello de botella no es el cómputo, es la integración.** Con cinco sesiones desde el mismo checkout hubo cuatro conflictos en este mismo fichero en una tarde, y dos fichas se quedaron en una línea con el CI en verde ([T-428]). Más trabajadores = más merge y más revisión de Manuel, que ya es el recurso escaso.
- **Antes de montar servidores propios, medir la alternativa:** los agentes remotos y las rutinas programadas de Claude Code hacen esto sin administrar VPS, secretos ni actualizaciones. El piloto debería comparar las dos, no dar por hecha la casera.
- **Cómo se sabe si salió bien** (declarado antes de empezar, para no juzgarlo por la impresión): tareas cerradas y **verificadas** por trabajador, ratio de escape de guardarraíles (`sesiones:friccion` — si sube, la flota está erosionando las protecciones), y horas de revisión de Manuel por tarea entregada. Si esa última sube, el piloto ha fallado aunque produzca más.

**✅ MEDIDO EL 08/08 (w1): LOS DOS HUECOS DEL ÚLTIMO CIERRE ("falta") ESTABAN DESACTUALIZADOS.**

- **Hueco 1 (credenciales de git en el VPS): RESUELTO, no arreglado por mí — solo medido y reflejado en código.** El registro (`lib/flota/maquinas.cjs`) y la ficha [T-628] seguían diciendo `tieneCredencialesGit:false`, pero hoy el VPS SÍ tiene una clave SSH funcional (`ssh -T git@github.com` autentica como una cuenta personal real, `git fetch` funciona, y esta sesión pusheó con esa misma credencial más de una decena de veces). Detalle completo, incluida la advertencia de que es una clave PERSONAL de acceso completo compartida por los 4 trabajadores —no la deploy key de solo escritura que se pedía— en [T-628]. Corregido `tieneCredencialesGit: true` + tests actualizados (`__tests__/flota/rescateSegundaFase.test.ts`) + `toolRegistry.ts`.
- **Hueco 2 ("w1/w2/w3/w4 comparten UN checkout... un solo índice de git"): NO es cierto, medido con `git worktree list`.** Los cuatro trabajadores (y una quinta sesión, `t450-examen-cupo`) son worktrees de git PROPIOS y separados (`/home/flota/vence-sessions/{w1,w2,w3,w4,t450-examen-cupo}`, cada uno con su propio `.git` apuntando a `worktrees/<w>` dentro del `.git` compartido de `~flota/vence`) — exactamente el mecanismo nativo de git para "un repo, varios índices". Cada worktree tiene su PROPIO índice/HEAD/working tree; lo único compartido de verdad es la base de objetos (normal y correcto) y `node_modules` (symlink deliberado a `~flota/vence/node_modules`, ahorro de espacio, no un índice de git). Este hueco ya estaba resuelto desde [T-592] (citado en el propio comentario de `maquinas.cjs`) — el texto de "falta" de este cierre no se actualizó cuando T-592 lo arregló.
- **Lo que SÍ sigue siendo cierto y no toqué:** el clon BASE compartido (`~flota/vence`, sin worktree propio) está correctamente protegido contra commits directos por el guard de "una sesión por directorio" (T-415) — lo comprobé al intentar una prueba de push ahí, que el guard rechazó con razón. Ese guard es el que hace que los 4 índices separados de arriba se respeten en la práctica.
- **Relacionadas:** [T-484] (prerequisito), [T-485] (obligatoria solo si algún día despliegan), [T-089] (Koigrid como hosting — decisión independiente de ésta), [T-628] (detalle completo del hueco 1), [T-592] (el fix real del hueco 2, de hace días).

