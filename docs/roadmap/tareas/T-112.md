### [T-112] 🟠 [ABIERTO 25/07] Bajar a cero el badge de `/admin/contenido` — campaña de triaje de `content_health_findings`
- **Qué:** el badge de Salud del contenido está en **210 hallazgos** (tras cerrar 7 el 25/07). NO se baja en una pasada: el 68% (142) es generación de contenido o adjudicación fila-a-fila que no se puede acelerar ni fingir. Esta ficha es el **mapa de campaña** para que cualquier sesión aislada sepa por dónde entrar y con qué runbook. Recordatorio de mecánica: `content_health_findings` es **proyección pura** del sweep nocturno (07:30 UTC, `content-health-sweep.service.ts` hace `TRUNCATE`+rebuild; NO hay columna `status`) → arreglar el dato + verificar que el detector ya no dispara + borrar la fila del finding (el sweep no la re-añade). Todos los detectores → runbook en `lib/admin/runbookRegistry.ts`.
- **Cerrado el 25/07 (7 findings, 217→210), como referencia de método:**
  - `hito_vencido_abierto` (6): 1 hito `oep_aprobada` real (enfermero-scs-cantabria, Decreto 94/2025 BOC 30/12/2025) → `completed` con su fecha citada; 6 placeholders inventados (`origen='estimacion'`, fechas día-1-de-mes, ocultos al usuario por el render) → quitados. `fecha` es NOT NULL: no se puede nulificar, hay que cerrar el hito o quitarlo.
  - `temas_card` (1, policia-nacional): era **stale** (la tarjeta ya decía 46 y los 46 topics son limpios) → borrado.
- **Mapa de las 210 restantes por CÓMO se cierra (no por capricho — los runbooks prohíben inventar):**
  1. **Generación de contenido — 142 (el 68%, la palanca real):**
     - `article_no_coverage` (**104**) → generar preguntas ancladas al texto del artículo + **doble auditoría ciega** antes de activar. Runbook `salud-contenido.md`. Es la mitad del badge; empezar por la oposición con más artículos huérfanos.
     - `dual_write` (**38**) → adjudicar campo a campo contra el boletín oficial (T-105). Runbook `salud-contenido.md`.
  2. **Verificación oficial por-ítem — ~49 (WebFetch a fuente c/u; COORDINAR con la sesión que puebla convocatorias para no pisarse):**
     - `seguimiento_url_stale` (**20**): OJO, **~14 son "página índice de portal de empleo" = el propio runbook dice que pueden ser legítimas** (banda `url_generica`; para ayuntamientos pequeños el índice ES lo único que vigilar) → eso es **sobre-marcado del detector, no dato roto** (ver cabo suelto abajo). Solo **~6 son año-viejo** (URL menciona 2025/2023-25 pero no 2026): administrativo-estado, auxiliar-administrativo-estado, ingesa, dip-león, dip-zaragoza, osakidetza → WebFetch a la fuente + repunte cuidadoso + `seguimiento_last_hash=NULL`. NUNCA repuntar sin confirmar la URL nueva. Runbook `oeps-convocatorias-seguimiento.md`.
     - `convocatoria_timeline_caducado` (**16**) = view `convocatoria_hito_incidencias` (I7_prevision_caducada `estimacion+fecha<hoy` + I8_status_contradice_fecha `completed+futuro`/`upcoming+pasado`). La view se recomputa sola al arreglar el hito. Runbook `verificar-convocatorias.md`. Mucho solapa con el modelado de convocatorias.
     - `convocatoria_docs_incompletos` (**7**) + `epigrafe_provenance_no_doc` (**6**, = 133 epígrafes `verified_literal` **sin `source_url` guardado** → hay que re-sourcear con fetch, NO es enlazar). Runbook `provenance-convocatorias.md`.
  3. **Código / infra (otra sesión, la de código) — 7:** `http_5xx` (6, transitorios 502/503/504 + algún 500 en /api/oposiciones/catalog; envejecen en la ventana 24h si no recurren) + `server_render_error` (1, `controller[kState].transformAlgorithm is not a function` en `/auxiliar-administrativo-madrid/temario/tema-11` — bug de render SSR, posiblemente disparado por contenido de ese tema). Runbook `health-check.md`.
  4. **Modelado de convocatorias (otra sesión) — 1:** `convocatoria_timeline_incoherente` (administrativo-madrid): la convocatoria 2026 tiene DOS ciclos de plazo en la MISMA fila (inscripción 2025-05→06 `completed` + 2026-07→08 `current`) → I1_orden compara inicio-2026 vs fin-2025. Fix = fila por ciclo (schema multi-convocatoria 20260718). **NO borrar los hitos 2025 (son históricos reales).**
  5. **Singles — 11 (1 runbook c/u):** `scope_cross_tema_dup`, `scope_titulo_huerfano`, `scope_over_inclusion_suspect`, `scope_phantom_article` (→ `verificar-epigrafes-scope.md`), `low_coverage` (generar), `law_unverified_source` (→ `completitud-leyes.md`), `audit_note_explanation` (explicación que es nota de auditoría → verificar clave + reescribir/`needs_human`), `temario_revision_pendiente`, `answer_in_annulled_fragment` (→ `incisos-anulados-tc.md`), `flattened_table` (→ `tablas-articulos.md`), `visual_deixis_no_image` (→ `salud-contenido.md`).
- **Cabos sueltos concretos que salieron del triaje del 25/07:**
  - **Detector `seguimiento_url_stale` sobre-marca índices de portal legítimos (14 de 20).** La banda `url_generica` no debería pingar el badge (o debería ser confianza baja separada). Es afinado de detector → **código** (mirror `scripts/health-sweep.cjs` + `content-health-sweep.service.ts`). Sin esto, esos 14 reaparecen cada noche aunque la URL sea correcta.
  - **OEP hito enfermero-scs-cantabria (`8737a213`) quedó `origen='estimacion'`** tras ponerlo `completed`+2025-12-30 → sigue disparando I7 (`estimacion+fecha<hoy`) en `convocatoria_timeline_caducado`. Lo limpio de verdad = convertirlo a `origen='registro'` + clonar el **Decreto 94/2025 (BOC Extra nº28, 30/12/2025)** al hub (`ensure_convocatoria_documento`) y enlazar `source_documento_id` (si no, cae en I5_registro_sin_fuente). Provenance, encaja con T-108.
- **Impacto:** 🟠 el badge es un forcing-function; a 210 el panel pierde señal (el ruido tapa lo accionable). Bajar `article_no_coverage` y el sobre-marcado de seguimiento es lo que más lo despeja.
- **Cómo trabajarlo:** sesión aislada por bloque (`scripts/worktrees/crear-worktree.sh`), del método de referencia arriba: arreglar dato → verificar detector → borrar fila. La generación (bloque 1) es el grueso y va tema por tema con doble auditoría. Coordinar el bloque 2 con la sesión de convocatorias.
- **Origen:** 25/07, sesión de triaje de `/admin/contenido` a petición de Manuel ("revisar todo hasta que el badge esté a cero"). Se cerraron los 7 limpios y se mapeó el resto como campaña porque cerrar a ciegas = inventar sobre contenido en producción.
- **Progreso (25/07, sesión t112-badge-fp):** bloque de código hecho — `seguimiento_url_stale` **20→14** (la banda `url_generica` SIN proceso vivo = índice de portal legítimo → severidad `ok`, ya no pinga; CON proceso vivo se mantiene `error`, caso Murcia) + arreglada la divergencia CLI↔backend (el sweep CLI no pasaba `procesoEnJuego`). Núcleo `seguimientoUrlSalud.cjs` + mirror backend + sweep CLI + tests. Commits `98b58b7d6`,`e76214bca`. **Materializa al re-desplegar el backend** (el @Cron aún usa el detector viejo). Quedan los 14 accionables (año-viejo + genérica-con-proceso) = verificar fuente + repuntar.
- **Progreso 3 (26/07, sesión `sesion-26jul-d`) — el bloque 1 se puede priorizar, y cambia el tamaño del problema.** La ficha leía los 104 `article_no_coverage` como 104 trabajos. **No lo son:** una pregunta cuelga del ARTÍCULO y el artículo es una fila COMPARTIDA, así que cubrirlo repara todas las oposiciones que lo escopan a la vez. Medido: 6.533 pares (artículo, tema) sobre **3.075 artículos únicos** → reuso 2,1×. Y la rentabilidad es desigualísima: la LPRL cerraba **19 temas con 6 artículos** (3,17 temas/artículo) frente a una media de ~0,4. Elegir por corazonada costaba ~8× más para el mismo resultado.
  - **⚠️ AUTOCRÍTICA — construí una herramienta que ya existía.** Hice `cobertura:huerfanos` sin comprobar que T-115 ya tenía `huerfanos:plan`, **documentada en el runbook al que apunta `runbookRegistry`**. La mía solo se referenciaba desde CLAUDE.md y esta ficha, o sea que quien siguiera el camino correcto ni la veía: un silo. **Retirada el 26/07**; sus dos señales útiles se fusionaron en la que ya estaba integrada: la columna `usuarios` (a cuánta gente llega el hueco) y el aviso ⚠️ de leyes con batch de las últimas 24h de otra sesión. Y para que no vuelva a pasar por memoria, el registro `runbookRegistry` tiene ahora campo **`comando`** con dos guardarraíles: que el script exista en `package.json` y que **el runbook del kind lo nombre** (si no, el punto de entrada no es descubrible y la siguiente sesión se construye el suyo).
  - **Lote 1 APLICADO — LPRL arts 10, 11, 12, 32 y 39: 16 preguntas vivas** (`gen_lprl_coord_2026-07-26`). Verificados verbatim contra BOE-A-1995-24292; gate mecánico 16/16; **triple auditoría** (auto-audit + Sonnet ciego + Sonnet de re-verificación post-aplicación, este último sobre la pregunta ya viva): 16/16 PERFECT/LIMPIA. Efecto medido: **455→413 temas** disparando y **2 findings cerrados** (tcae_canarias, tcae_murcia); badge 205→203. MV refrescadas + tags `test-counts`/`questions`/`laws`/`temario`/`test-config` purgados.
  - **Excluido a propósito el art. 54** (solo 4 temas): su remisión literal es a la Ley 13/1995 de Contratos, **derogada** → preguntarlo enseña una referencia muerta e invita a impugnación.
  - **Siguiente por rentabilidad** (salida del ranking tras el lote): RDL 5/2015 y Ley 16/1985 (ratio 1,0 · 4 arts c/u), Ley 55/2003 (0,86 · 14 arts, cierra 3 findings), Ley 53/1984 (0,75 · 4 arts). Las gordas (RDL 2/2004 con 71 arts y 37 temas, Ley 9/2017 con 98) rinden ~0,5 pero son las únicas que mueven el badge de verdad.
  - **Segunda lente añadida al ranking: USUARIOS.** El ratio dice cuánto cunde el esfuerzo, no a cuánta gente llega, y las dos cosas se contradicen: RDL 5/2015 y Ley 16/1985 **empatan a ratio 1,0 pero están delante de 3.130 y 733 opositores** respectivamente. El orden sigue siendo por ratio; la columna `usuarios` se imprime al lado para que quien decide vea ambas. 101 de las 102 oposiciones con hallazgo tienen usuarios reales, y las de más demanda (aux-admin-estado 2.172, Madrid 1.405) están entre ellas.
  - **Lote 2 APLICADO — TREBEP (RDL 5/2015) arts 47, 51, 72 y 99: 10 preguntas vivas** (`gen_trebep_2026-07-26`), verbatim contra BOE-A-2015-11719 con el art. 47 en redacción post RD-ley 2/2024. Triple auditoría 10/10. El gate cazó dos defectos propios antes de auditar (una cita truncada REAL y una opción no literal) y un falso positivo que se adjudicó reformulando la pregunta, no tocando el gate.
  - **🚨 COLISIÓN CON T-115 — lote entero desperdiciado, y la lección importa más que el lote.** Otra sesión generó `gen_lprl_2026-07-26` sobre los **MISMOS 5 artículos de la LPRL** 13 minutos antes que el mío. El dedup del Paso 3 no lo vio (compara enunciados literales); el **nivel 3 de §2.6** (Jaccard de la opción correcta) sí: **9 de mis 16 evaluaban el mismo hecho con la misma respuesta** → retiradas como `retired_duplicate`. **Causa raíz: `article_no_coverage` ES T-115, reclamada por otra sesión, y se trabajó desde el "bloque 1" de T-112.** Reclamar una ficha NO reserva el trabajo que vive en otra. **Antes de generar, mirar los batches recientes**: `SELECT unnest(tags), max(created_at) FROM questions WHERE created_at > now() - interval '12 hours' GROUP BY 1`. Cuesta una consulta.
  - **✅ REPARTO CERRADO CON T-115 (26/07):** el **bloque 1 lo EJECUTA T-115**; T-112 se queda con la **priorización** (la herramienta y su ranking) y con los bloques 2-5 del badge. Escrito también en la ficha de T-115 para que lo lea quien la tenga reclamada.
  - **🛡️ La coordinación ya no depende de acordarse:** `huerfanos:plan` **marca ⚠️ las leyes con batch de las últimas 24h** de cualquier sesión, con la lista al pie. Medido al implementarlo: **15 leyes en curso ahora mismo**, dos de ellas en el top del ranking. Es el mismo principio que el guardarraíl de push del backlog — una regla que solo vigila la memoria es una regla que se incumple.
- **Progreso 2 (25/07):** 2º refinamiento — `procesoEnJuego` ahora se deriva de `estado_proceso` (`procesoConFichaViva`), no de `is_convocatoria_activa` (que era true en `oep_aprobada` sin ficha). Neto 14→13: quita 3 FP `oep_aprobada` + caza 2 cegueras ocultas. Commit `76c839fec`. **seguimiento_url en su MÍNIMO por código.** Los 13 restantes NO bajan por detector — requieren trabajo de DATOS por-ítem: (a) **7 `url_generica` accionables** [aux-admin-ayto-córdoba, ayudante-iipp, admin-dip-jaén, admin-madrid, admin-junta-asturias, aux-admin-ayto-murcia, ordenanza-ayto-córdoba] = repuntar a la ficha concreta de la convocatoria viva (WebFetch a fuente oficial + `seguimiento_last_hash=NULL`); (b) **4 warn irreducibles-por-URL** [ingesa, dip-león, osakidetza, dip-zaragoza] = OPE plurianual/OPE-en-ejecución cuya ficha CONCRETA cita años viejos legítimamente (tocar el detector = riesgo de FN silencioso; se dejan como cola de revisión); (c) **2 warn estatales** [admin-estado, aux-admin-estado] = `oep_aprobada` 2026 con URL a la convocatoria 2025 cerrada; repuntar al índice del cuerpo en INAP cuando se coordine. **(a) y (c) COORDINAR con la sesión de convocatorias** (runbook).

