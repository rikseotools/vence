### [T-381] 🟠 [ABIERTO 31/07] El canary `served-rollup` está ROJO: el tráfico sintético se cuenta como servido y envenena el ratio anti-cosecha

- **Qué dice el canario, hoy** (`npm run canary:served-rollup`, exit 1):
  ```
  servidas 24h: 1762 filas (232 usuario / 853 dispositivo / 677 ip), 132.656 preguntas
  ❌ la medición anti-cosecha NO está viva
     · tráfico sintético contabilizado (2 servidas de un usuario smoke) → la exención
       x-vence-canary del writer no está aplicando; envenena el ratio del detector de cosecha
  ```
- **Por qué importa y no es cosmético:** desde el 27/07 la detección de cosecha del banco de preguntas **se apoya en `daily_questions_served`** (respondidas/servidas), no en las respuestas guardadas. Si en ese rollup entra tráfico que no es de un opositor, el ratio miente **en la dirección mala**: infla las servidas y hace que un cosechador real parezca normal. El propio runbook lo anticipaba (*«si ves un `smoke@` en el rollup, la exención del writer se rompió»*), y es justo lo que está pasando.
- **Hipótesis con la que empezar (NO comprobada):** el 29/07 se endureció la exención — `x-vence-canary` a secas ya no exime, ahora hace falta `x-vence-canary-secret` (`esCanaryDeConfianza` + `secretoCanaryEsperado`, commit `e049f57ac`). Es coherente con el síntoma: el endurecimiento arregló un agujero real (una petición anónima con ese header se llevaba las preguntas saltándose el Turnstile) y de paso dejó al **writer del rollup** contando el tráfico del smoke porque este no manda el secreto. **Confirmar antes de tocar**: mirar si el smoke envía el secreto y si el writer usa el mismo criterio que el gate.
- **Cómo se descubrió:** de rebote, arreglando la conexión a RDS de los canarios en [T-377]. Este NO estaba roto de conexión — conecta y falla por su propio motivo, y llevaba en rojo sin dueño.
- **Cuidado al arreglarlo:** la exención tiene DOS capas (writer por header, sweep por `email LIKE 'smoke@%'`). Tocar solo una deja el defecto medio vivo. Y **no relajar el secreto** para que el smoke pase: el agujero que cerró el endurecimiento era real.
- **Relacionadas:** [T-377] (de donde sale), runbook `revisar-fraudes.md`, `docs/runbooks/health-check.md`.

- **✅ CAUSA MEDIDA Y ARREGLADA (07-08/08, w2) — rama `flota/T-381-servedrollup-metrics-exemption` (commit `42c95502f`), pusheada, NO desplegada.** La hipótesis del 31/07 no encajaba con los números: no es que la exención se rompiera, es que `canary-questions-gate` (T-280) manda su sonda REAL a `/api/questions/filtered` con `x-vence-canary` pero **a propósito** sin `x-vence-canary-secret` — mandar el secreto invalidaría la prueba, que es justo comprobar que el gate NO reta a un usuario normal. `numQuestions=1`, nunca respondida: la firma exacta que el canario denuncia. Descartado `canary-por-leyes-scope` (el de 288 runs/día) mirando los top de `daily_questions_served` vía `VENCE_LECTOR_URL`: ningún `subject_key` tenía volumen compatible con esa cadencia.
  - **Arreglo:** cabecera nueva `x-vence-canary-metrics-secret` (mismo secreto compartido `CANARY_SECRET`/`CRON_SECRET`) que exime SOLO de `daily_questions_served` sin eximir del reto — `esCanaryParaMetricas()` en `lib/api/syntheticTrust.ts`, usada en `app/api/questions/filtered/route.ts` para el contador (`recordServedForSubjects`) mientras el reto sigue con el criterio estricto de siempre (`canaryDeConfianza`). La sonda real de `canary-questions-gate.service.ts` manda la cabecera nueva. `canary-por-leyes-scope` no cambia (ya manda el secreto completo, así que ya estaba exento de las dos cosas).
  - **La otra capa que el "cuidado al arreglarlo" de arriba pedía no tocar sin querer: confirmado intacta.** El sweep de fraude (`backend/src/fraud-sweep/fraud-sweep.service.ts:293`, `WHERE up.email LIKE 'smoke@%'`) sigue excluyendo a la cuenta smoke de la alerta de cosecha — no está en el diff de esta rama, y no hacía falta tocarlo: el defecto vivía en el WRITER (contador), no en el sweep (alerta).

- **🔎 RE-VERIFICADO Y COMPLETADO (08/08, w2) — 3 hallazgos más al hacer la comprobación que el `falta` de la vuelta anterior pedía ("¿hay un tercer canario que llegue sin ninguna de las dos cabeceras?").**
  1. **La rama estaba 782 commits por detrás de `origin/main`** (base `92090935`, 06/08 23:11). Antes de dar nada por bueno comprobé si mergearla de verdad producía conflictos o regresiones silenciosas (mismo patrón que costó caro en [T-613] con una rama parecida) — **merge de prueba en un scratch branch, luego abortado**: los ficheros que T-381 toca (`lib/api/syntheticTrust.ts`, `app/api/questions/filtered/route.ts`, `canary-questions-gate.service.ts`) no habían cambiado en `main` desde la base de la rama (`git log -1` de cada uno apunta a los mismos commits `e049f57ac`/`8568f6475` que la rama ya tiene), así que el merge es LIMPIO, sin conflictos ni pérdida de trabajo ajeno. Confirmado con los 20 tests relevantes en verde sobre el resultado del merge.
  2. **Encontrado un tercer sitio real: `scripts/sim/sim-scope-servido.ts`.** Simulación bajo demanda (T-607) que abre un navegador Playwright con la sesión de un **usuario REAL** (`user_profiles`, no un canario sintético) y espera la respuesta de `/api/questions/filtered` — sin mandar NINGUNA cabecera de canario. Contra `SIM_BASE` de producción (su valor por defecto), cada tanda contaba en `daily_questions_served` como servidas-sin-responder **bajo la identidad de esa persona real** — la misma firma de cosecha, pero contra alguien inocente, no contra una cuenta smoke. El propio canario `served-rollup` NO lo habría cazado (busca el patrón `smoke@`; aquí el JWT lleva un `userId` real). Arreglado añadiendo `x-vence-canary-secret` al contexto de Playwright (mismo patrón que `scripts/sim/run.ts`, que SÍ lo llevaba ya) — usa el secreto completo, no solo el de métricas, porque además evita el reto anti-scraping que un navegador sin persona delante no puede resolver.
  3. **Descartados sin cambios, verificado leyendo el código, no supuesto:** `scripts/audit-oposicion-completa.ts` llama a `getFilteredQuestions` directamente (bypassa la ruta HTTP entera; `recordServedForSubjects` solo se llama desde los `route.ts`, nunca desde la función interna — confirmado con `grep` de todos los call-sites) → no puede escribir en `daily_questions_served`. `scripts/scope/escopar-ley-entera.cjs` solo IMPRIME un `GET .../action=count` sugerido para que lo corra una persona a mano, no lo ejecuta — y `action=count` además es un camino de conteo, no de servir preguntas.
  - **Ramas resultantes, listas para revisar:** el merge limpio de `flota/T-381-servedrollup-metrics-exemption` sobre `main` actual + el fix de `sim-scope-servido.ts` + 2 tests de guardarraíl de fuente que se habían quedado desfasados con la forma nueva del código (`canaryGateAndOutboxPrune.test.ts`, `canaryGateAssertionReal.test.ts` — comprobaban un patrón de texto EXACTO que el propio fix de T-381 cambió sin romper la propiedad de seguridad que vigilaban; actualizados para seguir vigilando lo mismo con la forma nueva, no relajados). 20+8 tests relevantes en verde, suite completa del backend 1602/1603 (el único rojo, `canary-registry.spec.ts` de `identidad-pago`, es preexistente y no relacionado — reproducido también en `main` limpio). `tsc --noEmit` limpio en frontend y backend. `eslint` limpio en los ficheros de código tocados.
  - **Falta:** desplegar frontend Y backend → correr `npm run canary:served-rollup` (con credenciales completas, no las de un worker) y confirmar que ya no reporta tráfico sintético contabilizado. Un trabajador de la flota NO puede correr ese canario con ninguna de las dos credenciales (`DATABASE_URL`: permission denied en `daily_questions_served`; `VENCE_LECTOR_URL`: permission denied en el JOIN a `user_profiles`, denegado a propósito) — verificado intentándolo, no supuesto.
- **✅ Arreglo diseñado, implementado y revisado (06-08/08) — pendiente SOLO de deploy.** Rama
  `flota/T-381-servedrollup-final` (commit `b532b0e3e`), sobre `flota/T-381-servedrollup-metrics-exemption`.
  Causa medida: `canary-questions-gate` (T-280) manda su sonda real con `x-vence-canary` A
  PROPÓSITO sin el secreto del reto (está probando que el reto no le salta) y nunca responde —
  exactamente la firma que denunciaba el canario. Arreglo: cabecera nueva
  `x-vence-canary-metrics-secret` (`esCanaryParaMetricas()` en `lib/api/syntheticTrust.ts`) que
  exime SOLO del contador `daily_questions_served`, sin eximir del reto (que sigue exigiendo
  `canaryDeConfianza`). `w1-vence-flota` revisó a fondo y encontró UN problema real (no
  cosmético): la entrega actualizó dos guardarraíles hermanos que tocan la misma zona pero dejó
  un tercero (`exencion-antiscraping.test.ts`) con la invariante VIEJA ("el contador usa el
  MISMO criterio que el reto"), que es justo lo que T-381 rompe a propósito — habría quedado
  roto tras el merge. **Arreglado (08/08, w3):** reescrita la aserción para reflejar el criterio
  nuevo + test adicional que confirma que la laxitud sigue exigiendo secreto compartido (no es
  el agujero pre-29/07). 6/6 en el fichero corregido; suite guardrails completa con solo los 3
  fallos preexistentes de `temarioPdfQueue.integration.test.ts` (permission denied, sin relación,
  igual en main); 40/40 en syntheticTrust/canaryGateAndOutboxPrune/canaryGateAssertionReal/
  servedRollupCanaryExemption; tsc limpio; verificado que la rama sigue mergeando limpio contra
  `origin/main` actual (merge de prueba, abortado).
  - **Falta, y no lo puede hacer este worker:** desplegar frontend Y backend con esta rama. Tras
    el deploy: correr `npm run canary:served-rollup` (con credenciales completas, no las de un
    worker — este script está bloqueado por permisos para la flota, medido) y confirmar que ya
    no reporta tráfico sintético contabilizado. Si sigue en rojo por el MISMO motivo, revisar si
    hay un tercer canario que llegue a `/api/questions/filtered` sin ninguna de las dos cabeceras
    nuevas — solo se identificaron y arreglaron `canary-questions-gate` y `canary-por-leyes-scope`.

