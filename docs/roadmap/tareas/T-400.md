### [T-400] ✅ 🟠 [HECHA 31/07] El claim protege la TAREA, pero las sesiones chocan por los FICHEROS — ahora se ve en vivo

- **ORIGEN.** Pregunta de Manuel (31/07), cerrando la sesión de T-375/T-382: *«¿se puede mejorar algo para que las sesiones no trabajen en lo mismo o relacionado que puedan afectar unas a otras y lo sepan en vivo?»*.
- **EL HUECO, con tres casos medidos y el claim funcionando PERFECTAMENTE en los tres:**
  - **[T-361]** (31/07): el mismo bug encontrado por **DOS sesiones el mismo día**, cada una arreglando una mitad distinta. Su propia ficha lo dice: *«que dos sesiones tropiecen con lo mismo el mismo día no es casualidad»*.
  - **[T-130]** (26/07): se escribió un **QUINTO** escritor de `seguimiento_url` sin ver los otros cuatro.
  - **31/07**: [T-375] y [T-382] se cogieron como tareas SEPARADAS y resultaron ser **los mismos ficheros**.
  - La causa común: `backlog_tasks` reparte **ids de tarea**; el trabajo colisiona en **rutas de fichero**. Y eso se descubría al pushear (conflicto) o nunca.
- **✅ LA HUELLA SE OBSERVA, NO SE DECLARA — y es la decisión de diseño que lo sostiene.** Cada sesión publica los ficheros que toca de verdad (sucio + lo que va por delante de `origin/main`, sacado de git), no lo que alguien anote. Una intención declarada se pudre en cuanto el trabajo se desvía —y se desvía siempre—; el estado observado no puede mentir. Y **no pide disciplina a nadie**: lo publica el **latido** ([T-296]), que ya corría en cada comando de `backlog.cjs` y en cada `pre-push`.
- **✅ AVISA EN TRES MOMENTOS, y ninguno bloquea:**
  1. **Al `claim`** — antes de escribir una línea. Predice dónde vas a trabajar cruzando los commits que mencionan ese `T-` con las rutas citadas entre backticks en su ficha (lo único que hay si la tarea es nueva).
  2. **En `scripts/sessions/latidos.cjs`** — el mapa en vivo de quién pisa a quién.
  3. Y si no puede ver a alguien, **lo dice**: una sesión con el latido viejo sale como *«no puedo descartar solape»*, nunca como verde.
- **Por qué NUNCA bloquea:** dos sesiones pueden tocar el mismo fichero por motivos legítimos. Un corte por solape sería insufrible y se acabaría rodeando — la lección recién aprendida en [T-375], donde el bloqueo imposible de satisfacer enseñaba a apagar el guard **entero**. Esto es información para decidir, no una puerta.
- **La CALIBRACIÓN es lo que lo hace creíble, y se midió antes de escribirla.** Barrido de los worktrees vivos el 31/07: el **único** fichero compartido por 3+ era `docs/roadmap/tareas-pendientes.md` (que lo tocan todas por diseño, y cuyo problema es [T-387]); el resto de solapes eran scripts `_tmp_*` de usar y tirar. O sea **el solape real es escaso**, así que el aviso salta poco — que es justo lo que hace que se lea. Lista de exclusión corta y medida, más las rutas desechables (`scratchpad/`, `sim-reports/`, `.claude/`).
- **🚨 HALLAZGO EN CUANTO SE ENCENDIÓ, y es peor que el solape: CUATRO sesiones latiendo desde el MISMO checkout principal.** Va aparte y con más gravedad porque es un fenómeno distinto: en worktrees separados el choque acaba en un **conflicto de git** —feo pero visible y reversible—; en el mismo directorio **se sobrescriben en vivo** y no hay conflicto que resolver, porque no hay dos versiones, hay una que la otra ya pisó. Es exactamente el acoplamiento que describe [T-385].
- **Capas:** migración additiva (`touched_files`/`touched_at`, con `COALESCE` para no borrar la huella si git no contesta), núcleo puro `lib/sessions/solape.cjs` con **21 tests**, y fail-open total en las tres puertas — un aviso roto no puede impedir reclamar, latir ni pushear.
- **Anécdota que vale por un guardarraíl:** escribiendo esto, el latido petó con `SyntaxError` por meter backticks de markdown dentro de un template literal. Es **la tercera vez** que ese modo de fallo muerde ([T-282] tumbó el barrido de salud, [T-349] costó el deploy de una noche) y **[T-349] ya lo cierra** (31/07): el pre-commit pasa `node --check` sobre los ficheros en stage, así que este caso habría muerto en el commit. Aquí lo cazó ejecutarlo; en `main` habría bloqueado a todas las sesiones.
- **Relacionadas:** [T-296] (el latido), [T-385] (el checkout compartido), [T-387] (el markdown que todas escriben), [T-349] (el parseo que no se comprueba).


