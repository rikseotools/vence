### [T-543] 🔴 [ABIERTO 04/08] El umbral del 60% deja 221 temas sirviendo media materia sin que salte ningún aviso (lo destapó una premium preguntando tres veces)

**De dónde sale:** **Neus A.B.** (premium, `subalterno_gva`, alta hace 5 días) escribió **tres veces
en 19 horas** sobre el mismo tema. Las dos primeras las tratamos como un fallo del contador —y lo
era, [T-507] lo arregló: el tema 3 anunciaba 39 preguntas y servía 22—. Pero a la tercera dijo lo que
de verdad importaba: *«ok gracias por contestar pero no consideráis que hay pocas preguntas para
dominar el tema 3 (EACV)? Sugiero y os pido por favor si podéis añadir más?»*. Y tiene razón: el
problema no era el número que mostrábamos, era que **no hay preguntas**.

**El tema 3 de `subalterno_gva`** (Estatut d'Autonomia, Títulos I-III) escopa **48 artículos con
texto** y solo **22 sirven preguntas**: 26 artículos del programa oficial con su texto importado y
**cero** preguntas. Por eso repetía las mismas una y otra vez. El scope y el epígrafe están
**verificados y correctos** (15/15 en los dos pasos) — no es un fallo de temario, es cobertura.

**🕳️ Lo que convierte esto en ficha: no lo veía NINGÚN detector, y hay una banda entera ahí.** Los
tres que miran cobertura se reparten el espacio dejando un hueco en medio:

| detector | condición | por qué calla en el tema 3 |
|---|---|---|
| `empty_topic` | 0 preguntas servidas | sirve 39 |
| `low_coverage` | < 6 preguntas servidas | sirve 39 |
| `article_no_coverage` | cobertura de artículos **≥ 60 %** | está al **46 %** |

El `>= 0.6` de `article_no_coverage` es deliberado y está comentado en el código: busca el patrón
*«casi terminado, faltan un puñado»* y deja fuera las oposiciones poco desarrolladas *«porque eso ya
lo señala `empty_topic`/`low_coverage`»*. **Esa partición no es limpia:** un tema con muchas
preguntas concentradas en pocos artículos satisface las tres condiciones a la vez y sale invisible.
Y `npm run huerfanos:plan` hereda el punto ciego, porque solo lista huérfanos de los temas que
disparan el finding: para `subalterno-gva` saca 91 artículos de la Ley 5/1983 y el Decreto 42/2019, y
**del Estatut solo 2** — justo el tema por el que preguntaba la usuaria.

**Medido el 04/08 en todo el banco** (mismos filtros que el detector: artículo activo, con texto,
no derogado, numeración del universo de cobertura; solo temas `disponible`):

| | |
|---|---|
| temas con ≥6 preguntas, ≥4 artículos huérfanos y cobertura <60 % | **221** |
| oposiciones afectadas | **75** |
| artículos del temario con texto y 0 preguntas, invisibles | **10.684** |

Los peores no son marginales: `agente_hacienda` mete **7 temas** (T24 al 23 %, T16 al 28 %),
`administrativo_castilla_la_mancha` T3 tiene **62 huérfanos** de 80 artículos, y
`ujieres_cortes_generales` T7 sirve 25 preguntas con **7 de 55** artículos cubiertos (13 %) — la
misma oposición de [T-511], donde ya se anotó que *«el umbral está calibrado para detectar temas
VACÍOS, no temas anémicos»*. Esto es ese mismo apunte, medido y por artículo en vez de por pregunta.

**⚠️ Lo que NO hay que hacer: bajar el 0,6 y ya.** Eso mete **221 hallazgos `warn` de golpe** en el
badge de `/admin/contenido`, que es exactamente cómo se mata un badge (la lección de
`landing_cifra_sin_respaldo` y de las citas «retocadas»). Hace falta **banda**, no umbral: separar el
tema que se nota al estudiar del que es una oposición a medio construir. La señal más útil que salió
de la medida es **cruzar cobertura con preguntas servidas** — un tema con 39 preguntas y 46 % de
artículos es un opositor repitiendo material; uno con 6 preguntas y 30 % es una oposición sin
terminar, y eso ya lo dicen los otros dos. Ordenar por *preguntas servidas ascendente* puso arriba
justo los casos donde se sufre.

**Lo que queda:**
1. **Definir la banda** y calibrarla contra los 221 antes de tocar el detector, con simulación (mismo
   método que el resto de kinds: medir primero, mirar 3 casos a mano, y solo entonces elegir corte).
2. **Que `huerfanos:plan` deje de depender del finding** para listar huérfanos de un tema: hoy
   `--oposicion` no puede contestar *«¿qué le falta a este tema?»* si el tema no dispara.
3. **La deuda de generación**, que es la parte grande y va por ARTÍCULO (fila compartida entre
   oposiciones, así que cubrir uno repara varias): empezar por el tema 3 de `subalterno_gva`, que
   tiene una usuaria premium esperando y **26 artículos con el texto ya importado** — la parte cara
   está hecha, falta generar contra ese texto con doble auditoría ciega.
4. **Revisar si `low_coverage` (<6 preguntas) tiene el mismo problema** en su propio eje: 39
   preguntas repartidas en 22 artículos y 39 en 2 artículos no son lo mismo, y hoy cuentan igual.

**⚠️ Nada prometido a la usuaria todavía.** En la respuesta del 04/08 se le dijo que su sugerencia es
correcta y que el tema **se va a ampliar**, sin fecha. Si esto se aparca, hay una premium con la
expectativa puesta.

**Cómo se reproduce la medida:** `node scripts/medir-banda-ciega-cobertura.cjs` (usa el `SQL_UNIVERSO_COBERTURA` real de
`lib/generacion/huerfanosPlan.js`, no una copia; solo lee). **Re-medido el 04/08 tras servir 13
preguntas del Estatut valenciano: 221 → 219 temas**, porque el tema 3 de `subalterno_gva` subió al
72 % de cobertura y salió de la banda por su propio pie — la medida está viva.

