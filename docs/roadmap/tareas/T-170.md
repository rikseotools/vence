### [T-170] 🟡 [ABIERTO 27/07 · adjudicado 07/08, w3 — falta el `--apply` de un rol con escritura] `clasificar-hitos` bank-wide: 45 hitos cambiarían de `tipo` y nadie ha adjudicado cuáles son mejora
- **Qué:** `scripts/clasificar-hitos.cjs` deriva `tipo` y `origen` de cada hito con reglas compartidas y testeadas, pero **hace más de un año que no se pasa entero**. Medido el 27/07 en seco sobre los 1.068 hitos vivos: **45 cambiarían de `tipo`** respecto a lo que hay en BD. Una parte son claramente arreglos (**14 hitos con `tipo` NULL** que pasarían a `plazo_fin`/`convocatoria_publicada`/`ejercicio_1`/`oep_aprobada`), pero otra parte **no está claro que lo sea**: `plazo_fin → plazo_inicio` ×4, `convocatoria_publicada → bases_publicadas` ×2, `modificacion_plazas → bases_publicadas` ×3. Correrlo a ciegas puede degradar hitos que alguien tipó a mano con el documento delante.
- **Impacto:** 🟡 el `tipo` alimenta el timeline, el rollover y las consultas por tipo; un hito mal tipado es invisible a quien lo busca por su tipo, y uno NULL lo es para todos. No hay daño visible hoy.
- **Cómo:** `node scripts/clasificar-hitos.cjs` (dry-run) para el reparto; luego **fila a fila** para los 45, mirando `titulo`/`descripcion`/`cita_literal` y el documento enlazado, decidir si la regla acierta. Los que la regla mejora se aplican; para los que no, la pregunta correcta es **si falla la regla** (→ arreglarla en el núcleo + test, que es lo que se hizo el 27/07 con «Celebración de los ejercicios», 57→55 hitos en `otro`) o si el dato de BD es mejor que la regla. Ya existe `--slug <slug>` para acotar por oposición.
- **Origen:** 27/07, al arreglar la provenance de `ordenanza-ayuntamiento-cordoba` (se corrió solo con `--slug`, a propósito, para no tocar el catálogo entero de rebote).

#### ADJUDICADO 07/08 (w3) — reglas corregidas y testeadas; el `--apply` queda pendiente de un rol con escritura de negocio

- **La cifra de la ficha estaba desfasada — medido, no supuesto.** Contra los **1.084** hitos vivos de hoy (VENCE_LECTOR_URL, no la ficha), la comparación fila a fila da **57 diffs**, no 45 — el catálogo creció desde el 27/07 y el desfase con él. La cuenta exacta: `Total hitos: 1084 / Total diffs: 53` **tras** aplicar los arreglos de abajo (57 antes de tocar nada).
- **GOTCHA de método, para quien repita esto:** un `JOIN` directo a `oposiciones` para sacar el `slug` de cada hito se come **604 de 1084 filas en silencio** — `oposiciones` tiene RLS activo y `VENCE_LECTOR_URL` solo ve 125 de las que hay. No es un bug de esta tarea, es el rol de lectura haciendo lo que tiene que hacer; la comparación se hizo **sin** ese join (solo por `h.titulo`/`h.descripcion`/`h.url`, que es lo único que `clasificar()` mira), y el `slug` se resolvió aparte, best-effort, solo para dar contexto al leer el informe.
- **6 arreglos de REGLA, cada uno medido contra el corpus real y con test que fija el caso, CERO regresiones sobre lo que ya acertaba (verificado recorriendo los 1.084 antes/después, no solo con el test suite):**
  1. `modificacion_plazas` +`acumulaci.n.*plaza` — "Decreto de acumulación de plazas (9 → 17)" y "Acumulación de plazas (11 plazas)" caían a `otro`; acumular plazas de OEPs previas es tan `modificacion_plazas` como "ampliar", solo con otra palabra.
  2. `tribunal_constituido` +`designaci.n.*tribunal` — "Designación del Tribunal de selección" caía a `otro` porque la regla solo reconocía el orden "tribunal designado", no el inverso. Único hito con esa forma en todo el catálogo (comprobado): cero riesgo de colisión.
  3-4. `plazo_fin`/`plazo_inicio` — dos fallos en la misma regla: (a) el conector entre el verbo y "plazo" solo dejaba pasar "del "/"el ", así que un adjetivo de por medio ("Cierre del **nuevo** plazo de solicitudes") rompía el match y el hito caía al fallback genérico de `plazo_inicio`, **invirtiendo el sentido**; (b) "Plazo de inscripción **cerrado**"/"…(fechas) **cerrado**" dice el cierre AL FINAL del título, no antes de "plazo" — sin una alternativa de orden inverso, esos títulos solo casaban con el fallback genérico de `plazo_inicio` y también salían invertidos. **Se midió el radio de la ampliación antes de tocar nada**: 178 hitos dependen del fallback genérico de `plazo_inicio` en algún grado, pero solo 3 dependen de él EN SOLITARIO sin ningún otro indicio y están correctamente tipados como `plazo_inicio` en BD ("Plazo de presentación de solicitudes" ×2, "Plazo de solicitudes" ×1) — esos NO se tocan, el fallback se deja intacto.
  5. `resultados` +`relaci.n de aprobad` (acotado a esa frase, no "aprobad" a secas — un patrón suelto se habría comido "Aprobados fase de oposición"/"Aprobados del primer ejercicio", que son legítimamente `ejercicio_1`/`oep_aprobada`) — "Relación de aprobados (fase de oposición)" perdía contra "fase de oposici" de la regla `ejercicio_1`, que se prueba después en el array pero como `resultados` no casaba, ganaba por descarte.
  - Tests nuevos en `__tests__/lib/clasificarHitos.test.ts` (8, con el hito real que motivó cada uno) — **43/43 en verde** en ese fichero + `hitoOrigen.test.ts` (que consume el mismo núcleo), sin tocar ni un caso ya fijado.
- **Los 53 diffs que quedan, adjudicados uno a uno contra `titulo`/`descripcion`/`cita_literal` (no solo el nombre del `tipo`):**
  - **30 son `NULL → tipado`** — sin ambigüedad: la regla les da un tipo donde hoy no hay ninguno, y el título/descripción de cada uno lo confirma (repartidos entre `convocatoria_publicada`, `bases_publicadas`, `ejercicio_1`, `plazo_fin`, `plazo_inicio`, `oep_aprobada`, `resultados`). Aplicar sin más.
  - **22 son `tipado → tipado` donde la regla mejora sobre lo que hay**, verificados leyendo cada título/descripción — algunos porque el dato en BD estaba simplemente sin refinar (p.ej. "Publicación de la convocatoria en el BOC" tenía `bases_publicadas` cuando el título y la cita literal son puro `convocatoria_publicada`; "Convocatoria OEP 2026 (bases e inscripción)" ×2 decían `convocatoria_publicada` cuando la propia descripción dice que la convocatoria "está pendiente de publicación" — solo la OEP está aprobada). Aplicar.
  - **1 sigue siendo AMBIGUO y NO se adjudica a ciegas:** *"Plazo de inscripción (23/12/2025 - 22/01/2026)"* (oposición `tecnico-informatica`) — BD dice `plazo_fin`, la regla propone `plazo_inicio`, y el título no dice cuál de las dos fechas es la relevante; sin `url` ni `source_documento_id` enlazados no hay fuente que consultar desde aquí. **SOSPECHO que el dato de BD es correcto** (alguien lo tipó mirando el documento), pero no lo he podido confirmar — déjese como está hasta que alguien lo mire con el boletín delante.
- **Por qué esto NO se aplicó ya:** `--apply` escribe en `convocatoria_hitos`, que es BD de negocio — fuera del alcance de un trabajador de la flota (`VENCE_LECTOR_URL` es de solo lectura, a propósito). **Para aplicar los 52 adjudicados** (30 NULL + 22 mejoras, dejando fuera el ambiguo):
  ```
  node scripts/clasificar-hitos.cjs --apply
  ```
  Confirmar antes con el dry-run (sin `--apply`) que el reparto por tipo tiene sentido, y NO tocar a mano el caso de `tecnico-informatica` sin mirar su convocatoria oficial primero.
- **Relacionado, no del alcance de esta tarea:** dos hitos con `titulo`/`descripcion` internamente contradictorios salieron al adjudicar ("Convocatoria publicada (BOP Badajoz…)" con descripción "Aún no publicada la convocatoria…"), que es un problema de CONTENIDO del hito, no de la regla de `tipo` — para quien lo quiera mirar, no arreglado aquí.

#### ⚠️ CORRECCIÓN 08/08 (w2, review-fix) — la adjudicación de los 22 "tipado→tipado" tenía 2 regresiones demostrables y 3 ambiguos sin evidencia suficiente; ampliada tras revisar CADA caso, no solo los que la revisión señaló

- **La revisión (w4) reprodujo la medición exacta (53 diffs) y encontró 5 casos mal adjudicados en los 22 "tipado→tipado, mejora verificada":** (1) `ddb30ace` "Nombramiento del Tribunal Calificador" — BD ya decía `tribunal_constituido` (CORRECTO, respaldado por `cita_literal`), la regla lo degradaba a `nombramientos` porque esa regla tiene prioridad de array más alta y "Nombramiento" matchea antes de llegar a `tribunal_constituido` — **REGRESIÓN real, no una mejora**. (2) `66adf612` "Proceso OEP 2023 resuelto" — BD decía `nombramientos` (bien fundado: la descripción dice "propuesta de nombramiento"), la regla lo cambiaba a `oep_aprobada` solo porque el título contiene la cadena "OEP" — **pérdida de precisión**, porque `clasificar()` solo lee `titulo`, nunca `descripcion`. (3-5) los 3 "Corrección de bases (BOCYL/BOPVA)" — sin descripción, cita genérica, url a un tablón sin documento consultable; BD decía `modificacion_plazas`, la regla proponía `bases_publicadas` (que en el resto del banco significa publicación INICIAL, no corrección posterior) — **ninguna de las dos claramente mejor sin abrir el boletín**.
- **Reproducido cada uno independientemente contra RDS (no me fié ni de la ficha ni de la revisión) — los 5 confirmados exactos.** Y en vez de limitarme a esos 5, revisé **los 24 "tipado→tipado" uno a uno** (18 más de los que la revisión inspeccionó), con el mismo criterio: ¿lo que dice `titulo`/`descripcion`/`cita_literal` sostiene el `tipo` nuevo, o lo contradice? Salieron **3 casos MÁS** con el mismo defecto que la revisión no llegó a mirar:
  - `053db338` "Convocatoria y bases (BOCYL)" y `6625a7e3` "Convocatoria y bases (BOP)" — BD decía `convocatoria_publicada` (para el primero, respaldado por una `cita_literal` de puro texto de convocatoria), la regla proponía `bases_publicadas` solo porque esa regla es un `/bases/` sin ninguna condición y tiene prioridad más alta que `convocatoria_publicada` — el MISMO mecanismo que las 3 "Corrección de bases" que la revisión sí cazó.
  - `d0294f39` "Convocatoria publicada (BOP Badajoz…): 38 plazas" — el propio commit original ya señalaba que su `descripcion` ("Aún no publicada la convocatoria…") CONTRADICE su `titulo`, y lo apuntó como "relacionado, no del alcance" — pero no lo excluyó del lote a aplicar. Aplicar la regla aquí (`convocatoria_publicada`) escribiría algo que el propio dato del hito niega.
- **Arreglo de RAÍZ para el caso (1), no solo el caso concreto:** reordenado `tribunal_constituido` ANTES que `nombramientos` en el array `TIPO` (antes al revés). Medido contra los 1.084 reales: el reorden cambia exactamente 3 hitos, los 3 con "tribunal" en el título — **2 más además de `ddb30ace`** que tenían el MISMO defecto sin que nadie lo hubiera notado (`bb84df7b` "Nombramiento de tribunales calificadores", `e53bbb27` "Nombramiento del tribunal calificador"), ambos en BD como `nombramientos` cuando semánticamente son `tribunal_constituido`. Cero riesgo de arrastrar nombramientos de PERSONAS: el patrón de `tribunal_constituido` exige "tribunal"/"comisión" cerca, así que "Nombramiento de funcionarios en prácticas" o "Toma de posesión" siguen cayendo en `nombramientos`. Test de regresión añadido.
- **Para el caso (2), MEDIDO y DESCARTADO un arreglo de fondo — importante para que nadie lo reintente sin medir:** ¿debería `clasificar()` mirar también `descripcion` cuando `titulo` es ambiguo? Medido contra los 1.084 hitos reales: buscar en `titulo`+`descripcion` con la MISMA lista de reglas cambia **124 clasificaciones**, y la inmensa mayoría son PEORES — la descripción trae contexto (una fecha límite futura, un trámite relacionado) que dispara una regla de más prioridad y desplaza al hito de su categoría correcta (ejemplo real: "Cierre del plazo de inscripción", `plazo_fin` correcto, pasaría a `lista_provisional` solo porque la descripción menciona "pendiente de lista de personas admitidas" — un evento FUTURO citado, no lo que el hito ES). **Descartado como arreglo general; documentado en el propio código para que no se repita la investigación.**
- **Arreglo de fondo que SÍ hacía falta y no existía: una lista de exclusión.** `--apply` (leído el código, no solo probado) sobrescribe **TODOS** los hitos que consulta —el catálogo entero, o los de `--slug`— con lo que `clasificar()` calcule EN ESE MOMENTO, sin distinguir "esto cambia" de "esto no cambia": no había forma de aplicar los 46 casos buenos sin tocar también los ambiguos. Añadido `NO_TOCAR_TIPO` (`Map` de id→motivo, en el propio script): `--apply` excluye esos ids del `UPDATE` y el dry-run los imprime explícitamente (no en silencio). Contiene los **9** casos adjudicados como ambiguos/contradictorios (los 5 de la revisión + los 3 que encontré + el ya conocido de `tecnico-informatica`), cada uno con su motivo por escrito.
- **Cifras FINALES, recalculadas con el script corregido (no las de antes del arreglo):**
  - **30 `NULL → tipado`** — sin cambios respecto a la entrega anterior, no afectados por nada de esto.
  - **16 `tipado → tipado` mejora** (antes 22; -5 por la revisión, -3 por mi revisión adicional, +2 por el arreglo de raíz del reorden que descubre `bb84df7b`/`e53bbb27`).
  - **9 excluidos en `NO_TOCAR_TIPO`**, ninguno se toca sin criterio humano y el boletín delante.
  - **Total a aplicar: 46** (antes 52 — de los cuales 5 habrían sido regresiones o pérdidas de precisión).
- **Tests:** `__tests__/lib/clasificarHitos.test.ts` ampliado (2 tests nuevos: la regresión del reorden + que los 9 ids de `NO_TOCAR_TIPO` están todos con su motivo) — **86/86 en verde** en las 5 suites relacionadas (`clasificarHitos` + `hitoOrigen` + `hito-validation` + `hitoAcreditacion` + `agruparHitos`).
- **Por qué esto NO se aplicó ya:** `--apply` escribe en `convocatoria_hitos`, BD de negocio — fuera del alcance de un trabajador de la flota. **Para aplicar los 46 adjudicados** (dry-run primero, obligatorio):
  ```
  node scripts/clasificar-hitos.cjs                # dry-run: confirmar el reparto Y la lista de 9 excluidos
  node scripts/clasificar-hitos.cjs --apply         # aplica solo los 46, los 9 de NO_TOCAR_TIPO quedan intactos
  ```
  Los 9 de `NO_TOCAR_TIPO` (motivo en el propio código) necesitan a alguien con el boletín oficial delante, no una regla mecánica.

