### [T-454] ✅ 🟠 [HECHA 04/08] El modo EXAMEN nunca registra el dispositivo: 39 usuarios de 7 días responden y no dejan fila en `user_devices`

- **ORIGEN.** Investigando el cabo suelto de [T-371]: quedaban 2 usuarios de 24 h que respondían sin dejar ninguna fila de dispositivo, **uno de ellos con 70 respuestas**. Setenta oportunidades de registrarlo.
- **QUÉ ERA, y no es lo que T-371 describe.** Las 70 respuestas de ese usuario (`a71b1725`, alta 31/07 06:06) son **todas del mismo minuto**, las 06:09 — un solo `test_id` y `question_order` correlativo del 1 al 70. Esa es la firma de la **inserción en bloque de `/api/exam/validate`**, no de alguien respondiendo una a una.
- **LA CAUSA:** `/api/exam/validate` **lee** el `device_id` (`getDeviceIdFromRequest`, línea 444) y lo usa para el contador de servidas y para el gate… pero **nunca llama a `registerAndCheckDevice`**. De los cuatro endpoints que responden preguntas, tres SÍ registran (`answer-and-save`, `answer/psychometric`, `answer/spelling`, más `exam/answer`) y **`exam/validate` no**. Quien solo hace exámenes es invisible en `user_devices`.
- **MEDIDO:** **39 usuarios** respondieron en los últimos 7 días y no tienen ni una fila de dispositivo.
- **A QUIÉN deja ciego** (todos leen `user_devices`): el sweep de fraude (`multi_account_device`, `device_daily_farming`, `premium_sharing`), el límite de dispositivos por cuenta, y la comprobación **anti-autoreferido** de `lib/referrals/activeSignup.ts`. Es el mismo conjunto de consumidores que enumera [T-371] — el agujero es distinto, las consecuencias son las mismas.
- **NO CONFUNDIR CON [T-371]:** allí la huella **nace dentro del camino de marketing** y por eso falta en algunos clientes. Aquí el cliente **sí manda** `X-Device-Id` y `X-Hw-Fingerprint` (`answerSaveQueue` los pone) — lo que falta es que **el servidor los guarde en ese camino**. Son dos causas independientes del mismo síntoma, y por eso el arreglo de T-371 no cerró estos 39.
- **CÓMO ARREGLARLO, con el matiz que importa:** llamar a `registerAndCheckDevice` en `validate` **sin hacer cumplir el límite** — ignorando su veredicto `allowed`. En los otros caminos bloquear es aceptable porque se corrige pregunta a pregunta; aquí `validate` es **el final de un examen entero**, y cortar ahí le tiraría al usuario el trabajo de una hora. Registrar ≠ bloquear, que es exactamente la separación que ya hace `resolverAnclaDispositivo` con el ancla derivada de la huella.
- **Cuidado al tocar esto:** el camino de examen tiene historia de problemas de persistencia (memoria `project-exam-persistence-issues`), así que el cambio va con test y no se da por bueno hasta verlo vivo.
- **Relacionadas:** [T-371] (la otra causa del mismo síntoma), [T-304] (la huella que sobrevive al borrado), sweep de fraude y `lib/api/deviceLimit.ts`.


- **✅ ARREGLADO (01/08), en el sitio que ya tenía todo lo necesario.** El registro va dentro de `traceValidateCall`, que ya resuelve usuario, dispositivo e IP y cuyo contrato declarado es literalmente *«aquí NO se bloquea nada — se hace visible»*. Se le pasa también la **huella** (`getHwFingerprintFromRequest`): sin ella el registro dependería de un `localStorage` que se borra en dos clics, que es lo que [T-304] existe para no repetir.
- **Se registra y NO se hace cumplir el límite** — el veredicto se descarta con `.catch(() => {})`. Cortar en `validate` le tiraría al opositor el examen entero.
- **Capas:** guardarraíl `__tests__/security/examValidateRegistraDispositivo.test.ts` (9 tests) que enumera **las cuatro vías** de responder, para que añadir una quinta sin registro ponga el CI en rojo. Y comprueba que el veredicto se sigue descartando: si alguien empieza a usarlo para decidir, salta.
- **El guardarraíl era TEATRO hasta que lo mutilé a propósito:** la primera versión casaba `registerAndCheckDevice` como **subcadena**, así que `XXregisterAndCheckDevice(` lo pasaba tan campante. Es el mismo fallo de coincidencia laxa que me costó dos horas en [T-442] con `metadata->>'cron'`. Con `(?<![\w$])` delante, el mutante falla y el original pasa — comprobado en las dos direcciones.
- **⏳ Falta verlo VIVO:** exige deploy de frontend. Al desplegar, comprobar que aparecen filas nuevas en `user_devices` de usuarios que solo han hecho examen, y que el conteo de «activos de 7 días sin fila de dispositivo» baja desde los **39** medidos hoy.
- **✅ VERIFICADO EN PRODUCCIÓN (04/08).** Los activos de 7 días sin fila en `user_devices` bajan de **39 a 30**. Pero ese número por sí solo no dice nada (la población cambia cada día), así que la medida buena es la acotada al arreglo: de los **527 usuarios activos DESDE el deploy**, solo **12** no tienen fila, y de esos **3** han abierto un examen — dos **sin contestar ni una pregunta** (0 filas: no hay nada que registrar) y uno que sí lo completó.
- **Ese único caso NO es este agujero, y conviene dejarlo escrito para que nadie lo persiga por aquí:** su cliente no manda **ni `device-id` ni huella**, así que `registerAndCheckDevice` sale por *fail-open* **antes** de registrar. Es la causa de [T-371], que esta misma ficha ya separaba como independiente — y es la prueba de que estaban bien separadas.

