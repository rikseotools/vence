### [T-532] 🟠 [ABIERTO 04/08] Una ficha = un fichero: quitar la CAUSA de la contención que [T-400] dejó solo visible

- **Esfuerzo: sesion_propia.** Toca el andamiaje del que dependen 2-10 sesiones a la vez; hacerlo deprisa es peor que no hacerlo.
- **NO es un duplicado de [T-400], es su escalón siguiente.** T-400 midió el mismo hecho —*«el ÚNICO fichero compartido por 3+ sesiones era `docs/roadmap/tareas-pendientes.md`»*— y decidió **a propósito** solo AVISAR (al `claim` y en los latidos), sin tocar la causa, porque bloquear por solape sería insufrible. Esto no propone bloquear: propone que el conflicto **no exista**.
- **EL COSTE, medido en UNA sesión (04/08).** Cinco conflictos en ese fichero en una tarde, cada uno con su ciclo completo de tests + typecheck para reintentar el push. El fichero va por **506 fichas y 12.236 líneas**, con **1.307 commits** en su historia. Todas las sesiones insertan **en el mismo punto** (bajo `## Abiertas`), así que chocar no es mala suerte: es la geometría.
- **Y el daño no es solo tiempo.** Resolver quedándose con «su» lado borra el trabajo del otro **en silencio**: el guardarraíl de ids comprueba unicidad, y un id sigue siendo único después de vaciarle el cuerpo. Por eso existe `contexto-push-guard` ([T-428]) — un parche sobre el mismo problema. Hoy mismo, además, `origin/main` traía **T-517 duplicada** (una copia abierta y otra ✅ HECHA) por cerrarla copiando en vez de moviendo: otra cara del mismo fichero-monolito.
- **LA PROPUESTA: `docs/roadmap/tareas/T-nnn.md`, uno por ficha**, y el markdown grande pasa a ser **generado** (índice legible, no fuente).
  - Dos sesiones creando fichas **nunca tocan el mismo fichero** → el conflicto desaparece por construcción, no se mitiga.
  - Arregla de paso las huérfanas: la sección la decide el generador, no dónde pegaste el texto. ([T-515] las reubicó y puso trinquete, pero el modo de fallo seguiría existiendo sin esto.)
  - **Refuerza `contexto-push-guard`**: borrar una ficha pasa a ser borrar un FICHERO, que git sí sabe señalar solo.
  - Es lo que necesita la flota en VPS de [T-486]: su propia ficha ya avisa de que *«el cuello de botella no es el cómputo, es la integración»*.
- **LO QUE HAY QUE TOCAR (y por qué es sesion_propia):** `lib/backlog/parseMarkdown.cjs` (fuente única de parseo), `sync`, `backlog.cjs ficha`/`reubicar`, `backlogRegistry.guardrail`, `contexto-push-guard` (compara ESE fichero contra `origin/main`), `perdidaDeContexto.cjs`, y la migración de las 506 fichas. Son las piezas de las que depende el reparto entre sesiones: romperlas deja a todas sin coordinación.
- **DESCARTADO: el merge driver de git.** Parecía el atajo (unir por id al fusionar) pero se registra en `.git/config`, que **no se versiona**: en cada VPS habría que instalarlo aparte, y quien no lo tenga cae al comportamiento de siempre. Una protección que depende de que cada máquina esté bien configurada no es fiable, es optimista.
- **CÓMO SE SABRÁ SI SALIÓ BIEN** (declarado antes de empezar): conflictos en el backlog por sesión → 0; `npm run sesiones:friccion` sin subida de escapes; y ninguna ficha perdida en la migración (comparar el conjunto de ids Y el de líneas con contenido antes/después, que es como se verificó [T-515]).

- **🛠️ IMPLEMENTADO (07/08, w4), rama `flota/T-532-una-ficha-un-fichero` — PENDIENTE de revisión humana antes de fusionar. No es papeleo: esto cambia el andamiaje que TODAS las sesiones usan para coordinarse, y una sesión sola no puede ver el efecto de fusionarlo mientras otras siguen trabajando contra el `main` viejo.**

  **Lo construido, en orden de dependencia:**
  1. **`lib/backlog/dividirFichas.cjs`** (núcleo puro nuevo): trocea `tareas-pendientes.md` en bloques (`preambulo`/`ficha`/`marcador_seccion`/`suelto`) con UNA garantía verificable: `reconstruir(dividirEnBloques(md)) === md`, byte a byte. Probado contra el fichero REAL completo (17.286 líneas, 3.302.666 caracteres), no solo contra fixtures — incluye el hallazgo de que un `grep` codicioso da **8 "duplicados" falsos** (títulos que citan otra tarea); con la regex perezosa correcta (la misma que ya usaba `insertarFicha.cjs`), **0 duplicados reales**.
  2. **`lib/backlog/fichasDir.cjs`**: el almacén — leer/escribir una ficha por id, generar el índice (`## Abiertas`/`## Hechas` derivadas del ✅ de la cabecera, nunca de la posición), y `indiceEstaAlDia()` (detecta un índice desincronizado).
  3. **`scripts/backlog/migrar-a-ficheros.cjs`**, re-ejecutable (por si `main` se mueve antes de fusionar esto — el fichero lo edita todo el mundo a diario). **CORRIDA**: 603 fichas → `docs/roadmap/tareas/T-nnn.md`, con verificación POST-escritura de que cada ficha, el preámbulo y los sueltos sobreviven verbatim antes de dar la migración por buena.
  4. **6 bloques sin id** (`## Por dónde empezar`, `## Importar contenido para cerrar residuo CE-relink`, dos avisos de `## ✅ [DESPLEGADO…]`/`## ✅ Drenaje CE-mislink`, `## Cabos sueltos F0 antifraude`, `## Orden de ataque`) — texto real sin ficha, preservado en `docs/roadmap/tareas/_sueltos.md` con su propia cabecera de cuarentena en el índice. **Pendiente de triage humano**: convertirlos en ficha con `reserve`, o decidir que ya no hacen falta.
  5. **`scripts/backlog.cjs`**: `parseMd()`/`fichaBody()` leen de `fichasDir` (no del monolito); el comando `ficha` escribe el fichero nuevo directo (sin buscar dónde insertar — ya no hay «dónde»); `reubicar` queda retirado (avisa y regenera el índice si hace falta, por si algún script viejo lo invoca).
  6. **`lib/backlog/gitFichas.cjs`**: cuatro funciones nuevas (`estuvoEnElHistorialLocalFichero`/`hechosDeOrigenFichero`/`commitQueLaQuitoFichero`/`enAlgunaRamaFichero`) que preguntan por PATH (`git log -- docs/roadmap/tareas/T-nnn.md`) en vez de pickaxe sobre una marca dentro del monolito. Las viejas se CONSERVAN (sirven para investigar historia previa a esta migración); `sync` ya usa las nuevas. Reproducido el incidente exacto de T-427 (una sesión vieja se lleva por delante el fichero de otra sin verlo) contra la variante nueva, con la misma topología de test que probó la vieja.
  7. **`scripts/contexto-push-guard.cjs`**: añadida la protección PRINCIPAL — compara, para cada `docs/roadmap/tareas/T-nnn.md` que cambió, las dos versiones (`origin/main` vs `HEAD`) con la MISMA lógica pura de `perdidaDeContexto.cjs` (sin tocarla: un fichero de una ficha es, para esa función, un markdown con una entrada). La comprobación vieja sobre el índice se conserva como red secundaria. Simulación nueva (`npm run sim:contexto-guard-ficheros`, 7/7) además de la vieja (`npm run sim:contexto-guard`, sigue en 8/8 sin tocarla).
  8. **`lib/backlog/pushGuard.cjs`**: la exención «solo documento la ficha, no exige claim» ahora también cubre `docs/roadmap/tareas/T-nnn.md`, no solo el monolito.
  9. **Guardarraíles actualizados**: `backlogRegistry.guardrail.test.ts` gana dos tests nuevos (el índice generado coincide con lo que regenerarlo produciría; ningún id huérfano en ninguna dirección); `fichaHuerfanaMiraOrigin.guardrail.test.ts` actualizado a los nombres nuevos, misma garantía protegida.

  **Verificado, no solo escrito:** 1.780 tests corridos entre `__tests__/backlog/` y `__tests__/guardrails/` (los 3 fallos que hay son de OTRA suite, `temarioPdfQueue.integration.test.ts`, por falta de permiso de escritura del rol de trabajador — reproducido idéntico sin mis cambios, no es una regresión de esto). `tsc --noEmit` limpio. `node scripts/backlog.cjs list`/`mine` corridos de verdad contra la BD real y leen las 603 fichas correctamente desde los ficheros nuevos.

  **LO QUE FALTA — y por qué NO se ha hecho aquí:**
  - **El cutover en sí.** Esta rama tiene la migración YA CORRIDA sobre el `tareas-pendientes.md` que había al momento de escribir esto — no sobre el que haya cuando alguien la fusione (el fichero lo edita todo el mundo a diario). `migrar-a-ficheros.cjs` es re-ejecutable a propósito: antes de fusionar, hay que volver a correrlo sobre el `main` del momento y comprobar que sigue verificando limpio. **Demostrado en vivo mientras se escribía esto, no en teoría:** al crear la rama, `origin/main` tenía **602** fichas y mi migración (de un momento un poco anterior, dentro del mismo turno) traía **603** — la diferencia exacta es [T-637], una ficha propia entregada minutos antes por su propia rama y aún sin fusionar a `main`. No es un error: es la prueba de que el snapshot envejece en minutos, tal cual se avisa arriba. No hace falta rehacer la migración para esto (T-637 llegará también por su propio camino y no colisiona), pero SÍ hace falta repetirla si pasa más tiempo antes de fusionar.
  - **Auditoría exhaustiva de los ~20 ficheros que MENCIONAN `tareas-pendientes.md`** (grep hecho, la mayoría son comentarios citando la ficha como referencia — inofensivo, el fichero generado sigue existiendo). Dos tenían dependencia funcional real (`scripts/impugnaciones/revisar-feedback.cjs`, `revisar-impugnacion.cjs`) y siguen funcionando sin cambios porque LEEN el índice generado, que sigue vivo y actualizado.
  - **`backend/src/alerts/alert-rules.ts`** y cualquier proceso en el VPS/Fargate que lea `tareas-pendientes.md` directamente: no auditado a fondo — el índice generado tiene el mismo aspecto de siempre, así que debería seguir funcionando, pero no se ha verificado uno a uno.
  - **Coordinación del cutover con las sesiones vivas.** Fusionar esto mientras 2-10 sesiones tienen worktrees basados en el `main` viejo significa que, hasta que hagan `pull`, sus operaciones de `ficha`/`sync` seguirán escribiendo en el monolito viejo de SU copia — no es destructivo (el próximo `sync`/`pull` lo reconcilia), pero **es la clase de decisión de secuenciación que le corresponde a una persona**, no a un trabajador suelto.
  - **`lib/admin/toolRegistry.ts`**: no se ha registrado `migrar-a-ficheros.cjs` (no lo exige ningún guardarraíl existente, pero sería buena práctica antes de darlo por definitivo).
