### [T-157] 🟡 [ABIERTO 26/07] 38 preguntas invisibles que el detector NO cuenta: artículo inactivo **y además sin escopar**
- **Qué:** `scope_phantom_article` cuenta las preguntas que cuelgan de un artículo inactivo **que está escopado**. Pero hay artículos inactivos **que tampoco están en ningún `topic_scope`**, y sus preguntas activas son igual de invisibles — solo que **por partida doble**, así que ningún detector las ve. Medido el 26/07: **38 preguntas en 12 artículos**.
- **Reparto:** RGC 16 · Ley 1/1998 CyL 12 · LECrim 7 · ODM 2 · Ley 15/2015 1.
- **Cómo salió:** al cerrar [T-139] el recuento de invisibles dio 0 y era **correcto según su definición**, pero quedaban estas fuera. Las 7 de LECrim ya venían anotadas en aquella ficha como *"los que difieren del oficial"* (`588 bis b/k`, `588 ter d/g`, `588 quater b` y los arts. 130 y 140 rotulados «del Código Penal»): se creían pendientes de comparar, y además están sin escopar.
- **Cómo remediarlas — ya hay herramienta, no hace falta inventar nada:** `node scripts/reactivar-articulo-boe.cjs "<ley>" "<art>"` da el veredicto tipado (`identico`/`erratas`/`reordenado`/`incompleto`/`contaminado`) contra el bloque VIGENTE del BOE. **Ojo:** ese script **bloquea a propósito** reactivar un artículo que no esté en ningún scope, porque seguiría sin servirse — así que aquí el orden es al revés: primero decidir si el artículo entra en el temario de alguna oposición (y añadirlo al `topic_scope` con el criterio de `verificar-epigrafes-scope.md`), y solo entonces reactivar. Si no entra en ningún temario, la salida honesta es jubilar las preguntas, no reactivar el artículo.
- **Caso llamativo ya medido:** LECrim `588 bis k` tiene **1.622 caracteres en BD frente a 815 en el BOE** → huele a dos artículos fusionados en la importación.
- **Y conviene arreglar el detector, no solo los datos:** que `scope_phantom_article` solo mire lo escopado deja este hueco abierto para siempre. Falta decidir si se amplía ese kind o se añade uno hermano (`orphan_inactive_article`), midiendo antes el ruido — no todo artículo inactivo sin escopar es un defecto.
- **Origen:** cierre de [T-139] (26/07).

#### 📌 SESIÓN 07/08 (w4) — detector construido y en verde; corregido el recuento (40/14/6, no 38/12/5); las 6 leyes investigadas con evidencia (2 veredictos de alta confianza listos para aplicar, 2 jubilaciones confirmadas contra fuente, 2 pendientes de un último dato); nada aplicado (sin escritura de negocio)

- **✅ Detector hermano construido: `orphan_inactive_article`.** Añadido a `scripts/health-sweep.cjs` y su mirror en `backend/src/content-health-sweep/content-health-sweep.service.ts` (MANTENER EN SYNC), entrada en `lib/admin/runbookRegistry.ts` (frase-gatillo *"revisa los artículos huérfanos inactivos"*, registrada en CLAUDE.md) y `docs/runbooks/verificar-epigrafes-scope.md` como runbook. Criterio: `articles.is_active=false` AND sin match normalizado en ningún `topic_scope.article_numbers` activo AND con `questions.is_active=true` colgando de `primary_article_id`. **Calibrado contra ruido (medido, no supuesto):** de **379** artículos inactivos-y-sin-escopar en todo el banco, solo **14** tienen preguntas activas — los otros 365 son bajas legítimas sin nada colgando y el detector NO los reporta (cero falsos positivos en la calibración). Tests: `__tests__/health/content-sweep-parity.test.ts` (204 verdes, confirma paridad script↔backend) + `__tests__/lib/admin/runbookRegistry.test.ts` (frase-gatillo presente en CLAUDE.md, entrada completa). Corrí la query EXACTA del detector contra RDS real (`VENCE_LECTOR_URL`) y reproduce 14/40 letra por letra.
- **⚠️ EL RECUENTO ORIGINAL DE LA FICHA ERA INCOMPLETO — corregido con lo medido.** La query real da **40 preguntas en 14 artículos, 6 leyes** (no 38/12/5): faltaba **`Reglamento Bienes EELL` (RD 1372/1986)**, 2 artículos (`17-18` y `44, 56, 70`), 1 pregunta cada uno — ley real, con `boe_url`, no virtual. Reparto correcto: RGC 16 · Ley 1/1998 CyL 12 · LECrim 7 · ODM 2 · **Reglamento Bienes EELL 2** · Ley 15/2015 1.
- **✅ LECrim investigado ENTERO (7/7 artículos), con el veredicto tipado de `reactivar-articulo-boe.cjs` contra el BOE vigente — no supuesto, corrido de verdad en dry-run:**
  - **`588 bis b`** → IDÉNTICO (1.240ch nuestro / 1.266ch oficial, solo diferencias de formato).
  - **`588 quater b`** → IDÉNTICO (850ch / 865ch).
  - **`588 ter d`** → INCOMPLETO: falta el párrafo *"b) El conocimiento de su origen o destino, en el momento en el que la comunicación se realiza."*
  - **`588 bis k`** → CONTAMINADO: 1.622ch nuestro / 815ch oficial — **confirma la sospecha de la ficha original**. El párrafo sobrante (*"1. Una vez que se ponga término al procedimiento mediante resolución firme, se ordenará el borrado y eliminación de los registros originales..."*) no está en el bloque oficial `a588bisk` — huele a fusión con otro artículo, sin identificar cuál todavía.
  - **`588 ter g`** → CONTAMINADO: 440ch / 220ch — el sobrante (*"La duración máxima inicial de la intervención... tres meses, prorrogables..."*) tampoco está en el bloque oficial `a588terg` — mismo patrón, origen sin identificar.
  - **`130` y `140`** → **NO son artículos de LECrim: son Código Penal mal etiquetado.** Confirmado con certeza: `title` de ambas filas dice LITERALMENTE *"Artículo 130 del Código Penal."* / *"Artículo 140 del Código Penal."*, y su `content` es prácticamente idéntico letra a letra al de `CP` (LO 10/1995) art. 130/140, que YA existen activos y escopados en varios temas reales vivos (`policia_nacional` T16/T17, `ayudante_instituciones_penitenciarias` T104/T105, `ayudantes_ejecucion_penal_pais_vasco` T103/T104, `guardia_civil` T8). Son duplicados erróneos de importación, no contenido real de LECrim.
- **🔍 Hallazgo colateral (no de esta ficha, no tocado): la razón por la que `588 bis`/`588 ter`/`588 quater` (SIN letra) no cazan sus hijos con letra no es un bug de matching — es que `tramitacion_procesal T20` escopa la forma base como si fuera un paraguas** (`article_numbers` incluye literalmente `'588 bis'`, `'588 ter'`, `'588 quater'`) mientras la BD modela cada sub-letra como fila propia (`588 bis a`, `588 bis b`… hasta `588 bis k`). El match es por string exacto normalizado, así que el paraguas no cubre a sus hijos. Confirmado contra RDS: `588 ter` (sin letra) SÍ está activo y escopado (1 pregunta), pero sus hermanos con letra no. Arreglo (no aplicado, decisión de contenido): añadir explícitamente los `article_numbers` con letra al scope de T20 cuando se decida reactivar cada uno.
- **✅ Plan de re-anclado LECrim→CP construido y VERIFICADO EN DRY-RUN contra RDS real** (`scratchpad/t157/plan-reancla-lecrim130-140.json`, con `scripts/reanclar-preguntas.cjs`, herramienta ya existente de T-139 — no reinventada): mueve las 2 preguntas activas (`da6c1514…`→CP art.130, `081da324…`→CP art.140). El guardarraíl `evaluarReancla` confirma la relación más segura posible (`texto: contenido` — el origen está ÍNTEGRO dentro del destino) y **gana 5 temas cada una, pierde 0**. Listo para `--apply` por quien tenga `DATABASE_URL` de escritura de negocio.
- **✅ Las otras 5 leyes (33/40 preguntas) TAMBIÉN investigadas — con veredicto y evidencia, aunque sin aplicar nada:**
  - **`Ley 1/1998 CyL` "preámbulo" (12 preguntas) → JUBILAR, no es contenido de esta ley.** Bajé el preámbulo REAL de la fuente oficial (`https://www.boe.es/eli/es-cl/l/1998/06/04/1/con`): empieza *"La tradición concejil y municipal de León y Castilla surgió durante la repoblación..."* — nuestro `content` (47.495 caracteres) empieza *"JUAN CARLOS I REY DE ESPAÑA... EXPOSICIÓN DE MOTIVOS I. Justificación de la reforma. La Jurisdicción Contencioso-administrativa es una pieza capital..."*, que es el preámbulo de OTRA ley (huele a Ley 29/1998 de la Jurisdicción Contencioso-Administrativa, mismo año, sin confirmar cuál exactamente). **Contaminación de contenido reproducida contra la fuente**, igual de grave que el caso LECrim 130/140 pero de OTRA ley, no de un artículo hermano. Y aunque el contenido fuera el correcto, un preámbulo/exposición de motivos no es articulado examinable — doble motivo para jubilar, no escopar.
  - **`Ley 15/2015` art. 65 (1 pregunta) → ALTA CONFIANZA: escopar + reactivar.** `tramitacion_procesal T19` ("La jurisdicción voluntaria...") ya escopa Ley 15/2015 del **1 al 148 casi completo** y salta exactamente el 65 en medio de la secuencia (`...64,66,67...`) — hueco mecánico, no una exclusión deliberada. Contenido verificado contra `datosabiertos` del BOE (bloque `a65`): coincide en estructura y sentido; la única diferencia es terminológica («persona con discapacidad con medidas de apoyo para el ejercicio de su capacidad jurídica» en BD vs «persona con capacidad modificada judicialmente» en el bloque que bajé) — nuestra BD tiene la terminología POSTERIOR a la reforma de 2021 (Ley 8/2021), y el bloque que bajé venía fechado `fecha_vigencia="20150723"` (la versión ORIGINAL de 2015, no la vigente); no es una divergencia, es que mi fetch cogió una versión histórica por el id de bloque. Falta: reconfirmar contra el bloque VIGENTE (no el `20150723`) antes de aplicar, con `reactivar-articulo-boe.cjs`.
  - **`Reglamento Bienes EELL` arts. `17-18` y `44, 56, 70` (2 preguntas) → ALTA CONFIANZA: encajan con 4 oposiciones ya activas.** `auxiliar_administrativo_diputacion_ourense T14` ("El patrimonio de las entidades locales... **Las potestades de investigaci[ón]**...") y `auxiliar_administrativo_diputacion_huelva T13` ya escopan esta ley; `auxiliar_administrativo_diputacion_cadiz T20` y `auxiliar_administrativo_diputacion_cuenca T14` la escopan con listas de artículos concretas (`1,2,3,4,8,10` / `1,2,3,4,5-6,8,10`, ninguna incluye `17-18` ni `44,56,70`). El art. `44,56,70` se titula *"Prerrogativas: deslinde, recuperación de oficio y desahucio administrativo"* — son literalmente las **potestades** que pide el epígrafe de Ourense. **Ourense escopa la ley con `article_numbers=NULL` (ley completa)**, así que si el contenido se reactiva sin tocar el scope, ya se serviría ahí solo; Cádiz/Cuenca necesitarían añadir los números a su lista si se decide que también les toca. Falta: `reactivar-articulo-boe.cjs` contra el BOE (`BOE-A-1986-17958`) antes de nada — no verificado el contenido letra a letra todavía.
  - **`RGC` arts. `173` y `Anexo V1` (16 preguntas) → PENDIENTE DE DECISIÓN, no mecánico.** `policia_nacional T43` ("El vehículo prioritario. Definición... Facultades de los conductores de vehículos prioritarios...") ya escopa RGC con `67,68,69,70,112,113` — ni `173` ni `Anexo V1` — y el contenido de ambos ("señales en los vehículos", "V-1. VEHÍCULO PRIORITARIO...") encaja temáticamente. **Pero** T43 TAMBIÉN escopa una ley `Vehículo Prioritario PN` dedicada y sintetizada (1 artículo de 27.794 caracteres, `"El vehículo prioritario"`) que probablemente YA cubre este contenido de forma consolidada — no comparado carácter a carácter contra el `173`/`Anexo V1` de RGC para saber si añadirlos sería complementario o puramente redundante. **SOSPECHO que son redundantes con el contenedor sintetizado, sin haberlo confirmado leyendo los 27.794 caracteres enteros** — es lo que falta antes de decidir escopar o jubilar.
  - **`ODM` art. `General` (2 preguntas) → MODERADA CONFIANZA.** `auxiliar_administrativo_estado T6` ya escopa los 8 objetivos numerados de ODM y su epígrafe ("...la Agenda 2030 y los Objetivos de Desarrollo...") encaja. El contenido de `General` es un párrafo introductorio/resumen ("Los ODM fueron los antecedentes directos de los ODS...") — verificado correcto y sin contaminación, pero es material introductorio, no un objetivo numerado como los otros 8: podría ser un complemento legítimo o redundante con la introducción que ya traiga el propio tema. No se ha comparado contra el contenido de los otros 8 artículos ya escopados para juzgar solape.
- **Nada escrito en BD** (rol trabajador, sin `DATABASE_URL` de escritura de negocio) salvo el código del detector, que sí está listo para mergear tal cual (no necesita ninguna escritura de negocio). El plan de re-anclado LECrim→CP (`scratchpad/t157/plan-reancla-lecrim130-140.json`) está verificado en dry-run y listo para `--apply` por quien tenga esa credencial.


